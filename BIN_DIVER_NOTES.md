# Kataban 作業記録（旧称 Bin Diver）

最終更新: 2026-07-23

---

## これは何か

英語圏のレトロゲーム収集家が、知っている英語名から**日本語タイトル・型番・発売元**を引き、
日本の中古市場へ飛べる道具。1ファイル完結の静的HTML。

- 公開URL: ~~https://english-tadashi.github.io/bin-diver/~~ → **https://gamekataban.com/**（2026-07-23〜）
- リポジトリ: github.com/english-tadashi/bin-diver（`index.html` のみ。Web画面からD&Dで更新）
  **リポジトリ名は変えていない**——独自ドメインで見えないため。**`CNAME` ファイルを消さないこと**（消すと独自ドメインが外れる）。
- **改称（2026-07-23）**: 英語圏で "bin diving" は**ゴミ箱あさり**を指し、画像検索が汚染されていた。**訪問者0の時点が最安の変更点だった。**
- 想定ユーザー: 観光客コレクター / 転売アービトラージャー / 在住外国人 / **海外の完集家**

---

## 今日（7/17）やったこと

| | 朝 | 夜 |
|---|---|---|
| 行数 | 5,048 | **12,208** |
| 発売元 | なし | **1,274社（充足率100%）** |
| 英題 | 全行 | 5,213行（42.7%）※英語で到達可能は65.3% |
| 機能 | 検索・機種絞り込み | ＋発売元絞り込み・発売年・Load moreページャー |
| 状態 | ローカルのファイル | 公開URL |

---

## 今日（7/18）やったこと

| | |
|---|---|
| 画像 | 型番→Redump serial→libretro箱絵が**動く（31.1%）**と実測。**権利の問題で使わないと決めた** |
| 型番 | 主役にした。13px → **24px**。**逆引き**（`SLPS-00533` → AUBIRDFORCE）を実装 |
| 現物 | ブックオフではなく出品写真で確認。**PS1は `SLPS 00533`、スーファミは `SHVC-2U`** |
| Buyee | 25機種を実測。**リンクを実装（8,578行 / 70.3%）**。「次にやること1」完了 |
| 駿河屋 | 型番リンクは**生きている**（28件→1件）。「未解決2」完了 |
| DATA | 7要素だった。`data_line.js` は8だった。**貼り込みが古かっただけ** |
| 罠 | **11〜22。12個増えた** |

---

## 今日（7/19）やったこと

| | |
|---|---|
| 店リンク | **ブックオフ・ハードオフを追加**。BUY IN JAPAN が5リンクに。フッターの嘘(PC-8801)も消した |
| データ | 8列 → **10列**。`row[8]`=カタカナ正規タイトル、`row[9]`=ローマ字 |
| row[8] | Wikidataの日本語ラベル(P7886)。**カタログ店の検索を噛み合わせる**。5,010行(41.0%) |
| row[9] | `schema:name` の `@ja-hrkt` 読み → 決定的ローマ字。**11,860行(97.1%)** |
| 内部検索 | カタカナ・中黒・スペース・ローマ字の**どれで打っても当たる**(HAYにrow[8]/row[9]、中黒正規化) |
| Path B | MGS3救済を測って**却下**(+0.6ptのために1/4が誤タイトル) |
| Cowork | 修正を委譲。md5照合・DATA無傷を確認して着地。**長音の穴(ryu/ryuu)を1つ見つけた** |

---

## ファイル構成

```
bin-diver/
├── index.html              ← GitHubに上げるのはこれ「だけ」。DATAは13列（★2026-07-21現在）
├── build_master.py         ← パイプライン本体（romaji()追加）
├── swap_data.py            ← data_line.js を index.html に貼り込む（v2）
└── data/                   ← 絶対にGitHubに上げない
    ├── metadata301.ttl         81MB / MADBのゲームパッケージ
    ├── wikidata_en.json        M番号 → 英題
    ├── wikidata_pub.json       日本語社名 → 英語社名
    ├── wikidata_ja.json        madbid → カタカナ正規タイトル（28,969件）★2026-07-19追加
    ├── master_final.csv        39,782行 × 14列（madb_id + データ13列）★2026-07-21現在
    └── data_line.js            index.html に貼り込む材料（13列）★2026-07-21現在
```

（測定用の使い捨てスクリプトも作った: `check_block.py` / `measure_pathb.py` / `measure_yomi.py`。
リポジトリには要らない）

### 再生成コマンド

```bash
python3 build_master.py data/metadata301.ttl data/master_final.csv \
        data/wikidata_en.json data/wikidata_pub.json data/wikidata_ja.json
```

`master_final.csv` と `data_line.js` の**両方**が出る。片方だけ更新される事故を防ぐため、
1コマンドに統合してある。生成後は `python3 swap_data.py` で `index.html` に貼り込む
（`[0]〜[6]` 差分ガードつき。row[8]/row[9] は末尾なので通る）。

---

## データパイプライン

```
MADB metadata301.ttl (リリース1.2.18 / 2026-07-17)
  └─ 型番(schema:productID)を持つ 12,208件を抽出
       ├─ ma:publisher → schema:publisher → schema:copyrightHolder の順に発売元を取得（100%）
       ├─ schema:gamePlatform（日本語の正式機種名）
       ├─ schema:datePublished
       ├─ schema:name（パッケージの印刷どおり。日本語とは限らない）
       └─ schema:name @ja-hrkt（読み・カナ）→ 決定的ローマ字（row[9]、97.1%）★2026-07-19
  └─ Wikidata
       ├─ P7886（MADB ID）→ rdfs:label@en  ... 英題（42.7%、M直引きのみ）
       ├─ P7886（MADB ID）→ rdfs:label@ja  ... カタカナ正規タイトル（row[8]、41.0%）★2026-07-19
       └─ P123（発売元）の ja/en ラベル対応 ... 英語社名
  └─ master_final.csv / data_line.js
```

### データの現況（2026-07-23 実測・`data/data_line.js`）

上の図は**型番あり12,208件時代の構成**。7/21の型番フィルタ緩和と7/22の機種35絞り込みを経て、現況はこう:

| 項目 | 行数 | 割合 | 旧値 |
|---|---|---|---|
| 全行数 | **37,855** | 100% | ~~12,208~~ |
| row[3] 型番あり | **10,296** | **27.2%** | ~~12,208~~ ← 7/22の機種35絞り込みで約1,912行減った結果 |
| row[0] 英題あり | **13,487** | **35.6%** | ~~42.7%~~ ← 12,208行時代の値 |
| row[0]・row[3] 両方あり | **4,989** | 13.2% | — |
| row[5] 発売元（日本語） | **35,803** | **94.58%** | ~~100%~~ |
| row[6] 発売元（英語） | **35,782** | **94.52%** | — |

**発売元の充足率は母数で割れる**:
- **型番あり群では 10,296/10,296＝100.00%**（旧記述はこの群を指していた。その範囲では今も真）
- **型番なし群では 92.55% / 92.48%**

→ 「発売元は完全」は**母数を書かなかったために嘘になった**（罠#62）。`index.html` フッターからは削除済み。
なお本ノート中の `12,208` / `42.7%` の記述は**当時の実測記録としてそのまま残す**（各節の日付を見ること）。

### data_line.js の構造（13フィールド）★2026-07-19に 8→10 / 07-20に 10→12 / 07-21に 12→13

```
[0英題, 1日本語名, 2機種(英語), 3型番, 4発売年, 5発売元(日), 6発売元(英),
 7Buyeeキーワード, 8カタカナ正規タイトル, 9ローマ字, 10JAN, 11五十音初字, 12配信フラグ]
```

- **[8] カタカナ正規タイトル**（Wikidata日本語ラベル由来）。カタログ店(Book-Off/Suruga-ya)の
  検索キーワード用。5,010行(41.0%)。空なら UI は `title_ja`(row[1]) にフォールバック。
  - 壊れていた行(title_jaがラテン=3,248行)のうち1,431(44.1%)を日本語形で救った
- **[9] ローマ字**（`@ja-hrkt` 読み由来、決定的）。主に**検索(HAY)**用。11,860行(97.1%)。
  表示は「見出しが日本語(漢字・カナ)のときだけ」(英題・ラテン見出しの行では冗長なので出さない)
- **[10] JAN**（`schema:gtin` 由来、13桁に正規化）。カメラ(バーコード)の照合用。★2026-07-20追加
- **[11] 五十音初字**（`@ja-hrkt` 読みの先頭カナ→清音1字）。棚案内用。★2026-07-20追加
  （★2026-07-21確認: 実在。32,259行で値が入っている。row[12]追加時に列を実見した）
- **[12] 配信フラグ**（`ma:carrierType "オンライン資料"` なら1）。棚に物が無い行。★2026-07-21追加
  UIは `No disc / Download` バッジを出し、店リンクブロックを非表示にする。7,109行。
- **[0]〜[7] は 2026-07-19 に一切変えていない**（末尾に足しただけ。card()は先頭7個しか分割代入しない）


**旧記述の訂正**: このノートは以前「機種(日本語)は Buyee の検索キーワード用。変換して
はいけない」と書いていた。**誤りだった。** 根拠は「Buyeeは同義語を解決する」で、これは
ファミコンとPSPの2件からの一般化。同じ誤りが `build_master.py` のコメントにも残っており、
2026-07-18に両方訂正した。**結論（UIで変換しない）だけがたまたま合っていたので、
誰も気づかなかった。**

---

## データソースの重要事実

### MADBは毎月更新されている

**「2026年版の公開を待つ」という前提は誤りだった。**
2024年1月のver1.2を最後に年次リリースは廃止され、Releasesページで順次更新される方式に変わった。
リリースは32本あり、最新は 1.2.18（2026-07-17 04:12公開）。

- リポジトリ: https://github.com/mediaarts-db/dataset
- ファイル名: `metadata301_ttl.zip`（**READMEに書かれている `metadata_gm-item_gm301_ttl.zip` は古い。404になる**）
- 資源URIが 2024-11-26 に `bunka.go.jp` → `artmuseums.go.jp` へ変更済み

### MADBは発売元を持っている（Wikidataは不要だった）

`ma:publisher` に**日本語で、発売当時の日本の発売元**が入っている。充足率は型番あり12,208件で**100%**。
Wikidataに6本のクエリを投げて探したのは全部無駄だった。**外に探しに行く前に、手元のデータを見る。**

| | Wikidata (P123) | MADB (ma:publisher) |
|---|---|---|
| セガサターン | 65.9% | **100%** |
| スーパーファミコン | 75.9% | **100%** |
| 社名 | 現在の社名（コナミグループ） | 発売当時の社名（コナミ） |
| 地域 | 日本と欧米が混在。判別不能 | 日本のみ |

Wikidataの役割は**英題の付与だけ**に縮小した。

### schema:name は「日本語タイトル」ではない

パッケージに印刷されている表記。日本のゲームでもラテン文字が多い。

- ラテン文字を含む: 5,652件（46.3%）
- 完全にラテン文字: 3,398件（27.8%）
- 例: `CHRONO TRIGGER` `Bahamut Lagoon` `biohazard4` `Romancing Sa・Ga`

`クロノ・トリガー` で検索すると **0件**。

### schema:name は「読み(@ja-hrkt)」も持っている（2026-07-19発見）

`schema:name` には2つ目の値があり、`@ja-hrkt` タグが付く。**漢字タイトルの読み(カナ)**である。

```
schema:name "真・三國無双8 …",
            "シン ・ サンゴク ムソウ 8 …"@ja-hrkt ;
```

- 充足率 **97.1%**（型番あり12,208行のうち11,860）。
- 読みはカナなので**ローマ字が決定的に生成できる**。`真・三國無双` → `シン サンゴク ムソウ` → `shin sangoku musou`。
- `build_master.py` は `first(schema:name)`=1つ目(漢字)だけ取って**読みを捨てていた**。`@ja-hrkt` を拾えばいい。
- 注意: 読みが表示タイトルと語単位で一致しないことがある（罠#29）。

### Wikidataの日本語ラベルが、カタログ店のカタカナ正規形と噛み合う（2026-07-19）

英題と同じ P7886 の結合で、`rdfs:label@ja` を引くと**カタカナ正規タイトル**が取れる。
`Castlevania 白夜の協奏曲` → `キャッスルヴァニア 白夜の協奏曲`、`METAL GEAR SOLID 3 SNAKE EATER` → `メタルギアソリッド3 スネークイーター`。
これがブックオフ・駿河屋のカタログ検索と噛み合う（row[8]）。詳細は罠#25/#26。

### build_master.py の英題結合は「M直引きだけ」だった（2026-07-19、罠#19の再来）

ノートは以前「C番号(embodimentOf)とM番号の二刀流で~56%」と書いていた。**現物のコードは
`en_titles.get(madb_id)` の M直引きのみで、C経由の結合はどこにも実装されていない。**
実際の英題充足は 42.7%。「~56%」は**計画が事実として記録されていた**（「2026年版を待つ」
「Wikidataから発売元を取る」と同じ形）。row[8](カタカナ)も同じM直引きで結合している。

---

## Buyee連携（進行中）

### 確定事項（すべて実測）

```
URL        https://buyee.jp/item/search/query/{encodeURIComponent(キーワード)}?lang=en
中身       ヤフオク。駿河屋のヤフオクストア（200万件）も含む
言語       lang=en でUIは英語になる。商品名は日本語のまま
キーワード  {タイトル} {機種名}
```

- **駿河屋はBuyeeの対象サイトではない**。ヤフオクの駿河屋ストア経由でのみ届く
- Buyee ≠ 転送コム(tensojapan.com)。転送コムは「日本語が読める人」向けで、本人確認も必要。**繋いではいけない**
- 中黒は無視される。正規化不要
- 検索は**単純な部分一致**。同義語の解決は無い

### 実測値

| キーワード | hits |
|---|---|
| ドラゴンクエスト | 50,598 |
| ドラゴンクエスト ファミリーコンピュータ | 4,312 |
| ドラゴンクエスト ファミコン | 4,313 |
| クロノ・トリガー スーパーファミコン | 250 |
| CHRONO TRIGGER スーパーファミコン | 251 |
| ルドラの秘宝 | 59（14件は攻略本） |
| ルドラの秘宝 スーパーファミコン | 45 |
| BIOHAZARD OUTBREAK プレイステーション2 | 78 |
| テイルズ オブ アライズ プレイステーション4 | 56 |
| **biohazard4 ニンテンドーゲームキューブ** | **0** |
| **バイオハザード4 ニンテンドーゲームキューブ** | **0** |
| バイオハザード4 ゲームキューブ | 22 |
| ニンテンドーゲームキューブ（単独） | 1,323 |
| ゲームキューブ（単独） | 7,627 |

**機種名を足すと候補が91.5%減り、攻略本と他機種版が落ちる。マイナー作品でも45件残る。**

### 機種名の検索語テーブル（解決済み 2026-07-18）

**25機種を「有名タイトル + 機種名」2本ずつで実測した。0件だったのは1つだけ。**

| 合格 | ゲームボーイ(76/87) / PCエンジン(52) / ニンテンドーDS(103/226) / ドリームキャスト(4/11) / セガサターン(1/17) / ゲームボーイアドバンス(78/44) / メガドライブ(42/9) / ワンダースワン(8/15) / NINTENDO 64(23/488) / 3DO(22/18) / Wii(25/127) / ゲームギア(35/38) / ネオジオCD(3/7) / SEGAマーク3(2/3) / ネオジオ(124/43) / バーチャルボーイ(24/5) / ネオジオポケット(7/1) / PC-FX(10/6) + 既出6機種 |
|---|---|
| **不合格** | **ニンテンドーゲームキューブ → `ゲームキューブ` に差し替え** |

**落ちるのは「正式名称が通称より長い」機種だけ。** 他の24機種は正式名称がそのまま通称だった。
部分文字列は候補を**増やす**。減らさない。だから短い通称は構造的に落ちない。

`build_master.py` の `BUYEE_KEYWORD` は1行。`BUYEE_OK` に25機種。
未実測の機種（PC-8801 / Nintendo Switch / Microsoft Windows / PS5 / PS3 / Xbox 360 ほか）と、
MADBに機種が無い184行には、**リンクを出さない**。

```
Buyeeリンクを出す行 : 8,578/12,208 (70.3%)
```

**0件のリンクは、リンクが無いことより悪い。**「探したが無かった」と嘘をつくから。

**★未解決: `ネオジオ` は0件では落ちないが、目的を果たしていない。**
`ネオジオ ⊂ ネオジオCD ⊂ ネオジオポケット` なので、`龍虎の拳 ネオジオ`(124) には
AES・MVS・CD版が全部入る。**他機種版が落ちない。** ゲームキューブとは逆の壊れ方。
14行（0.1%）なので今は放置。海外のネオジオ収集家はカルト的なので、判断は本人。

---

## 店リンクの最終形（2026-07-19、すべて実測で配線）

```
BUY IN JAPAN     Suruga-ya · Suruga-ya (code) · Mercari · Book-Off · Hard-Off
SHIPS WORLDWIDE  Buyee · eBay
```

| 店 | キーワード | 根拠（実測） |
|---|---|---|
| Suruga-ya | `kwKana`(カタカナ row[8]) | 壊れた行を救い(DQMJ 1→49等)、回帰なし。中黒も112で正常 |
| Suruga-ya (code) | `kwCode`(型番) | 28件→1件。ヤフオクでは死ぬが駿河屋では生きる（未解決2） |
| Mercari | `kwName`(箱の形 row[1]) | 出品サイト=箱を書き写す |
| **Book-Off** | `kwKana` | クロノクロス→19件、全部クロノ・クロス関連。カタログはフレーズ一致 |
| **Hard-Off** | `kwKanaHard`(中黒→スペース)＋`/cate/00010012/` | 中黒暴発を回避（罠#25）。320→3件 |
| Buyee | `kwBuyee` | 箱の形。ヤフオクは中黒を無視 |
| eBay | `kwEn`(英題) | — |

- **Book-Off URL**: `https://shopping.bookoff.co.jp/search/keyword/{kw}`。全カテゴリ横断
  （サントラ・攻略本も出る）。ゲーム限定は `/search/genre/51/keyword/`。
  在庫切れでも**店頭在庫**（入荷した店舗数）を出す → **店内の観光客に効く数少ない導線**。
- **Hard-Off URL**: `https://netmall.hardoff.co.jp/cate/00010012/?q={kw}&s=7`。ライブ出品型。
  無名タイトルは0件になりやすい。中黒は`・→スペース`必須（罠#25）。
- **カタログ店は`kwKana`、フリマ店は`kwName`**（罠#26）。1キーワードで全店最適化はできない。

---

## パイプラインの罠（踏んで直したもの）

### 1. `ma:publisher` は `schema:publisher` に部分一致する

`sche**ma:publisher**`。grepや正規表現で述語を数えるときは、**行頭のインデントごとアンカーする**。
これを踏んで充足率を99.96%と誤報した（偶然、和集合を計算していて答えは合っていた）。

### 2. 中黒(`・`)で社名を割ってはいけない

`セガ・エンタープライゼス` `エレクトロニック・アーツ`。**3,840件の社名に含まれる。**
区切りは `,` `、` `/` `／` `;` `；` のみ。

### 3. カンマの後ろが法人格なら、社名の一部

`Ubisoft, Inc.` `SEGA Enterprises, Ltd.` `SQUARE CO., LTD.`。
素直に割ると `Inc.` という存在しない会社が生まれる（生んだ）。
カンマを含む22種のうち、本当に2社なのは `バンダイナムコゲームス, カプコン` など2件だけ。

### 4. 出力の区切り文字が入力に含まれることがある

`;` で連結したのに、MADBの値にも `;` があった（`三井物産株式会社; 日本テレネット`）。

### 5. 大文字小文字の割れは、完集家の敵

`SQUARE ENIX`(100) と `Square Enix`(137) が別項目 → **4割超が消える**。
`HUDSON SOFT`(48) と `Hudson Soft`(229) も同様。多数派の綴りに統一する。同数なら全部大文字でないほうを選ぶ。

### 6. 社名の変遷は「歴史」であって「ノイズ」ではない

**合併は分ける**（別法人）:
- スクウェア + エニックス → スクウェア・エニックス
- タカラ + トミー → タカラトミー（Wikidataは両方 "Tomy" を返すので上書きが必要）

**表記違いは統合する**（同一法人）:
- 任天堂 / Nintendo、SNK / エス・エヌ・ケイ、日本ファルコム / Nihon Falcom
- ※統合前は `任天堂`(481) と `Nintendo`(2) が別項目で、完集家が2本取りこぼしていた

### 7. Wikidataは現在の社名しか持たない

`コナミグループ → Konami`。MADBの `コナミ`（発売当時）とは一致しない。
歴史的な社名は `PUBLISHER_EN` に手書きで補う。手書きが常に優先。

### 8. UI側に変換テーブルを置かない

朝、`PLATFORM_LABELS` / `platLabel()` が原因で `ネオジオCD` が英語化されずに残っていた。
**変換はパイプラインで済ませ、変換漏れは生値のまま素通りさせる。** UIは渡された文字列を出すだけ。

### 9. gzipが効くので、手作業の最適化はほぼ無意味

辞書化して番号参照にすると生サイズは28%減るが、**gzip後の差は7.5%**。
gzipが繰り返し文字列を圧縮するのは、辞書化がやろうとしていたのと同じ仕事。素直な構造で行く。

### 10. `data_line.js` を外部ファイルとして読み込ませない

`file://` でCORSに阻まれ、ダブルキックで開けなくなる。リクエストも2回になる。
**1ファイル完結であることが、このツールの一番いい性質。**

### 11. 型番の書式は、MADBとRedumpで違う

`SLPS 00533` / `SLPS02441` / `SLPM 87308~9` は全部同じものの別表記。
英数字以外を落とし、5桁にゼロ埋めし、`~` を展開すると **0.2% → 95.4%**。

**訂正（現物確認後）**: 「MADBが揺れている」と読んで、直そうとした。**揺れているのは
現物のほうだった。** MADBは現物に従い、Redumpは自分の規約に従う。**どちらも正しい。**
だから照合には正規化が要り、**表示には正規化してはいけない。**

### 12. `except Exception:` は、通信の死をデータの不在に化けさせる

`0/30` を「箱絵が無い」と読んだ。実際は6件とも200で、落ちていたのはMacのPythonの
証明書（`CERTIFICATE_VERIFY_FAILED`）。**30件中0件は、データの顔をしていない。**
例外は**種類ごとに数えて出す**。404（無い）と SSLError（届いていない）は別のことを意味する。
対処: `python3 -m pip install --upgrade certifi` + `ssl.create_default_context(cafile=certifi.where())`

### 13. 確認用のコードは、本体と同じ経路を通らせる

本体は certifi を明示的に渡していたのに、確認用の1行は渡していなかった。
**本体より弱い検証は、本体の状態を教えてくれない。** 通っても落ちても、意味が読めない。

### 14. コードにはバージョンを名乗らせる

差し替えたつもりのブロックが差し替わっておらず、同じ出力を3回「新しい結果」として読んだ。
**出力を見ても、それがどのコードの出力かは分からない。** だから最初に名乗らせる。

### 15. n=1から書式を一般化しない

`DL-DOL-GENJ-JPN` を1件見て `DLDOL` と書いた。実際は `GS` `GB` `DC` も。
`SLPS-01204` を見て `/^[A-Z]{1,5}\d/` と書いた。FC/SFCの型番に数字は無い。
`HVC` だと思った。`BAN` `SQF` `CAP` もあった。**接頭辞は、いつでも揺れる。**

### 16. MADBに外国版が混ざっている（機種をまたぐ）

`DMG-TR-USA`(GB) / CHRONO CROSS `SLUS-01041`(PS1) / VAGRANT STORY `SLES02754` /
FF8 `SLES-02082` / `Nintendo Entertainment System` 5件。
**一番検索される行に集中している。**

### 17. 検索対象に何が入っているかを、先に読む

`HAY` には最初から型番が含まれていた。**型番は変更前から検索できていた。**
`/^[A-Z]{1,5}\d/` が塞いでいたのは逆引きそのものではなく、**書式ゆれ耐性だけ**。
「カセット機の型番が逆引きから丸ごと外れる」は誤り。**判断は生きたが、理由は死んだ。**

### 18. テストは、変更前に落ちるものを選ぶ

最初の確認表7行のうち5行が、変更前から通っていた。**通っても落ちても意味が読めない。**
`SHVC-VU`（生の値どおり）ではなく `SHVC VU`（スペース）を打つべきだった。
**旧/新を並べる。`旧0/新1` が出ない行は、その変更を検証していない。**

### 19. ノートは、計画を事実として記録する

「data_line.js の構造（8フィールド）」——現物は7だった。
「Buyeeは同義語を解決する」——`build_master.py` のコメントに現役で残っていた。
**結論だけがたまたま合っていたので、誰も気づかなかった。**
「2026年版を待つ」「Wikidataから発売元を取る」と同じ形。
**書いた時点で真だったことと、今も真であることは違う。**

### 20. 「1コマンドに統合した」は、生成側しか守っていない

`master_final.csv` と `data_line.js` が片方だけ更新される事故は防いだ。
**でも `data_line.js` を `index.html` に貼り込む工程は、誰も守っていなかった。**
生成が1コマンドでも、**その先に手作業が1つ残っていれば、そこで割れる。**
→ `swap_data.py` を作った。`[0]〜[6]` に差分があれば中止する。

### 21. 行番号を直書きすると、行が動いたときに黙って別の行を壊す

`swap_data.py` は `LINE = 107` を直書きしていた。Buyeeリンクの実装でCSSが2行増え、
**DATA行は109へ動いた。** そのまま回せば、107行目（CSS）をDATAで上書きしていた。
→ v2で `const DATA = [` を正規表現で探し、2つ以上あれば中止する形にした。

### 22. `list[:12]` はサンプルではない

ラテンのタイトルを `latin[:12]` で見て「全部PS4。レトロ機では起きていない」と読んだ。
**実際は PS1 736 / PS2 416 / PS4 197 で、PS1が最多。** データがPS4から始まっていただけ。
**n=12でも、偏っていればn=1と同じ。**

### 23. `grep -A` は前方しか見ない。述語が identifier より前だと隣のブロックを拾う（2026-07-19）

MGS3(M719803)の embodimentOf を `grep -A 60 'schema:identifier "M719803"'` で見たら、
`C33621 = Baldr Force` / `C44986 = Alchemist` が出た。**隣のブロック(Baldr Force)に漏れていた。**
ブロック内で embodimentOf は identifier より**前**にあり、前方検索では自分の値を取り逃す。
**ブロック単位で処理せよ**（`build_master.py` と同じ `re.split` 分割）。罠#1(grepのアンカー)のブロック版。

### 24. `2^15-1` は「切り捨ての顔」だが、切り捨てとは限らない（2026-07-19）

Wikidataダンプが **32,767 = 2¹⁵−1 ちょうど**で、切り捨てを疑った。だが実測すると、
M側のja行が別クエリと**完全一致(0/0差分)**、CとMが末尾で**交互**に並んでいた
→ 末尾を切ったならMも欠けるはず → 欠けていない → **偶然だった**。
数字の顔で結論せず、**比べて確かめる**。罠#12の逆（「30件中0件はデータの顔をしていない」）。

### 25. 同じ「タイトルで検索」でも、店ごとに検索エンジンの挙動が違う（2026-07-19）

- **Buyee/ヤフオク**: 中黒を無視。出品者が箱を書き写す → ラテンでも当たる。
- **駿河屋/Book-Off（カタログ）**: フレーズ一致。`クロノ・クロス` → クロノ・クロスだけ(19件)。カタカナ正規形が要る。
- **Hard-Off（オフモール）**: **中黒で割って部分一致。`クロノ・クロス` → 4530件**（`クロス`→マ**クロス**・ピ**クロス**）。
  `・→スペース`で2語ANDにし、ゲームカテゴリURL(`/cate/00010012/`)で絞ると **320→3件**。

「同じ型番リンクでも行き先で運命が変わる」（未解決2）の、**タイトル検索版**。店ごとに実測して配線した。

### 26. カタログ店とフリマ店で、欲しいキーワードが違う（2026-07-19）

出品サイト(Mercari/Buyee)は**箱の印字(row[1])**が正解（罠#25、実測でラテンでも当たる）。
カタログ店(Book-Off/Suruga-ya)は**正規カタカナ(row[8])**が正解。**1キーワードで全店を最適化できない。**
row[8]を足し、店ごとに配線した。row[8]が無ければ自動で row[1] に落ちる。

### 27. `workEmbodimentOf` は「作品レベル」なので、しばしば別物・上位概念に化ける（2026-07-19、Path B却下）

Path BでMGS3のカタカナは `workEmbodimentOf → C34205` に乗っていた（`embodimentOf` は表現レベルでほぼ空）。
だが同じ経路で **MGS5 Ground Zeroes → ファントムペイン**、**SF Anniversary → ストリートファイターII**、
**サクラ大戦 帝撃グラフ → サクラ大戦シリーズ** に化ける。**正しい79行と壊れた79行を、プログラムで見分けられない。**
救えるのは全体で79行(+0.6pt)だけ。**+0.6ptのために1/4を誤タイトルで汚す。却下。**
今のフォールバック(ラテンのtitle_ja)は少なくとも箱の本物の文字＝嘘ではない。**誤った値より、無いほうがまし。**

### 28. ローマ字の「易しい所」と「価値ある所」は、逆にある（2026-07-19）

純カナ(簡単に変換できる)の多くは**欧米タイトルの音写**で、ローマ字化しても `doragonkuesuto`（役に立たない）。
ローマ字が本当に効くのは**生粋の日本タイトル**(龍が如く→ryuu ga gotoku)で、それは大半が**漢字**=読みが要る。
**`@ja-hrkt` 読みがこの逆説を解いた**（漢字タイトルの読みもカナで持っている）。読みから変換すれば、
価値ある所(生粋の日本タイトル)がクリーンにローマ字化される。

### 29. 読み ≠ 表示 のことがある（2026-07-19）

`@ja-hrkt` 読みが表示タイトルと語単位で一致しないことがある（`エスカ&ロジーのアトリエ…` の読みは
`アトリエ…` から始まる＝接頭辞が落ちる、`仁王2 remastered` の読みが `ニオウ コレクション`）。
読みとしては正しいが、表示にそのまま出すとズレて見える。→ **ローマ字は検索を主・表示を従に。**
表示は「見出しが日本語のときだけ」。

### 30. 表示の正解と検索の正解は、長音でも割れる（→ 2026-07-20 実装・確定。ローマ字限定＋床8）

`龍が如く` の読みは `ryuu ga gotoku`（りゅう→ヘボン式で忠実に伸ばす）。**だが英語圏は `Ryu ga Gotoku`（u1つ）と書く。**
今のHAYは表示基準の `ryuu` だけを入れているので、`ryu ga gotoku` は**0件**（`ryuu ga gotoku` なら18件）。
表示は`ryuu`、**検索は長音を落とした形でも引けるべき**。ただし `uu/aa/oo` 同母音は潰せても、
`ou`(オウ)を潰すと `mu-so-u`(musou)が壊れる。**憶測で正規表現を書かず、どの長音パターンが何件かを実測してから。**
Hard-Offの中黒と同じ「表示と検索の正解が違う」構造（罠#25/#26）。

---

## ★未解決の判断（要・本人）

### 1. ~~Koei の統合可否~~ → 統合方向で確定（名寄せ v1、2026-07-22）

`コーエー`(193) と `光栄`(150) が両方 `Koei` に統合されている。
これは**合併ではなく1997年頃の漢字→カタカナの表記変更**。同一法人。

一方 `セガ・エンタープライゼス` と `セガ` は分けている。**これも同一法人の商号変更**で、一貫していない。

- 統合すれば「Koei 343本」
- 分ければ「Koei 193 / 光栄 150」

**棚をどう作るかの話なので、収集家本人が決めるべき。**

**（解決 7/22）** 名寄せ v1 で発売元を**統合方向に確定**した（`Sega` も `Koei Tecmo` も件数最多表記へ寄せる）。#1が指摘した「Koeiは統合・セガは分離」という非一貫は、**両方とも統合**にそろえて解消。「棚をどう作るか」は"多数派表記へ集約する"で決めた。ただし**別法人は分離維持**（Idea Factory / Compile Heart、罠#6）。詳細は本文「発売元の名寄せ（row[6]だけ・2段階）」節。

### 2. ~~駿河屋の型番リンクは使えたか~~ → 生きている（実測 2026-07-18）

```
駿河屋  CHRONO TRIGGER → 28件（PS1版・SFC版・2011年版が混ざる）
駿河屋  SLPS02430      →  1件（1999年のPS1版だけ）
Buyee   SLPS-02430     →  1件（クロノ・トリガー スーパーファミコンは250件）
```

**ヤフオクでは型番は死に、駿河屋では生きる。** 出品者の自由記述と、自社データベースの違い。
**同じ「型番リンク」でも、行き先で運命が変わる。**

「作品名+機種名で91.5%絞れるから型番リンクは役目を終えた」は**誤り**だった。
作品名+機種名はBuyee用の絞り込みで、駿河屋には機種名が付いていない。
**`駿河屋（型番）` は、28件を1件にする唯一のリンク。** 消してはいけない。英語にする。

### 3. ~~フッターの文言~~ → 差し替え済み（2026-07-19）

以前: `coverage is broadest on PlayStation, PlayStation 2 & PC-8801 lines`（PC-8801を機械的に並べた＝嘘）。
現在（出典表記を残して差し替え済み）:

```
Prototype · 12,208 titles with official product codes, publishers and release years.
Publisher data is complete; English titles cover 43% of entries.
Titles matched from Wikidata (CC0) against the Agency for Cultural Affairs Media Arts Database.
```

### 4. ~~MGS3型の行をC番号(embodimentOf)で救えるか~~ → 却下（実測 2026-07-19、罠#27）

`workEmbodimentOf` 経由でMGS3は届くが、救えるのは79行(+0.6pt)で、うち1/4が誤タイトルに化ける
（Ground Zeroes→ファントムペイン等）。**正誤をプログラムで見分けられない。** 却下。
**41.0%（M直引きのカタカナ）が、きれいな修復の天井。** MGS3は箱の `METAL GEAR SOLID 3 SNAKE EATER` のまま。

**（2026-07-20 追記）** 母集団12,208で本測定 → embodimentOf +0.0pt / workEmbodimentOf +0.3pt。gapの53.1%はembodimentOfを持つが作品Cに英語ラベルが無い。**「~56%」は結合バグではなく燃料不在で確定。42.7%が英語検索入口の意味上の天井。**（本文「C結合を測って却下」節を参照）

### 5. ~~ローマ字の長音を、検索で潰すか~~ → 実装・確定（ローマ字限定＋床8、2026-07-20。本文の「長音の検索穴」節を参照）

表示は `ryuu ga gotoku`（忠実）でよいが、検索は `ryu ga gotoku`（u1つ）でも引けたほうがいい。
`uu/aa/oo` は潰せても `ou`(オウ)を潰すと `musou` が壊れる。**どの長音パターンが何件かを実測してから決める。**

---

## 次にやること（優先順）

### 完了したもの（2026-07-19）

- **店リンク**: Book-Off / Hard-Off を追加。Suruga-ya=カタカナ / (code)=型番 / Mercari=箱 /
  Book-Off=カタカナ / Hard-Off=カタカナ(中黒→スペース)＋ゲーム絞り。**すべて実測で配線**（店リンクの最終形の節）。
- **カタカナ正規タイトル(row[8])** と **ローマ字(row[9])** を追加。内部検索もカタカナ・中黒・スペース・ローマ字で当たる。
- フッターの嘘を消した。

### 残り

### 1. ~~Buyeeリンクの実装~~ → 完了（2026-07-18）

```
BUY IN JAPAN     Suruga-ya · 駿河屋（型番） · Mercari
SHIPS WORLDWIDE  Buyee · eBay
```

- URL: `https://buyee.jp/item/search/query/{encodeURIComponent(kw)}?lang=en`
- kw: `row[1] + ' ' + row[7]`（日本語名 + Buyeeの検索キーワード）
- `row[7]` が空の3,630行では Buyee を出さない。`SHIPS WORLDWIDE` には eBay だけ残る
- **`Mercari` は `BUY IN JAPAN` に移した。** 日本の住所と電話番号が要るので、それが正しい

**残作業**: `駿河屋（型番）` を `Suruga-ya (code)` に。**日本語を、読めない人に見せない。**

### 2. アクセス解析

**訪問者はまだ0人。** 投稿してからでは、そのとき何が起きたかを測れない。
Cloudflare Web Analytics（無料・Cookieなし・同意バナー不要）。スクリプトタグ1行。

### 3. 投稿

r/gamecollecting、r/JapanTravelTips、レトロゲーム系Discord。
**サブレディットの自己宣伝ルールは必ず自分で読むこと。** 違反すると削除・BANで二度目はない。

刺さるのは「サイト作りました」ではなく、**「ブックオフの棚の前で、外国人がスマホと箱を見比べているのを見て作った」**という実話。

---

## 将来の候補

### バーコード検索（実現可能性は高い）

MADBは `schema:gtin`（JANコード）を持っている。**型番あり12,208件のうち70.1%。**

```
プレイステーション   99.0%      セガサターン       94.4%
プレイステーション2  99.2%      ゲームボーイアドバンス  96.0%
スーパーファミコン    97.2%      ニンテンドーDS      99.7%
プレイステーション・ポータブル 99.6%   ファミリーコンピュータ  78.2%
```

**狙っているレトロ機種だけ、ほぼ100%埋まっている**（Switch 4.5% / PS5 0% は対象外なので問題なし）。

OCRとバーコードは別物。バーコードは機械が読むための規格で、ブラウザだけで動くJSライブラリがある。
**箱に向けるだけでカタカナ照合が消滅する。**

制約: **バーコードは箱にあり、裸のカセットには無い。**
ディスク世代（PS1/PS2/PSP/DS/サターン/DC ≒ 全体の4割）は必ずケース入りで完全に効く。
カセット世代（ファミコン/スーファミ）は箱付きのみ。ブックオフのワゴンにあるのは裸が多い。

※ブラウザのカメラAPI周りは変化が速いので、実装前に現在の対応状況を要確認。

### 画像

**→ 使わないと決めた（2026-07-18）。理由は下の「画像は使わない」の節。**

**英語しか読めない人は、カタカナを棚と照合できない。**
`クロノ・トリガー` と表示されても、500本の背表紙と突き合わせるには図形を目視照合することになる。
`ソ`/`ン`、`シ`/`ツ` の区別を、その人は知らない。

Discogsが強いのはジャケット画像があるから。**文字ではなく絵で照合する。**
一番効くが、一番遠い。MADBに画像は無い（権利の問題と思われる）。

---

## 画像は使わない（2026-07-18）

### 動くことは確認した

MADBの型番は、Redumpの `serial` と同じもの。書式だけが違う。
libretro-database の redump DAT が serial ↔ Redump名 を持ち、
libretro-thumbnails が Redump名 で箱絵を持つ。**完全一致で繋がる。**

```
型番 → metadat/redump/{機種}.dat の serial → Named_Boxarts/{Redump名}.png
```

| | 型番 | 箱絵 | 通し | 行数 |
|---|---|---|---|---|
| PS1 | 95.7% | 93.3% | 89.3% | 1,988 |
| PS2 | 94.2% | 80.0% | 75.3% | 994 |
| セガサターン | 95.8% | 93.3% | 89.4% | 336 |
| ドリームキャスト | 94.7% | 93.3% | 88.4% | 335 |
| ゲームキューブ | 95% | 96.7% | 92% | 126 |
| **PSP** | 83.7% | **3.3%** | **2.8%** | **14** |
| **カセット機** | — | — | **0%** | **0** |

**合計 3,793 / 12,208 = 31.1%**

- **PSPのリポジトリは空**。北米版すら404（`Monster Hunter Freedom Unite (USA)` = 404）。
  名前の問題ではない（`Persona 3 Portable (Japan)` = 200）
- **カセット機のDATに serial が無い**（スーファミ4,256件中28行、しかもROM内部コード）。
  箱絵は在る（`Rudra no Hihou (Japan).png` = 200）。**鍵が無いだけ**
- **region を見ないと外国版に当たる**。CHRONO CROSS(SLUS) / VAGRANT STORY(SLES) /
  FF8(SLES) の3行はMADBが北米・欧州型番を持っており、**一番検索される行で
  一番ありえない絵を出すところだった**

### それでも使わない

**libretro-thumbnails にライセンス表記が無い。** READMEのCreditsはMobyGamesの
スクリーンショット・Fandom・有志の投稿。**libretroが持っていない権利は、こちらに渡せない。**

権利の問題が無いものは、**投稿できる・審査に出せる・消えない**。訪問者0の今、
条件付きの資産を土台にしない。

**他の候補も測った、あるいは潰した**:

| 候補 | 判定 |
|---|---|
| IGDB | **カバーは1ゲームに1枚**（`cover?: number`）。地域別カバーは要望であって実装ではない。 |
| | Skyscraperの地域対応表はIGDBから発売日しか取らず、カバーはMobyGamesから取る。 |
| | **APIが無料なのは非商用のみ。** A8.netで収益化した時点で対象外 |
| LaunchBox | **公開APIが無い。** 画像名はGUIDで組み立て不能。dumpの日次DLのみ。 |
| | ただしソース自体は本物で、日本版のBox-Frontを地域タグ付きで持っている |
| Wikipedia | **Commonsはフェアユース素材を受け入れない。** 英語版の箱絵は非フリー扱いで、 |
| | 「Wikipedia上でしか使うな」が規約。**CCではない。** 日本語版はフェアユース非採用 |
| Google CSE | 型番が一意なのと、画像検索1位が日本版パッケージなのは別の話。 |
| | 他人のサイトの画像を出すことになり、権利関係はlibretroより悪い |

### 代わりに、型番を主役にした

- ラテン文字。**カタカナが読めなくても照合できる**（`ソ`/`ン` 問題が無い）
- 充足率100%（12,208行）。画像の31%とは桁が違う
- 権利の問題ゼロ
- **★型番は裸のカセットのラベルに印字されている**（現物で確認）。
  箱絵（31%）もバーコード（70.1%だが箱のみ）も届かない「ワゴンの裸カセット」に、
  型番だけが届く
- **逆引きが無料で付いてくる**（下記）

---

## 現物の実測（2026-07-18）

| 現物 | 印字 |
|---|---|
| PS1 AUBIRDFORCE（**裏面**） | **`SLPS 00533`** スペース |
| スーファミ ゴエモン3（カセット表） | **`SHVC-2U`** ハイフン |
| スーファミ スーパーチャイニーズ2（カセット表） | **`SHVC-CU`** ハイフン |

**書式は機種ごとに違う。MADBは現物どおりに転記している。**
→ **表示は生の値のまま。正規化してはいけない。** 揃えるとPS1で棚と食い違う。

### 未測定（棚に立たないと出ない）

1. **場所**: 確認したのは出品写真で、PS1の**裏面**。**背表紙に型番があるかは未確認。**
   棚に挿さっていると背表紙しか見えない。**裏だけなら、1本ずつ抜いて裏返すことになる**
2. **24px / .05em**: 出品写真（1200px）では測れない。**現物の横にスマホを並べる以外に手段がない**

---

## 型番の逆引き（2026-07-18）

```
棚で箱を拾う → SLPS-00533 → 打ち込む → AUBIRDFORCE と分かる
```

Discogsのカタログ番号検索と同じ。**一方通行が双方向になった。**

`index.html` に5か所（DATA行には触っていない）:

- `codeKey()` = `NFKC → 大文字 → 英数字以外を落とす`（**照合用の内部正規化。表示には使わない**）
- `CODEKEYS` = 行ごとに `r[3].split(';').map(codeKey)` を起動時1回
- 照合はハイブリッド: `HAY[i].includes(lo)` を必ず先に見て、外れたときだけ
  `qk.length >= 4 && CODEKEYS[i].some(c => c.includes(qk))` を足す
- 型番の表示: 13px → **24px / letter-spacing .05em**。値は `r[3]` の生のまま
- ラベル: `CART CODE` → **`PRODUCT CODE`**（PS1はカセットではない。ディスク機は4,946行）

**実測（旧＝変更前 / 新＝変更後。同一DATAのmd5照合済み）**

| | 旧 | 新 |
|---|---|---|
| `SLPS-00533` | **0** | **1** |
| `slps00533` | **0** | **1** |
| `SHVC VU` | **0** | **1** |
| `SHVCVU` | **0** | **1** |
| `BANHDR` | **0** | **1** |
| `SLPS 00533` / `SHVC-VU` | 1 | 1 |
| `Chrono Trigger` | 3 | 3 |
| `ドラゴンクエスト` | 42 | 42 |
| `GAME` | 1063 | 1063 |

**効いているのは書式ゆれの吸収だけ。** 生の値どおり打てば、変更前から通っていた
（`HAY` に型番が最初から入っていた）。

### 「型番っぽさ」は測れない

型番の形は、機種でも発売元でも揃っていない:

| 機種 | 生の型番 |
|---|---|
| ファミコン | `BAN-HDR` `SQF-FO` `HVC-ZL` `CAP-6V` ← **接頭辞が発売元ごとに違う** |
| スーファミ | `SHVC-VU` と `SHVC-AO5J-JPN` ← **2つの採番体系が同居** |
| ゲームボーイ | `DMG-TR-USA` `CGB-BD2J-JPN` |
| PS1 | `SLPS 00533` `SLPM87242` `SLUS-01041;SLUS-01080` |
| セガサターン | `T-9104G` |
| PS4 | `KTGS-40401;PLJM-16080` ← メーカー独自番号 + ソニー公式番号 |

`/^[A-Z]{1,5}\d/`（英字→数字）は、**狙っている機種の型番を過半数落とす**:

| 機種 | 型番 | regex非該当 |
|---|---|---|
| スーパーファミコン | 656 | **506 (77%)** |
| ゲームボーイ | 144 | 101 (70%) |
| ゲームボーイアドバンス | 239 | 157 (66%) |
| ファミリーコンピュータ | 386 | 197 (51%) |
| セガサターン | 535 | 151 (28%) |
| プレイステーション | 2,487 | 25 (1%) |

**英題到達率が一番高いFC(71%)・SFC(68%)が、一番落ちる。**

採用したのは `qk.length >= 4` だけ。**判定ではなく、爆発を防ぐ床**（3文字だと `JPN` が
数千件に部分一致する）。捨てるのはPC-8801の3桁13件＝狙っていない機種のみ。
ノイズ検証（英単語40個）で `KING` `GAME` `MOON` が各1件。ハイブリッドなので**増えるだけ**。

**`isCode` という変数を作らなかった。** 名前を残すと、半年後に「ここで型番を判定している」
と誤読する。**測れないものを判定しようとすると、n=1の形が仕様になる。**

---

## ラテンのタイトルは、Buyeeで当たる（実測 2026-07-18）

```
biohazard4 ゲームキューブ       → 22
バイオハザード4 ゲームキューブ  → 22    ← 同数。同じ22件が両方を含む
Bahamut Lagoon スーパーファミコン → 100
Romancing Sa・Ga スーパーファミコン → 52
```

**出品者は箱に書いてある文字を書き写す。`schema:name` は箱の印字そのもの。**
だから噛み合う。`CHRONO TRIGGER`(251) ≒ `クロノ・トリガー`(250) は偶然ではなく**構造**。

**「ラテンだから当たらない」は誤りだった。** リンクを出す行のうち完全ラテンは
2,534行（29.5%、PS1が最多で736）。**パイプラインを直す必要はない。**

## 収益化について

### 構造的な問題

```
店内の観光客  → 棚の前で困っている（痛みが最大）→ レジで買う → 収入 0円
海外の完集家  → 家で探している           → 通販で買う → 収益化できる
```

**痛みが一番大きい人からは1円も入らない。** 収益が立つのは海外の完集家だけで、
その人たちのためのリンク（Buyee）が、今はまだ機能していない。

### アフィリエイト

- ~~BuyeeのアフィリエイトはA8.net経由にあるらしい（2022年の第三者情報。**未確認**）~~
  → **2026-07-28 確認済み。** Indoleads（英語・全世界・CPS）にBuyee公式アフィリエイトあり。**Indoleadsを採用**し publisher登録・source登録済み（詳細は末尾 2026-07-28 セクション）。A8.net にも掲載は残る（第三者集計・2022）。
- A8.netの会員登録には審査がない。サイトURLが無くても登録できる
- ただし**個別プログラムとの提携審査は別**。広告主が審査する
- 落ちた後の再申請可否はプログラムごとに違う。**実質一発勝負と考えるべき**
- 中身のないページにリンクだけ貼った媒体は落ちやすい
- **申請前に、英語で「これは何で、なぜ必要で、どう使うか」を説明するセクションが要る**
- 日本の広告表示ルール（アフィリエイト広告である旨の明示）への対応も必要。要件はA8.netの規約で確認

**今の判断: まだ申請しない。** 訪問者0でIDだけ取っても収益は0円。
サイトに中身を足し、人が来てから、一発しかない審査を使う。

### 値段について

**「スクレイピングは法的・技術的に無理」→「だから値段には道がない」は誤りだった。**

Buyeeのリンクを叩けば、その先に値段が並んでいる。
ブックオフで `¥2,980` の値札を見た人が、2タップで「ヤフオクなら箱説付きで980円」と分かる。
**スクレイピングは要らない。リンクの先が答えを持っている。**

---
## 店舗フィールド調査（3店・現物）— 棚の並び方
**MADBもWikidataも世界中の誰でも落とせる。棚がどう並んでいるかだけは、店に立った人間しか知らない。
これはexportできない資産で、このツールの一番のアドバンテージ。** 3店を回って分類を実測した。
### 確定した3つのモデル
| 店 | 分類方式 | 粒度 |
|---|---|---|
| **ブックオフ** | カセット機=**五十音** ／ ディスク機=**ジャンル＋五十音**（第1キー=ジャンル, 第2キー=読み五十音） | 行（あ行・は行…） |
| **駿河屋** | **全機種=五十音**（機種を問わず1方式） | 行 |
| **スーパーポテト** | **全機種=五十音**、同シリーズは番号順 | **一字**（あ/い/う/え/お…） |
- **案内の核は、どの店も「読みの先頭文字」に帰着する。** ジャンルが要るのはブックオフのディスク機だけで、
  `schema:genre`（全51,827行=100%）で完備。残りは全部、読みに帰着する。
- **粒度は「一字」で持てば全店対応できる。** 行でまとめる店（ブックオフ・駿河屋）は「あ→あ行」に丸めるだけ。
  一字の店（スーパーポテト）は丸めない。**行しか持たないと一字の店に粗すぎる。だから読みは"行"ではなく"一字"で持つ。**
- **ラテンも読みに直して五十音に混ざる。アルファベット専用棚は無い**（ブックオフのファミコンで確認）。
  実例: `SDバトル大相撲`＝エスディー→**あ行**の籠にあった。`ATLAS FALLEN`＝ブックオフPS5のRPG棚で**あ**の位置。
  駿河屋DCの`あ行`に`アクアゾーン`と並んで`SUIGETSU`。**ラテンにも読みが要る。**
- **「PSはシリーズでまとめ」は五十音の中の話**（スーパーポテトPS1で確認）。`ポポローグ→ポポロクロイス物語→物語II`、
  `ワールドスタジアム3→4→5` が固まるが、全体は `ボ→ポ→ホ→マ→ミ→リ→ル` と読み五十音で流れている。
  第1キー=五十音、第2キー=同シリーズ番号順。案内（「ま行のあたり」）はこれでも成立する。
### 撮った現物（証拠）
- ブックオフ: ファミコン籠（`あ行`の中身=え/オ/イ/SD、`は行``ま行`が複数籠）、PS3/PS4 ジャンル棚（`アドベンチャー``スポーツ``ロールプレイング`の仕切り、RPG棚に`GUNVOLT→ATLAS FALLEN→イース→ヴァレット`=読み順）
- 駿河屋: PS Vita(`か行``た行`)、セガサターン/ドリームキャスト(`た行`〜`わ行``あ行`)、スイッチ(`あ行`) …全機種五十音
- スーパーポテト: PS1(五十音)、N64(`は行``ら行``わ行`)、ゲームボーイ(`い``お``し``す`=一字)、スーファミ(`う``え``か`=一字)
### まだ観測していない（罠15: n=店3）
- **ハードオフのジャンク籠**が無分類か（"探すには型番の逆引きが要る店"かもしれない）。今回撮れず。
- **まんだらけ**（新宿の大規模店は分類が違う可能性、と本人談）。今回撮れず。
- ジャンル棚の中の第2キーが全店で「読み五十音」か（ブックオフのディスクで1本ずつ読めば分かる）。
---
## カメラ機能 — 測って、OCRは却下。バーコードに役割を移す
### 背表紙OCRは、実際に撮れる解像度では読めない（Tesseractで2回実測）
- **型番（`eng`）**: PS1棚全体の写真で `SLPM 86500~1` 等を掛けて **0件**。前処理（拡大・二値化・
  グレースケール・オートコントラスト・PSM 6/7/8/11/13）を一通り変えても読めない。原因は解像度で、
  棚全体だと型番の文字高が**約15px**＝Tesseractの下限（20〜30px）を下回る。
- **タイトル（`jpn_vert`）**: `モンスターハンター：ワールド` の背表紙1本を3倍に拡大して掛けて、
  出たのは `て闘っ和にmo`。回転版でも `1O引Aaーーペke`。**読めていない。**
  原因: ビニールの反射、背表紙の幅の狭さ、Tesseractの学習が「文書」で背表紙のロゴ組ではないこと。
- **これは本人が「これ以上寄れない」と言った、その解像度での結果。** Tesseract.js は同じエンジン。
  → **「ブラウザ・無料・権利問題なし」のOCRは、この用途では不成立。** 型番もタイトルも読めない。
- 公平のための但し書き: ラテンの大きいタイトル(`DEATH STRANDING`)なら読めた可能性はあるが、それは客も目で読める。
  クラウドOCR(Google Vision)なら読めるはずだが、有料・非ブラウザ・APIキー必須で設計思想から外れる。
### 媒体で、カメラが解く問題が割れる（現物で確認）
- **ディスク機（PS1〜5、たぶんサターン・DC・GCも）**: 背に**タイトルと型番の両方**が、
  棚挿しのまま正面を向いて印刷されている（背の下端に `SLPM/SLPS/BLJM/PLJM/ELJM …`）。全世代で確認。
- **カセット機（FC・SFC・GB）**: 籠にラテ入れで、**背表紙のタイトルは存在しない**。見えるのは
  カセット上端の握り部・店ラベル・仕切り板だけ。型番は店ラベルかカセット面（めくる/抜く）。
- **つまり「背表紙のタイトルを読む」は、狙っている機種（カセット機）では対象そのものが無い。**
### カメラの役割: 「棚を探す」ではなく「1本を抜いて確定・価格に飛ぶ」
- OCRが弱い以上、カメラで棚を舐めて探すのは無理。棚を探すのは**五十音ガイド**（下記）が解く。
- カメラの現実的な出番は、手に持ったケースの**バーコード**。`schema:gtin`（JAN）を型番あり12,208行の
  **70.1%**、狙うレトロ機種はほぼ100%。**バーコードは機械可読で解像度に崩れない。**
- ただし棚挿し時はバーコードが裏を向いている（裏表紙）。**だから「探す」ではなく「抜いた1本を確定して
  Buyeeの値段に飛ぶ」瞬間の道具。** ブラウザのバーコードライブラリで実装可（実装前にカメラAPIの現状要確認）。
---
## ★重要な訂正: 五十音の「読み」は、既に手元にあった（@ja-hrkt）
**フィールド調査中、`metadata301.ttl` の述語をgrepで数えて「ヨミのフィールドは無い」と結論した。
これは誤り。読みは既にある。** `schema:name` の2つ目の値＝`@ja-hrkt` タグ付きのカナ読みで、
**充足率97.1%**。これは row[9]（ローマ字）の生成元として、7/19に既にノートへ記録済みだった。
- なぜgrepで見えなかったか（罠#31）: `grep -oE '^\s+[a-z]+:[a-zA-Z]+'` は**述語トークン**を数える。
  `schema:name "漢字", "カナ"@ja-hrkt ;` は述語 `schema:name` が1回、値が2つ。2つ目の値は継続行で
  `"カナ"@ja-hrkt` から始まる＝**述語ではないのでgrepに映らない。** 述語census は「2つ目の値」を構造的に見逃す。
- **五十音の行は、@ja-hrkt読みの先頭1文字で出る。** カナなので決定的。`真・三國無双`→`シンサンゴクムソウ`→さ行。
  漢字始まりも解ける。97.1%が読みを持つ。
- **ラテン始まりも、読みを持っている可能性が高い。** ラテン始まりは title_ja の37.3%。もし読みが非ラテンだけを
  覆っていたら上限は約62.7%のはず。**実測97.1% > 62.7% なので、読みはラテン始まりの多くにも及んでいる。**
  → `ATLAS FALLEN` 等にMADBのカナ読みが付いている見込み。**要実測（下記）。**
### だから、棚案内に必要な作業（読みは"探す"から"検証する"に変わった）
1. **build_master.py に「五十音初字」列を足す**: @ja-hrkt読みの先頭カナ→五十音行（一字で持つ）。
   row[9]はローマ字化済みなので、初字は読みの生カナから取る（build内で@ja-hrktに直接アクセスできる）。
2. **@ja-hrktの充足を、表示タイトルの字種別に割って測る**（特にラテン始まり・漢字始まり）。
   ラテン始まりで97%近く付いていれば、5店・全機種の棚案内がほぼ成立する。
3. **初字→行が、店の並びと一致するか**（罠#29の懸念: 読み≠表示）。ただし罠#29の実例
   （`エスカ&ロジー`の読みが`アトリエ…`から, `仁王2`の読みが`ニオウ…`）は、いずれも**行としては一致**する
   （エ/ア=あ行, に=に行）。**接頭辞の入れ替わりは行を跨がないことが多い**＝行レベルでは害が小さい見込み。要確認。
### title_ja 先頭字の内訳（2026-07-19、参考値）
`master_final.csv` の row[2]（title_ja=箱の印字 `schema:name`）の先頭字:
| 割合 | 字種 |
|---|---|
| 32.8% | カタカナ |
| 5.6% | ひらがな |
| 37.3% | ラテン |
| 23.5% | 漢字 |
| 0.8% | その他 |
- **これは「表示タイトルの先頭字」であって、店が並べる基準（読み）ではない。** @ja-hrkt読みが
  取れた今、この内訳は二次的。棚の行は「表示の先頭字」ではなく「読みの先頭字」で決まる
  （罠26と同型: 表示≠検索/整列キー）。
- 測る途中で **row[1]（title_en）と row[2]（title_ja）を取り違えた**（罠#32）。最初 row[1] で測って
  「空57.3%・ラテン42.3%」が出て、これが既知の英題充足42.7%と噛み合ったため誤りに気づかなかった。
  **結論だけ既知の数字と合うと、誰も気づかない**（罠19・22の再来）。
---
## パイプラインの罠（追加）
### 31. 述語のgrep census は「2つ目の値」を構造的に見逃す（2026-07-19）
`grep -oE '^\s+[a-z]+:[a-zA-Z]+' | uniq -c` で述語を数え、`schema:name` が1レコード1回なので
「読みのフィールドは無い」と結論した。**だが `schema:name "漢字", "カナ"@ja-hrkt ;` は述語1回に値2つ。**
2つ目の値（@ja-hrkt読み、97.1%）は継続行で `"..."@ja-hrkt` から始まる＝述語トークンではなく、grepに映らない。
**しかも読みが在ることは、7/19のノートに既に書いてあった**（row[9]の生成元）。
述語censusで「無い」と出ても、それは「述語として無い」だけ。**値・言語タグ・継続行は別に見る。**
罠#1（grepのアンカー）・罠#23（grep -A は前方しか見ない）の、値レベル版。
### 32. row[1]/row[2] を取り違え、既知の数字と噛み合って気づかなかった（2026-07-19）
title_ja の先頭字を数えるつもりで row[1]（=title_en）を渡した。出た「空57.3%・ラテン42.3%」が
既知の英題充足42.7%と一致したため、間違いに気づかなかった。ヘッダー行も飛ばしていなかった。
**「解決済み/既知」と噛み合う出力は、検証をすり抜ける。** 罠#19・#22と同じ形。
対処: 列は必ずヘッダーで確認（`head -3`）。`next(r)` でヘッダーを飛ばす。**出力が既知の数字と一致したら、
まず疑う。**
---
## ★未解決の判断（追加）
### 6. 店ごとに棚案内を変えるか（要・設計判断）
3店で分類が違う。同じPS4ソフトが、ブックオフでは「RPG棚の"あ"」、駿河屋・スーパーポテトでは「あ行の棚」。
**ツールが1つの答えしか返せないと、片方の店で外れる。** 店を選ばせる（Book-Off / 駿河屋 / Super Potato /
ハードオフ…）か、両方併記するか。想定ユーザー（観光客）が真っ先に入るのはブックオフ・ハードオフ。
まず2〜3店に対応し、読み(一字)＋ジャンル(`schema:genre`)を持てば、表示の出し分けだけで済む。
### 7. @ja-hrkt初字を、五十音行として採用してよいか（要・実測）
読みは97.1%あるが、(a)ラテン始まりでの充足、(b)初字→行が店の並びと一致するか（罠#29）、を測っていない。
測ってから列に採用する。**「読める行にだけ一字を出し、出せない行は行なし」**（罠8の思想）が最悪ケースの線引き。
---
## 次にやること（更新）
### 棚案内（五十音＋ジャンル）— データはほぼ揃っている
1. **@ja-hrkt初字の充足を字種別に測る**（特にラテン始まり）。97%近ければ全機種成立。
2. **build_master.py に五十音初字列を追加**（読みの生カナの先頭→行、一字で保持）。
3. **UIに店選択＋「◯◯ジャンルの棚の "あ" のあたり」表示**。ジャンルは `schema:genre`（100%）。
4. 初字→行が店と一致するかを、撮った写真の仕切り板と数本の中身で突き合わせる（罠#29の実害を測る）。
### カメラ — OCRは却下、バーコードのみ
- **背表紙OCRはやらない**（2回実測で不成立、上記）。
- バーコード（`schema:gtin` 70.1%、レトロ機ほぼ100%）を「抜いた1本の確定＋価格」に。実装前にカメラAPI現状要確認。
---
## この日の教訓（7/19・店舗調査）
**机で3回切り分けたモデルを、棚が毎回1段細かくした。**
| 予想 | 実測 |
|---|---|
| 五十音がこのツールの棚案内だ | 五十音は**カセット機だけ**。ディスク機はジャンル（ブックオフ） |
| ディスク機はジャンルだけ出せば読み不要 | ジャンルの**中が読み五十音**。結局読みが要る（ブックオフPS5のRPG棚） |
| 粒度は「行」でいい | スーパーポテトは**一字**。行では粗すぎる |
| ラテンは客が読めるからタダ | 店は**読みで並べる**（SD→エスディー→あ行）。読めることは籠に導かない |
| カメラで背表紙タイトルを読める | 型番もタイトルも**この解像度では0**（Tesseract実測） |
| 読みのフィールドは無い | **@ja-hrktに97.1%あった**。述語grepが2つ目の値を見逃していた |
**現地は、机上の分類をいつも1段深くする。そして「無い」と結論する前に、自分のノートと、値の在り処（述語ではなく値）を見る。**

---

## この日の教訓（7/19）

**予想は、また外れたり当たったりした。** そして本当の問題は、前2日と同じく「解決済み/実装済み」と思っていた所に隠れていた。

| 予想 | 実測 |
|---|---|
| build_master は C+M 二刀流で結合している | **M直引きだけだった**（罠#19の再来。~56%は計画） |
| Hard-Off もカタカナでいい | **中黒で4530件に暴発**（クロス→マクロス） |
| Book-Off も中黒が危ない | **カタログなので無事**（19件、全部関連） |
| Path B で MGS3 が救える | 救えるが**1/4が誤タイトル**。却下 |
| ローマ字は漢字で詰む | **`@ja-hrkt` 読みが97.1%で解いた** |
| Wikidataダンプは 2¹⁵−1 で切り捨てられている | **偶然だった**（比べたら無傷） |
| grep -A で embodimentOf が見える | **隣のブロックを拾っていた** |

**そして、隠れていた本当の問題:**

- ノートとコードの「C+M二刀流~56%」は、**実装されたことが一度もなかった**（罠#19）。
- 内部検索(HAY)に row[8] が入っていなかった → `キャッスルヴァニア` で「Castlevania 白夜の協奏曲」に**当たらなかった**。
- Coworkに委譲したら丁寧に着地したが、**Coworkが長音の穴(ryu/ryuu)を見つけた**。自分が組んだ変換(romajiの長音)が、検索で取りこぼしていた。

**測る前に決めない。店ごとに実測する。手元のデータ（@ja-hrktの読み）を、外に探しに行く前に見る。**
**そして、自分が書いた記録も、自分が組んだ変換も疑う。**

---

## この日の教訓（7/18）

**予想は10回中9回外れた。** 前日の6回中5回より、悪くなっている。

| 予想 | 実測 |
|---|---|
| 画像は集まらない | **31.1%が型番で完全一致で繋がる**（権利で不採用） |
| カセット機の型番が逆引きから丸ごと外れる | 書式ゆれ耐性だけの話だった |
| ゲームボーイ・GBA・DSはBuyeeで危ない | **76 / 78 / 103。全部通る** |
| NINTENDO 64・SEGAマーク3は危ない | **23 / 2。通る** |
| 無名タイトルは単独でも絞れている | **`ゴルフ` 1,498,759件** |
| ラテンのタイトルはBuyeeで当たらない | **22 = 22。完全に同数** |
| 型番リンクは役目を終えた | **28件を1件にする唯一のリンク** |
| ラテンはPS4に集中している | **PS1が最多**（`[:12]` を見ていた） |
| Vitaの表記ゆれがプルダウンを壊している | 英語側で既に畳まれていた |
| 出品者は箱の文字を書き写す | **当たった（1勝）** |

**そして、本当の問題は「解決済み」と思っていたところに隠れていた（前日と同じ）:**

- `HAY` に型番は最初から入っていた → 直そうとしたものは、半分壊れていなかった
- ノートの「8フィールド」は現物と違った → **次の一手が、存在しないフィールドを指していた**
- `build_master.py` のコメントに、訂正済みの誤りが現役で残っていた

**測る前に決めない。手元のデータを見る前に、外に探しに行かない。**
**そして、自分が書いた記録も疑う。**

---

## この日の教訓

**実測が予想を6回中5回覆した。** 机上で組んだ心配のほとんどは、実在しない問題だった。
そして本当の問題は、「解決済み」と思っていたところに隠れていた。

- 「2026年版を待つ」→ 毎月出ていた
- 「Wikidataから発売元を取る」→ MADBが最初から持っていた
- 「中黒で0件になる」→ 無視される
- 「正式名称では引けない」→ ほぼ全部引けた（ゲームキューブを除く）
- 「値段には道がない」→ リンクの先にあった
- 「カメラは難しい」→ バーコードなら現実的
- 「Buyeeは同義語を解決する」→ n=2からの一般化。**誤り**

**測る前に決めない。手元のデータを見る前に、外に探しに行かない。**

---
## カメラ機能（バーコード）— 実測してGO（2026-07-19）
3つの関門を全部測った。全部抜けた。**OCRは2回測って却下、バーコードは3回測って合格。**
「将来の候補: バーコード検索（実現可能性は高い）」を、現行実装へ格上げする。
| 関門 | 結果 |
|---|---|
| データ（gtin充足） | 13桁JAN、レトロ機ほぼ100%、正規化1手 |
| ブラウザ構成 | BarcodeDetector（Android）＋ ZBar-WASM（iOS）の二段 |
| 光学（ビニール越し） | **全体写真のまま一発。25%縮小まで耐える** |
| 誤読リスク | 読めなければ空を返す。誤ったJANは出ない |
| 結合列 | `data_line.js` に11列目（JAN）を足すだけ |
### 1. データ: `schema:gtin` は13桁JAN、レトロ機ほぼ100%（実測）
型番あり12,208行のうち gtinあり 8,562行 = **70.1%**。機種別:
| 機種 | gtin | | 機種 | gtin |
|---|---|---|---|---|
| プレイステーション | 98% | | **PS5** | **0%** |
| プレイステーション2 | 99% | | **Switch** | **4%** |
| プレイステーション・ポータブル | 99% | | **PS4** | **7%** |
| ニンテンドーDS | 99% | | **PC-8801** | **21%** |
| スーパーファミコン | 97% | | | |
| プレイステーション3 | 97% | | | |
| ゲームボーイアドバンス | 96% | | | |
| セガサターン | 94% | | | |
| メガドライブ | 81% | | | |
| ファミリーコンピュータ | 78% | | | |
- **書式は素直な13桁。** 先頭 `49`/`45` は日本のJAN。1件だけ `T4959067977403`（14桁・T始まり）＝
  型番の書式ゆれと同型。→ **照合正規化: `T` を落とし、`^\d{13}$` に合う行だけ結合。表示は不問。**
- **充足が媒体の話と噛み合った。** PS5=0 / PS4=7 / Switch=4 は狙っていない新機種なので問題なし。
  **古いディスク機がほぼ100%**という一番きれいな形。カメラが効く範囲＝ターゲット（レトロ）と一致。
### 2. ブラウザ: BarcodeDetector は iPhoneで動かない → 二段構え（現状確認済み）
- ネイティブの **BarcodeDetector API は Chrome系（Android）のみ。Safari・iOS（WebKit）・Firefox は非対応**。
  iPhoneでは依存すると黙って失敗する（2026時点。MDN / caniuse / dev.to の2026-02記事で確認）。
- **ターゲット＝外国人観光客はiPhone比率が高い。** ネイティブだけに賭けると一番の客層で沈黙する。
- フォールバック: **ZBar を WebAssembly 化したもの**が iOS で実用（ネイティブ近い性能との報告）。
- 構成: **BarcodeDetector（Androidで速い）＋ ZBar-WASM（iOS用）の二段。** 片方では足りない。
- 代償: WASMを積む＝**1ファイル完結が崩れる**（罠10とのトレードオフ）。ただしOCRと違い
  バーコードは実際に読めるので、崩す価値がある。
### 3. 光学: ブックオフのビニール越しに一発（実測）
- 題材: `龍が如く8`(PS5) の裏面JAN `4974365837785`。店のシュリンク越し、手持ち接写。
- **OpenCVの barcode detector で、切り出さず全体写真のままデコード成功 = `4974365837785`**（印字と完全一致）。
- 縮小して床を測定:
| 縮小 | サイズ | 結果 |
|---|---|---|
| 100% | 2256×4000 | OK |
| 50% | 1128×2000 | OK |
| 25% | 564×1000 | **OK** |
| 15% | 338×600 | ×（空） |
- **バーコード横幅 約600px → 約150pxまで耐える。** OCRの型番が15pxで壊れたのと**桁違いに寛容**。「向ければ読める」。
- **落ち方が素直: 15%は誤読ではなく空(`''`)を返す。** 誤ったJANを出さない＝誤タイトルに飛ばない
  （OCRの `ソ`/`ン` 誤読と違う）。実装では「読めません、もう少し寄って」と言えばいい。
### 媒体の切り分け（確定・再掲）
- **ディスク機（必ずケース入り）**: 裏面JAN → バーコードが効く。
- **裸カセット（ブックオフの籠のFC/SFC/GB）**: 箱ごとJANが無い → 型番の逆引き（既存・打ち込み）に落ちる。
- gtinの数字上はFC78/GB94/SFC97でも、それは**箱付きの話**。ワゴンの裸には届かない。
### 正直な注記
- 今回の題材 `龍が如く8` はPS5で、MADBのPS5 gtinは0% → **このタイトル自体はデータに無い**。
  今日測ったのは**光学（読めるか）だけ**。結合（gtinがデータにあるか）はレトロ機で別途確認済み。
- **実装後の通しテストは、PS1かPS2の実物1本で**（光学→結合→表示が繋がるか）。
- 表面のバーコードは店のSKU（`0020201491`）。**JANは裏面**。スキャン対象を間違えない。
### カメラの役割（確定）
**「棚を探す」ではなく「抜いた1本を確定してBuyeeの値段に飛ぶ」。** 棚を探すのは五十音ガイドが解く。
---
## 次にやること（カメラ・更新）
1. **build_master.py に JAN列（row[10]）を追加**: `schema:gtin` → `T`落とし → `^\d{13}$` のみ採用 → 13桁。
   五十音初字と同じ末尾追加。
2. **`data_line.js` 再生成** → `swap_data.py` で貼り込み（差分ガード `[0]〜[6]`、末尾追加なので通る）。
3. **最小のスキャナUI**: `BarcodeDetector` があれば使い、無ければ ZBar-WASM。
   読めたJANでデータを引き、既存の詳細（英題・型番・Buyee）へ飛ばす。読めなければ「もう少し寄って」。
4. **通しテスト**: PS1かPS2の実物1本で、光学→結合→表示が繋がるか。
（五十音ガイドの `@ja-hrkt初字` 列と、このJAN列は、どちらも build_master.py への末尾列追加。
まとめて1回の再生成で入れられる。）
---
## この日の教訓（カメラ）
**「カメラ」で一括りにしていたが、OCRとバーコードは別物だった。**
| | OCR（文字） | バーコード |
|---|---|---|
| 崩れ方 | 解像度で崩れる（型番15pxで**0**） | 機械可読で崩れない（**150pxまで**） |
| 誤り方 | 誤読する（`ソ`/`ン`） | 空を返す（誤JANは出ない） |
| ブラウザ・無料 | Tesseract.js＝不成立 | BarcodeDetector＋ZBar-WASM＝成立 |
「カメラは難しい」でも「カメラでOCR」でもなく、**「カメラでバーコード」が答え**だった。
そして題材が何順・何機種でも、**光学の可否は題材に依らない**（PS5で光学だけ測り、結合はレトロで別途）。
測って初めて切り分いた。**OCR 2回×、バーコード 3回○。**

---
## 今日（7/20）やったこと — カメラ実機テストと「無し」の解明
| | |
|---|---|
| 実機 | **Android実機でブックオフのビニール越しにデコード成功**。合成テストが実店舗で再現 |
| 「無し」調査 | 店で5件スキャンして持ち帰り、grepで原因を**3層**に分けた |
| 発見 | **JAN=確定・型番=救済**。同じゲームでも版ごとにJANが違う。型番は版をまたぐ |
| UX | not-foundメッセージを「読めた＋3つの理由＋型番へ誘導」に書き換え（実装済み） |
---
## カメラは実機で動く（Android、ブックオフ店頭）
- **ビニール越し・手持ちで、全体写真のままデコード成功。** 合成テスト(OpenCV, 7/19)が実機で再現された。
- `龍が如く8`(PS5) JAN `4974365837785` → 正しく「無し」（PS5はgtin 0%）。**デコード→照合→not-found描画まで、実機で一連が動いた。**
- 手入力欄で `AUBIRDFORCE`(SLPS 00533) 等が実データでカード表示（結合→描画は7/19に確認済み）。
- **正直な注記**: 「レトロの**収録済み**ディスクを実機で読んで、その場でカードが出る」単発の一連は、まだ撮っていない。
  ただしそれは(a)実機デコード成功 と (b)結合→カード描画 という**2つの検証済み半分の合成**にすぎない。単発の実地確認は次の機会でよい。
---
## 「無し」の正体は3層だった（店で5件・実測 → 帰宅後grep）
観光客が最も探すレトロのディスク機で「無し」が続き、構造的な穴かと疑った。5件持ち帰って `master_final.csv` と `metadata301.ttl` の両方をgrepした結果:
| タイトル | 機種 | スキャンJAN | 型番 | ttl | master | 判定 |
|---|---|---|---|---|---|---|
| ニーア オートマタ | PS4 | 4988601009638 | (PLJM84079※) | — | 0 | **層2: 範囲外**(PS4 gtin 7%) |
| ポケモンダッシュ | DS | 4902370509724 | NTR-P-APDJ | 0 | 0 | **層1: MADBに無い**(型番も空) |
| スターウォーズ バトルフロント | PS2 | 4938833007162 | SLPM66148(BEST/再版) | 0 | 0 | **層1: MADBに無い** |
| ギャラクシーエンジェル | PS2 | 4510417030393 | SLPM65254 | 0 | 0 | **層1: MADBに無い** |
| 悪魔城ドラキュラ 蒼月の十字架 | DS | 4988602121568 | NTR-P-ACVJ | 1 | 0 | **層3: 別JANで収録あり** |
- **層1（3件）: MADBにパッケージごと無い**。JANも型番(`APDJ` `SLPM66148` `SLPM65254`)もttlに一切ヒットしない。
  中古実店舗の全在庫（廉価版・再版・マイナー作品）を文化庁DBが持つことはない。**データソースの限界。救えない。**
- **層2（1件）: 範囲外機種**(PS4)。gtin 7%。狙っていない新機種なので「無し」で正しい。ゼルダTOTK(Switch)と同型。
  ※ニーアの店ラベル型番 `PLJM84079` は実型番(`PLJM-16xxx`系)と違う＝店の内部番号。今は無視。
- **層3（1件）: 収録はあるが、スキャンしたJANが違う**（下記）。**型番なら救える**。
---
## ★重要な発見: JAN=確定, 型番=救済（同じゲームでも版でJANが違う）
蒼月の十字架を掘って分かった:
- スキャンしたJAN `4988602121568` は `master_final` に無い。だがttlには**1回**出る＝別レコードに在る。
- `master_final` の該当作品（型番 `NTR-ACVJ-JPN-2` = `Castlevania: Dawn of Sorrow`）が持つJANは **`4988602156287`**＝**スキャン値と別物**。
- **つまり: 同じゲームでも、版（初版・再版・廉価版）ごとにJANが振り直される。** 店の個体のJANと、MADBが収録した版のJANが、食い違った。
  （どちらが初版でどちらが再版かの断定は保留。確かなのは「同一作品・別JAN」という事実。罠19: 断定は事実だけに。）
**JANの完全一致が、この行を救わなかったのは正しい。**
- JANが違う＝別パッケージ（版）。相場も状態も違う。「近いから」で初版の情報を再版に出すのは罠27（誤った値より無いほうがまし）。
- **JAN照合は厳格でよい。誤って別版に飛ばさない。**
**一方、型番は版をまたぐ。**
- `codeKey()`（英数字以外を落とす正規化）＋部分一致で `NTR-ACVJ`(打ち込み) が `NTR-ACVJ-JPN-2`(MADB) に当たる。
- 版番号サフィックス(`-2`)を除けば型番の頭は共通だから。**実測: `NTR-ACVJ` → `Castlevania: Dawn of Sorrow`（英題つきで到達）。**
- 符合だが、これは初回シミュレーションでユーザーが例に挙げた **Castlevania シリーズそのもの**。想定ユーザーが探す作品が、型番経由で救われた。
**結論: JANは「確定」(厳格な完全一致)、型番は「救済」(版をまたぐ寛容な部分一致)。役割が違う。カメラで外れたら型番へ。**
これは既存機能で成立している（型番の部分一致・逆引きは実装済み）。新ロジック不要。
---
## not-foundメッセージ（実装済み 2026-07-20）
`index.html` の `showByJan()` の not-found 分岐だけ差し替え（既存のスキャナ・検索・DATAは無傷、構文チェック済み）。
今日の3層構造をそのまま文面にした:
1. **「読めた」と明示** — バーコードは機能した（壊れていない）
2. **なぜ無いか3つ** — 再版/廉価版・新機種・未収録
3. **型番を打て、と具体例つき**（`SLPS-` `NTR-` `SHVC-`）— 今日実証した救済経路へ誘導
これで「読めたのに何も出ない＝壊れて見える」を防ぐ。JAN(確定)と型番(救済)の二段が、UIの言葉として繋がった。
---
## パイプラインの罠（追加）
### 33. スキャンの「無し」を「gtin欠け・型番で救える」と誤診断しかけた（2026-07-20）
「無し」の原因を、実測前に2つ予想した。**両方外れた。**
- 予想「MADBはタイトルを持つがgtinだけ欠けている」→ 実測: 3/5は**パッケージごと無い**（型番もttlに無い）。gtin欠けではない。
- 予想「蒼月が落ちたのは `a class:GamePackage` でないクラス除外」→ 実測: クラス宣言は分割範囲外。真因は**スキャンJANがMADB収録版のJANと別物**（`121568` vs `156287`）。
- 予想「`-2`サフィックスや `codeKey` の不一致が原因」→ 実測: 単に**別JAN**。型番 `NTR-ACVJ` は版をまたいで当たる。
**教訓の核**: JANは版ごとに振り直される。だからJAN完全一致は「同一作品の別版」を救わない——そしてそれは**正しい挙動**（別物を混ぜない、罠27）。救済は型番が担う。
grep(実測)の前に、原因を機構(gtin/クラス/正規表現)で決めつけた。罠12(通信の死をデータ不在に化けさせた)・罠31(述語censusの見逃し)と同じ「機構で決めつけ、値を見なかった」形。

**（2026-07-21 訂正）** タイトルでgrepし直したところ、ポケモンダッシュ・バトルフロント・ギャラクシーエンジェルは**すべてMADBに在った**（型番とJANだけを引いてタイトルを引いていなかった＝罠自身が「機構で決めつけ値を見ない」を踏んでいた）。3件とも層1ではなく**層b**（型番が無いのでフィルタで落ちた）で、型番フィルタ緩和で救えた。実測5件のうち「救えない層1」は**0件**。「中古の全在庫は持てない＝限界」は原理としては真だが、実測した5件はどれも限界の例ではなかった。
---
## この日の教訓（7/20・カメラ実機と「無し」調査）
| 予想 | 実測 |
|---|---|
| カメラは実機・ビニール越しで読めないかも | **Androidで一発。合成テストが店頭で再現** |
| 「無し」はカメラの不具合か | **カメラは正常。読めて照合して正直に「無し」と言った** |
| 「無し」の主因はgtin欠け（型番で救える） | 5件中3件は**MADBにパッケージごと無い**（救えない） |
| 蒼月はクラス除外で落ちた | **スキャンJANが収録版と別物**（版でJANが振り直される） |
| カバレッジは上げれば埋まる | 中古の全在庫（再版含む）をMADBが持つことはない。**限界を受け入れる** |
| JAN照合が厳格すぎるのが問題 | **厳格でよい**。別版に誤結合しないのが正しい。救済は型番の役目 |
**カメラは機能・データ・UXの3つとも閉じた。** 穴はデータソース由来で、逃げ道（型番）は既存機能にある。
残るは五十音ガイドのUI（棚案内、データは80.2%揃済み、`row[11]`＋店の出し分け）。次の本丸。

---
## 英語検索の記号穴を塞いだ（2026-07-20・実測でGO）
カメラの次に、**英語の入口そのものが3割欠けていた**ことが分かった。想定ユーザー（英語しか読めない収集家）が
一番使う経路なので、記録を残す。

### 症状: 表示できているのに、その英語で検索できない
カードに英題が出るのに、その英語で打つと0件になる行があった。

```
表示  "Resident Evil: Revelations"   ← カードに出ている
検索  "Resident Evil Revelations"    → 0件
検索  "BIOHAZARD REVELATIONS"        → 1件（日本語名では当たる）
```

### 規模（実測）
英題を持つ **5,213行のうち 1,561行(29.9%)** が、記号を抜いた自然な入力で自分自身に到達できなかった。

| 原因記号 | 行数 |
|---|---|
| `:` コロン | **1,064** |
| `'` アポストロフィ | 302 |
| `-` ハイフン | 215 |
| `.` ピリオド | 194 |
| `!` | 122 |
| `&` | 63 |

- 原因は**非対称ではない**。HAY側もクエリ側も `toLowerCase()` と `・→スペース` の2つだけで、
  **どちらもコロンを落とさない**。HAYに `evil:` があり、打った `evil ` は部分文字列として含まれない。
- 数え方の注記: 記号の定義を「明示リスト」にすると1,557、「Unicode記号カテゴリ(P*/S*)」にすると**1,561**。
  実装は後者なので **1,561 が実装と噛み合う数字**。4行の差はダッシュ等の拾い漏れ。

### 直し（`index.html` の2箇所だけ。DATA・スキャナ・店リンク・パイプラインは無傷）
```js
const fold = s => (s||'').replace(/[\p{P}\p{S}]/gu,' ').replace(/\s+/g,' ').trim().toLowerCase();
const HAY  = DATA.map(r => [r[0],r[1],r[2],r[3],r[5],r[6],r[8]||'',r[9]||''].map(fold).join('\n'));
const lo   = fold(q.value);   // クエリ側も同じ畳み方
```
- **畳むのは Unicode記号カテゴリ(`\p{P}` 約物・`\p{S}` 記号)だけ。** 字義どおり「英数字以外」を畳むと、
  かな・漢字も英数字ではないので**日本語検索が全滅する**。長音符 `ー` は Lm なので残る。中黒 `・` は P* なので従来どおり。
- **列ごとに畳んでから `\n` で連結し、列境界を維持する。** 連結してから畳むと `\n` が空白になり、
  英題の末尾と日本語名の先頭を**またいだ誤ヒット**が出る（実測: `revelations biohazard` が 0→1件）。
- 照合は `includes` の部分一致のまま。`codeKey()` / `CODEKEYS` / `qk>=4` の床は**一切触っていない**。
- Hard-Off の `kwKanaHard`（罠#25の中黒→スペース）は店リンク用なので別物。触らない。

### 実測（旧＝bak3の実物HAY / 新＝実装後の実物HAY を並べて検証。罠13: 本体と同じ経路で確認）
| 項目 | 結果 |
|---|---|
| **効果** | 英題の自己ヒット **旧 3,652 → 新 5,213（+1,561、取りこぼし0）** |
| 型番の逆引き | `SLPS-00533` `slps00533` `SLPS 00533` `SHVC-VU` `SHVCVU` `BANHDR` **6件すべて変化なし** |
| 複数型番 | `SLUS` `KTGS` `SLUS-01041` `KTGS-40401` `SLPS-01204;SLPS-01205` **変化なし** |
| 床(qk>=4) | `JPN` `USA` `SLP` `VU` `CU` **変化なし** |
| 一般語 | `ドラゴンクエスト` 旧69/新69、`chrono trigger` 3/3、`GAME` 1066/1066 **すべて変化なし**（★訂正: 下記） |
| 日本語の部分一致 | `真・三國無双` `クロノ・クロス` `Sa・Ga` `ロマサガ` `メタルギア ソリッド` 他13件 **すべて変化なし** |
| 列境界のまたぎ | `revelations biohazard` **変化なし**（誤ヒットなし） |

**回帰0件。** ただし検証したのは49クエリで全数ではない（罠15: nは小さい）。
**ブラウザ実機では動かしていない。** `\p{P}` は ES2018（Safari 11.1+ / Chrome 64+）。
古い端末では HAY の構築で例外→**ページ全体が止まる**形の失敗になる。実機で1度開いて確認すること。

---
## パイプラインの罠（追加）

### 34. 「表示できているのに検索できない」（2026-07-20）
表示は `row[0]` を素通しで出すので**必ず見える**。検索は `includes` の部分一致なので**記号まで一致しないと当たらない**。
だから「画面に出ている文字をそのまま打ったのに0件」が成立する。**表示の正解と検索の正解が違う**という、
罠#25/#26/#30（Hard-Offの中黒・カタログ店のカタカナ・ローマ字の長音）と**同じ構造の英題版**。
表示用の値をそのまま検索インデックスに入れても、検索の正解にはならない。

### 35. 「英題をそのまま打つ」テストは、この問題を測れない（2026-07-20）
規模を測るとき、最初に「英題を持つ全行について、その英題をクエリにして自分に当たるか」を試験した。
**結果は 0件未達（0.00%）**。HAYは row[0] を素通しで含むので、完全一致は必ず当たるからである。
危うく「問題なし」と誤報するところだった。**実ユーザーは記号を抜いて打つ。** 記号を落とした入力に変えて
初めて 1,561行(29.9%) が見えた。**テストは、変更前に落ちる入力を選ぶ**（罠#18の再来）。
- 同じ回で、母数の条件を `strip(x) != norm(x)` と書いたため（norm が小文字化する）
  「記号を含む行 5,209(99.9%)」と膨らみ、2つの割合が**どちらも29.9%**になった。
  **割合が偶然一致したのが、母数を間違えている合図だった**（罠#32と同型）。

### 36. 直したら、守っている経路が黙って移ることがある（2026-07-20）
`SLPS-00533` は**件数1のまま不変**だが、**旧は型番経路(CODEKEYS)で拾い、新はHAY側で拾う**（型番経路 旧1/新0）。
結果が同じなので回帰テストには映らない。だが将来 `codeKey()` か `fold()` のどちらかを触ったとき、
**この行がどちらに守られているか**が分からなくなる。罠#17（「判断は生きたが、理由は死んだ」）の変種で、
今回は**理由が動いた**。件数の一致は、経路の一致を意味しない。

---
## まだ本番に出ていない取りこぼし（次の作業）
**バイオハザード リベレーションズは、記号穴を塞いでも3DS版が出ない。** 別の層だから。

| | 件数 |
|---|---|
| MADB(ttl)にある リベレーションズ系 | **37件** |
| うち型番(`schema:productID`)を持つ | **1件**（M720523 / `JES100302` / Xbox 360） |
| 型番なしで本番12,208件から落ちている | **36件**（PS3 8 / PS4 6 / Xbox One 5 / Switch 5 / **Vita 4 / 3DS 2** / Xbox360 2 / WiiU 2 / Win 1 / PS Now 1） |

**層b（型番フィルタで落ちる）。** データはMADBにある。`build_master.py` の `if not codes: continue` で捨てている。
→ **棚案内版（型番なしを機種で絞って27,574件取り込む案）に進めば、自動的に現れる。**
副産物: MADB側の誤字を発見。M753594〜M753599 の6件が `バイオハザード リべレーションズ2`＝**「べ」がひらがな**。

---
## この日の教訓（7/20・英語検索）
| 予想 | 実測 |
|---|---|
| リベレーションズが出ないのはバグか表記ゆれ | **型番なしで本番から落ちていた(36件)＋別に英語検索の記号穴があった**。原因は2つ |
| 英題は素通しで HAY に入っているから英語検索は効いている | **3割(1,561行)が記号で自分に届かない** |
| 記号を全部畳めばいい | **かな・漢字も「英数字以外」＝日本語全滅の罠。** 記号カテゴリ(P*/S*)に限定して回避 |
| 英題を打てば当たるか試験すれば測れる | **完全一致は必ず当たる。0件と誤報しかけた**（罠35） |
| 件数が変わらなければ何も動いていない | **`SLPS-00533` は件数1のまま経路が移った**（罠36） |

**「表示できている」は「検索できる」を意味しない。** 3日連続で、本当の問題は「解決済み・実装済み」と
思っていた場所にあった（7/18: HAYに型番は最初から入っていた。7/19: C+M二刀流は実装されていなかった。
7/20: 英題はHAYに入っていたが、記号で3割届かなかった）。**入っていることと、届くことは違う。**

---
## ★訂正: 「メモの実測値と一致」は誤りだった（2026-07-20）
記号畳みの回帰テストで、一般語の欄に「`ドラゴンクエスト`(42) `GAME`(1063) **メモの実測値と一致**」と書いた。
**照合していなかった。** 実際の値は **69 / 1066** で、メモの 42 / 1063 とは最初から違っていた。

### 何が起きたか
検証スクリプトは **旧(bak3)と新(現行)を比べて「変化なし」を出す**もので、その横に
「メモの実測値=42」を**ただ表示していただけ**だった。旧69・新69なので「変化なし」は正しい。
だが**42とは一度も比較していない**のに、出力を見て「メモの実測値と一致」とノートに書いた。

### 42 と 69 の差は何だったか（実測で裏取り済み）
**記号畳みのせいではない**（旧も新も69）。原因は **2026-07-19 の HAY 拡張**だった。

| クエリ | 旧範囲(row0-3,5,6)のみ | row[8]/row[9] を足すと | 現行 |
|---|---|---|---|
| ドラゴンクエスト | **42**（＝メモの値と一致） | **+27**（27件すべて row[8] カタカナ） | **69** |
| GAME | **1063**（＝メモの値と一致） | **+3**（3件すべて row[9] ローマ字） | **1066** |

`69-42=27` / `1066-1063=3` と差が完全に一致した。**メモの 42 / 1063 は 7/18 時点の値として正しく、
7/19 に row[8](カタカナ)・row[9](ローマ字)を HAY に入れた時点で 69 / 1066 に増えていた。**
増分の中身は `DRAGON QUEST MONSTERS JOKER` `DRAGON QUEST 8 空と海と大地と呪われし姫君` など、
**ラテン表記の行がカタカナ列で拾えるようになったもの**＝罠#26で狙ったとおりの増加。`GAME` の+3 は
`ガメラ2000` `鮫亀` 等がローマ字経由で当たったもの。
**古い基準値を、更新した理由を忘れて持ち歩いていた。**（`chrono trigger` 3 は当時も今も3で一致。）

### 記号畳みの検証そのものは有効
**誤っていたのは「基準値42/1063との一致」の部分だけ。** 旧69/新69・旧1066/新1066 で
**記号畳みが一般語を壊していないことは、実測どおり成立している。** 回帰0件の結論は変わらない。

---
## パイプラインの罠（追加）

### 37. 「照合したつもり」で、ただ横に並べて表示していた（2026-07-20）
基準値(メモの42)を**画面に出しただけ**で、コードは旧値と新値しか比較していなかった。
出力に `42` の文字が見えるので、目視では「照合している」ように読める。**表示は検証ではない。**
- **罠#19（想定・計画を事実として記録する）** を、よりによって**罠#35を書いたその回**で踏んだ。
  罠#35に「テストは変更前に落ちる入力を選ぶ」と書きながら、隣の欄で**落ちようのない比較**をしていた。
- **自分が書いた基準値も、いつの時点の値かを持っていないと腐る。** 42 は 7/18 の HAY（row[8]/row[9]なし）
  の値で、7/19 に自分で HAY を広げた時点で無効になっていた。**基準値には「いつ・どの構成で測ったか」を添える。**
- 検出できた理由: 別件（実機でどのファイルを開いていたかの特定）で**同じクエリをたまたま生の件数で表示した**から。
  「変化なし」とだけ出す表示は、**絶対値の誤りを構造的に隠す**。差分だけを見るテストは、基準のズレを見つけられない。

対処: 基準値と比較するなら**コードで assert する**（一致しなければ落とす）。できないなら
「参考値・未照合」と書く。**表に並んでいる数字は、照合された数字とは限らない。**

---
## 登録タイトルを増やす — 捨てていた4分の3を測った（2026-07-20）
「登録を増やす方法」を考えるところから始め、**外に探しに行く前に手元のMADBを測った。**
今の12,208件は「型番(`schema:productID`)を持つ」1つのフィルタで切った**4分の1**にすぎなかった。

### 全体の内訳（実測）
| | 件数 | |
|---|---|---|
| ゲームパッケージ総数 | **51,827** | |
| 型番あり（＝現行の本番） | 12,208 | 23.6% |
| 型番なし（捨てていた） | **39,619** | 76.4% |

### 捨てていた39,619件が持っているもの（型番なし群を母数）
| キー | 充足 | 効く機能 |
|---|---|---|
| @ja-hrkt 読み | **39,540 (99.8%)** | 五十音の棚案内・日本語検索 |
| schema:genre | **39,619 (100%)** | 棚案内（ジャンル棚） |
| Wikidataカタカナ(P7886) | 9,835 (24.8%) | カタログ店検索 |
| schema:gtin | 4,601 (11.6%) | カメラ（バーコード） |
| **英題(P7886)** | **0 (0.0%)** | **英語検索（＝今の入口）** |
- `schema:identifier` は100%あり、Wikidata照合の鍵自体は全行そろっている。
- **英題0%は壊れているのではなく構造**（カタカナは24.8%当たる＝照合機構は生きている）。この群は
  **英語名がそもそも存在しない日本国内タイトル**の集まり。棚案内(読み99.8%・ジャンル100%)の燃料で、
  英語検索の入口ではない。n=0は罠12の顔なので、いつか wikidata_en.json のキー突き合わせで裏取りする価値あり（未実施）。

### 機種別の内訳（型番なし群・実測）— 同人PCではなく、狙った家庭用機だった
上位: PS2 3,477(8.78%) / PSP 3,136 / PS3 2,383 / PS1 2,332 / **Vita 2,328** / **3DS 2,212** / DS 2,150 /
PS4 1,834 / Wii 1,398 / Xbox360 1,316 / GB 1,208 / PC-8801 1,177 / **ゲームアーカイブス 967** / FC 889 /
セガサターン 862 / SFC 822 / GBA 677 / **Microsoft Windows 649(20位・1.64%)**。
機種入り合計 34,062(85.97%) / 機種空 5,557(14.03%)。
- **予想「多くは同人・Windows」はn=1(厨二姫の帝国)からの誤り**（罠15/22）。実測ではWindowsは20位。上位は家庭用機がびっしり。
- **足すべきでない「機種の顔をした配信」**: ゲームアーカイブス967・PS Now170は**棚に物が無い**（ストリーミング/DL）。
  棚案内が空振りする（「0件のリンクは、リンクが無いことより悪い」の棚版）。
- **物理はあるが未検証の新機種**: Xbox360 1,316 / WiiU 840 / XboxOne 683 / Switch 318 / Xbox 188（+3,345件）。今回は保留。

### 重さ（gzip後＝GitHub Pagesが実際に流す量。罠9のとおり生サイズではなくgzip後が本命）
| 版 | 行数 | 生 | gzip(lv9) | 現行比 |
|---|---|---|---|---|
| 現行 | 12,208 | 2.44 MB | **0.65 MB** | 1.00x |
| 絞り込み版 | **39,782** | 7.55 MB | **1.44 MB** | 2.22x |
| 全件 | 51,827 | 9.17 MB | 1.71 MB | 2.64x |
- **絞り込みの節約はサイズでは26%どまり**（1.71→1.44）。落とした12,045行が残りと語彙を共有するため（罠9）。
- **実機で開いてもたつかないことを確認済み**（=技術的にGO）。ただし増えた分は英題0なので英語検索・型番逆引きには出ない。眠る行。

### 決めたこと: 絞り込み版（27,574件を取り込む）。ただし軸はサイズではなくUX
- 採用集合 = 既存12,208(型番あり全部) ＋ 型番なしで機種が `build_master.py` の **PLATFORM_EN ∪ BUYEE_OK** に入る **27,574件** = **合計39,782行**。
  （配信サービス・Xbox系・WiiU・Switch・機種空は、この集合に手を触れず**自動で外れた**。新しい判断を足していない＝一番安全な線）
- **価値は0.27 MBではなく「棚に物が無い行を土台に混ぜない」こと。**
- **非対称は意図どおり**: 型番あり側の機種空184行は残す／型番なし側の機種空5,557件は落とす。
  切っているのは「機種の有無」ではなく**「ユーザーに返せる手がかりがあるか」**（型番あり184行は型番で役に立つ）。
- **取り込む正しい瞬間は「棚案内UIができたとき」。** 今入れても英語で探せず眠るだけ。→ **現行12,208はそのまま。絞り込み版は寝かせる。**
  **（2026-07-21 この判断は意図的に覆した）** 原因が「MADBの限界」ではなく「型番フィルタで捨てていたバグ」だと分かったため。しかも `fold()`（記号畳み）と row[8]/row[9] の検索が既に入っていたので、**入れた瞬間に日本語・カタカナ・ローマ字で当たる**＝「眠るだけ」が誤りだった。棚案内を待たずに取り込んだ（39,782行）。

---
## 検索結果カードを店員に見せる形に作り直した（2026-07-20）
目的: **この1枚を日本の店員にスマホで見せれば、店員が日本語タイトルと型番で商品を特定できる。**
この画面は**2人が読む**: ユーザー(英語しか読めない)は English と Romaji を、店員は 日本語タイトルと型番を読む。

### カードの階層（`index.html` の `card()` を作り直し。DATA・スキャナ・店リンクは無傷）
```
ENGLISH（小ラベル） English title = row[0]   ← 英題が空(57%)の行はブロックごと出さない（罠27: 無いほうがまし）
─────
日本語（小ラベル）  Japanese title = row[1]  ← 一番大きく(26px)。店員が読む主役
Romaji（小ラベル）
row[9]の読み                                 ← 2行に改行（.lbl の display:block）
[CONSOLE row[2]（濃青バッジ）] [PRODUCT CODE row[3]（破線枠・等幅23px）]  ← 色と枠で別物と分かる
発売元(row[6]||row[5]) · 発売年 row[4]
（店リンク BUY IN JAPAN / SHIPS WORLDWIDE はそのまま）
```
- **Romajiのガード**: `r[9] && /[ぁ-ヿ㐀-鿿]/.test(row[1])`。**見出し(row[1])が漢字・カナのときだけ出す。**
  ラテン見出し(`Resident Evil`)に `baiohazaado` は冗長だから消す。生粋の日本タイトル(`龍が如く`→`ryuu ga gotoku`)にだけ残す（罠28）。
  実測: 読みを持つ11,860行のうち **Romajiを出すのは8,790行**、ガードで抑えた3,070行。
  ※判定対象は旧版の title(英題優先)ではなく **row[1]**。英題があり見出しが日本語の行では、旧版は抑え、新版は出す（意図どおり）。
- バックアップ3世代: `.bak`(カード改修前) / `.bak2`(Romaji変更前) / `.bak3`(記号畳み前)。

### ★訂正: ノートの古い記述（罠19）
- **data_line.js は「10列」ではなく現物は12列**（`row[10]`=JAN、`row[11]`=五十音初字が7/20に追加済み。計測時に発見）。
  「ファイル構成」「data_line.js の構造（10フィールド）」の記述は古い。→ **12列に読み替える。**
  （※`row[11]`五十音初字の存在と「次にやること」の五十音To-Doは要・整合確認。棚案内で突き合わせる。）
  **（2026-07-21 追記）** さらに `row[12]`（配信フラグ）を足したので**現在は13列**。`row[11]`の実在もこのとき実見して確認した（32,259行）。
- **`card()` は「先頭7個しか分割代入しない」も古い**。今は `row[9]`(ローマ字)も使う。

---
## ファイル整理と _experiments/（2026-07-20）
今日「実機で本番と別ファイルを開いていた」事故（罠38）の原因＝ルートに紛らわしい `index_*.html` が並んでいたこと。
本番ディレクトリから実験ファイルを避難させた（**消さない。棚案内で使う**）。

- `_experiments/` に13個を `mv`（計34.4MB）: `index_filtered.html`(39,782行) / `index_full.html`(51,827行) /
  `data_line_full.js` / `data_line_filtered.js` / 使い捨て `measure_*.py` 9本。
- ルートに残る `index*.html` は **`index.html` 1つだけ**（＝D&Dの取り違え防止）。`.bak` 11世代はルートに残置（gitを入れるまで唯一の履歴）。
- 実行方法が変わった: `cd`でルート → `PYTHONPATH=. python3 _experiments/measure_xxx.py`。
  **`_experiments/` の中で直接実行してはいけない**（対象ファイル不在でもエラーにならず、終了コード0で完走する＝罠38と同じ形）。
- `_experiments/README.md` を作成（各ファイルの用途・実行方法・測定時点の値・下の時限爆弾警告）。

### ★時限爆弾: 実験HTMLをそのまま本番化すると、今日塞いだ記号穴が復活する
`index_filtered.html` / `index_full.html` は **13:56〜14:06 の生成物＝`fold()`実装(15:17)より前**。検索が旧仕様のまま。
→ **棚案内で27,574件を入れるときは、この実験HTMLを流用しない。**
`build_master.py` で27,574件のデータを作り直し、**`fold()`込みの現行 `index.html` にDATAを差し替える**のが正しい順序。
実験HTMLは「データの中身と重さの参考」であって「本番の骨格」ではない。

---
## 本番URLへ反映済み（2026-07-20）— 今日の成果は公開された
`index.html`(2.44MB) を GitHub Web画面から D&D 済み。公開URLで確認:
- `https://english-tadashi.github.io/bin-diver/?v=99` → **`ドラゴンクエスト` 69件**（本番12,208件での正しい値。42は7/18の旧値＝罠37）。
- `Resident Evil Revelations`(記号抜き) → **1件**（記号畳みが公開版に効いている）。新カードデザインも反映。
- 記号畳み(+1,561行) と 店員向けカード改修が、訪問者に届く状態になった。
- 補足: このリポジトリはWeb画面からのD&D運用で、ローカルは git 管理されていない（`.bak` 11個だけが履歴）。

---
## パイプラインの罠（追加）

### 38. どのファイル／どのURLを見ているか、画面では分からない（2026-07-20）
実機確認中、ローカルサーバで `index_filtered.html`(39,782行・fold前) を開いたまま本番を直したため、
**「表示できているのに `Resident Evil Revelations` が0件」**を本番の症状と誤読しかけた。実際は本番(`index.html`)では1件出る。
- ブラウザは履歴で古いURLを補完する。**アドレスバーを消して打ち直し、`?v=N` でキャッシュを外す**まで、見ているものが分からない。
- 同型: `_experiments/` 内で `measure_whichfile.py` を実行すると、対象ファイルを「(存在しない)」としたまま**終了コード0で完走**する。
  取り違えはエラーにならない。**「どのファイルの結果か」は、コードにも画面にも名乗らせないと分からない**（罠14の実行環境版）。
- 3日連続で踏んだ「入っている≠届く／表示≠検索／件数一致≠経路一致」の家族。**今回は「見ている画面≠本番」**。

---
## この日の教訓（7/20・増量調査とカード改修と公開）
| 予想 | 実測 |
|---|---|
| 捨てている型番なし群の多くは同人・Windows | **家庭用機がびっしり**（PS2/PSP/PS3/PS1/Vita…）。Windowsは20位1.64% |
| 機種で絞ればサイズをかなり削れる | **26%だけ**（gzipが語彙を吸う、罠9）。絞る価値はサイズでなくUX |
| 増やすのは新データを外から探す話 | **MADBの中に27,574件眠っていた**。手元を測るのが先 |
| 本番URLは直せば反映される | **手動D&Dするまで公開版は7/19版のまま**（罠20の公開工程版） |
| 実機で開いているのは本番 | **別ファイル(index_filtered)を見ていた**（罠38） |
| ドラゴンクエストは42件（メモの値） | **69件**。42は7/18の旧値、7/19のHAY拡張で69に（罠37） |

**「増やす」の前に、もっと手前の「今あるものが英語で届いていない穴(3割)」を掘り当てて塞ぎ、公開した。**
増やす準備（27,574件・重さ・カード）も全部そろった。次はその受け皿＝棚案内。

---
## C結合を測って却下 — 英語カバレッジの天井を 42.7% で確定（2026-07-20）

「1番（英語検索）を改善」の入口として、英題42.7%を上げられるかを測った。結論: 上げられない。42.7%が意味上の天井。

測った順（安いプローブ → 1クエリ → 本測定 → 診断）:
1. wikidata_en.json のキーを見た → 5,213件すべてM番号、C番号ゼロ（=現行英題数と一致）。ここで一度「C側に燃料なし」と結論したが、これはクエリ文（機構）からの推論で、値を見ていなかった（罠#39）。
2. query.wikidata.org で C番号の英題を直接引いた → 14,194件が実在。推論は覆った。ただし会社(Gearbox Software, SNK)・人名(Aoi Nishimata)・シリーズ・ローマ字が混在（罠#41）。dcterms:publisher も C番号（だから会社が居た）。
3. 本測定 measure_cjoin.py（本体と同じブロック分割・まず42.7%を再現するゲート）: embodimentOf(クリーン)経由の新規英題 = 1行(+0.0pt)。workEmbodimentOfのみ経由 = 34行(+0.3pt)＝Path B領域（罠#27、1/4化ける）。
4. 診断: gap 6,995行の 53.1%は embodimentOf リンクを持つが、その作品Cが英題表に在るのは1件だけ。→ リンクは在る。作品(Cノード)に英語ラベルが無い＝英語版が出ていない国内タイトルだから英題が存在しない。

確定:
- 「C番号とM番号の二刀流で~56%」は結合バグではなく燃料不在。IGDB/MobyGames等の別ソースも同理由で不毛（無い英題は拾えない）。
- 42.7% は英語検索入口の意味上の天井。57%を届かせる道は英語検索ではなく棚案内（読み99.8%・ジャンル100%）。
- Cは建てない。動くパイプラインを+1行のために触らない（罠#21）。
（measure_cjoin.py と query__10_.json＝C英題14k は使い捨て。リポジトリには要らない。）

## 長音の検索穴を実装・確定 — ローマ字限定＋床8（2026-07-20）

未解決#5/#30。表示は忠実(ryuu ga gotoku)でよいが、英語話者は ryu ga gotoku と打つ→0件だった。

却下した案: fold本体にグローバル同母音潰し。英題・機種・型番まで潰れて壊れた（Dragoon→Dragon で+148、ローマ数字 II/III/VIII→i/vi が混在、wii→wi で 75→1,791、moon→mon 38→488）。18項目+GAMEの回帰ではこの範囲を測っていなかった（罠#40）。

採った案: row[9](ローマ字)だけの第2レーン ＋ 潰しクエリ長≥8。
    collapseV = s => s.replace(/([aeiou])\1+/g,'$1')
    RJ = DATA.map(r => collapseV(fold(r[9]||'')))
    ヒット条件に  loc=collapseV(lo); loc.length>=8 && RJ[i].includes(loc)  を OR で追加
fold本体は記号のみ(bak4)据え置き。codeKey/CODEKEYS/qk>=4/店リンク/showByJan/card() 不変。DATA md5 同一。

実測（旧=bak4 / 新=実装後、実物DATA）:
- 効果: ryu ga gotoku 0→18（龍が如く/Yakuza）、eiyu densetsu 0→41（英雄伝説/Trails）。ローマ字列の長音が潰れて一致（ryuu ga gotoku 8 / eiyuu densetsu sen no kiseki 1）。
- 暴発なし: Dragoon 9=9・moon 38=38・wii 75=75・book/zoo/cool/doom/queen すべて旧=新。
- no-change: ドラクエ69・chrono trigger3・GAME1066・真・三國無双36・型番6件・musou90・rurouni6・meiji17・sensei6 すべて不変。
- 英語辞書234,450語を総当たり: 発火し得る4,606語中、実際に誤爆は8語、純粋な誤爆は2語(challengee→challenge / commandeer→commander)のみ＝無害。honeymoon/bloodmoon/bookkeeper/guarantee 等は不発かヒット0。
- 床8は当てずっぽうではない: 「dragon(6字)と多語タイトル(12字+)の間」＋辞書全走査で裏取り済み。

意識して残した穴（別問題・未着手）:
- Tokyo型（toukyou≠tokyo）: ouのオウは英語が落とす場合があり、同母音潰しでは解けない。
- 外来語音写（sutoorii=Story, rimasutaa=Remaster）: ローマ字が英語と根本的に噛み合わない（罠#28）。
（バックアップ .bak4＝ローマ字レーン実装前。戻すなら cp index.html.bak4 index.html。）

## パイプラインの罠（追加）

### 39. クエリ文（機構）から「無い」と結論しかけた。1クエリが覆した（2026-07-20）
wikidata_en.json が全部M番号だったので「C側に英題は無い」と、元のSPARQLの文面から推論した。だがC番号を名指しで引くと14,194件が実在した。値を見る前に機構で決めた。罠#12（通信の死をデータ不在に化けさせた）・罠#33（gtin欠けと決めつけた）の家族。安いプローブは有効だが、その解釈まで安く済ませない。決定的な1クエリを先に叩く。

### 40. 回帰テストが「変更が触る範囲」を外していた（2026-07-20）
長音のグローバル潰しは18項目+GAMEの回帰で「0件」に見えた。だが英題の母音重なり（Dragoon・ローマ数字II/III/VIII）を狙って測っていなかった。狙うと Dragoon+148・ローマ数字混在・wii→1,791 が出た。罠#18（テストは変更前に落ちる入力を選ぶ）の拡張＝加えて、変更が触る列・字種を狙って選ぶ。今回は「同母音を含む英単語」。英題608件＋辞書23万語を走らせて初めて「英題側でも安全」と言えた。

### 41. C番号は「作品」だけの空間ではない（会社・人名・シリーズ混在）（2026-07-20）
C英題14kには会社(Gearbox Software, SNK)・人名(Aoi Nishimata)・ローマ字が混じる。dcterms:publisher も C番号だから。作品レベルで素直に結合すると会社名を英題に貼る。罠#27（workEmbodimentOfが上位概念・別物に化ける）の一般化＝C空間そのものが異種混在。embodimentOf(表現レベル)経由なら publisher/creator のCを踏まないが、収量は+0.0pt（本文）。

## この日の教訓（7/20・英語カバレッジと長音）
| 予想 | 実測 |
|---|---|
| 英題42.7%はC結合で~56%まで上がる | +0.0pt（作品Cに英語ラベルが無い＝英語版が無い） |
| C側に英題の燃料は無い（クエリ文から） | 14kが実在。1クエリで覆った（罠#39） |
| 長音はfoldに同母音潰しを1行足せば済む | 英題を巻き込み全壊（Dragoon/ローマ数字/wii） |
| 18項目+GAMEの回帰が通れば安全 | 変更が触る範囲(英題の母音重なり)を外していた（罠#40） |
| 床8は当てずっぽう | 辞書23万語で裏取り、純粋な誤爆2語のみ |

測る前に決めない。値を見る前に機構で決めない。回帰は変更が触る範囲を狙う。3日続いた「入っている≠届く／表示≠検索」に、今回の「機構≠値」が加わった。

## 次にやること（2026-07-20 更新）
### 本丸: 棚案内（五十音＋ジャンル）— 27,574件が働き始める場所
- データは揃っている（読み99.8%・ジャンル100%）。ここで初めて型番なし群（リベレーションズ36件・国内専売タイトル）が英語話者に届く。
- **未解決6（要・設計判断）**: 店ごとに棚の並べ方が違う（ブックオフ=ジャンル棚、駿河屋/スーパーポテト=五十音棚）。
  ツールが1つの答えしか返せないと片方で外れる。店を選ばせるか併記するか。まず2〜3店。
- 取り込みは**実験HTMLを流用せず** `build_master.py`＋`fold()`込み `index.html` で再生成（上の時限爆弾）。
- ~~`row[11]`五十音初字が既に在るのか（12列の件）を、棚案内着手時に突き合わせる。~~
  **（2026-07-21確認: row[11]は実在。32,259行に値が入っている。row[12]追加時に列を実見した。）**

### その前に: git 導入（しらふの頭で1本）
- 現状の履歴は `.bak` 11個のファイル名だけ。`git init` で `swap_data.py` の差分ガードと同じ守りをファイル全体に。
- **`.gitignore` 必須**（`data/` は上げない。`metadata301.ttl` 81MB がGitHub 100MB制限に近い）。`_experiments/` の34MBも要判断。

---
## 今日（7/21）やったこと — 有名作が出ない問題を外部DBゼロで解いた

| | |
|---|---|
| 発端 | ポケモンダッシュ等の有名作が英語で出ない、という指摘から。原因は1つでなく複数だった |
| 型番フィルタ緩和 | `if not codes: continue` を「型番あり **または** 機種が `PLATFORM_EN ∪ BUYEE_OK`」に変更。**+27,574行**（12,208→39,782）。MADBに在るのに型番が無いだけで捨てていた4分の3が登場（ポケモンダッシュ・バトルフロント・リベレーションズ3DS 等） |
| 英題の複数値再export | `wikidata_en.json` を取り直し。**P7886は1項目に複数MADB IDが張られるのに、旧exportは1つだけ拾い兄弟を落としていた**（5,213→16,393キー）。空だった英題の**24.6%=8,499行**が埋まった。英題充足 13.1%→**34.5%**。`resident evil revelations` 1→14件 |
| 手書き英題6件 | `TITLE_EN`（M番号キー、`PUBLISHER_EN`の英題版、手書き優先）を新設。descriptionにしか無い6件（帝国の逆襲ほか）を手書き。descriptionの一括取り込みは却下（下記） |
| No discバッジ | 配信版（`ma:carrierType "オンライン資料"`、**7,109行**）に `No disc / Download` バッジ。CONSOLE=濃青塗り / DOWNLOAD=アクセント実線枠 / PRODUCT CODE=破線枠 の3系統で区別 |
| 配信版の店リンク非表示 | 配信版のカードから BUY IN JAPAN / SHIPS WORLDWIDE を非表示（`No disc`と矛盾しないため。罠#27）。検索には残す |
| 状態 | **全部ローカルの index.html で検証済み・GitHub未反映（公開待ち）**。公開版は 7/19版のまま（罠#20） |

---
## 英題の網羅は Wikidata再export が最初で最後の鉱脈だった（2026-07-21）

「英語版が実在するのに英題が出ない」を3つの原因に分けて全部潰した。

- **原因1: 型番フィルタで行ごと落ちていた**（ポケモンダッシュ）。→ フィルタ緩和で解決。
- **原因2: Wikidata英題の複数値取りこぼし**（リベレーションズ3DS）。P7886は複数MADB IDを1項目に列挙するのに旧exportが1つだけ拾っていた。→ 再exportで+8,499行。**これが最初で最後の大きな鉱脈。**
- **原因3: MADBのdescriptionにしか無い**（帝国の逆襲）。→ 一括取り込みは却下（下記）、6件だけ手書き。

**却下した経路（測って規模が無いと確定）**:
- **description一括取り込み**: 英題が空でdescriptionを持つ8,844行のうち、英題として使えるのは「英語表記」注記付き**6行**、出典注記まで広げて**13行**。残りは書誌データ（キャリア/システム要件）。13行のために英題欄を汚す経路は作らない（罠#27）。全部ファミコン書誌担当者の癖で、鉱脈ではない。
- **No-Intro（英語名）**: 地域タグ方式で「(USA)を持つ=英語版実在」を数えたが**上限170件**（Wikidata再exportの2%）。しかも基底名をタグごと剥がすと日本専売作（ナムコ版スター・ウォーズ1987）に偽英題が付く（罠#27）。`(En)(Namco)(FamicomBox)` が同じ括弧記法に同居し機械判別不能（罠#41同型）。却下。

**確定**: 英題充足34.5%が実質の天井。残り（日本専売作）は英語版が世界に存在しないので、どの経路でも埋まらない。**英語で届けられない大半を届ける道は英題ではなく棚案内**（読み99.8%・ジャンル100%）。7/20の「42.7%が天井」は型番あり12,208件内の話で、その下に複数値バグがあった。

## 型番は掘り尽くして「埋まらない」と確定（2026-07-21）

型番なしで登場した有名作（マリオ・ワリオランド・帝国の逆襲、いずれもファミコン/GB）に型番を付けられるか、在り処を全部当たった。

- **MADB**: 機種ぐるみで持っていない（GB充足~10%、FC初期も薄い）。マリオ・ワリオランドとも `schema:productID` 無しをgrepで確認。
- **No-Intro**: カセットDATに serial 0.06%（FC 14,131件中8件）。7/18のredump（SFC 4,256中28）と同型。
- **gbhwdb（Game Boy Hardware Database）**: ワリオランドは `DMG-WJ-USA/UKV/EUR` のみ、**日本版なし**（有志が手元の欧米カセットを登録するため）。CC-BY-SA-4.0。
- **game-boy-database.com**: `USA-1 FRG-1` 等、海外版中心。

**確定**: 日本版カセット型番を名寄せできる形で持つ供給源が、手元にも外部にも無い。**カセット機の型番は自動では埋まらない**（時間でも変わらない）。手書きするなら日本版の出所を自分で用意（**gbhwdbの海外版型番は出所にできない=罠#16**）。ただし型番は逆引き・駿河屋リンク用で、有名作は英題・読みで既に見つかるため、埋める価値は英題より一段低い。**空のままが妥当。**

---
## 配信版は削除ではなく「No discバッジ＋店リンク非表示」（2026-07-21）

型番緩和で入った27,574件に、棚に物が無い配信版（バーチャルコンソール等）が混ざる（マリオVC版で発覚）。

- **判定は `ma:carrierType "オンライン資料"` で確定**。mediaFormat/additionalGenre は174件取りこぼす（包含関係を実測: B=オンライン資料 が A・C を含み、B∖A=171 / B∖C=3）。**7,109行**。
- **削除は却下**: (a)物理版が別に在る4,132 / (b)物理版が無い2,977 の切り分けがタイトル一致依存で不正確（ポケモンΩルビーが表記ゆれで(b)に誤入）。単純削除は有名作を巻き込む。
- **採用: No discバッジ（row[12]）＋店リンク非表示。検索には残す。** 「物理では棚に無い」を正直に伝え、買えない導線を出さない。物理版の有無を判定しないので、切り分け精度の問題を解く必要がない。
- ラベルは `Download` ではなく **`No disc`** を主に（Downloadだと「入手できる」と読める。サービス終了済みなので導線も死んでいる）。

---
## パイプラインの罠（追加）

### 42. wikidata_en.json は P7886 の複数値を取りこぼしていた（2026-07-21）
P7886（MADB ID）は1つのWikidata項目に複数MADB IDが張られる（機種・版ごと）。旧exportは1項目1つだけ拾い、兄弟（例: リベレーションズ3DS）を落としていた。5,213→16,393キー、空英題の24.6%=8,499行が再exportで埋まった。**「英語版が実在するのに出ない」の主因はこれ。** 罠#19（計画を事実として記録）の英題版に見えたが、実際は結合バグですらなく取得バグだった。

### 43. swap_data の [1:7] グループ判定（2026-07-21）
再exportで空→値ありが数千行出たとき、swap_dataの中身ベース照合が最初285件を「説明のつかない消失」と誤判定。原因は同一メタデータ（[1:7]一致）の兄弟行で一部だけ英題が付き、空で残った兄弟のせいで判定が外れたこと。グループ単位（行数保存）で見る形に直し0件に。罠#36（件数一致≠経路一致）の親戚。

### 44. No-Intro のタグ空間は不均質（2026-07-21）
No-Introの括弧タグは `(Japan)(USA)`（地域）と `(Namco)`（発売元＝同名異作の区別）と `(En)(FamicomBox)`（属性）が同じ記法に同居。基底名を作ろうとタグを剥がすと、日本専売のナムコ版1987と英語版ありのビクター版1991が同じ `Star Wars` に潰れ、日本専売作に偽英題が付く。どのタグが「別作品」でどれが「属性」かの機械判別規則が無い。罠#41（C空間に会社・人名・シリーズ混在）と同型。

### 45. gbhwdb等の外部型番DBは「World」表記でも中身は欧米版（2026-07-21）
gbhwdb はワリオランドを「(World)」として括るが、登録カセットは `DMG-WJ-USA/UKV/EUR` で日本版が無い（有志が手元の欧米カセットを登録するため）。「Worldと書いてあるから全地域」ではない。貼ると日本版の行に北米版型番が付く（罠#16の外部DB版）。

---
## この日の教訓（7/21・有名作が出ない問題）
| 予想 | 実測 |
|---|---|
| ポケモンダッシュはMADBに無い（層1） | MADBに在った。型番フィルタで落ちていた（層b） |
| 英題42.7%が天井、これ以上増えない | 複数値取りこぼしを直して+8,499、34.5%まで |
| 帝国の逆襲の英題はWikidataかNo-Intro | MADBのdescriptionに在った（が鉱脈ではない、6〜13行） |
| No-Introで英語版の実在を数えられる | 地域タグは有用だが上限170・偽英題混入で却下 |
| マリオ/ワリオの型番はどこかにある | 日本版は手元にも4つの外部DBにも無い。自動では埋まらない |
| 配信版は削除すればいい | 有名作を巻き込む。No discバッジ＋店リンク非表示で正直に |

**有名作が出ない問題を、外部DB（No-Intro/gbhwdb）もユーザー投稿も使わず、手元のMADBとWikidataの中で解いた。** Geminiが勧めたNo-Intro主軸は、測ったら170件・偽英題混入で却下。手元を測るのが先、機構で決めず値を見る、有名なものが消えたら疑う、立ち止まって照会する——全部効いた。

## まだ本番に出ていない取りこぼし（次の作業・更新）
- **pokémon dash のダイアクリティカルマーク穴**: 英題が `Pokémon Dash`（é付き）で入り、素のASCII `pokemon dash` で0件。非ASCII英題は**696行**（`Tōka Gettan``Ōkamiden``Code: Realize − …`）。`fold()`にNFKD正規化を1手足せば解けそうだが、型番・機種列やカナ濁点への副作用を測ってから（罠#34の3度目＝記号・長音に続くダイアクリティカルマーク版）。**未着手。**
- **棚案内（本丸）**: 型番なし27,574行の大半（日本専売作）を五十音・ジャンルで届ける。**配信版はここで棚から外す判定に row[12] を使う**（棚に物が無いから）。
- **公開**: 7/21の成果一式（フィルタ緩和・英題再export・手書き6件・No discバッジ・配信店リンク非表示）はGitHub未反映。公開時は `ポケモンダッシュ`/`resident evil revelations`14件/`empire strikes back`/`スーパーマリオワールド`でNo discバッジ/`ドラゴンクエスト`153件 を `?v=` で確認（罠#20/#38）。

---
## 画像は「使わない」から「使える」へ — 楽天APIで実測してGO（2026-07-21）

7/18に「画像は使わない」と決めた（libretro=権利が宙ぶらりん、31%・カセット機0%）。**その判断は当時正しかったが、理由は「技術的に無理」ではなく「権利がクリーンでない」だった。権利がクリーンな道を、まだ全部は測っていなかった。** 3つの道を順に測った。

### 道1: Wikidata P18 — 却下（薄い＋汚染、罠#41がそのまま発火）
既にP7886で結合済みのWikidataに画像プロパティP18がある。指す先はCommons＝必ずフリー。権利ゼロ。だが測ると：
- 天井 = **1,563 / 32,620 = 4.8%**（libretro 31%を大きく下回る）。CSV 39,782行に乗せて多くて3.9%。
- しかも中身が**箱絵ではない**。ファイル名を分類したら 箱/カバー74・ロゴ58・画面写真50・現物35・**その他1,346**。その他の実体が建物・人物（東京ミッドタウンタワー / 東村アキコ / Gearbox本社ビル / 西村京太郎記念館…）。
- **原因は罠#41そのもの**: P7886（MADB ID）空間は作品だけでなく会社・人名・シリーズが混在。作品ノードなら箱絵/ロゴ、会社ノードなら社屋、人物ノード（作者）なら顔写真がP18に乗る。Gearboxが出たのは罠#41で名指しした当のGearbox。
- 採用すると罠#27（誤った値より無いほうがまし）に真正面から当たる（ゲームの横に東京ミッドタウン）。作品ノードだけP31で濾すと箱絵74件のさらに一部で収量が消える。**却下。**

### 道2: 楽天商品検索API — 実測90%でGO（本採用の価値あり）
アフィリエイトの商品画像。**契約でクリーン・出品ページに必ず写真・収益と同方向。** 既にBuyee/駿河屋/メルカリ/ブックオフへリンクしている先の写真を、カードに引き込む形。

**測定結果（20タイトル・JAN全件あり・7機種に分散・ブラウザのコンソールから実行）:**

| | JAN検索 | 名前検索 |
|---|---|---|
| 全体 | **18/20 (90%)** | 12/20 (60%) |
| ディスク機 | 11/12 (92%) | — |
| カセット機 | **7/8 (88%)** | — |

- **カセット機88%が最大の発見。** ファミコン・スーファミが各3/3(100%)、しかも中古業者が撮った**現物写真付き**（`SFC スーパーファミコンソフト…` のサムネURL）。libretro箱絵0%・Wikidata 4.8%と桁が違う。「出品者は箱を書き写す」（ラテン題がBuyeeで当たった話）の**画像版**が成立。裸カセットでも業者が撮って出品しているから写真がある。
- **割れたのはディスク/カセットではなく「JAN検索 vs 名前検索」だった。** 予想（媒体で割れる）は外れ。JAN検索90%は安定、名前検索60%はPSP/サターンで0%（HTTP 400が4件＝日本語名＋機種名の組み立てを楽天が弾く）。**だがJANが主役なので問題なし**（レトロのディスク機はJANほぼ100%充足）。名前検索はJAN無し行の保険。
- 落ちた2件（PAD NIFTY 1.1＝サターン通信ソフト / Dance Dance Revolution GB2）は極端な無名作＝楽天在庫に無い＝**正しく0件**（罠#27を守った。誤写真を出さない）。

**三道の比較:**

| 道 | 写真率 | カセット機 | 権利 | 収益 |
|---|---|---|---|---|
| 道1 Wikidata P18 | 4.8%（大半が非ゲーム） | — | クリーン | なし |
| 道3 libretro | 31% | **0%** | 宙ぶらりん | なし |
| **道2 楽天** | **90%(JAN)** | **88%** | 契約でクリーン | アフィリエイト |

### 但し書き（未解決）
- **n=20**。JAN全件ありの行から選抜＝**JAN無し行ではこの90%は出ない**。道2の守備範囲は「JANがある行」＝レトロのディスク機＋メジャーなカセット作＝ターゲット層と重なる。**全行に写真が付くわけではない。**
- **表示の権利は提携審査後に最終確認**。今日測ったのは「写真が取れるか」であって「自サイトに表示してよいか」ではない。道1(P18)がクリーンだったのと違い、道2は提携が前提。実測GOでも表示可否は規約で確認。
- **楽天API本体はローカルPythonから叩けない**（新仕様=Referer必須・登録ドメインからのみ）。測定・本番とも**登録ドメイン上のブラウザJS**で回す。あなたのツールは1枚HTML＝相性がいい。

---
## 楽天APIは2026年5月に全面刷新されていた（半日の400の正体）

画像測定の前に、楽天APIが全く通らず半日溶かした。**根本原因はClaudeが古い仕様で走らせたこと**（罠#48）。楽天は2026年2月にインフラ刷新、移行期間2/10〜5/13、5月14日で旧API完全停止。今日は7月＝**旧URLは死んでいた**。

**エラーが1つずつ変わる＝関門を1つずつ越えた記録:**

| エラー | 意味 | 対処 |
|---|---|---|
| `specify valid applicationId` | 旧URL(停止済み)を叩いていた | 新URL `openapi.rakuten.co.jp/ichibams/api/IchibaItem/Search/20220601` へ |
| `accessKey must be present` | 新APIはaccessKey必須（旧はappIdだけ） | accessKey追加 |
| `REFERRER_MISSING` | 新APIはReferer必須 | 登録ドメイン上のコンソールから叩く |
| `REQUESTED_SCOPES_NOT_ALLOWED` | アプリにIchiba権限が無い | APIアクセススコープで「楽天市場API」にチェック |

- 新旧の差 = **ベースURL差し替え**＋**accessKey追加**＋**Referer必須**＋**スコープ設定**＋**レート制限1.5秒**。
- アプリIDは `e` 始まりのUUID形式（旧知識の「19桁数字」は失効）。アクセスキーは `pk_` 始まり＝**秘密度が高い**。
- **ローカルPythonは新APIを叩けない**（Referer/Originはブラウザ管理下でスクリプトから設定不可＝WHATWG Fetch仕様）。GAS等でも同じ報告。→ 測定はブラウザのコンソールに移した。

---
## パイプラインの罠（追加）

### 46. 「0/20」を写真ゼロと読みかけた——40件全部が届いていなかった（2026-07-21）
最初のPython測定が0/20。写真ヒット率0%と読みかけたが、エラー内訳を見ると**40件全部HTTP 400**＝楽天に届いてすらいない。原因は旧URL(停止済み)。**0/20は「写真が無い」ではなく「門前払い」。** Claude Codeが「0件」と「通信エラー」を表で分けていたおかげで誤読を免れた。罠#12（通信の死をデータ不在に化けさせる）そのもの。

### 47. `.catch()` の無いIIFEは、例外を握り潰して「undefinedで無音」になる（2026-07-21）
測定スクリプトを貼っても `undefined` が出るだけで進捗もエラーも何も出ない。単発fetchは成功するのに。原因は即時実行関数(IIFE)に `.catch()` が無く、中で出た例外が握り潰されていたこと。**「何も起きない」の正体が「例外が見えない」だった。** `.then()/.catch()` と先頭の `console.log('=== 測定開始 ===')` を足したら、止まっていた場所（鍵未置換で中止）が即見えた。罠#12（握り潰し）・罠#38（画面では分からない）の実行環境版。**エラーは握り潰さず必ず表に出す。**

### 48. Claudeの「仕様」も、書いた時点で真だっただけ（2026-07-21）
Claudeが「エンドポイントは仕様どおり」と何度も確認しながら、その仕様自体が2026年5月で失効していた。「19桁の数字」も古い知識。**罠#19（書いた時点で真だったことと今も真は違う）の、Claude側の実例。** 値（楽天が返すエラー本文）を見る前に、持っている仕様の鮮度を疑うべきだった。→ 外部APIの仕様は**使う前にweb検索で現在の姿を確認**する。

---
## この日の教訓（7/21・画像は使えた）
| 予想 | 実測 |
|---|---|
| 画像は権利で無理（7/18の結論） | 権利がクリーンな道(楽天)を測っていなかっただけ |
| Wikidata P18はクリーンで使える | 4.8%＋大半が建物・人物（罠#41） |
| 楽天に写真は無い（0/20） | 40件全部400で届いていなかった（罠#46） |
| ディスク機とカセット機で割れる | 割れたのはJAN検索(90%) vs 名前検索(60%) |
| カセット機の画像は絶望的（3日間の前提） | **楽天で88%。現物写真付き** |
| 楽天APIは前と同じ作法 | 5月に全面刷新（URL/accessKey/Referer/scope/1.5秒） |

**3日間「画像は無理」と積み上げた前提を、実データで覆した。** 測る前に決めない・値を見る前に機構で決めない・有名な結論(19桁数字)も疑う——全部効いた。**カセット機88%がその象徴。**

---
## ★未解決の判断（追加）

### 8. 楽天の商品画像を、自サイトに表示してよいか（要・提携審査＋規約確認）
実測では写真が90%取れる。だが「取れる」と「表示してよい」は別。楽天アフィリエイト提携と商品画像の利用規約で最終確認が要る。道1(P18)と違い提携が前提。**GOは実測まで。表示可否は審査後。**

---
## 次にやること（画像・更新）〔2026-07-21 作成 / 07-23 全面改訂〕

**★このブロックは7/23の実装で1〜3が失効した。4だけが生きている。**

1. ~~鍵をプレースホルダに戻す~~ → 対応済み。
   ~~アクセスキーは再発行~~ → **取り消し（2026-07-23）**。
   7/21に「`pk_` ＝秘密度が高い」と書いた前提が、7/23の実測（accessKeyも
   ページに載せないと動かない＝client-side前提）で**失効していた**。
   **鍵は公開前提なので、替えても露出状態は同じ＝減らせるリスクが無い。**
   守りは「許可されたWebサイト」（`english-tadashi.github.io` を確認済み・
   `github.io` 単体でないので他人のPagesからは使えない）と**スコープ最小化**。
   ★ただし Referer はブラウザ外（curl等）からは偽装可能。防壁は
   「他サイトからの利用」までで、スクリプト悪用は止まらない。
   → **やるべきはスコープ確認であって再発行ではない。**（未実施）

2. ~~楽天アフィリエイト提携＋表示可否を規約で確認~~ → **完了（7/23）**。
   未解決#8は公式FAQで解決＝表示OK。PR明示・`Supported by Rakuten Developers`
   も設置済み。残りは他社アフィリンク併置の個別確認のみ（7/23リスト3）。

3. ~~画像URL列を末尾追加（row[13]）~~ → **却下（7/23）。逆を採用した。**
   中古出品は売り切れるので、事前計算だと画像だけ残ってリンク先が消える
   （規約・罠#27の両方に反する）。→ **ランタイム取得**で実装・公開済み。
   **この項目を復活させないこと。**

4. **（生きている）** 測定・本番とも**ブラウザ上のJS**が本筋。
   罠#53でさらに確定＝localhostは「許可されたWebサイト」に登録できないので、
   **ローカル検証の道が構造的に無い**。公開ドメインでしか確かめられない。
   git導入時は `_experiments/` を `.gitignore` へ（鍵が入りうる）。

→ **現行のToDoは「次にやること（2026-07-23 更新）」を見ること。**

---

## 今日（7/22）やったこと — 機種を35に絞り、発売元を名寄せし、検索を手入力化して公開

| | |
|---|---|
| 機種 | 58→**35のホワイトリスト化**。`build_master.py` の `data_line.js` 生成段に焼き込み。`master_final.csv` は全58機種のまま温存（`check_disc.py`/`check_ps1.py` 用）。約1,927行減 → **37,855行**（data_line.js のみ） |
| 発売元 | `row[6]`(発売元・英) だけを**2段階で名寄せ**（v1=PUBLISHER_CANON / v2=掃除強化＋casefold自動集約＋UPPER_FIX）。`row[5]`(日本語)と master は不変 |
| 検索UI | 機種・発売元のプルダウンを**アルファベット順**化、発売元は**50本以上**に短縮、さらに発売元を**手入力（タイプアヘッド）**に置換 |
| swap_data.py | `--approve-removed N`（上限つき行削除承認）を追加。破損は**フラグ有無に関わらず必ず中止** |
| デザイン | 「検索端末(Field readout)」を **v1として確定**（litim は借り物なので不採用） |
| 公開 | GitHub Pages に `index.html` を手動アップロード（Upload files）。**1枚で自己完結**（data_line.js inline済み・ローカル相対参照ゼロ） |

---

## 機種を58→35にホワイトリスト化（2026-07-22）

- **焼き込み場所**: `build_master.py` の `data_line.js` **生成段**。`master_final.csv` は**全58機種のまま温存**する（`check_disc.py`/`check_ps1.py` が使う）。→ **絞りは `data_line.js` だけ**。生成物の片方だけを絞る非対称は意図どおり。
- **表記統一**: `NINTENDO 64` → `Nintendo 64`。**名寄せ**: `Nintendo Entertainment System` → `Famicom`（罠#16で外国版として混ざっていた機種名を、機種の実体へ寄せる）。
- **除外の内訳**: 大半は `PC-8801`(1012) ・ `Microsoft Windows`(591) ・空欄(184)。約**1,927行減 → 37,855行**。
- **★KEEP見直しが要る**: ホワイトリスト方式なので、**次回のMADB更新で新機種が来ても自動では出ない**。更新のたびに KEEP を見直すこと。（7/20に「PLATFORM_EN ∪ BUYEE_OK で自動的に外れる＝新しい判断を足さない一番安全な線」とした集合を、今回は明示ホワイトリストへ切り替えた。安全さと引き換えに保守コストが乗る。）
- **★ビルド入力の正典**: 英題JSONは **`wikidata_en.json`**（control ビルドで現行を再現できた方）。**`_new` は使わない**。再生成のたびに入力を取り違えないこと。

## 発売元の名寄せ（row[6]だけ・2段階）（2026-07-22）

**`row[6]`(発売元・英) だけを名寄せする。`row[5]`(日本語)と master は不変。** 表示の主役は日本語名で、英側は英語話者の絞り込み用（罠#5＝大文字小文字の割れは完集家の敵、の続き）。

### v1
- 掃除（`©`＋年除去・`[括弧]`除去・`;`連結は先頭社）＋ **PUBLISHER_CANON**。
- 統合: Bandai Namco / Koei Tecmo / Sega / Konami / Square Enix / SNK / Spike Chunsoft / Takara Tomy / Marvelous / SIE / Electronic Arts。
- **分離維持**: Idea Factory と Compile Heart（別法人＝罠#6）。

### v2（v1の穴埋め）
- **掃除強化**: 先頭 `©` を年のあるなしを問わず除去（`^©\s*(\d{4}\s*)?`）。
- **casefold自動集約**: 掃除後の綴りが完全一致する組を、**件数最多の表記に寄せる**（誤爆ゼロ＝同一綴りの大小違いだけを畳む。罠#5の自動版）。
- **UPPER_FIX**: 正規名が全大文字になる組**だけ** Title Case を明示（`MILESTONE`→`Milestone` 等）。**`SNK`/`KID`/`KSS` 等の正当な全大文字社名は対象外**（罠#5「同数なら全部大文字でないほうを選ぶ」を、正当な全大文字を壊さない形へ精緻化）。
- **バケットB追記**: `SQUARESOFT`/`SQUARE SOFT`→Square Enix、`FROM SOFTWARE`→FromSoftware、`HUDSON`→Hudson Soft、`HOT・B`→Hot B、`WOLF TEAM`/`WOLFTEAM`→Wolf Team、`SUNSOFT`→Sunsoft、`KANEKO`→Kaneko。
- **射程外（意図して放置）**: 全角の `（C）…` は低件数・プルダウン外なので放置。

## 検索UIを手入力（タイプアヘッド）に（2026-07-22）

- **プルダウンのアルファベット順化**: `localeCompare`（`numeric:true`）で `PlayStation 2→3→4→5` も数字順に並ぶ。
- **発売元プルダウンは50本以上に短縮**（`PUB_COUNTS` 使用）。
- **発売元を手入力（タイプアヘッド）に置換**: `#pub` を hidden input 化して**確定社名を保持**、可視の `#pubInput` ＋候補 `#pubList` を自前JSで描く。照合は**大小無視・部分一致** ＋ 最小 **ALIAS辞書**（`squaresoft`→Square Enix、`tecmo koei`→Koei Tecmo、`sce`/`scei`/`sie`/`sony`→SIE 等）。`namco`/`hudson`/`d3`/`koei` は部分一致で自動ヒットするので ALIAS 不要。
- **既存の `run()` の `pub.value` フィルタ（433行）は無改変**（表示層に変換を足さない＝罠#8）。

## swap_data.py に --approve-removed を追加（2026-07-22）

- **`--approve-removed N`**（上限つきの行削除承認）を追加。**無条件バイパスはしない設計**（Nを超える削除は止まる）。
- **破損（`[0]〜[6]` の想定外変化）は、フラグの有無に関わらず必ず中止**（罠#21のガードは残したまま、削除だけを上限つきで通す）。
- **発売元の `row[6]` 変更は、行削除ではなく既存の `--allow-publisher-recase` で注入する**。差分ガードv3は `[0]〜[6]` を**多重集合**で照合し、`[6]` のみ変化＝recase として承認する（今日の名寄せは行を消さず値を替えるので、削除承認とは別経路）。

## デザイン基盤: 検索端末（Field readout）v1（2026-07-22）

- 「検索端末(Field readout)」デザインを **v1として確定**。litim（語学アプリ由来の借り物）は**不採用**。
- 明るい計器グレー地。差し色は**朱色1色**を「一致」と「入手経路(Buyee)」の**2箇所だけ**。**型番スラブが顔**（型番を主役にした7/18の判断を、画面デザインでも中心に据えた）。
- 書体: **Space Grotesk / IBM Plex Sans JP / IBM Plex Mono**。**明朝は使わない**。

## 公開（2026-07-22）

- **GitHub Pages** に `index.html` を手動アップロード（web の Upload files）。
- `index.html` **1枚で自己完結**（`data_line.js` は inline 済み、ローカル相対参照ゼロ）。

---

## パイプラインの罠（追加）

### 49. swap_data.py は master から生成しない（役割分担の再確認）（2026-07-22）
`build_master.py` が `master_final.csv` と `data_line.js` の**両方**を生成する。`swap_data.py` は**既製の `data_line.js` を `index.html` に注入するだけ**で、master からは何も作らない。長い作業の中で「swap で master から作り直す」と混同しかけた。罠#20（「1コマンドに統合」は生成側しか守っていない）の役割分担版。**生成は build_master、注入は swap_data。境界を跨がせない。**

### 50. 「注入したつもりで完走していない」を、公開前に const DATA を数えて捕まえる（2026-07-22）
長い作業中、**注入したつもりで完走していない**（DATAが古いまま／途中で止まっている）が**複数回**起きた。→ **公開前に必ず `index.html` の `const DATA` を数え、`SQUARESOFT=0`（名寄せが効いている）／`rows=37855`（機種絞りが効いている）を確認する**習慣にする。罠#14（コードにバージョンを名乗らせる）・罠#20（貼り込み工程は誰も守っていない）・罠#38（画面では分からない）の家族＝**「やったつもり」は、生成物を直接数えるまで確定しない。**

### 51. row[6] の変更は行削除ではなく --allow-publisher-recase で通す（2026-07-22）
発売元の名寄せは `[6]` の**値だけ**を替え、行は1つも消さない。だが `swap_data.py` の差分ガードv3は `[0]〜[6]` を**多重集合**で照合するので、素で流すと「`[6]` が変わった＝想定外変化」で止まる。→ 削除承認（`--approve-removed`）ではなく、**既存の `--allow-publisher-recase`**（`[6]` のみ変化＝recase として承認）で通す。**「値を替える」と「行を消す」は別の承認経路。** 混同すると recase を削除枠で通そうとして上限に当たる（または無関係な削除を承認してしまう）。罠#21（ガードは残す）の運用面。

### 52. 全角の「（C）…」は射程外＝意図して放置（2026-07-22）
発売元掃除は先頭の半角 `©`（＋年）を除去するが、**全角の `（C）…` は射程外**。件数が少なくプルダウンにも出ないため、**今回は意図して放置**した。「掃除し残し」ではなく「掃除しないと決めた」＝罠#19（計画・想定を事実として記録する）を避けるため、**放置は放置と明記する。** 将来プルダウンに全角©付き社名が現れたら、そのとき射程に入れる。

---

## この日の教訓（7/22・絞りと名寄せと手入力）
| 予想 | 実測 |
|---|---|
| 発売元は v1（PUBLISHER_CANON）で名寄せし切れる | **v1に穴**（掃除後の綴り違い・全大文字化）。v2で casefold自動集約＋UPPER_FIX が要った |
| 全大文字は一律で畳めばいい | `SNK`/`KID`/`KSS` は**正当な全大文字社名**。一律 Title Case は壊す。UPPER_FIX を組限定に |
| 注入は1回で完走する | **複数回、未完走のまま進みかけた**。`const DATA` を数えて初めて発覚（罠#50） |
| 発売元はプルダウンで足りる | 数が多すぎる。50本以上に絞り、さらに手入力（タイプアヘッド）へ |

**表示（日本語・master）を一切触らず、英側の絞り込みキー（row[6]・プルダウン）だけを直した。** 変換は表示層に置かない（罠#8）を守ったまま、名寄せはパイプライン側で済ませた。

---

## 次にやること（2026-07-22 更新）
- **フッターの「12,208 titles」を直す**: これは**ハードコードの旧文言**で未修正（実害なし）。直す前に、**その数字が何を数えているか**（全件か・型番付きか・35機種か）を確認してから直すこと（罠#37＝基準値には「いつ・どの構成で測ったか」を添える、の文言版）。
- **ホワイトリストの KEEP 見直し**: 次回MADB更新で新機種は自動では出ない。更新のたびに KEEP を見直す（今日の絞りの保守コスト）。
- （継続）棚案内（本丸）／楽天画像の提携審査（未解決#8）は 7/21 のまま。

---

## ★未解決の判断（追加）

### 9. ~~フッターの「12,208 titles」を、いつ・何の数字に直すか（未修正・次回対応）~~ → **解決・差し替え済み（2026-07-23夕）**

**差し替え後のフッター:**
```
Kataban · 37,855 entries from the Agency for Cultural Affairs Media Arts Database.
10,296 carry official product codes; 13,487 have English titles.
English titles matched from Wikidata (CC0).
Photos & prices via Rakuten affiliate links (PR) · Supported by Rakuten Developers
```

**★「Publisher data is complete」を削除した経緯**: この記述は**12,208行時代（＝型番あり群）には100%で真だった**。
7/21に27,574行を足した瞬間に嘘になり、誰も気づかないまま残っていた。全37,855行では**94.58%**。
**実測値への書き換えではなく削除を選んだ**——**文が増えるより減るほうを選ぶ**。
（罠#62。数字は目立つので気づくが、`complete` のような**断定の形容詞は数字より危険**＝更新漏れが目に見えない。）

<details><summary>（当時の記述・記録として保存）</summary>
フッターは `Prototype · 12,208 titles …` と**ハードコードの旧文言**のまま（7/19に本文は差し替えたが数字は据え置き）。今日 `data_line.js` は **37,855行 / 35機種**になったので、`12,208`（＝型番あり時代の値）は**もう現状と合っていない**。ただし**実害はない**（表示のみ）。→ **次回対応**。直す前に、**その数字が何を数えるべきか**を先に決める: 全件(37,855)か・型番付きか・35機種の作品数か。罠#37（基準値には「いつ・どの構成で測ったか」を添える）の文言版＝**数える対象を決めてから、数字を入れる。**
</details>

## 楽天の登録情報とID — 記録漏れの補記（2026-07-23）

**7/21に楽天ウェブサービスのアプリ登録を済ませ、applicationId を発行済み**（セッション「画像の追加方法」）。当時ノートに記録し忘れていた。楽天WSの**「アプリ情報」画面に4項目**が並ぶ：アプリケーションURL / **applicationId** / **affiliateId** / accessKey。**applicationId と affiliateId は両方この画面で取得済み**（画像＝商品検索APIは applicationId で動く。affiliateId は本番リンクの報酬紐付け用）。

- **ID・鍵の生値はノートに書かない。** 正典は楽天WSの「アプリ情報」画面。必要になったらそこで読む。
- **書式（桁数・接頭辞）は記憶・過去メモを信じない。** アプリIDが何桁か等は、画面のラベルが唯一の答え（罠#48：Claudeの「19桁」も古い知識だった、の運用）。
- ~~★accessKey は公開前に再発行する~~ → **取り消し（2026-07-23）。
  上記「次にやること（画像・更新）」1 を参照。鍵は公開前提のため再発行に意味がない。**
  applicationId・affiliateId も同様に露出前提。**危ないのは鍵の露出ではなく、
  許可Webサイトの緩さとスコープの広さ。**

### 楽天の画像利用規約 — web確認できたこと（2026-07-23）
罠#48（外部仕様は使う前に web で現在の姿を確認）に従って現行規約を確認した。
- **OK**：楽天アフィリエイトのリンク作成ページ由来の商品画像を、**アフィリエイトリンクと一緒に**掲載。リンク先が楽天市場と分かる形であること。
- **NG**：リンク作成ページ以外の楽天画面の無断スクショ/DL。画像の**切り抜き・文字乗せ**（サイズ変更・周辺加工は可）。
- 楽天トラベルは「API取得画像のみ可」＝**API は正規の取得経路として認められている**（市場APIで測った道はこの線）。
- **PR明示**（アフィリエイト広告である旨）が要る（ステマ規制, 2023-）。

### ★未確認（出す前に読む）
**「アプリが API の画像URLを表示してよいか」は、公式の楽天ウェブサービス規約で最終確認する。** 上の OK/NG は主に「ブログがリンク作成ページの画像を DL 掲載」する話で、**アプリの API 画像表示は別条項のことがある**。表示可否は条項を読んでから（未解決#8 の最終確認）。罠#19（計画を事実として書かない）：ここは「未確認」と明記する。

### ~~★提案（要・本人確認）：画像は row[13] 事前計算 → ランタイム取得に見直す案~~ → **決着：ランタイム取得を採用・実装済み（2026-07-23）**
7/21 の「次にやること」は「画像URL列を末尾追加（row[13]）」だった。これを見直す案：**中古出品は売り切れる**ので、事前計算だと画像だけ残ってリンク先が消える＝「画像はリンクと一緒に」の規約にも罠#27（誤った値より無い方がまし）にも反する。→ **JAN で都度検索し、今ある出品があれば画像＋アフィリンクを一緒に出し、無ければ何も出さない**（ランタイム取得）。代償：カードごとに API を1回叩く＝1ファイル完結が少し崩れる（罠#10 のトレードオフ、バーコード WASM と同じ）。**まだ決定ではない。本人が row[13] 案と比べて選ぶ。**
**→ 選んだ結果（同日）: ランタイム取得を採用し、実装・公開まで完了。
下記「今日（7/23）やったこと」の表を参照。この節は「提案」ではなく
「採用された案の検討記録」として読むこと。**

## 今日（7/23）やったこと — 楽天の商品画像を実装して公開（アフィリエイト導通まで確認）

| | |
|---|---|
| 画像 | **実装して公開**。JAN(row[10]) → 楽天Ichiba検索 → **画像＋価格＋アフィリンク**をカードに表示 |
| 取得方式 | **row[13]の事前計算ではなくランタイム取得**（中古は売り切れる。保存しないので規約にも適合） |
| 位置 | 「Buy this game」ブロックの**直前**。画像は「識別」ではなく**買う判断の錨**（識別は型番が exact match で果たしている） |
| 価格 | `itemPrice` を **`Buy on Rakuten from ¥1,070 (PR)`** の形で表示。クリックで買えることを文言で明示 |
| 導通 | **着地URLに `rafcid=` を確認＝アフィリエイト経由で繋がった**（今日いちばん重要な検証） |
| 規約 | **未解決#8が解決**（下記）。フッターに PR明示 と `Supported by Rakuten Developers` の**2つを別々に**設置 |
| 守備範囲 | JAN持ちは **37,855行中 11,417行（30%）**。非13桁のゴミはゼロ。JAN無し行は自然に画像なし（罠#27） |

→ ★2026-07-31訂正: 上表「価格」の `Buy on Rakuten from ¥1,070 (PR)` は本日2行構成に変更した。行1（リンク内・(PR)あり）= `Buy this one on Rakuten: ¥{在庫あり最安} (PR)`、行2（リンク外・(PR)なし・`.rak-market`）= `Used on Rakuten: ~¥{中央値}`。詳細は当該日セクション参照。罠#117（表示を消すと、それを説明する記述が嘘になる）と同型の対応。

### 楽天API — 現在の姿（2026-07-23時点で実測）

- **エンドポイントは `.../IchibaItem/Search/20260401`。** 7/21に測った `20220601` は **2026-08-17で完全廃止**。
- **検索は `applicationId` と `accessKey` の両方が必須**（片方だけだと `must be present` で弾かれる）。
  → **accessKey はページに載る**＝client-side前提の設計。**守りは「許可されたWebサイト」だけ**。
- **鍵の正典は楽天WSの「アプリ情報」画面**。桁数・接頭辞を記憶や過去メモで判断しない（罠#48）。
- **アプリケーションURL ≠ 許可されたWebサイト。** 認証で効くのは**後者**。前者は看板。

### 未解決#8（楽天画像を自サイトに表示してよいか）→ **解決・表示OK**（2026-07-23）

公式FAQ（楽天ウェブサービス「各APIで取得した情報はどのような目的で利用できますか？」）に、
**「利用者ソフトウェア上で当社の商品を紹介し、かつ当社内の当該商品のページにリンクを設ける目的で利用できる。この目的以外での複製・改変は禁止」**とある。Bin Diver がやること（現物写真＋`affiliateUrl` で楽天の商品ページへ送る）は、そのままこの目的に当たる。

**設計が規約要件を自動的に満たしている点:**
- **保存しない**（ランタイム取得＝表示のたび取り直す）→「複製」に当たらず、更新頻度の条項もクリア
- **切り抜かない・文字を乗せない**（`object-fit`/`max-height` を使わず、キャプションは画像の外）→「改変」禁止に適合
- **画像は必ず `affiliateUrl` の `<a>` で包む** → 収益は楽天アフィリエイト経由のみ（規約第10条4項＝APIで楽天アフィ以外の方法で収入を得るのは禁止）
- **PR明示**（ステマ規制）と**帰属表示**（`Supported by Rakuten Developers`）は**別要件**なので両方置く

**★残る未確認（意図して未確認と明記）**: 同じ画面に Buyee/eBay のアフィリンクが併置されている点が第10条4項に触れないか。安全側の線＝**楽天画像は楽天アフィリンクとだけ組にする**は満たしている。確実にするなら公式の「利用規約に違反しているか個別に確認したい」フォームで構成を書いて確認する（一発勝負にしないための正攻法）。

---

## 今日（7/23夕）やったこと — 改名・ドメイン・駿河屋

| | |
|---|---|
| 改名 | **Bin Diver → Kataban**。`<title>`・`<h1>` を変更（`<h1>Kata<span class="blk">ban</span></h1>`。span と `class="blk"` は維持し、分割位置だけ変えた） |
| ドメイン | **gamekataban.com 取得**（Cloudflare Registrar **$10.46/年・更新同額**） |
| DNS | `CNAME @ / www → english-tadashi.github.io`。**Proxy は必ず DNS only（グレー雲）** |
| 楽天 | 「許可されたWebサイト」に **gamekataban.com を追加**。旧 github.io は**残置** |
| 検証 | ★**追加前は画像なし → 追加後に表示**。**Referer防壁が実際に効いていることを実測**した |
| フッター | **嘘を2つ削除**（下記・未解決#9） |
| OGタグ | `description` / `og:title` / `og:description` / `og:url` / `og:type` / `twitter:card` を**新設**。**`og:image` は素材が無いので入れていない** |
| 駿河屋 | **.com を解明**、**アフィリエイト登録**、**.jp 型番リンクの改善を2つ確定**（下記） |

---

## 駿河屋.com（海外向け）— 2026-07-23 実測

**URL 形式**: `https://www.suruga-ya.com/en/products?category=2&keyword={英題}[+機種名]`

| 項目 | 実測 |
|---|---|
| `category=2` | **必須**。Video Games。無いとサントラ・同人・攻略本が混ざる（**Terranigma 429件 → 2件**） |
| `in_stock=t` | 「在庫のみ」ではなく**「品切れも表示」**。★**既定が在庫のみ** |
| 索引 | **英題のみ**。型番×（**商品ページに印字すらない**）・日本語×（Japanese title として表示されているのに `クロノトリガー` で0件） |
| 索引範囲 | 商品名 + **ジャンル** + カテゴリ（`keyword=Fighting` で格闘ゲームが並ぶ） |
| 訳 | **商品ごとに独立**。サクラ大戦3=`Sakura Taisen` / 4=`SAKURA WARS` → **row[0] を磨いても、キーを変えても届かない。相手側の問題** |
| 的中 | ★**予測不能**。Chrono Trigger ○ / Super Mario World ×（同条件・同知名度） |
| 機種名 | **足しても壊さない**（Chrono Trigger 1→2件）。他機種版が落ちる（Sakura Wars 4 で PS4版が消え、本体が6位→2位に） |
| 判定 | **Kataban 側で当たり外れを機械判定する道は無い。** 1件でも別物が返る（Real Bout Fatal Fury 2 → 餓狼伝説バトルアーカイブズ2） |
| 文言 | Buy ではなく **"Search Suruga-ya.com (English · ships overseas)"** |
| 価値 | 当たれば**中古価格・USD換算・カートまで1ページで完結**する唯一のリンク |
| 副産物 | **商品IDは .jp と共通**（`/en/product/{id}` ↔ `/product/detail/{id}`）。ただし **Kataban は商品IDを持たない**ので今は使えない |
| その他 | 20通貨表示・多言語（英/日/中簡繁/西/葡）・免税店リストあり・**地域制限あり** |

**★未測定**: seed 42 の30本（乱数シード固定・`scratchpad/sample30.py`）。
指標は**「目的の物が1ページ目にあるか」**。**出す/出さないを決める唯一の数字。**

---

## 駿河屋アフィリエイト（2026-07-23 登録完了）

**リンク形式**:
```
https://affiliate.suruga-ya.jp/modules/af/af_jump.php
  ?user_id={ID}&goods_url={任意の suruga-ya.jp URL をエンコード}
```
★**転送型の汎用ラッパー**。任意URLに貼れる。**検索結果ページも公式に対象**（「商品リスト検索結果をご紹介頂く場合」と明記あり）。
→ 第5条1項（広告素材の改変禁止）は**「URLを入れ替えて使う」を許す解釈で確定**。

| 項目 | 内容 |
|---|---|
| ★実装罠 | **`goods_url` は二重エンコードが必要**。inner を作ってから `encodeURIComponent(inner)` を**もう一度**かける。1回だと `&` で割れて**静かにトップページへ着地する** |
| rel | **`nofollow` を必ず付ける**（提供素材に入っている。外すと改変と見られ得る） |
| user_id | ★**ノートに書かない**。正典は駿河屋アフィリエイト管理画面（楽天の鍵と同じ運用。罠#48） |
| Cookie | **14日間**。別のアフィリエイター経由が挟まると**最後の訪問者に移る** |
| 審査 | ★**事前審査なし**。ただし規約違反発覚時は**即時退会＋その時点のコミッションも支払われない**（事前審査が事後審査になっただけ） |
| スマホ | **対応・計測される**。ガラケーのみ不可（FAQで確認。旧「PCサイトに限ります」は古い文言） |
| ★自己クリック | **対象外**。→ **測定・動作確認は必ず生URLで行い、`af_jump` を通さない** |
| 最低支払 | **5,000円**から。振込手数料**300円**が差し引かれる |
| 成立 | **発送＋支払い完了時**。**2ヶ月成立しなければキャンセル**（中古は品切れが起きる） |
| 対象外 | 駿河屋**マーケットプレイス**の売上 |
| 禁止 | ポイントサイト / 「駿河屋」名でのリスティング広告 |
| SNS | 掲載可。ただし**「登録者情報変更」下部のURL欄にSNSのURLを申請してから**。個人宛メッセージなど**非公開メディアでの告知は不可** → ★**Reddit/Discord に貼る前にやること** |

**★未確認1**: **.com が成果対象か。** 規約第2条は「駿河屋JPに出品されている商品」と名指し。
→ **対象外と想定して設計する**（.com は**収益なしの純サービスリンク**）。
**★未確認2**: **料率。** 管理画面「販売も買取も4%」／公開ページ「ゲーム中古4.2%（ランクで5.0%）」で**食い違う**。成果発生時に**コミッション一覧の実額**で確認。

**データフィード**: ★**無い見込み**。管理画面のメニューは 売上一覧／アクセス一覧／コミッション一覧／登録者情報変更／アフィリエイトタグ **のみ**。
→ **商品ID直リンクへの道は現状なし。検索リンクで進める。**

---

## .jp 型番リンクの改善 — 2つ確定（2026-07-23）

**確定した生成ルール:**
```js
const code = row[3].replace(/-JPN$/, '');
https://www.suruga-ya.jp/search?category=2&search_word={code}&searchbox=1
// これを af_jump の goods_url に入れる（二重エンコード）
```

**(1) `category=2` を足す — 採用**
9本測って**回帰ゼロ、改善1件**。
スーチーパイアドベンチャー ドキドキナイトメア **9件→1件**。
★**落ちたのはサントラと公式ガイドブック、残った1件はPS本体＝落としたかったものが落ちた。**
型番リンク（`SLPS02430` / `HVC-UW` / `CGB-BFWJ-JPN` / `PLJM-16617`）は**全て件数維持**。

**(2) 型番末尾の `-JPN` を外す — 採用**

| 型番 | 変更前 | 変更後 | 備考 |
|---|---|---|---|
| `SHVC-ACTJ-JPN` | **0件** | `SHVC-ACTJ` **2件** | クロノ・トリガー／スーファミ救済 |
| `SHVC-A3CJ-JPN` | **0件** | `SHVC-A3CJ` **4件** | スーパードンキーコング3 |
| `SHVC-AQ3J-JPN` | **0件** | `SHVC-AQ3J` **7件** | ドラゴンクエスト3 |
| `CGB-BFWJ-JPN` | 1件 | `CGB-BFWJ` 1件 | ★**壊れない側を測って壊れなかった** |

→ **無条件で外してよい。実装は1行。**

**その他の実測:**
- **.jp はハイフンの有無を吸収する**（`SLPS-02430` も `SLPS02430` も通る）
- **駿河屋自身が生成するリンクには `category=2` が入っている**
- ★**未測定**: `&ck=true&inStock=Off` の意味。.com の Out of stock トグルと同じ役割かもしれない
- ★**未測定**: `-JPN` が `data_line.js` に何行あるか、**機種別の内訳**。**実装前に手元で数える価値がある**

---

## パイプラインの罠（追加）

### 53. `localhost` は「許可されたWebサイト」に登録できない＝ローカル検証の道が構造的に無い（2026-07-23）
localhost で叩くと **HTTP 503 `Authentication service error`** が返り続けた。レート制限(`too_many_requests`)でもパラメータ不正(`wrong_parameter`)でもない。原因は**許可されたWebサイトに localhost が入っていないこと**で、しかも**入れようとしても登録できない**（欄が受け付けるのは実在する公開ドメインのみ。ワイルドカード対応だが localhost/127.0.0.1 は不可）。
**→ 新APIを使う機能は、ローカルでは検証できない。公開ドメインで確かめるしかない。** 7/21の「ローカルPythonは新APIを叩けない」（Referer/Originがスクリプトから設定不可）に続く**第2の壁**で、こちらは**ブラウザでも塞がっている**。
教訓: **503は「サーバの不調」と読みたくなるが、本文を読むまで決めない。** `Authentication service error` という文言が、レート制限説を否定して認証説に寄せた（罠#12の実行環境版）。**エラーは種類ごとに本文を見る。**

### 54. `max-width` は上限であって下限ではない（画像が「大きくならない」の正体）（2026-07-23）
CSS で `max-width:280px` にしても画像が 140px のままで、しかも `min-height:290px` だけが効いて**下に大きな空白**ができた。原因は、楽天の `mediumImageUrls` が **`?_ex=128x128` の128px実物**だったこと。**上限を上げても実物は大きくならない。**
→ 正解は**画像URLの `_ex` を書き換える**（`_ex=300x300`）。楽天が用意しているサイズ違いを要求するだけなので、**切り抜き・加工には当たらない**（規約適合）。実測で `getBoundingClientRect().height = 300` を確認。
**「大きくならない」と「下の空白」は、同じ症状の表と裏だった。** 罠#38（画面では分からない）の逆＝**画面に出た2つの異常が、1つの原因を指していた。**

### 55. 入れた対策のうち、実際に効いているのはどれかを言い分ける（2026-07-23）
「クリックできると分かるように」と `cursor:pointer` と下線の2つを入れたが、**`cursor:pointer` は実質no-op**（`href` を持つ `<a>` はブラウザ既定でポインタになる）。**効いていたのは下線だけ。** 害はないので残したが、**「対策を入れた」と「その対策が効いた」は別**。罠#17（判断は生きたが理由は死んだ）の小型版。

### 56. 失効したのは判断ではなくToDoだった（2026-07-23）
7/21の「accessKeyを再発行する」は、当時の前提（`pk_`＝秘密度が高い）の上では
正しかった。2日後の実測がその前提を壊したが、**壊れたのは前提だけで、ToDoは
生き残った**。しかも「再掲」として2箇所に増えていた。
罠#17（判断は生きたが理由は死んだ）の**裏返し＝理由が死んだのに判断だけ生きた**。
→ **前提を覆す発見をしたら、その前提から派生したToDoを検索して回収する。**
発見の記録だけでは足りない。**「再掲」は特に危険＝訂正が届かない複製。**

### 57. 「実測値に合わせる」の、その実測が n=1 だった（2026-07-23）
7/23午前に `min-height:310px`（実測300px＋1割）を次の一手に置いた。
午後に8枚測ると高さは **187 / 216 / 225 / 300×5**、幅は150〜300。
`_ex=300x300` は**パディングではなく上限（バウンディングボックス）**で、
楽天は**引き伸ばさない**（元が150x187ならそのまま返る）。
**300ちょうどは8枚中1枚。** 午前の「実測300px」は、たまたま正方形の1枚だった。
→ 固定 `min-height` はどの値でも「短い画像の下の空白」か「跳ね残り」を作る。
**入れないと決めた**（罠#52＝やらないと決めたことは、やらないと明記する）。

同時に、**跳ねは2回あった**ことが判明。実測16件中8件が `0x0`＝
`<img>` はDOMにあるがバイト未着の状態。数秒後の再測で解消したので、
壊れた画像ではなく読み込み中だった（罠#12を踏まずに済んだ）。

罠#37（基準値には「いつ・どの構成で測ったか」を添える）の**標本数版＝
n=1は基準値ではない。**

### 58. ログが1行も出ないのは「その分岐に入っていない」という情報（2026-07-23）
画像の先読み（`_rakPreload()`）を実装したら**画像が0枚**になった。
切り分けの記録：
- Network: 画像は**全件200で成功**（Initiatorが `_rakPreload` の行）＝DL は通っている
- Console: **赤いエラーはゼロ**＝例外ではなく、条件分岐で return している
- スロット調査: 旧版は `connected:true children:1` が23件全部
  ＝`isConnected` は容疑から外れた
- 診断ログ（DECODE_REJECT / ZERO_SIZE）を仕込んで公開 → **1行も出ない**
- 同じURLで `decode()` を単体実行 → **`OK 150 192`＝decodeは正常に動く**

**ログの不在が答えだった。** どちらの分岐にも入っていない＝
`_rakPreload()` の中では落ちていない。原因は `dim` を受け取った**後ろ**にある。
**未確定のまま、旧版に復元して打ち切った。**

教訓: **「ログが出ない」を「測定失敗」と読まない。** どの分岐に入らなかったかを
指している。罠#46（0/20は写真ゼロではなく門前払いだった）の裏返し＝
**沈黙も値である。**

### 59. 短くすれば救えるとは限らない（2026-07-23）
駿河屋.com で0件だった長い題を、コロンの前で切って再測した。
  `Real Bout Fatal Fury 2: The Newcomers` → **0件**（正直な空振り）
  `Real Bout Fatal Fury 2` → **1件**（餓狼伝説バトルアーカイブズ2＝**別物**）
**3本中0本しか救えず、代わりに誤ヒットを1つ作った。**
緩い部分一致では、**クエリを削る＝ANDの条件を捨てる**ことなので、増えた分は全部ノイズになりうる。
罠#27（誤った値より無いほうがまし）の検索版。→ **`split(':')` は実装しないと決めた。**

### 60. 「同じ会社の同じ商品」でも、索引は別物（2026-07-23）
駿河屋.com の商品ページには `Japanese title: スーパーファミコンソフト クロノトリガー` と
**表示されている**のに、`クロノトリガー` で検索すると**0件**。型番は商品ページに**印字すらない**。
**表示用のフィールドと検索インデックスは別。** .jp と .com は同じ会社の同じ商品でも**別DBとして扱う**。
罠#25（表示の正解と検索の正解が違う）の上位版＝**表示されているものが、引けるとは限らない。**

### 61. 件数は「当たり」の指標にならない（2026-07-23）
  **1件** `Terranigma` → 正しい
  **1件** `Real Bout Fatal Fury 2` → 別物
  **11件** `Art of Fighting 3` → 全部別物（`Fighting` がジャンルとして索引されている）
  **8件** `Sakura Wars 4` → **6・7番目に本体**（1件目で採点すると誤って×になる）
**少ないことは正しさを意味せず、多いことは外れを意味しない。**
→ 指標は**「目的の物が1ページ目にあるか」**。順位は問わない（ユーザーの行動そのまま）。
この日、**指標を3回作り直した**（件数 → 1〜10件帯 → 1件目 → 1ページ目）。
罠#37（基準値には条件を添える）の指標版＝**何を数えるかを決めてから測る。**

### 62. UIの記述は、行を足した瞬間に嘘になる（2026-07-23）
フッターの「Publisher data is complete」は、書かれた時点（**12,208行・型番あり群**）では
**100%で真だった**。7/21に**27,574行を足した瞬間に94.58%**になり、誰も気づかないまま残っていた。
罠#57（母数の取り違え）の表示版。
→ **データを増やしたら、UIに書いてある数字と断定文を全部洗う。**
数字は目立つので気づくが、**`complete` のような断定の形容詞は数字より危険**＝
**更新漏れが目に見えない。**

### 63. バックアップのファイル名に連番を使うと、世代を潰す（2026-07-23）
改名作業で「`index.html.bak4` にバックアップ」と指示したが、**`bak4` は7/20の別世代（2.5MB）で既に存在していた**。
git が無く `.bak` だけが履歴という状況で、**既存の連番を指定するのは世代を1つ消す指示**だった。
Claude Code が上書きせず別名（`index.html.20260723-bak-pre-kataban`）で退避して報告したため事故にならなかった。
→ **バックアップ名は日付＋目的にする。連番は使わない。**

### 64. 単位の違う数字を照合させない（2026-07-23）
改名の検証基準値として「`ドラゴンクエスト` 197件」を置いたが、**197は「ファイル内の文字列出現回数」**で、
**UIに出る148は「行数」**だった。1行に `row[1]`/`row[8]`/`row[7]` と複数回入るので、
**197回が148行に収まるのは正常。**
→ 正しい検証は **DATA行単独の md5 照合**（`d271f504…` 一致）と、**ローカル＝公開の一致**。
罠#37の単位版＝**基準値には「何を数えたか」を添える。**
（なお `ドラゴンクエスト` は 7/21時点で**153行**、現在**148行**。差の5行は7/22の機種35絞り込み
（`Microsoft Windows` 591行等の除外）による見込み。**★未確認**）

---

## 実装メモ（画像レイヤーの構造）

- `card()` は**要素ではなく文字列を返す**ので、「要素に結線」は成立しない。→ `card()` は `<div class="rak-slot" data-jan="…">` を吐くだけにし、**DOM投入後に `paint()` が `attachRakutenSlots()` で結線**（`:not([data-rak])` で二重結線を防止）。罠#49（生成と注入の境界）と同型の分離。
- **キャッシュ短絡は `rakutenLookup` 側（鎖に入る前）に置く。** `_rakFetch` 内だと、既読JANでも直列チェーンに並んで1.6秒スペーサーを積む。`paint()` は Load more のたびに全カードを作り直すので、2ページ目以降で列が伸び続ける。
- **`slotEl.isConnected` を見る。** 1.6秒待つ間にユーザーが検索し直すとカードは捨てられている。捨てられたノードに書かない。
- **`innerHTML` を使わない。** `hit.img`/`hit.url`/`itemName` はAPI由来の文字列で、`"` が入ると属性を割られる。`img.src=` と `textContent` なら割られない。
- **`toLocaleString('ja-JP')` と明示する。** 閲覧者は英語圏＝ロケール未指定だと環境依存になり、`hi-IN` では `100000` が `1,00,000` になる（日本円の桁区切りとして誤り）。**桁を読み違えさせたら、この機能の目的そのものを損なう。**
- ~~`min-height` は実測値に合わせる（画像300px → `min-height:310px`）~~
  → **撤回（2026-07-23午後・罠#57）。** この「実測300px」は**n=1**だった。
  8枚測ると高さは **187/216/225/300×5** に割れる（`_ex=300x300` は上限であって
  パディングではない）。**固定 `min-height` は入れない。**
  ただし後半の観察は生きている＝**跳ねと空白は同じつまみの両端**。
  正解は固定値ではなく、バイト到着後に `naturalWidth/Height` を
  width/height 属性に入れて挿すこと（未実装・罠#58）。
- レート対策: **見えたカードだけ**（IntersectionObserver）× **直列1.6秒間隔** × **JANキャッシュ**。

### ドル併記は見送った（2026-07-23）
理由3つ: (1) **正確なレートを持っていない**——固定レートは必ずいつか嘘になる（罠#19）。(2) 為替APIを足すと依存が増える（1ファイル完結の代償に見合わない）。(3) **目的が違う**——この価格は「送金額」ではなく**日本での相場感**で、代行手数料・国際送料が乗るので結局その数字では買えない。
→ やるならフッターに**ざっくり注記**（`¥1,000 ≈ $6.5 · rates vary`）。本命は将来の **landed cost 計算機**（代行手数料＋国際送料＋為替）＝プレミアム候補。

### 導線の序列（画像を足しても変えない）
**Buyee が主役の導線**（海外発送・英語対応＝ターゲットの本命）。楽天は国内発送・日本語決済なので**海外の完集家には第2候補**。だから `Buy on Rakuten` は `Buyee` ボタンより**控えめのまま**にする。**目立たせると、届かない相手に一番大きい導線を出すことになる。**

---

## この日の教訓（7/23・画像）
| 予想 | 実測 |
|---|---|
| localhost で検証してから公開する | **localhost は許可Webサイトに登録できない**。検証の道が無い（罠#53） |
| 503 はサーバ不調かレート制限 | **本文は `Authentication service error`**＝認証。文言が説を分けた |
| CSS で `max-width` を上げれば大きくなる | **実物が128px**。上限は下限ではない（罠#54） |
| 画像は「識別」を助ける | 識別は**型番が exact match で完了済み**。画像の役は**買う判断**（コンディション＋価格） |
| 7/21の「applicationIdだけで動く」 | **accessKey も必須**。罠#48の表のほうが正しかった |

**3日前に「画像は権利で無理」だったものが、測って・規約を読んで・実装して・アフィリンクの導通まで通った。** 決め手は毎回同じ——**測る前に決めない。エラーは本文を読む。仕様は使う前に現在の姿を確認する。**

---

## 店員モード・コピー・リージョン注記（2026-07-24）

退避: `index.html.20260724-bak-pre-clerk` / `BIN_DIVER_NOTES.md.20260724-bak-pre-clerk`
**DATA行は無傷**（md5 `c7f3a74aee3e283d6408e080c239e55d` が作業前後で一致。37,855行・13列のまま）。
`build_master.py` / `swap_data.py` / `data/` には触っていない。UI（CSS+JS）だけで +341 / −17 行。

### 「書体を直す」は、直すものが無かった

作業指示は「日本語タイトルを IBM Plex Sans JP に、型番を IBM Plex Mono に、`text-shadow` を削除、
Press Start 2P はラベル・ボタン・ロゴだけに残す」だったが、**現物にはその状態が既に無かった**:

| 探したもの | 実測 |
|---|---|
| `Press Start 2P` | **0件**（`index.html` にも `_experiments/` にも、リポジトリ全体で0） |
| `text-shadow` | **0件** |
| `.ja-title` / `.jp-chip` / `.spec .v.jp` | 既に `--f-jp` = `IBM Plex Sans JP` |
| `.code`（型番） | 既に `--f-mono` = `IBM Plex Mono` |

7/22の「検索端末(Field readout) v1」で入れ替わっていた。**罠#19の逆向き**——
ノートが計画を事実として記録するのと同じ理屈で、**記憶は「直っていない」を保持し続ける**。
→ **直す前に、まず現物を grep する。** 0件なら、その作業はもう終わっている。

### コピーは「成功の顔をして落ちる」実装だった（罠#65）

```js
try{ if(navigator.clipboard) navigator.clipboard.writeText(text); }catch(err){}
label.textContent = '✓';        // ← 成否に関係なく必ず出ていた
```

穴が2つ。**(1) `writeText` は Promise を返すので、reject は `try/catch` の外を通る**
（捕まえていたのは同期例外だけ＝実質何も捕まえていない）。
**(2) 成否を見ずに `✓` を出していた。** 落ちる条件は棚の前で普通に起きる——
非セキュアコンテキスト、アプリ内WebView（Instagram/X のブラウザ）、権限拒否。
**「コピーしたつもりで、店の検索窓に何も貼れない」**＝罠#12（例外がデータの不在に化ける）のUI版。

直した形（`copyText()` が `true/false` を返す。推測しない）:

| 経路 | 実測 |
|---|---|
| clipboard 成功 | `✓` / トーストなし |
| `navigator.clipboard` が無い → execCommand | `✓`（await を1つも通らないので iOS のユーザー操作の文脈が生きる） |
| clipboard reject → execCommand 成功 | `✓` |
| **両方失敗** | **`Hold` ＋「長押しで選択」トースト** |

- `.code` / `.jp-chip` に `user-select:all`。**落ちた端末では長押し選択が唯一の経路**で、
  単語単位で切れると型番が半分しか取れない。
- ~~★**iOS の実機はまだ通していない**~~（4経路はヘッドレスでクリップボードを差し替えて検証した）。
  → **Mac / Android / LINE WebView は実機で通した（下の「コピー機能の実機検証」節）。iOS は依然として未検証。**

### スマホの COPY ボタンは、無地の黒い枠だった

`@media(max-width:420px){ .card .slab-copy .txt{display:none} }` ——
**ラベルを消しただけでアイコンは無かった**ので、iPhone幅では**何も書いていない四角**になっていた。
`.code` は折り返すのでラベルを残しても型番は溢れない。→ `display:none` をやめ、`font-size:11px` に縮めた。
**押せると分からないボタンは、無いのと同じ**（しかもコピーはこの道具の中心操作）。

### 型番が桁の途中で割れていた（罠#66）

区切りが `&nbsp;·&nbsp;` ＋ `word-break:break-all` だったので、
**「唯一折り返せる場所が型番の内側」**になり、4型番の行で `KTGS-40470` が `KTGS-4047` / `0` に割れていた。
→ 中黒の**前**は `&nbsp;`（前の型番に固定）、**後ろ**は普通の空白（ここで折る）。`break-all` は廃止。
**桁が割れた型番は読み間違えられる＝この道具の中心の値を壊す。**
（3型番以上は26行、2型番は1,152行。単独の最長は26字 `0609 Part No. X15-77496-02`＝MADBの生値のまま。）

### リージョン注記は、機種の**46.5%で嘘**だった（罠#67）

旧: 全行に `Japan region — runs only on a Japanese {機種}.` を出していた。
**GB/GBA/GG/DS/PSP/Vita/PS3/PS4/PS5/Switch/Xbox One/WonderSwan/NGP はリージョンフリー**なので、
**17,593行（46.5%）で偽**。機種→英文の対応表（`REGION`）を作り、3分類にした:

| 分類 | 行数 | 文 |
|---|---|---|
| `lock` | 19,675 (52.0%) | `needs a Japanese {機種}: {理由}` ※理由は機種ごと（60ピンがNESに挿さらない／HuCardがTG-16に挿さらない 等） |
| `free` | 17,593 (46.5%) | `plays on any {機種}: {理由}` |
| 未収載 | 587 (1.6%) | `Check region compatibility` ＝**断定しない** |

- **未収載に落としたのは Xbox 360 / Neo Geo / Neo Geo CD / 3DO / Virtual Boy。**
  Xbox 360 は**タイトルごとに発行元がロックを選ぶ**ので、機種単位の断定がそもそも成り立たない。
  残り4つは未確認。**罠#62（`complete` のような断定の形容詞は数字より危険）に従って、書かない。**
  ★機種を足しても表に足さなければ未収載に落ちるだけ＝**放置しても嘘にはならない**設計にした。
- 配信専用（row[12]=1）は本体ではなく**日本のストアアカウント**の話なので、別文にした。
- 朱色(`--signal`)は使わない＝「型番の一致」「届く経路」の2意味を薄めない（`BIN_DIVER_DESIGN.md`）。

### 罠#68: flex は、裸のテキストと `<b>` を別々のフレックスアイテムにする

`.card .region{display:flex}` の中に `<b>` を入れたら、
`「Japanese release — needs」`／`「a Japanese PlayStation」`／`「: PS1 discs are…」` が
**3列に割れて並んだ**（flex が匿名フレックスアイテムを作るため）。
→ 本文は必ず `<span class="t">` で1つに包む。**スクショを撮るまで気づかなかった。**

### 店員モード（棚案内の第一歩）

カードに1ボタン（`Show to shop staff` / `店員に見せる`）、全画面・白の `#clerk` を1つ。
中身は**日本語だけ**（読むのは日本の店員）＋最下部に英語の説明1ブロック（画面の持ち主向け）。

出すもの: 挨拶文 → **日本語タイトル**（clamp 26–38px）→ **型番** → 機種 → **棚の目安**。

- **棚の目安は `row[11]`（@ja-hrkt 読みの先頭カナ→清音1字）から出す。**
  フィールド調査（3店）の結論どおり、**一字**（スーパーポテト型）と**行**（ブックオフ・駿河屋型）を
  **両方**出す。`GYO` の44字は **DATA に実在する44字と完全一致**（未カバー0を実測）。
- **`row[11]` が空の行（7,243行 / 19.1%）では棚ブロックを出さない。**
  間違った棚に案内するより黙るほうがいい（罠#27「誤った値より、無いほうがまし」）。
- **配信専用の行では挨拶を変える**（「在庫はありますか？」と聞かせない＝棚に無いものを店員に探させない）。
  代わりに「※配信（ダウンロード）専用です。店頭に現物の在庫はありません」を出す。
- 添字は `current[]` のもの（`card(r,idx)` は `current.slice(0,shown).map(card)` の第2引数）。
  **Load more で74枚に伸ばしても末尾カードが正しい行を開くことを実測**した。

**★機種は英語のまま出している**（`row[2]` = `Super Famicom` 等）。店員に見せる画面なので
日本語の機種名のほうがいいが、**UIに変換テーブルを置かない**（罠#8）ので入れなかった。
**正しい直し方はパイプラインで `row[13] = 機種(日本語)` を足すこと**（MADB の
`schema:gamePlatform` は元々日本語で、`build_master.py` が英語に変換している＝**捨てているだけ**）。

### 検証の道具（ヘッドレスChromeの罠）

**この Chrome は `--window-size` を「スクショの画布」には効かせるが、「レイアウトの
ビューポート」には効かせない**（`innerWidth` は何を指定しても常に 500）。
そのせいで最初の1枚は**414px幅の画布に500px幅のレイアウト**が写り、
右端が切れて「CSSが壊れている」ように見えた。**壊れていたのは道具のほう。**
→ 390px の `<iframe>` に本物の `index.html` を入れて撮る。iframe はレイアウトの
ビューポートを本当に作る。罠#13（確認用のコードは本体と同じ経路を通らせる）の測定器版。

---

## コピー機能の実機検証（2026-07-24）

`copytest.html` を公開（noindex・どこからもリンクしない診断ページ）。
貼り付け欄で1文字ずつ照合する MATCH 判定を実装＝**「✓が出た」ではなく「クリップボードに
正しい値が入った」を測れる**ようにした。

| 環境 | isSecure | clipboard | 型番 | 日本語 |
|---|---|---|---|---|
| Mac Chrome | yes | yes | writeText / MATCH | writeText / MATCH |
| Mac Safari | yes | yes | writeText / MATCH | writeText / MATCH |
| Android Chrome（X経由・Custom Tabs） | yes | yes | writeText / MATCH | writeText / MATCH |
| **LINE WebView（wv, Line/26.11.0/IAB）** | yes | yes | **writeText / MATCH** | **writeText / MATCH** |

- **中黒が往復して無傷**（`クロノ・トリガー`）。日本語タイトルのCOPYは店の検索窓に貼るための
  機能なので、**ここが本体**。
- **LINEのWebViewはクリップボードAPIを塞いでいない。** 想定していた最悪の環境が、実は塞いでいなかった。
- **X のリンクは WebView ではなく Chrome Custom Tabs で開く**（UAに `wv` なし）。
  X から来た人は本物のChromeで見る。
- ★**execCommand フォールバックは、実環境で一度も踏まれていない。** 4経路の人工テストでは
  動いたが、**野生での成功実績はゼロ**。`execCommand` が嘘の `true` を返す既知の穴は、まだ測れていない。
  → 解決の引き金: 「コピーできない」という報告が来たとき。
     `copytest.html` のURLを渡して、path と MATCH の画面を送ってもらう。
     （診断ページを消さずに残している理由がこれ）
- 実機4環境すべてが writeText を通った＝フォールバックは滅多に発火しない。
  **これは不安材料ではなく正常。** フォールバックは機能ではなく保険で、
  **発火しない保険は正しく動いている。「検証できていない」と「壊れている」は別物。**
- **iOS 実機は未検証（端末なし）。**

### 罠#69. userAgent から端末・OSを判定してはいけない（2026-07-24）

同一端末なのに LINE では `Android 16; Pixel 10`、Chrome では `Android 10; K`。
後者は Chrome の **UA reduction** による意図的な丸め。
**UAは環境の種類（`wv` の有無）を見る用途にだけ使う。**

---

## row[11] の穴を @ja-latn で埋めた（2026-07-24）

row[11]（五十音初字）の空欄 7,243行(19.13%) → 1,132行(2.99%)。**6,111行が埋まった。**
新しいデータ源はゼロ。**MADBが最初から持っていた @ja-latn を build_master.py が捨てていただけ。**

### 却下した案と採用した案（同じ基準で並べる）

| 案 | 誤り率 | 判定 |
|---|---|---|
| Path B（MGS3救済・7/19） | 1/4が誤タイトル | 却下 |
| row[9]（ローマ字）から導出 | **30.04%** | 却下 |
| **@ja-latn から導出＋素通しガード** | **上限9.81%（真値はより低い）** | **採用** |

### なぜ row[9] は使えず @ja-latn は使えるのか

- **ローマ字(row[9])の先頭は英単語の先頭であって、読みの先頭ではない。**
  `GUNDAM`→ガンダム→棚はカ、導出はク。`World`→ワールド→棚はワ、導出はウ（ワ→ウが57件で最多）。
- **@ja-latn は転写されている。** `METAL GEAR SOLID 3` の @ja-hrkt は素通しだが、
  @ja-latn は `Metaru gia soriddo 3`。`AUBIRDFORCE`→`Obado fosu`。
- @ja-latn は 49,713ブロックに存在。**ノートに記録が無かった。** 述語censusではなく
  **言語タグcensus**で見つかった（罠#31の続き＝述語名だけ見ていては出てこない）。

### 素通しガード（675行を意図して空のままにした）

@ja-latn の先頭トークンが同じブロックの schema:name にトークンとして現れる行は埋めない。

- **代償は561行の欠落。得たのは「測れた範囲で誤った棚字が1行も出ない」状態。**
- 過検出は欠落しか生まず、誤りは生まない。**安全な方向にしか外れない検出器。**
- 675には正しい転写も混ざる（`Sega`→セ、`Konohana`→コ、`Kanon`→カ）。
  だから9.81%は**誤り率ではなく上限**。真値は測れない（正解が無いため）。

### 濁点の扱いは「決める」必要がなかった

(a)濁点を残す / (b)清音に寄せる で**導ける行数は同じ**（照合は同一、最後に落とすかだけ）。
row[11] 自身が清音なので、既存30,612行との一貫性が答え。
**棚の慣習より、同じ列の中で表記が割れないことが優先。** 店頭で確かめる宿題は消えた。

### エ の偏り(元2.02%→追加10.87%)を全件開けた＝664行

| 型 | 件数 | 割合 |
|---|---|---|
| 英字1字の読み(F=エフ37, A=エー25, X=エックス18…) | 123 | 18.52% |
| 頭字語(NBA 91, SD 89, NFL 18, SNK 17, AKB 16…) | 372 | 56.02% |
| 普通の日本語の語 | 142 | 21.39% |
| それ以外(Efu1, emu3, Eba17 等・英字読み＋算用数字) | 27 | 4.07% |

- **「普通の英単語がそのまま残った行」は0件。素通しガードが効いている証拠。**
- **A F H L M N S X の8字が「エ」で始まる読みを持つ。**母数がラテン原題の集合なので頭字語に偏る。
  **偏りは異常ではなく母数の性質。**
- ①bの記号割り当て(`enubiei`→NBAE)は**意図して未検証**。初字はエで確定＝棚案内に影響しない。
  **判断に効かないものを判断材料に混ぜない。**

### 期待値6,108 vs 実測6,111 — 実測が正しかった

測定は (schema:name,型番) 鍵の**先頭ブロック**を採る。build_master.py は**ブロックを1件ずつ**処理する。
同じ鍵に複数ブロックがあると別の値を見る。差は+4-1=3行。

- `BEYOND: Two Souls`(6ブロック): 先頭`Beyond tsu soruzu`は素通しガードで空 / `Biyondo…`→ヒ（2行）
- `WAR THUNDER`(3ブロック): 先頭`who sanda`は導けず / `Uo sanda`→ウ（2行）
- `Gダライアス`(3ブロック): 先頭`Ji daraiasu`→シ / 出荷行のブロックは`G daraiasu`で導けず（1行）

**期待値は仮説であって正解ではない。実装を測定の便宜に合わせるのは正しいものを歪める。**
測定Dで数えておいた「初字が食い違う23行(0.32%)」の一部が顕在化したもの＝**未測定として測っておいたものが後で説明に使われた。**

### 注入（swap_data.py v3）

- **フラグを1つも要求されずに通った。** `[0]〜[6]` 中身照合で 旧→新 0行/0行。
- 全列突き合わせ: row[0]〜[10], row[12] すべて 37,855/37,855 一致。row[11] 埋め6,111/上書き0/消失0。
- DATA md5: `c7f3a74a…` → `aac85cc4a9d2632796effe1d704e65c3`。`const DATA = [` は1つ（罠#50）。
- **2026-07-24 公開済み**（GitHub Web画面からD&D）。公開URLで確認:
  WAR THUNDER の物理行に「店員に見せる」ボタンが出て、配信行には出ないこと。
  row[11] を @ja-latn から埋めた行（ウ）でカードの棚ブロックが出ること。

### ★確認する行の選び方（罠#37の棚版）

**元から row[11] を持っていた行で確認すると、何も変えていなくても成功に見える。**
埋めた行そのものを開くこと。実測: `WAR THUNDER`(ウ) / `NBA 2K2`(エ) で棚ブロックが出た。
空の行(`BLAZBLUE cross tag battle`)では出ず、英文注記
`a wrong shelf is worse than none` が出た＝675行を捨てた判断がそのまま文言になっている。

### 罠#70. 棚が出ないのを見てバグと決めない — row[12] を見る（2026-07-24）

`BEYOND: Two Souls` の ヒ が入った2行で棚ブロックが出なかった。原因は `row[12]=1`（配信専用）で、
店員モードが**仕様どおり**棚を出さず配信注記を出していた。
（この行を確認例に挙げたのは私の指定ミス。実際の値を見たので切り分けられた。）
**「出ない」の原因を、機構で決めつけず値で確かめる**（罠#33と同じ形）。

### 残る限界（意図して残す）

- **row[11] が空の1,132行(2.99%)**: @ja-latn なし360、導けなかった214（`3di` `150` `007` 等の数字始まり）、
  素通しガードで意図的に空にした675。
- **真の誤り率は測れない**（埋めたい行に正解が無い）。上限9.81%で採用を決めた。
- 検算Aの99.13%は**母数が違う**（row[11]を持つ行＝@ja-hrktカナ始まり94.09%）。
  **同じ源から作られた2列の一致率は精度の証明にならない**——5番の100.00%がそれだった。

### 罠#71. 検算は、埋めたい母数と同じ母数で行う（2026-07-24）

`row[9]`案の検算で 19,723/19,723 = **100.00%** が出た。row[9]とrow[11]が両方 @ja-hrkt 由来だから
完全に戻るだけで、**埋めたい行(row[11]空＝読みがカナで始まらない)には一切効いていない。**
母数を揃えて測り直したら **69.96%** だった（6番）。
100%を根拠に実装していたら**約1,200行を誤った棚に送っていた。**
**「重要な検算」と指定されたものでも、母数を確認する。**

### 罠#72. 正解が無い母数でも、失敗の型は数えられる（2026-07-24）

埋めたい7,243行に正解は無い。だが「先頭が原語のまま残っている」という**失敗の型**は
schema:name とのトークン照合で機械的に拾える(B=9.81%)。
**正解が無い＝測れない、ではない。誤り率の上限は測れる。**

---

## 店員画面とカードの手直し（2026-07-24）

| | |
|---|---|
| 配信の注記 | 灰色帯（`#f0eee7`＋hair-strong）＋先頭を太字。**朱色は使わない**（「一致」とBuyeeに予約） |
| 店員画面の英訳 | 最下部 → **挨拶の直下**へ移動。位置が変わったので罫を2px黒→1px灰に（日本語より目立たせない） |
| 店員画面 | 棚の目安を外し、**発売元(row[5]優先・空ならrow[6])と発売年(row[4])**を入れた |
| 棚の目安 | 店員画面 → **結果カード側へ移動**。棚を探すのは店員ではなく**ユーザー本人**。スマホを渡している間、本人はその情報を読めない |
| 配信行 | 「店員に見せる」ボタンを**要素ごと出さない**（disabledにしない＝押せないボタンは「壊れている」と読まれる） |
| 空欄 | row[4][5][6]が全部空の行（クレイジー・ホスピタル）で**行ごと出さない**。英文の列挙も `the Japanese title and the console` と自動で縮む |

### 到達不能コードを3箇所削除した — 「動く保険」より「読んだ人が止まる注記」

配信行でボタンを出さなくした結果、`clerkOpen()` 内の `r[12]` 分岐3箇所（挨拶・英文・警告ブロック）が
**到達不能**になった。入口は1箇所、呼び出しも1箇所と確認して削除。

**残す案を却下した理由**: 「将来ボタンの条件を変えたとき、配信行が『在庫はありますか？』で
店員に渡るのを防げる」——**防げない。その未来では分岐が正しく動いてしまう。**
残した分岐は防波堤ではなく**抜け道**になる。**動いてしまうものは疑われない**
（100.00%を根拠に row[9] 案を採用しかけたのと同じ構図）。

代わりに `clerkOpen()` 先頭に前提を明示し、**ボタン側からも関数を指して双方向に辿れるように**した。
片方だけでは、もう片方を先に読んだ人に見えない。

### 罠#73. コメントを書く行為がコードを壊す — テンプレートリテラル内のバッククォート（2026-07-24）

相互参照コメントに `` `function clerkOpen(r)` `` とバッククォートを付けた。
そのコメントは `card()` の**テンプレートリテラルの中**にあるため文字列がそこで閉じ、
JS全体が `SyntaxError: Unexpected token 'function'` で落ちた。
**エラーは原因の場所を指していない。** `node --check` で捕まえて修正し、
**その場に注意書きを残した**（同じ場所で二度踏まないための置き方）。
教訓: **注釈を足す変更にも構文チェックを通す。** 「コメントだから安全」は成り立たない。

---

## レトロアーケード配色へリニューアル（2026-07-24）— 公開済み

zipのデザイン案（Claude Design出力）を基に、**明るい計器グレー（Field readout v1）から
暗色のレトロアーケード配色へ**。3ステップに分けて実施、**変更は `<style>` 内のみ**。
HTML構造・JS・DATA・スキャナ・clerkOpen()・店リンク生成は**1文字も触っていない**
（各ステップで `</style>` 以降が旧ファイルと byte 一致を確認）。

### ★zipは本番コードではない — 「見た目の設計図であって骨格ではない」
zipの中身は Claude Design の部品形式（`<x-dc>` `<dc-import>` `<sc-if>` `<sc-for>` `support.js` 依存）。
**ブラウザにそのまま置いても動かない。** 実験HTMLの時限爆弾と同じ形なので、
**現行 `index.html` を骨格として残し、CSSだけを移した。**

### 採ったもの／採らなかったもの
| zipの提案 | 判定 |
|---|---|
| 配色（暗い紫地・桃 `#ff37c7`・水色 `#22d3ee`） | **採用** |
| 店員モード・棚案内・買い方の階層 | 採用済み（7/24 午前に実装） |
| **書体 Press Start 2P / DotGothic16 を型番・日本語タイトルに** | **不採用。v1からの後退案だった** |
| スキャンライン・ビネット・床グリッド | **保留（入れていない）** |

### ルール: 枠はレトロ、中身は計器
- **Press Start 2P** = ロゴ・小見出しラベル・ボタンの英字のみ（短い語専用）
- **VT323** = CONSOLE値のみ
- **型番は IBM Plex Mono 24px、日本語タイトルは IBM Plex Sans JP 29px のまま。text-shadow / glow は全体で0件。**
- **日本語が1文字でも混ざる文字列には当てない**（両書体とも日本語グリフを持たない＝文字列の途中で書体が入れ替わる）
- **説明文の平文には当てない**（読ませる文章であってラベルではない）
- **店員に見せる画面は白のまま・現行書体のまま**（他人が読む画面。蛍光灯の下で読む）

### コントラストを全部測ってから決めた（カード地 `#0f0a1c` 基準・AA 4.5）
本文 11.44 / ink-2 8.02 / muted 5.54 / **faint 4.78（最小）** / 桃 6.13 / 琥珀 11.42。**割れた文字ゼロ。**
- `--accent-ink` 白→地色: SCANボタンの文字が **3.17 → 6.41**。
  **店頭で押すボタンの文字が読めないのは配色ではなく機能の問題。**
- `--edge-input` を追加: 入力欄の枠 **1.37 → 3.89**。「押せる場所」が分かるように。
- `--muted` `#7a7398` → `#8b84ab`: AA 4.5 を確実に超えさせた。
- `.btn-copy.fail` の琥珀 **3.94 → 11.42**。**今朝直した「✓の嘘」の対になる表示。
  FAILEDが読めなければバグを直した意味が消える。**
- 配信専用の帯 `#f0eee7`（暖色の紙グレー）→ 暗色反転。**暖色グレーは紫地の上では「汚れた白」になる。**

### 判断してやったこと（指示になかったが正しかった）
- **`body::before` の固定層にグラデーションを敷いた。** `body` の `background-image` だと高さが本文に追従し、
  **結果が何百件も並ぶと `90%` が数千pxになって紫が画面全部を覆う。**
  `background-attachment:fixed` は **iOS Safari で効かない**ので固定層1枚にした。想定ユーザーの多くはiOS。
- **「店員に見せる」を塗り潰し→枠線に。** 反転すると明るい板になり**型番スラブより目立つ**＝
  7/18の「型番が主役」が配色で崩れるところだった。
- `color-scheme:dark` と `option{}` を追加。**プルダウンの中身はOSが描く**ので、
  地だけ暗くすると環境によっては白地に白文字になる。
- **カード地 `#0b0714` → `#0f0a1c`。** 指定値のままだとページ地と **1.02:1**＝
  74枚並んだとき**どこまでが1つの商品か分からない**。**境界は装飾ではなく機能。**
  （指定値は zip から写しただけで測っていない。測った側の値を採った。）

### 罠#74. CSSの上書きは「書いたから効いている」とは限らない
`.scanbtn` の `font-family` 上書きが、後方の scanner セクションの元定義に **source order で負けていた**。
computed style を測って発覚（Space Mono のままだった）。
**測らなければ、店頭で一番押されるボタンだけ書体が変わらないまま公開されていた。**

### 罠#75. フォント読み込み失敗は「悪化」ではなく「改善」に倒せる
`--f-pixel:'Press Start 2P','Space Mono',…` とフォールバックに現行書体を残した。
**店頭は電波が悪い**（ブックオフの地下・店の奥）ので、これは実害のある想定。
glyph単位のフォールバックなので ✓ や日本語が pixel/VT323 に無くてもその字だけ次の書体で出る（tofu化しない）。

### 折り返しの原因は書体ではなかった（実測が予想を覆した）
`.micro`（JAPANESE TITLE · COPY TO PASTE INTO A STORE）と `.slab-label`（PRODUCT CODE · EXACT MATCH）が
2行に折り返した。**「Press Start 2P が大きいせい、失敗時は1行に戻る」と予想したが、
Space Mono でも2行だった＝元から2行。** 結果、行間 1.5 は**成功時も失敗時も自然**で条件分岐が不要になった。
`:has()` で1箇所に限定（`.card .field:has(.jp-copy-row) .micro`）。他7つの `.micro` は基底のまま。
**全部に効かせる方が簡単だが、必要な範囲だけを変える方が後で読める。**

### VT323 はここで止めた（意図して適用範囲を狭めた）
`.spec .v` は **PUBLISHER と共有**されており、**6,248行が日本語**。CSSだけでは切り分けられない。
統計値・価格は平文に埋まっており `span` で囲む＝HTML変更が要る。
**得られるのは「数字が少しレトロに見える」だけ、リスクは日本語の書体崩れ。** CONSOLE値のみで打ち止め。

### 保留中（未決）
**スキャンライン・ビネット・床グリッドは入れていない。**
CRT効果は `rgba(0,0,0,.26)` の横縞で、**店頭の蛍光灯下と屋外でコントラストを削る**。
全文字で AA 4.5 を通した意味が失われる。入れるならロゴまわりの床グリッドだけ、が限度。

### 罠#76. デザイン案にあるものを、リポジトリの現状として指示に書いた（2回踏んだ）
朝: 「Press Start 2P と text-shadow を直せ」→ **リポジトリには0件**（7/22のv1で既にIBM Plexだった）。
夕: 「統計の3枠を配色変更せよ」→ **そんな枠は存在しない**（フッター1行の地の文の中にあるだけ）。
**どちらも zip のデザイン案を読んで、それをリポジトリの現状として書いた。**
対策: **デザイン案由来の指示には「既存の改修」か「新規に作るもの」かを明記する。**
教訓: **批評の対象と作業の対象は別物。grepしてから編集するのが常に正しい。**

---

## 次にやること（2026-07-23 更新）
1. ~~`min-height:310px` を入れる~~ → **やらないと決定（2026-07-23午後・罠#57）**。
   画像の高さは187〜300に割れるので、固定値はどれも空白か跳ね残りを作る。
   **画像到着時のガタつきは未解決のまま残る**（下記5）。
2. ~~**フッターの「12,208 titles」を直す**（未解決#9・今日も未修正）~~ → **完了（2026-07-23夕）**。
   全37,855行を母数に差し替え、**「Publisher data is complete」は削除**（罠#62・未解決#9）。
3. （任意）楽天の**個別確認フォーム**で、他社アフィリンク併置の可否を確認（未解決#8の残り）。
4. （継続）**棚案内（本丸）**＝五十音＋ジャンル。データ（row[11] 初字・`schema:genre`）は揃っている。UIが無いだけ。
5. **（未解決・任意）画像到着時のレイアウトシフト。**
   画像バイトが届いた瞬間に「Buy this game」ボタンが下にずれる（実測16件中8件が
   `0x0`＝バイト未着で居座っている）。
   **正解の方向は分かっている**＝バイト到着後に `naturalWidth/Height` を
   img の **width/height 属性**に入れてから挿す（ブラウザが比率で箱を先に確保する）。
   `height:auto` を残すことが必須（`max-width:300px` で縮んだ後に
   元の高さが当たると崩れる）。
   **実装を試みたが画像が0枚になり、原因未確定のまま復元した**（罠#58）。
   コードは `index.html.20260723-bak-with-decode` に退避済み。
   **機能ではなく表示の問題なので、優先度は棚案内より下。**
6. **駿河屋リンクを実装**（`category=2` / `-JPN` 除去 / `af_jump` ラップを**一度に**）。
   ★**実装前に手元で `-JPN` の行数と機種別内訳を数える。**
7. **駿河屋.com リンクの採否**＝**seed 42 の30本を測る**。
   URL: `/en/products?category=2&keyword={row[0]}`
   指標: **目的の物が1ページ目にあるか**。★**生URLで測る**（自己クリック回避）。
8. **Reddit/Discord 投稿の前に、SNSのURLを駿河屋の登録者情報に申請**する。
9. （任意）**`og:image` 用の画像素材（1200×630）**。**型番スラブを大きく置いた絵**が候補。

---

## バックアップ・退避ファイル

- **`index.html.20260723-bak-pre-decode`** … 画像先読みを入れる**直前**の正常版。
  現行 `index.html` はこれを復元したもの（diff / cmp / MD5 の3手法でバイト一致を確認）。
- **`index.html.20260723-bak-with-decode`** … `_rakPreload()` を入れた版（+25行）。
  **公開したら画像が0枚になったので復元した。**
  Networkでは画像が全件200＝DLは成功。decode単体も `OK 150 192` で正常。
  原因は `dim` を受け取った後ろのどこか、**未確定**。
  **捨てないこと。** 原因が分かれば25行を拾い直せる（罠#58）。
- **`_experiments/measure_gojuon_from_romaji` / `measure_ttl_yomi_census` / `measure_jalatn_initial` /
  エの内訳（2026-07-24）** … row[11] を @ja-latn で埋める判断の根拠。**使い捨てではない。**
  30.04% で row[9] 案を却下し、9.81%（上限）で @ja-latn を採用した数字はここにある。
5. （継続）カメラ（バーコード）＝ row[10] のJANで実装可。測定は7/19に合格済み。
---

## カードの分岐を4つ足した（2026-07-24 午後）

**測った数字（母数を明記）:**

- 全行 **37,855**
- 型番が空 **27,559**（72.80% / 全行）
  - ├ 配信専用 `row[12]==1` … **7,109**（18.78% / 全行）
  - └ 型番の無い物理ソフト … **20,450**（54.02% / 全行）
    - └ うち英題あり … **5,829**（28.50% / **20,450**）
    - └ 機種上位: PS2 3,477 / PS1 2,332 / PSP 2,111
- jaLatin（`row[1]` がラテン文字だけ）… **6,236**（16.47% / 全行）
  - ├ 正規化して `row[0]==row[1]` ＝見出しを消す … **2,437**（39.08% / **6,236**）
  - └ 不一致 ＝見出しを残す … **3,799**（60.92% / **6,236**）
    - └ うち英題あり・別表記 … **1,115**

**入れた分岐（すべて `card()` 内）:**

1. **配信行(`row[12]==1`)で日本語タイトルのCopyと店案内を出さない。**
   ＝**実在したバグ**。現物の無い行が「店の検索欄に貼れ」と指示していた。
2. **`noCode = codes.length===0 && !r[12]`。**
   日本語タイトル欄を桃色スラブに格上げ。**20,450行が手ぶらでなくなった。**
3. **jaLatin で `row[9]` ローマ字を出す**（従来は見出しが日本語のときだけ）。
4. **`hideJa = jaLatin && !!en && codeKey(en)===codeKey(jp)`。**

### 罠#77. 確認用の行を、デザイン案の絵から選んで2回外した（Rez / 超兄貴）
どちらも**絵に描いてあった値を、データの事実として指示に書いた**。**罠#76と同じ形。**
対策: **確認用の行は必ず DATA から抽出して選ぶ。**

### 罠#78. hideJa の初版は `jaLatin && !!en` だった
これだと `row[0]` と `row[1]` が**別表記の1,115行**で、**箱に印刷された表記が消えていた**。
**ラテン文字同士でも同じ文字列とは限らない**（例: `Resident Evil 4` / `biohazard4`）。
対策: `codeKey(en)===codeKey(jp)` で正規化一致した行だけ消す（`codeKey`＝NFKC＋大文字化＋英数字以外除去）。

### 罠#79. `node --check` は `.html` を弾く
`ERR_UNKNOWN_FILE_EXTENSION`。`<script>` 本体を抽出して `.js` として通す。
**この手順を毎回再発見していた**——次からは最初からそうする。

### 未解決（2026-07-24 追加）
- **未解決#10.** → ✅ **解決（2026-07-24 夕）** `.cap` がスラブ内で暗すぎる（`This game's Japanese box uses Latin letters too.` が読めない）。
  ＝分岐4で追加した注記。`.cap` の `--muted` はカード地前提の色で、暗色スラブ上ではコントラスト不足。
  → **解決**: `.card .slab .cap` を `--ink-2` に。コントラスト **5.38:1 → 7.79:1**。Winning Post(3DO) の画面で確認済み。
  （後にこの注記は英題直下・型番スラブ外へ移設したが、スラブ内 `.cap` の明色化ルール自体は残置。）
- ~~**未解決#11.** PUBLISHER が `row[5]` 日本語のまま。**`row[6]` 英語が 94.52% あるのに使っていない。**~~
  → ✅ **解決（2026-07-25）＝実は既に実装済みだった。** 末尾のセクション
  「未解決#11 は既に実装済みだった（2026-07-25）」参照。
  **この行が「未解決」のまま2日残っていたことが罠#84の実例。**
  ユーザーが読むカードは英語、店員に見せる画面（clerk）は日本語が正しい。
- **未解決#12.** → ✅ **解決（2026-07-24 夕）** 「You can't type this」が**ラテン文字の日本語タイトル行にも出る**（打てる行に出ている）。
  → **解決**: 注記を1行に統合し jaLatin で出し分け。jaLatin=「Latin letters — you can type this yourself, or copy.」／ それ以外=従来文言「You can't type this — copy, then paste into the search box.」。metal gear の画面で確認済み。
- ~~**未解決#13.** トグル（I'm in Japan / outside）未実装。**デザインは確定済み。**~~
  → ✅ **解決（2026-07-25）。** 末尾のセクション
  「位置トグル（I'm in Japan / outside）を実装した」参照。

---

## 完全重複の畳み込みと、英題の限界（2026-07-24 夕）

### 入れたもの
1. **描画直前の重複除去**（13列すべて一致する行だけ畳む）。
   実測: **37,855 → 36,462（消える1,393行 / 3.68%）**。
   DATA は無傷。`res` を `res2` に差し替える方式なので、いつでも戻せる。
   ★前回集計の1,291行とずれたのは、**集計側が row[1]/row[2] 非空に絞っていた**ため。
     母数が違っただけで実測（13列 JSON 連結キーで全行 dedup）が正しい。
2. **英題の曖昧さ注記**（**4,365行 / 11.53%**）。
   条件: **strong 正規化した row[0] が2種類以上の row[1] に付いている**行の、英題直下に `.cap` で1行。
   `strong = NFKC → 小文字化 → 空白・中黒・約物(P)・記号(S) を除去`。

### 測って却下したもの（3件）
- **薄い行の畳み込み** → 対象129行。効果が無い。
- **注記の文面を「本編＋語足し」と「別物」で分ける** → 分類が当てにならない。
  **バイオハザード と BIO HAZARD DIRECTOR'S CUT は実際は語足し版**だが、カタカナとラテン文字なので
  「先頭から含む」関係では判定できない。**分類に頼った文面は書けない**（単一文面にした）。
- **英題を兄弟行から借りて検索に当てる** → 対象2,061行と出たが、例に「**Island / 借りられる英題 Island**」が
  あった。**日本語名が元からラテン文字＝既に英語で当たる行**が混ざっている。実質はもっと小さい。

### 罠として記録
- **罠#80. 成功条件を誤って指定した。** 「14件すべてに注記が出れば成功」と書いたが、
  **6件は英題そのものが無いので出ない方が正しい**。条件は「英題がある行」であって「全行」ではなかった。
  対策: **成功条件は、母集団（全行 / 英題あり / …）を明示してから書く。**
- **罠#81. 正規化の強さで結論が変わる。** fold（NFKC無し）では **4,841行**だったものが strong では **4,365行**。
  差の**485行は表記ゆれ**で、**ここに警告を出すと嘘になる**。
  対策: **集計の正規化と実装の判定は必ず同じ関数を使う**（今回は strong を集計・実装で共有）。

### 確認された限界（実装では埋まらない）
- **英語で到達できるのは 13,487行（35.63%）が上限。**
  row[0]空 24,368行のうち、英題付きの兄弟がある行は **2,061行だけ**で、
  残り**約22,307行は英語検索で原理的に到達不能**。
  これは **Wikidata(P7886) の結合率そのもの**で、UI では埋まらない（データ供給側の問題）。

### 未解決として残す
- ~~**未解決#11（既出）.** PUBLISHER が `row[5]` 日本語のまま（`row[6]` 英語が **94.52%** あるのに未使用）。~~
  → ✅ **解決（2026-07-25）。** 上記と同じ。参照先はセクション名で辿ること（行番号は追記でずれる）。
- ~~**未解決#13（既出）.** トグル（I'm in Japan / outside）未実装。**デザインは確定済み。**~~
  → ✅ **解決（2026-07-25）。** 末尾のセクション
  「位置トグル（I'm in Japan / outside）を実装した」参照。
- **未解決#14.** 英語タイトルの書体がデザイン案と違う。**ラテン文字のみの題名なら適用可**（要・適用範囲の切り分け）。

---

## デザイン案への追随（2026-07-24 夜）

### 入れたもの
- **`.en-title`** を weight 700 / `#ece9f5` / letter-spacing .02em（対カード地 **12.80:1**、カード内で最も明るい階調）。書体は不変。
- **店ブロックを `.instore` 枠にまとめた**。見出し「In a store right now?」（水色 `--tag`）＋ Show to shop staff ＋ 棚案内。
  **配信行(row[12]==1)では枠ごと出さない**（空枠を残さない）。
- **`.card` の枠線を `--hair-strong` → `--edge-input`（#6f6790）**。対ページ **3.89:1** / 対カード **3.72:1**。WCAG 非文字 3:1 を満たし、カードが板として立つ。四隅の「+」・番号バッジは枠線に潰されない（ズーム実測で確認）。

### 罠#82. 変数を動かす前に、それがどこで使われているかを見る
カードを板に見せるため `--surface` を `#0f0a1c → #281d4c` に上げた。
**`--surface` はカード地とボタン地の両方**に使われており、`store`/`btn-copy` の枠（`--hair-strong`）が
ボタン地に対して **1.03:1** になり、Suruga-ya / Mercari / Book-Off / Hard-Off が**枠を失ってただの浮いた文字**に。
**購入への出口が見た目の上で全部塞がった。** 7/24 に `--edge-input` を足して解決した問題（罠#… スラブ枠不可視）の再発。
巻き戻して**枠線（案A）で解決**した。
★対策: **変数を動かす指示を出す前に、その変数の使用箇所を grep する。**

### 罠#83. 基準の数字を、案を測らずに書いた
「カード地とページ地を 1.3:1 以上に」と指示したが、**この 1.3 はデザイン案を測らずに決めた値**。
達成した結果、案より明るくなり**奥行きが反転した**（案ではスラブが浮く／実装では沈んだ）。
★対策: **基準値は、まずデザイン案そのものを測ってから決める。**

### 測って却下
- **案B（ページ地を暗くしてカードを浮かせる）**: カード地 `#0f0a1c` がほぼ黒のため、
  **ページを純黒にしても 1.08:1 止まり**。原理的に到達不能。
- **スマホでの2段組（箱絵=左・購入=右）**: 380px 実測でカード内幅 **318px**。
  箱絵 300px なら購入リンクに残るのは **2px**。170px まで縮めても購入カラムは **132px** で
  ストアボタンが1列になる。→ **広い画面のみ2段組、狭い画面は縦積みを維持**する方針に決定。**未実装。**

### 未解決に追加
- **未解決#15.** 2段組は**広い画面のみ**実装予定。ただし画像のレイアウトシフト（罠#58）が未解決のため、
  **左カラムの高さが 0→300px に跳ねる挙動**と絡む。**先に #58 を解く**こと。
  （380px 実測: カード内幅 318px、箱絵最大 300px → 縦積み維持が妥当。箱絵は width/height 属性なし・`max-width:300;height:auto`。）
- **未解決#14（既出・追記）.** 英語タイトルの書体はラテン文字のみなので適用可能だが、
  **長い題名の折り返しが未検証**。適用範囲の切り分けと合わせて要検証。

---

## 未解決#11 は既に実装済みだった（2026-07-25）

**発端**: 未解決#11「PUBLISHER が `row[5]` 日本語のまま」を実装しようとして、まず数えた。

**測った数字（母数 = 全 37,855行）**
- `row[6]`（発売元・英）が非空 … 35,782（94.5%）
- `row[6]` 空 かつ `row[5]` 非空 … **21**（0.06%）
- 両方空 … 2,052（5.4%）→ 7/24 に行ごと非表示済み
- `row[6]` 非空だが日本語を含む … **5,819**（15.4%）
- `row[5]`≠`row[6]` かつ `row[6]` が日本語なし ＝ **実際に英語で出る 28,935（76.4%）**
- `row[5]`≠`row[6]` だが `row[6]` にも日本語が残る … 363

**grep の結果 ＝ 既に入っていた**
- `card()` 846行: `const pubs = (puben||pubja).split(';')…` ＝ **英語優先**
- `clerkOpen()` 1150行: `const pubShow = (r[5]||r[6]||'')…` ＝ **日本語優先（逆向き）**
- 公開版でも確認: `gamekataban.com` で `一騎当千` → PUBLISHER が `Koei Tecmo`
→ **未解決#11 は解決。ノートだけが古かった。**

**意図して放置すると決めたもの**
- **5,819行**（英名欄が日本語）: Wikidata に英語ラベルが無く、`row[5]` の値がそのまま
  コピーされている行。**UI では埋まらないデータ供給側の限界。**
  箱に印刷されている名前であり、店員に見せる場面では日本語のほうが役に立つ。
  → **放置と決めた**（罠#52 の形＝掃除し残しではなく、放置と明記する）。
- **363行**（値は変わるが日本語が残る）: 同上。
- **フォールバック先は掃除前の生値**: 21行の中に `©2001;Keynet.;NDCUBE`
  （EZ-TALK 初級編1～6 / GBA）のような未掃除の値がある。21行なので実害なし。
  だが **「フォールバックすれば安全」ではない**ことは記録しておく。

### 罠#84. 未解決リストは、実装で解決しても自動では消えない
未解決#11 は 7/24 夕・夜のノートに「（既出）」として2回残っていたが、
**リポジトリには既に実装されていた。**
罠#76・#77（デザイン案を現状として指示に書いた）と同じ形だが、
**今回の古い情報源は自分たちのノート。** 危険度はこちらが高い——
**デザイン案は「案」と分かるが、ノートは正典として読まれる。**
→ 対策: **編集の指示を書く前に必ず grep する。** 今回はそれで空振りを1回防いだ。

### PUBLISHER に日本語が残る行数 — 正典（2026-07-25）

`index.html` の CSS コメントがこの数字を参照している。**移動・削除しないこと。**

| スコープ | 行数 |
|---|---|
| `row[6]`（発売元・英）が非空かつ日本語を含む | **5,819** |
| `card()` が実際に表示する PUBLISHER が日本語（`puben‖pubja`） | **5,821** |
| `clerkOpen()` の表示（`r[5]‖r[6]`）が日本語 | 33,260 |
| `row[5]` が非空かつ日本語（生の日本語列） | 33,260 |

母数 37,855行 / DATA 配列本体 md5 `aac85cc4a9d2632796effe1d704e65c3` 時点。
**5,819 と 5,821 の差 +2** は、フォールバックが効く21行のうち2行が `row[5]` に
日本語を持つため。**別々に測った2値が整合したので計測は信用できる。**

### index.html の CSSコメントから「6,248行」を消した（数字を直さず消した）

351行・357行に **旧世代の計測値 6,248** が埋まっていた（現行は 5,821 で約400行ズレ）。
機能影響はゼロ（`.v.jp` はクラスで効く）。だが **「6,248行が日本語だから VT323 を当てない」**
という判断の根拠として書かれていたため、次に触る人が400行ズレた前提で設計する。

**書き換えではなく削除を選んだ**（罠#62 でフッターの `Publisher data is complete` を
削除で片付けたのと同じ判断）。**直しても次のMADB更新でまた腐る。**
→ 351: `（PUBLISHER には日本語を含む行が残る。件数は BIN_DIVER_NOTES.md 参照）`
→ 357: `(=日本語を含む PUBLISHER に VT323 を当てない)`

**残した理由は更新を要求しない文である。** VT323を当てない根拠は件数ではなく
**「PUBLISHER欄に日本語が混ざり、CSSだけでは切り分けられない」**——日本語が1行でも
残る限り真であり、5,819行はデータ供給側の限界で埋まらないと確認済み（同日）。
**コメントに置いていいのは、更新を要求しない文だけ。数字は日付と母数を添えられる場所に置く。**

退避: `index.html.20260725-bak-pre-comment-fix`

### 罠#84 の運用に追加 — ソース内コメントの数字も grep 対象
今回、古い数字はノートだけでなく **`index.html` の CSSコメント内**にもあった。
**コメントはノートより正典度が高い**（コードと同じ場所にあるため疑われない）。
→ 対策: **データを増やしたら、UI表示文だけでなくソース内コメントの数字も grep する**
（罠#62 の「UIの記述は行を足した瞬間に嘘になる」の、コメント版）。

---

## 位置トグル（I'm in Japan / outside）を実装した — 未解決#13 解決（2026-07-25）

**発端**: 同じカードが2つの場面で使われるのに、表示が「海外在住」に固定されていた。
日本の店頭に立っている人には、並び順と説明文の**両方**が外れていた。

### デザイン案には、トグルが存在しなかった
未解決#13 は「**デザインは確定済み**」と2日間書かれていたが、
渡された案（トップページ・検索結果）のどちらにもトグルは無く、
カードの説明文は現状の実装と**一字一句同じ**だった＝案は「海外在住固定」の設計。
→ **Claude Design に新規で発注した**（文言をこちらで固定してから渡した。
  空白で渡すと Design が文言を発明し、案とリポジトリが再びズレる）。

### Design の判断で採用したもの
- **置き場所＝ヘッダー常設(sticky)。** 理由が正しい：
  **トグルは1つのクエリではなくページ全体の意味を変えるので、クエリ側ではなくページの枠に属する。**
  店頭でカードの途中にいる人が、上に戻らずに切り替えられることが決定的。
- **アニメーション不要。** 並び替えのモーションは「読み込み中」に見え、店頭の判断を遅らせる。
- **蛍光灯下の可読性＝合図を3つ重ねる**（ベタ塗り・文字コントラスト・四角マーカー）。
  色相が飛んでも「塗られた明るい面」の形で残る。

### Design の提案を却下したもの
- **`#a99fd0` という新しい生hex** → `--muted`(#8b84ab・面で5.7:1) で足りる。
  Design が上げた理由は `#6f6790`(=`--faint`) がAAを割ることだが、`--muted` は既に解決済み。
  **生hexを増やすと、次に配色を変える人がそこだけ取り残す**（罠#82の裏返し）。
- **IN JAPAN で「日本語タイトルの4店を主役扱い」** → **塗りは常に1つだけ**にした。
  5枚塗ると強調が強調でなくなる。蛍光灯下で「まずどれを押すか」を伝えるのが塗りの役目。

### 自動判定はしない（意図した設計）
端末ロケールでの自動判定は**却下**。日本にいる観光客のスマホはロケールが海外のままなので、
**一番大事な場面でこそ外れる。** 手で選ばせる。

### 測った数字（母数を明記）
購入ブロックが出るのは `row[12]!=1` の行だけ。**母数 M = 30,746**（全37,855の81.2%）。

| | 行数 | 対M |
|---|---|---|
| A: OUTSIDE / `row[7]`非空 → Buyee に塗り | 25,647 | 83.42% |
| B: OUTSIDE / `row[7]`空 → **塗りゼロ** | 5,099 | 16.58% |
| C: IN JAPAN / `row[3]`非空 → 型番の駿河屋に塗り | 10,296 | 33.49% |
| D: IN JAPAN / `row[3]`空 → 日本語タイトルの駿河屋に塗り | 20,450 | 66.51% |

検算: A+B = C+D = 30,746（母数と一致）。IN JAPAN は塗り抜けゼロ。**非対称なのは OUTSIDE だけ。**

### eBayフォールバックを却下した — 推論ではなく観測で決まった
Bの5,099行（16.58%）で塗りがゼロになる。eBayに回す案を**却下**した。
`row[7]` が空なのは「**検索キーを持っていない**」だけで、Buyeeで買えない証拠ではない。
それを根拠に**転売価格のeBayを主役にすると、中間業者の上乗せ回避というこの道具の目的に反する。**
誤った塗りより塗りなしのほうがまし（罠#27の形）。

★**実データが推論を観測に変えた**: 検証中、`BLAZBLUE cross tag battle` が2行出て、
**Switch版は `row[7]` が空・PS4版は非空**だった。**同一タイトルなのに片方だけ空。**
→ 「空欄＝Buyeeで買えない」ではないことが**実データで確認された。**
→ 同時に、**空欄は機種で決まっている可能性**が見えた（7/18の `BUYEE_OK` 25機種に
  Switch が入っていないだけかもしれない）。**これは仮説で、まだ測っていない**（罠#33）。

### 塗りゼロは壊れて見えないことを実地確認した
7/24 に `--edge-input` で全ボタンに見える枠を入れてあるため、
塗りゼロのカードは「推薦が無い」と読めるだけで、壊れて見えない。**実機で確認済み。**

### 実装（4段に割って1段ずつ検証した）
- **第1段-a** トグルUIと `paint()` への結線のみ（`card()` は触らない）。
  この時点では押しても表示は変わらない＝**想定どおり**。
- **第1段-b** 購入ブロックを `card()` から **`buyBlock(k)` に切り出し**、
  並び順と説明文6本を `LOC` で切り替え。テンプレート文字列の中では変数を作れないため関数化が必要だった。
  **店のURLは1文字も変えず、`grep -c` で1対1移設を証明**（駿河屋2/Buyee1/eBay1/Mercari1/Book-Off1/Hard-Off1）。
- **第1段-c** `primary`（塗り）を `clsBuyee`/`clsCode`/`clsTitle` で動的化。
- **第2段** `position:sticky; top:0; z-index:20`（モーダル群40〜70より下）。
- **第3段** 状態保持（URL > localStorage > 既定out）。

### 第2段で踏んだこと — 帯は `.wrap` の中では全幅にならない
`margin:0 -18px` は `.wrap` の **padding** を打ち消すが、`.wrap` の **`max-width:760px`** は打ち消せない。
広い画面で**帯が中央760pxの暗い長方形として浮いた**（実測・スクリーンショットで確認）。
→ **`#locbar` を `.wrap` の外（body直下）へ移動**して解決。CSSは負マージンが不要になり簡単になった。
★**予測を書いてから撮ったので、1枚で判定できた。**

### 第3段の設計 — URLとlocalStorageは代替関係にない
**両方使う。優先順は URL > localStorage > 既定(out)。**
- **URL(`?loc=jp`)** … 共有リンクが状態を運ぶ。Reddit/Discord に「日本にいる人向け」として貼れる。
- **localStorage(`kataban_loc`)** … リロードで戻らない。
  **店頭は電波が悪く再読み込みが起きやすい**ので、日本にいる人が知らないうちに海外向け表示へ戻るのを防ぐ。

**`skipSave` フラグを入れた理由**: 起動時の復元で保存処理を走らせると
**`?loc=out` が常にURLへ焼き付く**。誰も選んでいないのに既定値が共有リンクに乗る。
→ **選択の結果だけを保存する。**

**`try/catch` が必須**: `localStorage` は**アクセス自体が例外を投げる環境がある**
（プライベートブラウズ・一部WebView）。囲まないと `setLoc()` が途中で死んで
**`paint()` に到達せず、トグルが押せなくなる。** 7/24 に LINE WebView を検証済みなので実在の想定。
**保存値の検証も必須**（`localStorage` は人が書き換えられる）＝`'out'/'jp'` 以外は捨てる。

**起動時の復元は `run()` の前**に置く。後だと `run()` が既定outで一度描いてから
切り替わってちらつく。（この配慮は Claude Code の判断で、指示には書いていなかった。）

### 罠#85. 編集のたびに現物を再取得しないと、次の指示のold_strが必ず腐る
今日、**コメントの範囲指定を3回連続で外した**（6,248の置換 / フォールバック置換 / コメント整理）。
原因は共通で、**「直前に見た行構成」を前提に次の指示を書いていた**——
**1回置換すると行構成が変わる。**
3回目は Claude Code が **old_str と new_str の矛盾を検出して中止・確認を求めた**ため、
コメントの二重破損を免れた。
→ 対策: **編集の指示を書く前に、対象箇所を毎回ファイルから再取得する。**
  **指示にフォールバック規則を書いておく**（今日それで1回救われた）。
  **Claude Code に「一致しなければ中止して報告」を毎回書く。**

### 罠#86. 成功条件の grep が、コメント内の文字列を拾う
`grep -c 'class="store primary"' → 0` を成功条件に書いたが、**結果は1**。
実コードは全て変数化されており、ヒットしたのは**説明コメント内の文字列**だった。
罠#80（成功条件は母集団を明示してから書く）の再発。
→ 対策: **コード対象の grep を成功条件にするときは、コメントを除外するか、
  「実コードで0件」と母集団を書く。**

### 未解決に追加
- **未解決#16.** `row[7]`（Buyee検索キー）が空の **5,099行（対M 16.58%）** を埋められるか未測定。
  `BLAZBLUE` の Switch/PS4 の対比から、**機種で決まっている可能性**（7/18の `BUYEE_OK` 25機種）。
  ★**機種別内訳を数えるのが最初の一手。** 埋まればパターンBがほぼ消え、塗りゼロの分岐が不要になる。
- **未解決#17.** localStorage の `'jp'` に**期限が無い**。帰国後も日本向け表示のまま。
  日数の根拠が無いので期限を設けなかった。**トグルは常設なので気づいて押し直せる**——
  常設化しておいた副産物。
- **未解決#18.** 「I'm in Japan」は**観光客と在住外国人を1つにまとめている**。
  在住者の「Japanese site で直接買う」は正しいが、観光客は日本の住所・決済を持たないことがある。
  3状態にすると店頭で迷うので**2状態のままにした＝意図した妥協。**

### 今日わかった、トグルとは別の未着手
- **駿河屋のアフィリエイトが入っていない。** `buyBlock()` の2本は生の
  `https://www.suruga-ya.jp/search?search_word=` で、**`af_jump` ラップ・`category=2`・
  `-JPN` 除去はすべて未実装**。7/23のToDo項目6は**完了していない。**
  ★トグルの編集に混ぜなかった（どちらが原因で壊れたか分からなくなる）。**次の単独作業。**
- **`kicker` が `JDM retro game finder` のまま**（7/23に本人が疑問を出した文言）。
- **デザイン案との差分が大きい**: 案にあるナビ（型番とは / DATA）・ピクセルロゴ・
  統計3枠・SUPPORTS チップ・床グリッドは**リポジトリに1つも無い。**

### 未解決#13 の旧エントリを閉じた（2026-07-25）
「トグル未実装。デザインは確定済み。」と書かれていた2箇所に打ち消し線と解決注記を付けた。

★このとき私（指示側）は「既存行は一切変更しない」と書きながら、同じ追記ブロックの中に
  「既存行に打ち消し線を付けよ」を混ぜていた。**追記と既存行の変更は別の承認経路**
  （罠#51と同じ形）。Claude Code が矛盾を検出して追記のみで停止し、確認を求めたため
  事故にならなかった。→ **1回の指示に、承認経路の違うものを混ぜない。**

---

## 店リンクへのカテゴリ絞り込み — 4サイトすべて却下（2026-07-25）

**発端**: 駿河屋 `category=2` が効いた（429→2件・7/23）ので、他店にも同じことをする案。
→ **測ったら全部却下になった。**

### 前提の整理 — カテゴリを付ける人が違う
| | 分類する人 | |
|---|---|---|
| 駿河屋 | **自社**（仕入れて自社で分類するカタログ店） | 分類が商品と一致 |
| Book-Off Online | 店 | **一致するとは限らなかった（下記）** |
| Buyee（ヤフオク等を横断） | 出品者（個人） | 誤分類・無分類が消える |
| Mercari | 出品者（個人） | 同上 |
| eBay | 出品者（個人） | 同上 |
| Hard-Off | — | **7/19に実装済み**（`/cate/00010012/`）。今回の対象外 |

### 実測（2題・生URLで測定＝自己クリック回避）
| | ゴーストバベル（日本専売・グッズ無し） | ポケモン赤（グッズ大量） |
|---|---|---|
| Book-Off | **1 → 0件** | 24 → 5（79%減） |
| Buyee | 30 → 27（10%減） | 3,956 → 3,176（20%減） |
| eBay | （クロノトリガーで）870 → 767（12%減） | — |
| Mercari | **件数が表示されない**（効果を測れない） | 同 |

### 却下の理由
- **Book-Off**: ★決定的。**在庫1件だけの日本専売タイトルが、ゲームカテゴリで0件になった。**
  ポケモン赤の79%減は本物だが、**ゴーストバベルが消える代償のほうが重い。**
  この道具は「英語話者が日本専売のマイナータイトルに到達する」ためのもので、
  **消えてはいけないのはまさにその側。**
- **Buyee**: 10〜20%しか減らないのに、消えた3件・780件の正体が読めない。
  出品者分類なので**ゲーム本体が無分類で消えている可能性がある。**
- **eBay**: 12%減。しかも**eBayの本当のノイズはカテゴリではなく国**——
  `Chrono Trigger` で出るのは主に北米版で、それはゲームカテゴリの中にいる。
  つまり**日本版に近づく効果がゼロ。** URL構造も `/sch/139973/i.html` に変わり保守が増える。
  → **国で絞る話は別の設計課題**として立てる。
- **Mercari**: 出品者分類 ＋ **件数が出ないので効果を測れない。**

### 罠#87. 「店が分類している」は、精度の保証にならない
7/23に駿河屋 `category=2` を採用した根拠は「**店が分類しているから取りこぼしにくい**」だった。
同じ理屈で Book-Off Online も安全だと予想した。**外れた。**
駿河屋は**自社で仕入れて自社で分類するカタログ店**、Book-Off Online は在庫の分類粒度が違う。
→ **「誰が分類しているか」は必要条件だが十分条件ではない。サイトごとに測る。**

### 副産物: Buyee は category と lang=en が併存できる
`.../category/25464?translationType=98&lang=en` で **27件 = カテゴリのみの27件**。
カテゴリ絞り込み自体は却下したが、**この事実は記録しておく**（将来 Buyee のURLを触るとき、
`lang=en` が他パラメータと競合しないことの確認済み事例）。
なお**手動でカテゴリを選ぶと `lang=en` ではなく `translationType=98` が付く**——
現行実装は `?lang=en`。**同じ英語表示に2つの経路がある。**

### この測定の限界（意図して残す）
- **2題しか測っていない**（罠#57）。削減率は他の題を代表しない。
  ただし**却下の判断には足りている**——1題でも「本体が消える」が出たら却下でよい。
- 指標は**件数ではなく「目的の物が1ページ目にあるか」**（罠#61）。
  上表の件数は判断材料であって指標ではない。**Book-Off の 1→0 は件数ではなく消滅。**

**測ったから却下できた。** 件数だけ見て4つとも入れていたら、
**日本専売タイトルの在庫が静かに消える実装**になっていた。

## 駿河屋アフィリエイトを実装して公開した（2026-07-25）

7/23 ToDo項目6を完了。`buyBlock()` の駿河屋2本を `af_jump` ラップ・`category=2`・
`-JPN` 除去まで一度に入れ、人間向けPR開示を2箇所（フッター／購入ブロック直下）に置いた。

| | |
|---|---|
| 生成ルール | `sgAf(w) = SG_AF + encodeURIComponent('…/search?category=2&search_word=' + w)`。`w` は既に `enc()` 済み＝ここで二重エンコードが成立 |
| 形式の証明 | ★**本人が駿河屋の生成器で作った実物3本と、スクリプト生成値がバイト一致。** 形式は構成上証明済み＝**確認のために af_jump を踏む必要が無い** |
| 型番 | 駿河屋専用に `kwCodeSg = enc((codes[0]||'').replace(/-JPN$/,''))` を新設。既存 `kwCode` は無変更（Buyee等が使う） |
| rel | `sponsored nofollow noopener` |
| 開示（全体） | フッター: `Some links and product images on this page are affiliate links, and Kataban may earn a commission (PR).` ★**店名を列挙しない**＝店を足しても腐らない（罠#62/#84 の予防） |
| 開示（近接） | 購入ブロック見出し直下に `.cap` で `Some store links are affiliate links (PR).` ★フッターだけの開示は**米国FTCの考え方では弱い**（読者は北米・欧州中心）ので、リンク直前にも置いた |
| 言語 | ★**開示は英語。** ステマ規制は日本の法律だが、**読者が読めない言語の開示は機能しない** |

### 開示行が偽になるカードは無い（実測 2026-07-25）
「カードごとに常時出る開示行は、駿河屋リンクが無いカードで偽になる」と懸念したが、実機で否定された。
- 配信専用（`row[12]==1`）… **購入ブロックが丸ごと出ない**ので開示行も出ない（MGS V PS4 で確認）
- 型番なし … `BY PRODUCT CODE` グループは消えるが `BY JAPANESE TITLE` の駿河屋が残る（Max Payne PS2 で確認）
→ **購入ブロックが出る行には必ず駿河屋リンクが1本ある。開示は出るとき必ず真。**
「意図した妥協」ではなく**実測で正しい**。

### 駿河屋は検索語の記号揺れを吸収する（実測 n=3・2026-07-25）
シード42の10行でA(row[8])/B(row[1])の件数を比較した際の副産物。
文字列が違うのに件数が一致した3組:
- 波ダッシュ `〜` vs `～`（BUSHI青龍伝 1=1 / 影牢 6=6）
- 全半角アンパサンド `＆` vs `&`（ラチェット＆クランク5 3=3）
- 空白位置の差
→ **検索語の記号正規化は実装しない。** ハードオフの中黒（罠#25/#26）と違い不要だった。
★ただし n=3、かつ**同数 ≠ 同一結果**。「不要と断定した」ではなく
**「作る理由が見つからなかった」**として読むこと。

### アフィリエイト棚卸し（2026-07-25・全リンクをパラメータ名で機械判定）
| 店 | 判定 | 根拠 |
|---|---|---|
| 駿河屋（型番／タイトル） | **アフィリエイト** | ホスト `affiliate.suruga-ya.jp` ＋ `user_id` |
| 楽天 商品画像 | **アフィリエイト** | 生成元が `affiliateId` 付き、`affiliateUrl` を href に入れている |
| Buyee / eBay / Mercari / Book-Off / Hard-Off | **素のリンク** | アフィリエイト系パラメータが1つも無い |

**Hard-Off の `s=7` は「要調査」ではなく素のリンクで確定。**
理由: アフィリエイトリンクは**成果をどのアカウントに帰属させるかの識別子を必ず含む**。
`s=7` は全ユーザー共通の定数でアカウントを identify していない＝**構造上アフィリエイトではあり得ない。**
`s=7` の機能（並び順か表示件数か）は未確定だが、それは**規約の問題ではなくUXの問題**。分けて扱う。

★**この棚卸しで未解決#8 の前提が崩れた。**
未解決#8 には「同じ画面に Buyee/eBay のアフィリンクが併置されている」と書いてあるが、
**併置されていなかった。7/23〜7/25午前まで、このページのアフィリエイトは楽天1本だけだった。**
**そして今日、駿河屋で2本目を足した。** → 未解決#8 は「理論上の懸念」から
**「実際に発生している構成」**に変わった。次回、未解決#8 の本文を更新する（今回は追記のみのため未編集）。

**構造的な反論（＝Claudeの解釈。確認済みの事実ではない）**:
駿河屋リンクの検索語は `row[3]`(MADB) と `row[8]`(Wikidata) から作られ、**楽天APIが返した情報を一切使っていない。**
楽天API由来のデータで換金しているのは楽天アフィリンクだけ。第10条4項が禁じるのが
「APIで得た情報を楽天アフィリエイト以外で換金すること」なら Kataban は当たらない。
→ **確実にするには楽天の「利用規約に違反しているか個別に確認したい」フォームで構成を書いて確認する。一発勝負にしない。**

### 罠#88. 正しく測った値を、指示文に手で写す途中で壊した
逆転裁判3 の期待 href をスクリプトで生成したのに、指示文へ転記する際に `%25` を2つ落とした
（正 `%25E5%2588%25A4` → 誤 `%25E5%88%A4`）。Claude Code の生成値が正しく、**期待値が壊れていた。**
罠#76/#77 は「絵の値をデータの事実にした」形。これは**「自分が正しく測った値を転記で壊した」**形。
対策: 指示文の期待文字列は**スクリプト出力をコピペする。手で打たない。**
★さらに良い対策: **期待値との文字列一致ではなく、`decode`×2 で元の語に復元するかを成功条件にする。**
復元テストは自己検証的で、転記ミスの影響を受けない。（誤った期待値は復元不能でエラーになる＝自動的に検出される）

### 罠#89. 変数を「定義せよ」と「呼び出し側へ渡せ」は別命令
`kwCodeSg` の定義と参照だけ指示し、**`buyBlock({...})` 呼び出しへの受け渡しを書き忘れた。**
実行されれば `sgAf(undefined)` で**全カードの型番リンクが壊れていた。** Claude Code が補って事故を止めた。
対策: 新しい変数を `card()` 系に足すときは**「定義／呼び出し側へ追加／参照」の3点セット**で書く。

### 次にやること（2026-07-25 夕・更新）
1. **未解決#8 の本文を更新**（上記の棚卸し結果を反映。既存行の変更＝別作業）
2. **開示行のコントラストを測る。** `.cap` は `--muted` なので本文より暗い。
   FTC の "clear and conspicuous" に対して十分か。**測ってから `--ink-2` の採否を決める**（未解決#10 と同じ手順）
3. **駿河屋の検索語を row[8] → row[1] にするか。** 件数では決着しなかった
   （シード42の10行で 同数7 / B勝ち2 / A勝ち1）。指標は件数ではなく**1ページ目に目的の物があるか**。
   ★測るのは食い違った3行のみ: #4 リンクの冒険 / #5 三國志2 / #7 ぼくとシムのまち。生URLで測る。
   推奨は row[1]（**アプリが表示し「店の検索欄に貼れ」と言っているのが row[1]**＝表示・コピー・リンクを一致させる）。
   row[8] の癖（`(ゲーム)`・別名・副題落ち）は**Wikidataの目録規約で、箱にも店の商品名にも現れない文字。**
4. **Buyee / eBay / Mercari は素のリンク＝未回収。** Buyee と eBay Partner Network には
   アフィリエイトプログラムがある。「小さな収益を複数積む」方針なら**駿河屋の次に測る候補。**
5. `kicker` が `JDM RETRO GAME FINDER` のまま（本人が7/25に疑問を出した文言・未着手）

## 検索語の品質を測って直した（2026-07-25 夜）

### row[8] 末尾括弧の条件付き除去 — 実装した
末尾括弧は **139行（0.37% / 全37,855行）**。中身は Wikidata の曖昧さ回避が大半（`ゲーム` 74件）。

**判定基準はデータが決めた**: `row[1]` が同じ括弧を含むなら残す、含まないなら除去候補。
→ `萌え萌え2次大戦(略)` の4行が正しく「残す」に落ちた。**`(略)` は箱の正式表記**で、
その証拠は **`row[1]` 側にも `(略)` があること**。私の知識ではなくデータが証明した。

**135行を全数測定（駿河屋・生URL）:**
| | 行数 |
|---|---|
| 救済（A=0 → B≥1） | **23** |
| 不変 | 74 |
| 件数増 | 35 |
| 回帰（B<A） | **0** |

★**ここで「回帰0だから安全」と結論しかけた。それは件数の話だった。**
件数増の35行を**到達（1ページ目に目的の物があるか）**で測ると、**9行が破綻**していた:
- `機動戦士ガンダム (プレイステーション)` 系 **5行** … 括弧を外すとシリーズ216件に埋もれる
- `幽☆遊☆白書 (スーパーファミコン)` … 続編のみになる
- `北斗の拳 (パチスロ)` / `ONE (ワン)` … ONE は ONE PIECE 702件に化けて完全消滅

**★機種名の括弧は AND フィルタとして正しく効いていた。** 一律除去は9行を壊す。

**採用した形＝括弧の中身が目録用語に完全一致するときだけ除去（ホワイトリスト）:**
```
ゲーム / ビデオゲーム / コンピュータゲーム / テレビゲーム / シューティングゲーム /
ゲーム イッキ編 / 1996年のビデオゲーム / ゲームソフト / リメイク版 / STG / 第1作
```
結果: **96行に適用・救済21・実測された破綻0。**
`kwKana` / `kwKanaHard` に適用＝**駿河屋・ブックオフ・ハードオフの3店に効く**。
★**駿河屋以外での効果は未測定**（ブックオフは `パワーストーン` 1件のみ目視確認）。

**捨てた救済2件**: `英単語ターゲット (ニンテンドーDS)`(0→5) / `ウルトラマンネクサス (プレイステーション2版)`(0→1)。
**機種名だから除去しない。**「機種名は絶対に除去しない」と一文で言い切れる形を、2行の救済より優先した。

### row[1] の括弧は永久に却下（2026-07-25）
row[1] 末尾に括弧 **224行（0.59%）**。中身は目録用語ではなく**箱に印刷された読み・版名**:
`ウルトラマンR/B (ルーブ)` `鋼鉄霊域（スティールダム）` `おまかせ! 退魔業（セイバーズ）`。
**出品者は「ルーブ」と書くので、括弧の中身こそが当たる文字列。** row[8] と正反対の性質。

★**決定的だったのは `void tRrLM()` 2行。** 括弧の中身が空で、**括弧そのものがタイトルの一部**。
正規表現 `[（(][^）)]*[）)]\s*$` は `[^）)]*` が0文字にマッチするので、
**これを `void tRrLM` に変える。** row[1] に適用していればタイトルを破壊していた。
→ **row[1] は触らない。数えて正解だった。**

### row[8] → row[1] への入れ替えは「しない」で確定（2026-07-25）
食い違った3行を到達で測定。**到達は6/6で全勝＝壊れているリンクは無い。** 順位で:
| 行 | A(row[8]) | B(row[1]) | |
|---|---|---|---|
| リンクの冒険 | 1位/2件 | 1位/7件 | 分 |
| 三國志2 | **2位/38件** | 12位/251件 | **A** |
| ぼくとシムのまち | 5位/10件 | **1位/1件** | **B** |
**1勝1敗1分。** row[1] 一律置換は 三國志型で 2位→12位の回帰を買う。
**測って「現状維持が正しい」と出た。** n=3 で条件分岐を書くのは却下（小標本から一般化しない）。

★**残る非対称**: 表示・コピー欄は `row[1]`（jpMain）、店リンクは `row[8]`。
**ユーザーがコピーする文字列と、ボタンが検索する文字列が違う。** 未解決。

### 駿河屋は記号揺れを吸収する（実測 n=3）
`〜`/`～`、全角`＆`/半角`&`、空白位置の差で**件数が一致**（1=1 / 6=6 / 3=3）。
→ **記号正規化は作らない。** ★n=3、かつ**同数 ≠ 同一結果**。
「不要と断定した」ではなく**「作る理由が見つからなかった」**と読むこと。

## ブックオフの genre 絞り込み — 却下を撤回して実装した（2026-07-25）

**★午前の却下は誤診だった。** 根拠は「ゴーストバベル 1件→0件」だったが、
夕方に商品ページを開いたら、その1件は**ゲーム本体ではなく攻略本**
（`メタルギアゴーストバベルパーフェクトガイド` / パンくず `書籍 > 趣味 > ゲーム攻略本`）。
**ブックオフの分類は正しく、genre51 も正しく動作していた。** ゲーム本体は在庫0だっただけ。

**100本で再測定（母集団 row[12]!=1 の 30,746行 / seed 42）:**
| | 行数 |
|---|---|
| genre51 でゲーム本体を失った | **0** |
| genre51=0 だが失ったのがゲーム本体でない | 6（CD・書籍・コミック・攻略本のみ） |
| 無絞りも0 | 14 |
| genre51≥1 | 80 |

★**n=100 より強い根拠が出た**: **facet の合計が総件数と 86/86 で一致。**
ブックオフは全商品を必ずどれかのジャンルに分類しており、**未分類が構造的に存在しない。**
ゲーム本体なら genre51 に必ず入る。**これは標本数ではなく仕組みの話。**

→ `https://shopping.bookoff.co.jp/search/genre/51/keyword/{語}` を実装。
削減率の中央値 22.5%（`HARD BLOW` 63件の邦楽CDが落ちてゲームだけ残る）。
★**未測定**: genre51≥1 の80行で目的の物が1ページ目に残るか（件数しか見ていない）。

## メルカリ category_id=76 — 実装した。ただし根拠は弱い（2026-07-25）

★**メルカリは Claude Code から測定できない。** React Server Components のSPAで、
取得したHTMLは殻（`/item/m...` リンク0件・`__NEXT_DATA__` 0件・`ld+json` 0件）。
**件数・商品名・カテゴリのいずれもHTMLから取れない。** 駿河屋・ブックオフとの決定的な差。
→ **ノートの旧却下理由「件数が表示されないので測れない」は、今も有効だった。**

**本人がブラウザで目視して測った（低ノイズ2本・高ノイズ1本）:**
- 低ノイズ2本 … 絞り込み前後で**ソフトは同じ**。落ちたのは**サントラ**
- `テニス` … 絞り込みでゲームだけになった

→ `&category_id=76` を実装。
★**根拠の強度がブックオフと違う。同じ「実測した」と書いてはいけない。**
ブックオフ＝n=100＋facet完全一致の構造保証 / メルカリ＝**目視2本・構造保証なし**。
★**残るリスク**: メルカリの分類者は**出品者個人**。カセットを「おもちゃ・ホビー」に
出品されたら `category_id=76` はそれを消す。**これは測れない。**
**将来「メルカリで見つかるのに Kataban のリンクに出ない」報告が来たら第一容疑者。**

## 日本語直後の数字の前に半角スペース — ブックオフ限定で実装（2026-07-25）

**発端**: 本人が店頭ではなく手元で発見。`メタルギアソリッド2 サブスタンス` は
ブックオフで**0件**、`メタルギアソリッド 2 サブスタンス` は**6件**。
→ **MADB の `row[8]` は空白の無い形を持っていた**（row 2659/2660/14228/28473）。
**本物の死にリンクだった。**

**★同じルールが店ごとに真逆に働いた。**
| | 救済 | 順位 |
|---|---|---|
| **ブックオフ** | **0件だった44行のうち15行＝34.1%**（150行標本・seed 4242） | 件数減0 |
| **駿河屋** | **0行**（30行標本・seed 42） | 2行が低下（+3 / +4） |

→ **ブックオフだけに適用**（`kwKanaBo`）。駿河屋は効果ゼロで順位だけ下がるので入れない。
**罠#87「店ごとに測る」の教科書的な実例。**

**採用した形**（★lookbehind不使用＝旧iOS Safari対策）:
```js
const spaceBeforeNum = s => s.replace(/([ぁ-んァ-ヴー一-龥々〆])(\d)/g, '$1 $2');
```
**日本語文字の直後の数字だけ**に限ることで `F1レース` `PS2ソフト` `MOTHER2`
`SLPS02430` `AGB-A3JJ` `1943` はすべて不変（実測確認）。
無条件に入れると `F 1レース` `SLPS 02430` になる。**型番が死ぬ。**

該当 **5,115行**。救済見込み **約428〜512行**（★点推定であって実測ではない）。
**ruleC（末尾数字限定）は採らなかった**: 救済例 `FP技能検定試験 2級 3級`(0→3件) は
ruleB でしか救済されず、**ruleB の「数詞を割る」性質はブックオフでは利益だった。**
ruleC は未測定なので、測っていない方を選ぶ理由がない。
★**未測定**: ブックオフで目的の物が1ページ目に残るか（件数しか見ていない）。

## アフィリエイト棚卸しで未解決#8 の前提が崩れた（2026-07-25）
| 店 | 判定 | 根拠（パラメータ名のみで機械判定） |
|---|---|---|
| 駿河屋 ×2 | **アフィリエイト** | `affiliate.suruga-ya.jp` ＋ `user_id` |
| 楽天 商品画像 | **アフィリエイト** | `affiliateId` 付きで叩き `affiliateUrl` を href に |
| Buyee / eBay / Mercari / Book-Off / Hard-Off | **素のリンク** | 該当パラメータ0 |

**Hard-Off の `s=7` は素のリンクで確定。** アフィリエイトリンクは
**成果をどのアカウントに帰属させるかの識別子を必ず含む**。`s=7` は全ユーザー共通の定数で
アカウントを identify していない＝**構造上アフィリエイトではあり得ない。**
`s=7` の機能は未確定だが、それは**規約の問題ではなくUXの問題**。分けて扱う。

★**未解決#8 は「理論上の懸念」から「実際に発生している構成」に変わった。**
7/23〜7/25午前まで、このページのアフィリエイトは**楽天1本だけだった**（Buyee/eBay は素のリンク）。
**今日、駿河屋で2本目を足した。**
**構造的な反論（＝Claudeの解釈。確認済みの事実ではない）**: 駿河屋リンクの検索語は
`row[3]`(MADB) と `row[8]`(Wikidata) 由来で、**楽天APIが返した情報を一切使っていない。**
→ **確実にするには楽天の個別確認フォームで構成を書いて確認する。一発勝負にしない。**

## 罠（2026-07-25 夜）

### 罠#90. ホワイトリストを部分一致で書いたら、危険な方向に漏れた
括弧内の目録用語を `/[^)）]*ゲーム[^)）]*/` と部分一致で書いた結果、
**`(ゲームボーイ)` 2行が「機種名は絶対に除去しない」というコメントに反して除去された。**
`(ゲームギア)` `(ゲームキューブ)` も将来同じ漏れ方をする。
対策: **除去系のホワイトリストは完全一致（Set.has）で書く。**
完全一致なら未知の語は「除去しない」＝現状維持＝**無害**に倒れる。
部分一致は「除去する」＝**破壊**に倒れる。
★**失敗する方向を先に決めてから条件を書く。**

### 罠#91. 通過率100%のテストが、母集団に穴があれば何も保証しない
25本（15変わる/10不変）が全通過したが `(ゲームボーイ)` の漏れを検出できなかった。
「不変」側に並べた機種名5つが**どれも「ゲーム」を含まなかった**ため。
**漏れを見つけたのはテストではなく、頻度表の算数**（74+5+3+3+3+2+2+1+1 が98に4足りない）。
対策: **テストの母集団を実データの頻度表から作る。** 思いついた例を並べるのではなく、
**実際に存在する値の全種類を通す。** 罠#80 の再発。

### 罠#92. 件数の変化だけを見て、消えた1件の中身を見なかった
ブックオフの genre 絞り込みを「1件→0件」を理由に却下したが、
その1件は**ゲーム本体ではなく攻略本**だった。**却下の根拠が消えた。**
対策: **件数が減ったとき、消えたものの中身を必ず1件は開く。**
「減った＝取りこぼし」ではない。ノイズが正しく落ちた可能性が常にある。
★罠#61 の裏返し。**増えたときも減ったときも、見るのは件数ではなく中身。**

### 罠#93. 自動の商品名照合が、表記系の違いで誤判定した
- カタカナ ↔ ラテン（検索`ウイニングポスト5` / 商品名`Winning Post5` → 「無し」と誤判定）
- 算用数字 ↔ ローマ数字（検索`三國志11` / 商品名`三國志XI` → 順位1→13と誤判定）
Claude Code が**件数の動いた6行だけ手で原文照合**して訂正した。
対策: **到達・順位の判定を自動化するときは、表記系の違いを先に列挙する。**
最低でも カタカナ/ラテン・算用/ローマ数字・全半角 の3系統。
★**件数の動いた行だけ手で見る**、が現実的な落とし所。

### 罠#94. ノートに書かれたファイルパスを、実在するものとして指示に書いた
`scratchpad/sample30.py` を既存ファイルとして指示に書いたが、
ノートの当該行は**「★未測定: seed 42 の30本（`scratchpad/sample30.py`）」**——
**これから作る予定のもの**だった。Claude Code が停止して事故にならなかった。
罠#76（デザイン案をリポジトリの現状として書いた）の**ノート版**。
対策: **ノートの「未測定」「予定」セクションのパスは実在しない。**
指示に書く前に必ず `ls` する。

### 罠#95. 先に決めた判定基準が、片方の指標しか含んでいなかった
括弧除去で「救済>0・回帰0なら実装」と**先に基準を書いた**。基準どおりなら一律除去を実装し、
**到達で9行を壊していた。** 事前登録は正しいが、**基準に件数しか入れていなかった。**
対策: **判定基準には必ず「件数」と「到達（1ページ目に目的の物があるか）」の両方を入れる。**
★事前登録は「何を測るか」を固定するもので、**測る指標が足りなければ意味がない。**

### 次にやること（2026-07-25 夜・更新）
1. ★**既存行の訂正**（これが残っている限りノートは嘘を持っている）
   - 「店リンクへのカテゴリ絞り込み — 4サイトすべて却下」の表と Book-Off / Mercari の行
   - 「Book-Off は店が分類するが一致するとは限らなかった」（根拠が消えた）
   - 未解決#8 の本文（Buyee/eBay は素のリンクだった）
2. **git 初期化。** 今日 `git diff` が使えず md5 で純追加を証明する手間が実際に発生した
3. **開示行のコントラストを測る。** `.cap` は `--muted`。FTC の "clear and conspicuous" に足るか
4. **表示・コピー欄(row[1]) と 店リンク(row[8]) の非対称**をどうするか
5. **Buyee / eBay / Mercari は素のリンク＝未回収。** Buyee と eBay Partner Network には
   アフィリエイトプログラムがある。「小さな収益を複数積む」方針なら次の候補
6. **棚ナビ（本丸）** ← 本来の主役。7/23から待っている
7. `kicker` が `JDM RETRO GAME FINDER` のまま

---

## 駿河屋アフィリエイトに UTM を追加した（2026-07-26）

**発端**: 駿河屋アフィリエイト管理画面が出力するリンクと、Kataban が生成するリンクを並べた。
**差分は1箇所だけだった**——`search_word` の終わりまでバイト一致し、公式側は末尾に UTM を4本足していた。

| | |
|---|---|
| 追加した4本 | `&utm_medium=affiliate` / `&utm_source=gamekataban.com` / `&utm_content=%2F` / `&utm_campaign=afid{ID}` |
| 入れる位置 | `enc()` の後・外側 `encodeURIComponent` の**前**。inner 文字列の末尾に連結する |
| `utm_content` | ★**`%2F` をエンコード済みのまま書く**（`/` に直さない）。外側で `%252F` になって公式と一致 |
| 実装 | `SG_UTM` を新設し `sgAf()` の inner 末尾に `+ SG_UTM`。**追加1行・変更1行** |
| 副産物 | 呼び出し2箇所が同じ `sgAf()` を通るので、**型番リンクにも自動で入った** |
| 検証 | **公開版の href を管理画面の出力とバイト比較して一致**（タイトル側434文字 / 型番側261文字・`cmp` IDENTICAL） |
| ★ID | **`user_id` と `utm_campaign=afid…` の両方にIDが入る。ノートには生値を書かない**（正典は管理画面。罠#48の運用） |
| バックアップ | `index.html.20260726-bak-pre-sgutm` |

**二重エンコードの独立確認が取れた**: `%25E7%25AC%25AC`（=「第」）まで駿河屋自身の生成物と一致した。
「inner を作って `encodeURIComponent` をもう一度」は正しかった。**相手側の生成物で裏が取れたのは初めて。**

**UTM は検索を壊さない — n=2**（生URLで測定＝自己クリック回避）

| 題 | UTMなし | UTMあり |
|---|---|---|
| 第3次スーパーロボット大戦Z（タイトル検索） | 7件 | 7件 |
| `BLJS50041`（型番検索） | 同一 | 同一 |

### 決めたこと: `searchbox=1` は入れない
7/23の型番リンク生成規則には `&searchbox=1` が書かれているが、**index.html には0件**（`grep -c`=0）。
**駿河屋自身が生成するリンクにも入っていない。** 効果を測っていないので**入れないと決めた**——
入れ忘れではない（罠#52と同じ形＝放置は放置と明記する）。

### 罠#96. n=1 の測定に「確定」と書いた（指示側の再発）
タイトル検索1題（7件→7件）だけで「UTM は検索を壊さないと確定」と書いた。**罠#57そのもの。**
型番検索を足して n=2 にしてから書き直した。
**「回帰ゼロ」は母数を書かないと意味を持たない。** 1題は判断材料、確定は別。

### 7/25「駿河屋のアフィリエイトが入っていない」は失効していた
7/25のリストに「`buyBlock()` の2本は生URL、`af_jump` ラップ・`category=2`・`-JPN` 除去はすべて未実装」
と書かれているが、**今日 grep したら全部実装済みだった**（`SG_AF` / `category=2` / `.replace(/-JPN$/,'')`）。
**いつ実装されたかは記録が無い。** 罠#84（解決済みが「未解決」のまま残る）の**逆向き**＝
**未着手の行が完了後も残る。**
→ 該当行への打ち消し線は**別作業**とする（追記と既存行の変更は承認経路が違う）。

### 罠#97. 指示側が参照しているノートのコピーが、repo より古かった
罠番号の最大値を「#87」として指示に書いた。**repo の実測は #95** で、
**#88 は既存の別項目（正しく測った値を指示文に手で写す途中で壊した）に使われていた。**
そのまま追記すれば **同番号の罠が2つ並び、`grep 罠#88` がどちらも指さなくなっていた。**
原因は、指示側に添付されているノートのコピーが repo の現状より古いこと。
→ **番号・行番号・ファイル構成を、添付コピーから読んで指示に書かないこと。**
  **必ず repo を grep させ、想定と違えば止める手順を先に置く。**
  罠#76 / #94 と同じ形。**今回は手順1が置かれていたので事故にならなかった。**
  ★この「先に番号を数えさせて、想定と違えば止める」手順を、以後の追記の定型にする。

### 未確認（意図して残す）
- **`-JPN` 除去は今日の検証では通っていない。** 検証に使った `BLJS50041` は `-JPN` を持たない。
  除去ロジックが壊れていないことは**確認できていない。**

### 出所の明記（罠#96の適用・後追い記録）

上のブロックの数字は**測った主体が3つに分かれている**。追記時に書かなかったので補う。

| 数字 | 測った主体 | 何と比べたか |
|---|---|---|
| タイトル側 434文字 一致 | Claude Code（node vm で `sgAf()` を実行）＋ 指示側（公開版 href を `cmp`） | ★**駿河屋管理画面の出力**＝外部の正典 |
| 型番側 261文字 一致 | **指示側のみ**（`SG_AF`/`SG_UTM`/`sgAf` を node で再現） | **自分の再現物**。★管理画面の出力とは比べていない |
| 7件→7件 / 型番側も同一 | **本人**（ブラウザで生URL） | 同一URLの UTM 有無 |

★**型番側は「公式生成物と一致」ではない。** 比較対象は指示側が仕様から書き直した再現物で、
その仕様は Claude Code の報告から書いた。
**検証欄の「管理画面の出力とバイト比較」は、タイトル側にだけ当たる。**
型番側について言えるのは**「公開版の href が、報告された実装から導かれる文字列と一致した」**まで。
（駿河屋の管理画面は任意の型番のリンクを出さないので、**この比較は現状作れない。**
 作る道があるとすれば、管理画面で型番を検索してリンクを取得すること。未試行。）

**この節そのものが罠#96の実例**＝数字には、母数だけでなく**誰が測ったか**も要る。
「一致した」は**何と一致したか**を書かないと、強さが読めない。

---

## Buyee を PS3 / PS Vita / 3DS に拡張した（2026-07-26）

**発端**: 未解決#16「row[7] が空の5,099行を埋められるか未測定。機種で決まっている可能性」。
**機種で決まっていた。** タイトルごとに埋まらなかったのではなく、
`BUYEE_OK`（25機種の実測ホワイトリスト）に載っていない機種が丸ごと空だった。
空を持つ11機種のうち10機種は**その機種の全行が空**。Famicom の5行だけが例外で、
それは `-USA` 型番の北米版混入（罠#16）＝別問題。

### 測定 — キーワード単体（Buyee・生URL）

| キーワード | 単体件数 | 判定 |
|---|---|---|
| プレイステーション3 | 38,725 | 通る |
| PS3 | 38,727 | 通る（差2件） |
| プレイステーションVita | **601** | ★使えない |
| PSVita | **23,226** | 通る（38倍） |
| ニンテンドー3DS | 35,485 | 通る |
| 3DS | 39,555 | 通る（1.11倍） |
| 参照 プレイステーション2 | 63,837 | 現行で機能中 |
| 参照 ニンテンドーゲームキューブ | 1,402 | — |
| 参照 ゲームキューブ | 7,578 | — |

★**キーワード単体の測定が、タイトル試験より強い。** 「売り手がその文字列を書くか」を
直接測れて、行の当たり外れに左右されない。**次にキーワードを検討するときは、これを最初にやる。**

### 「Buyee は同義語を解決しない」は言い過ぎだった
`プレイステーション3` 38,725 と `PS3` 38,727 で**差が2件**。ヤフオク側で同義語展開が働いている。
ただし万能ではない: `ニンテンドーゲームキューブ` 1,402 vs `ゲームキューブ` 7,578（0.19倍）、
`ニンテンドー3DS` 35,485 vs `3DS` 39,555（0.90倍）。
→ 正しくは**「一部の標準的な略称対にだけ働く。機種ごとに測る以外に知る方法がない」**。
`build_master.py` のコメントも訂正済み。

**7/18の測定の意味も変わった。** 「`バイオハザード4 ニンテンドーゲームキューブ` = 0件」は
キーワードが死んでいたのではなく、**1,402件の中にその題が無かっただけ**。
AND検索として一貫している。**置換の判断は正しかったが、根拠の説明が違っていた。**

### 効果（実測・3方向から検算して一致）

| | 変更前 | 変更後 |
|---|---|---|
| row[7] が空（全 37,855 中） | 8,641 | **1,389**（3.67%） |
| row[7] が空・物理（30,746 中） | 5,099 | **1,389** |

配信専用の空 3,542行は全部が PS3・Vita・3DS だったので、**配信側の空はゼロになり2つの数字が一致する。**
残り1,389の内訳: Switch 845 / PS5 258 / Xbox 360 144 / Xbox 82 / Xbox One 29 / Wii U 20 / 64DD 6 / Famicom 5。

★**「埋まればこの分岐はほぼ消える」（`index.html` 933行のコメント）は外れた。**
73%減ったが 3.67% が残った。**しかも残った顔ぶれの価値は上がっている**——
Xbox・Wii U・**64DD** は、英語話者が日本専売のマイナータイトルを探すという
この道具の存在理由に最も近い側。**1行あたりの重みは前より重い。**

### 検算の連鎖（この日の作業の型）
`master_final.csv` を全 39,782行 × 15列で突き合わせ、**`buyee_kw` 列以外の差分ゼロ**。
`data_line.js` / `index.html` の DATA も **index 7 以外の差分ゼロ**。
`swap_data.py` は `[0]〜[6]` で差分ゼロを報告（フラグ無しで素通り＝row[7] は照合範囲外）。
→ **ガードと自前の測定という独立した2経路が同じことを言った。**
md5 の連鎖: `build_master.py` 33e3bf52 → 44949dcd /
`index.html` cb91b493（UTM後）→ 04d9b04d（公開したもの）。

### 表示文の監査 — 嘘になった文言はゼロ
`Buyee` が画面に出るのは**ボタンのラベル1箇所だけ**。「この機種は非対応」「レトロ機のみ」に
相当する断定は無く、フッターの3数字（37,855 / 10,296 / 13,487）は row[7] を参照しないのでずれない。
★**これは偶然ではない。** `index.html` 762行のコメントが記録しているとおり、
過去に `runs only on a Japanese {機種}` を全行に出して**機種の3割で嘘になった**経験から
機種依存の断定を撤去済みだった。**その設計が今回の追加を無条件で受け止めた。**
→ **機種依存の断定を画面に置かない、は今後も守る。**

### 罠#98. 対照実験の「対照」が、測りたいものを測っていなかった
タイトル単体の件数を対照(C)に置き、「C>0 かつ +キーワードが0 → キーワードが原因」と判定表を書いた。
**`AFRO SAMURAI` で破れた。** C=56 なのに両キーワードが0。
56件は漫画・アニメDVD・北米版で、**日本版が存在しない**（型番 `BLUS30264` は北米接頭辞）。
**C は「その文字列が何かに当たるか」しか測っていない。「その品が日本の市場にあるか」は測っていない。**
→ 対照を置くときは**その対照が何を測っているかを言葉にする。**
  「C>0 かつ両方0」は**キーワードが原因とは言えない**（日本版が存在しない可能性が残る）。

### 罠#99. 除外の理由を1つの束にすると、理由の届かないものが混ざる
7/18の除外理由は「**現行機と国産PCで、英語圏の収集家がこの道具を必要としない**」だった。
理由・測定日・測定方法まで書かれた良いコメントだったが、
**10機種のうち3機種（PS5 / Switch / Xbox One）にしか当たっていなかった。**
PS3(2006) / Vita / 3DS / Xbox 360 / Xbox / Wii U / **64DD(1999・日本専売)** は生産終了機。
**64DD が「現行機だから不要」に入っているのは、理由の対義語。**
→ **除外リストは、理由ごとに分けて書く。** 1つの理由文に複数の理由を持つ要素を並べると、
  **後から読んだ人（＝自分）が全部に理由が付いていると信じる。**

### 罠#100. 同じ機種の生値が2表記あり、片方だけ登録すれば静かに残った
MADB の `platform_ja` に `プレイステーション Vita`（半角スペース有・2,353行）と
`プレイステーションVita`（スペース無・54行）が**両方実在**する。
`PLATFORM_EN` は最初から両方を持っていたが、`BUYEE_OK` は生値で照合するので
**片方だけ足せば54行が空のまま静かに残っていた。**
`build_master.py` のコメントに「片方だけ書かないこと」を明記した。
→ **生値で照合する辞書を触るときは、その生値の異なり値を先に数える。**
  Vita に気づけたのは「PLATFORM_EN の全文を原文で出せ」と要求したから。**要約させたら消えていた。**

### 指示側が同じ日に3回推測して3回外した（罠#76 / #94 / #97 の家族）
除外10機種について、現物のコメントを読む前に:
1回目「一貫した線が見えない＝**書き漏れの可能性が高い**」→ 外れ
2回目「7/18に**測って落とした**可能性」→ 外れ
現物は第3の答え＝**測っていないし、測る必要が無いと判断した**（理由も測定日も書かれていた）。
→ **推測を3回重ねるより、`build_master.py` を1回読ませるほうが速い。**
  ★**「現物を読ませる指示」を、推測を述べる前に出す。**

### 未解決#19（今日見つけた・未修正）
`index.html` 934行 `const clsBuyee = jp ? 'store' : 'store primary';` が
**`k.kwBuyee` を見ずに primary を決めている。**
→ 海外モードで row[7] が空の**1,389行は、強調ボタンが1つも無いカードになる。**
さらに `Ships worldwide` の見出しと説明
`proxy & international — English, they handle Japanese` が出たまま、
下には **eBay だけ**が並ぶ。**eBay は代理購入業者ではないので説明文が中身と合わない。**
選択肢は「row[7] が空なら eBay に primary を移す」か「説明文を分岐させる」の2つ。
★**どちらにするかは未決。設計判断が要る。**

### 次にやること（この作業の続き）
1. **未解決#19 の修正**（設計判断が先）。
2. `index.html` 927 / 933行のコメントが `5,099` のまま古い。現在 **1,389**（物理30,746 の 4.52%）。
   933行の「埋まればこの分岐はほぼ消える」は**外れたので書き換える。**
3. **Xbox 360 144 / Xbox 82 / Wii U 20 / 64DD 6（計252行）のキーワード測定。**
   ★これらは `PLATFORM_EN` に和名が無く `platform_ja` が英字のまま入るので、
   **Buyee に通る日本語キーワードが未知**（GameCube と同じく置換が要る可能性）。
   ★測り方は**キーワード単体から**（上記）。`64DD` は6行なので最後に手で見るのが早い。
4. **`-USA` / `BLUS` など外国版型番を持つ行を数える。**
   今日2件見つかった（Famicom 5行の `-USA`、PS3 の `BLUS30264`）。罠#16の残骸。
   **何行あるか未測定。**

★**未解決#16 はこの節で答えが出た**（機種で決まっていた・73%を埋めた）。
既存の未解決#16 の行への打ち消し線は**別作業**とする（追記と既存行の変更は承認経路が違う）。

---

## 未解決#19 を解決した — 海外モードの塗りゼロ（2026-07-26）

**症状**: `clsBuyee` が `k.kwBuyee` を見ずに primary を決めていたため、
海外モードで row[7] が空の**1,389行は強調ボタンが1つも無いカード**になっていた。
さらに `Ships worldwide` の見出しと説明 `proxy & international — English, they handle Japanese`
が出たまま、下には eBay だけが並ぶ＝**eBay は代理購入業者ではないので説明が中身と合っていない。**

**採った案 = B + C**（A は却下）

| | 内容 | 判断 |
|---|---|---|
| A | eBay に primary を移す | ★**却下**。eBay を英題で引くと主に北米版が返る（7/25実測）。日本専売タイトルを探している人に別物を最短距離で指すのは、**誤った値を出すのと同じ害**（罠#27） |
| **B** | 駿河屋の型番リンクへ primary を移す（型番も無ければ題名リンクへ落とす） | 採用。型番は exact match で日本版に届き、**1,389行すべてが型番を持っている** |
| **C** | `Ships worldwide` の説明文を row[7] の有無で分岐 | 採用 |

### ★この修正で変えたのは「強調の基準」であって並び順ではない
963行の `order`（届く可能性の順）は**変えていない**。変えたのは強調だけで、
**「届く可能性」から「正しい物である可能性」に寄せた。この2つは別の指標である。**
Kataban が果たしている仕事は「**正しい1本を同定する**」であって
「最も速いレジに案内する」ではない——という価値判断に基づく。**記録しておく。**

### ★文言: 「代理購入は使えない」とは書かない
row[7] が空なのは**Kataban が検索キーを持っていないだけ**で、
Buyee にその機種の品が無い証拠ではない（929行のコメントが元から指摘していた）。
**Kataban 側の限界として書く。** 実装した4種:

| LOC | row[7] | 説明文 |
|---|---|---|
| 国内 | 有 | `for buyers abroad — adds proxy fees`（不変） |
| 国内 | 空 | `for buyers abroad — eBay only for this console` |
| 海外 | 有 | `proxy & international — English, they handle Japanese`（不変） |
| 海外 | 空 | `eBay only — our proxy search does not cover this console yet` |

### 検算 — 真理値表8通りすべて primary が正確に1個

| # | LOC | 型番 | row[7] | 強調が付く先 | |
|---|---|---|---|---|---|
| 1-4 | 国内 | 有/無 | 有/空 | 駿河屋（型番 or 題名） | 不変 |
| 5,6 | 海外 | 有/無 | 有 | Buyee | 不変 |
| 7 | 海外 | 有 | 空 | 駿河屋（型番） | ★変更 |
| 8 | 海外 | 無 | 空 | 駿河屋（題名） | ★変更・保険 |

★**8番は現在の1,389行では発動しない**（型番100%）。
次のMADB更新で型番の無い行が来ても塗りゼロに戻らないための保険。
★**回帰検査: row[7] が有る4通り（1/2/5/6）はバックアップ版とバイト一致。**
今日 row[7] を埋めた7,252行の挙動を壊していないことを機械で確認した。
md5: `04d9b04d`（Buyee拡張の公開版）→ `2d15edac`（この修正）。

### 私（指示側）の心配が空振りだった — 現物は堅牢だった
「`gWorld` の枠だけが出て中身が空になる行があるのではないか」と疑ったが、**0件だった。**
- 942行の eBay リンクは**無条件出力**（`k.kwBuyee` のような空分岐が無い）
- `kwEn = enc(en||jp)` に**フォールバックがある**
- row[0] と row[1] が**両方空の行は DATA 全体で0件**
→ **枠だけになる経路は存在しない。** 想像で仕様バグを疑う前に数えたのは正しかったが、
  **疑い自体は現物の設計を読めていなかった。**

### 予想外だった数字: 1,389行の型番保持率 100.00%
全体では 27.2%（10,296/37,855）しかない。
**残った1,389行は、データベース中で最も同定精度が高い行の集まり**だった
（現行機・準現行機なので MADB のメタデータが完備している）。
→ **Bが成立したのはこの数字のおかげ。** 型番が7割欠けていたらBは選べなかった。
英題は機種で真っ二つ: Xbox系/Wii U **68〜85%** vs Switch **0.36%** / PS5 **0%**。

### 罠#85 の再発（scratchpad 側・実害なし）
検証用の `extract.py` が `<script>` の行番号（608/1538）を直書きしていたため、
今回の +22行で `</script>` の位置がずれて assert で落ちた。
タグを動的に探す版に直して通した。
★**`swap_data.py` が34行目で同じことを避けている**（行番号を直書きせず、編集で動く前提で書く）。
→ **検証スクリプトも「編集で行番号が動く」前提で書く。** 使い捨てでも同じ。

### 次にやること
1. `index.html` の**未解決#19 の行**（このノート内）への打ち消し線は**別作業**とする
   （追記と既存行の変更は承認経路が違う）。
2. **Xbox 360 144 / Xbox 82 / Wii U 20 / 64DD 6（計252行）のキーワード測定。**
   ★測り方は**キーワード単体から**。`platform_ja` が英字なので通る日本語キーワードは未知。
3. ★**新案: 「キーワードが無ければタイトル単体で検索する」。**
   64DD の6行を見て気づいた——`ランドネット` / `マリオアーティスト コミュニケーションキット` /
   `巨人のドシン1` は**機種名を足す必要がないほど固有の題名**。
   現在の設計は「キーワードが無い → リンクを出さない」だが、
   **「キーワードが無い → タイトル単体で検索する」**という第3の道がある。
   0件リンクを出さない原則は、**タイトル単体の件数を測れば守れる。**
   ★**機種別の測定より射程が広い可能性がある**（機種を1つずつ足す作業が要らなくなる）。
   **未測定。次に検討する価値がいちばん高い。**
4. **`-USA` / `BLUS` など外国版型番を持つ行の行数測定。**
   今日2件見つかった（Famicom 5行の `-USA`、PS3 の `BLUS30264`）。罠#16の残骸。未測定。
5. **git 導入。** 今日だけでバックアップが11本増えた（うち2本は7.5MB超）。
   **git があれば今日の md5 の連鎖は全部無料で残っていた。** 優先度が上がった。

★**未解決#19 はこの節で解決した。**

---

## 棚ナビ（本丸）は完了 — 静的ヒントで確定（2026-07-26）

「棚ナビ（本丸）← 7/23から待っている」は失効していた。
grep で確認したところ、棚案内は既に card() 内の shelfNote() として実装済み:
- ラベル `On the shelf · kana order` ＋ カナ枠（row[11] の1字）＋ 読み方の説明文
- GYO テーブルで行（サ行 等）に丸める
- row[11] 空（1,132行）→ 文言差し替え「wrong shelf is worse than none」
- row[12] 配信行 → .instore ブロックごと非表示（二重防御）
- クリックは無反応（静的表示のみ・これが仕様）

本人が「今あるカード内の静的ヒントで正解＝完了」と確定した。
★これは罠#84の逆向き（未着手のはずが実装済み）。
grep してから設計する方針が、shelfNote() の隣にもう1つ棚案内を作る
二重実装を実際に止めた。

「棚ナビ（本丸）」を未着手として参照している既存行への打ち消し線は別作業とする
（追記と既存行の変更は承認経路が違う）。

---

## 2026-07-26（続き）— 新案の測定が片付いた・実装待ちが2件・独立の発見が4件

### 決着1: フォールバックBuyeeの primary は型番のまま（未解決#19 を維持）
row[7] が空の1,389行に Buyee リンクを復活させる新案を測った結果:
- 和名単体 Buyee は n=10 で全て1ページ目に目的の物が残った（到達する）
- だが混機種は row[0]/row[1] どちらでも6割で起きる（衝突率 61.3% vs 51.6%、
  差9.7pt）。「和名は混機種に強い」という予想は測定で否定された。
  強いのは言語ではなく題名の固有性。
- primary 判断のため型番駿河屋 vs 和名Buyee を比較しようとしたが、
  ★codes[0]（primary が使う型番）は 1,389行中 97.1%（1,349行）で健全と判明。
  未解決#19「型番は exact match で確実」は無傷。
- → 結論: リンクは出すが primary は型番のまま。
  937 の clsBuyee を (jp || noProxy) にして、フォールバック行の Buyee は
  枠線のみ・塗りは駿河屋型番に残す。罠#27（確定した導線から未確定へ
  primary を移さない）。★実装待ち。
  （補足・訂正 2026-08-03）★実装済み。index.html:1357 の clsBuyee が既にこの条件。詳細は3730行以降「2026-07-26（実装完了）」参照。

### 決着2: 検索がスペース無しクエリを取りこぼす → タイトル2列L4・床N=5
症状: `三國志8remake`（スペース無し）で `三國志8 remake` がヒットしない。
`halo5` も同様。原因は「スペースの不一致」ではない（HAY/クエリは同じ fold で
対称・確認済み）。真因は2つ:
  (1) 照合が String.includes の連続部分一致。両側スペース保持だと
      「打った側にスペースが無い」だけで必ず落ちる。
  (2) スペース除去の救済（codeKey）が型番列(row[3])にしか繋がっていない。
対策: 第4レーン L4 を足す。squash = fold してさらにスペース除去。
  ★対象は row[0]/row[1] のタイトル2列のみ（全8列は機種列を同一視し
   nintendoswitch で845行を巻き込むため却下・測定4）。
  ★床 N=5（squash(query).length >= 5）。halo5(5)を残す上限N≤5と
   star/gear(4)を弾く下限N≥5の唯一解。lane2=8・code=4 と同じく床を持たせる。
  ★collapseV の Dragoon→Dragon +148 は起きない。L4は文字を変えず
   スペースを消すだけなので増分は境界跨ぎのみ（dragon +1）。
  ★実装待ち。
  （補足・訂正 2026-08-03）★実装済み。index.html:1009-1712 に squash/HAY4/L4照合が全て存在。詳細は3730行以降「2026-07-26（実装完了）」参照。

### 独立の発見（新案・L4 とは別問題・未着手）
1. **-JPN 除去漏れ 22行**: 1011行 replace(/-JPN$/,'') は末尾のみ削るため
   WUP-P-AMKJ-JPN-0（中間-JPN）や -JPN1 が残る。設計意図か未確認。
2. **括弧正規化 9行**: HAC-P-ADALA(JPN) 等。codes[1]切替より括弧除去のほうが
   筋がよい（同一IDの表記違い）。
3. **Ⅶ/Ⅱ 字形問題**: finalfantasy7 が FF本編に届かない（DATA側が全角Ⅶ）。
   L4 と独立・DATA全体に効く。#10 で既出のローマ数字問題と同根。
4. **Famicom 5行が全部 -USA 型番**: SNS-F4-USA / NES-XV-USA 等。SNS-はSNES接頭辞で
   Famicom行にSNES型番の二重の誤り。罠#16残骸・実数確認済み（5行と少数）。

### 罠#98. 測定サンプルの層化が、母集団にない偏りを作った
primary比較の8本サンプルで型番0件が4本出て「型番リンク全体が信頼できない」
と一般化しかけたが、母集団を数えたら健全97.1%。0件4本は層化抽出が
Famicom5行全数(-USA)とXbox空白書式を過剰に拾った結果だった。
対策: サンプルで異常率が高いとき、母集団の該当率を数えるまで一般化しない。
罠#57（n=1はベースラインでない）の、層化バイアス版。

（補足・訂正）この罠の番号を #98 → **罠#145** に付け替える（2026-08-04）
上の見出しは 3506行の「罠#98. 対照実験の『対照』が、測りたいものを測っていなかった」と番号が衝突していた
（内容の異なる別の罠が同番号・未解決#23 として登録済み）。append-only のため**見出し行はそのまま残す**。
**以後この罠は「罠#145. 測定サンプルの層化が、母集団にない偏りを作った」として参照する。**
- 先に定義された 3506行のほうが #98 を保持する（後から来たこちらを繰り下げ）。
- 新番号の根拠: 全ファイル（BIN_DIVER_NOTES.md / index.html / build_master.py / swap_data.py）の
  `罠#[0-9]+` を集計した使用済み最大値は **144**（#80/#81 のような箇条書き定義も含む）。その次の **145** を採った。
- 参照の付け替え作業は不要: `罠#98` の出現は定義2件と未解決#23の記述のみで、
  **この罠（層化バイアス）を番号で引用している箇所は他に存在しない**（全ファイル grep 済み）。

---

## 2026-07-26（実装完了）— L4検索 と Buyeeフォールバック を公開

「決着1・2」の実装待ち2件を実装・全数検証した。公開済み。

### 実装1: L4検索レーン（スペース無しクエリの救済）
- 632行直後: squash = fold してスペース除去 / HAY4 = row[0],row[1] を squash
- 1172行: sq = squash(q.value)（ループ外で1回）
- 1191行: if(!hit && sq.length>=5) hit = HAY4[i].includes(sq)
- 全数検証: 三國志8remake 0→3件・halo5 0→2件・mariokart8 1→2件。
  既存検索は件数・先頭とも不変（ドラゴンクエスト138 / SEKIRO 1 / mario 231 /
  final fantasy 180）。dragon +1 は DRAG ON DRAGOON の境界跨ぎ（事前測定どおり）。

### 実装2: Buyeeフォールバック（row[7]空の1,389行にリンク）
- 1021行: kwBuyeeFallback = r[7] ? '' : enc(r[1])（和名・スペース保持）
- 945行: clsBuyee = (jp || noProxy) ? 'store' : 'store primary'
  ＝フォールバック行の Buyee は枠線のみ、塗りは駿河屋型番に残す
- 971行: 描画条件と href を (k.kwBuyee||k.kwBuyeeFallback) に
- ★リンクと塗りを分離した。リンクは出す（探す手段）が primary は型番のまま
  （未解決#19 維持・罠#27）。
- 全数検証: 物理ソフト30,746行すべてで primary=ちょうど1個（海外・日本とも）。
  前段で1度出た「primary 2個」は945を先に入れる順序で解消・全数で再発なし。
  対照(row[7]あり SEKIRO)は Buyee primary のまま不変＝既存3万行に影響なし。

### 順序の教訓（罠#76 の再発を1件防いだ）
971(リンク描画)を先に単独で入れたとき海外モードで primary 2個になり revert
した。にもかかわらず次の指示で「971は実装済み」と書いてしまった（指示側＝私）。
実装されていないものを実装済みとして扱った＝罠#76。Claude Code が971を独断で
触らず前提の誤りを指摘して止めたため事故にならなかった。
正しい順序は 945(塗り)→971(リンク)。塗りを先に直せば971を足しても2個は起きない。
→ ★revert した実装を、後の指示で「済み」と書かない。実装状態は grep で確認する。

### 通し検証の記録
最初のバックアップ(md5 2d15edac) → 公開版(md5 ff16881a)。
差分は想定7箇所と完全一致・想定外ゼロ。DATA md5 は
2dacc3ca6e65aec4d7e31ea329fa7eb5 で全作業を通して不変・37,855行13列。

### 公開後にデバッグするときの起点
Buyeeリンクが出る1,389行のうち到達を人手判定したのは10行(0.7%)。
残り1,379行は未検証。「Buyeeが的外れ」報告が来たらこの母集団を最初に疑う。
和名単体Buyeeが全機種で機能する裏付けは n=10 のまま（意図した範囲）。

★実装待ちは全て消化。残る独立発見4件（-JPN除去漏れ22行・括弧正規化9行・
Ⅶ字形問題・Famicom -USA 5行）は別作業。

## 2026-07-26（さらに続き）— バーコードスキャン導線を詰めた・「外れ」を行き止まりから型番検索に着地させた

在庫スキャン（JAN→行）は前から実装済みだったが、「該当なし」時の体験を主要パスとして設計し直した。**実機検証はまだ。公開は検証後。**

### 発端: JAN 充足率を測ったら、主要パスが「該当なし」だった

| 母数 | JAN有 | 割合 |
|---|---|---|
| 全行 37,855 | 11,417 | 30.16% |
| 物理ソフト 30,746（row[12]≠"1"） | 11,396 | 37.06% |

DL版 7,109行のうち JAN 保有は **21行のみ** → **row[10] は「箱に物理バーコードがあるか」を正しく追えている**（列としては健全・ノイズではない）。

### ★仮説が外れた（罠#99）: 欠損を「物理」で説明しようとしたが、真因は「カタログの遅れ」

私（設計側）は「古いカセット機は箱にバーコードが無い→低い→ユーザーは試さない→痛くない／新機種は高い」と予想した。**機種別に割ったら逆だった。**

| 群 | 例 | 充足率 |
|---|---|---|
| 最新機（物理バーコードは必ずある） | PS5 258行 **0%** / Switch 845行 **4.5%** / PS4 1,377行 23.1% | 最低 |
| Xbox系・少数現行 | Xbox360 95.1% / Xbox 93.9% / WiiU 95.0% / VB 92.3% / 64DD 100% | 最高 |
| レトロ中核（収集家が実際に漁る） | PS1 48.4% / PS2 38.1% / PSP 46.1% / SFC 42.7% / PS3 50.9% / Vita 52.3% / PCE 46.9% | 40〜50% |
| 古い携帯機ほか | GB 9.8% / 3DO 6.0% / Neo Geo CD 4.1% | 最低 |

row[2] 空欄の物理行は0件（35機種で全行カバー）。

→ **物理バーコードが必ずある PS5/Switch が最低。** 欠損の原因は「箱にバーコードが無い」ではなく **MADB が GTIN をまだ記録していないというカタログの遅れ**。Xbox系94〜95% と Switch 4.5% の差は物理現実で説明不能。
→ 帰結が重い: **カジュアルな来店者がいちばん自信を持ってスキャンする現行機（Switch/PS5/PS4）で、いちばん外れる。** これは罠#12（有名タイトルの空結果は疑う／間違った値は無い値より悪い）の顔。
★**充足率の欠損を物理的理由で説明する前に、機種で割って分布を見る。「試さないから痛くない」は測定なしの慰め。** 罠#57（層化バイアス）の親戚。

### 採った方針: スキャンは主役でなく「正直なオマケ」。外れたら型番検索に着地

- 現状の showByJan は**嘘の行に飛ばしていない**（exact match のみ・外れたら正直に「無い」と出す）。ここは無傷で正しい。触らない。
- 問題は「正直だが行き止まりに近い」こと。外れ画面はオーバーレイ(z-index:50)の背後に描かれ、Close するまで見えない。かつ検索欄へ運ぶ動線が無い。
- 方針: **外れたスキャンを Kataban の主戦場に着地させる。** 45%の当たりは喜び、55%の外れは救済に。

### ★誘導先は「英語名」でなく「型番」— 店頭観察が机上を修正した

私は「英語名検索が Kataban の得意技だから外れたら英語名へ」と押した。**Koha が店頭観察で型番に修正した。**
理由: 英字ロゴはデザイン化されて検索欄に打ちにくい（`biohazard`/`BIOHAZARD`、`Romancing Sa・Ga` の中黒問題を散々踏んできた）。型番は箱裏の機械可読な一意キーで、exact/部分一致で確実に日本版へ届く。読めない人が「書き写す」なら崩れないアルファベット＋数字が速く正確。
★**店頭で撮った棚の記憶は、机上の一般化より正確。measure-first の現場版。**

### ★grep-before-design が重複実装を1件止めた（罠#84 の再確認）

「外れ画面を手前に描き直す」実装を足そうとしたが、closeScan を読ませたら **overlay を外した瞬間に既存の外れ文面が前面に出る**と判明。描画処理の追加は不要で、必要なのは「Close 時にフォーカスを運ぶ」一手だけだった。想像で設計する前に現物を読ませて、重複実装を回避。

### 実装（2段・最小差分・後戻り可能）

iOS のカメラ停止×フォーカス移動は未検証ゾーンなので、そこには触れず末尾1行から。

**1. closeScan 末尾に「外れたときだけ q.focus()」**
- `lastScanHit` フラグ導入（null=未スキャン / true=当たり / false=外れ）
- closeScan 末尾: `if(lastScanHit === false){ q.focus(); }`（当たり・未スキャンは何もしない）
- openScan 冒頭で `lastScanHit=null` 初期化（★指示漏れを Claude Code が状態遷移を追って補完。無いと「一度外す→再度開いて何も読まず Close」で前回の false が残り誤爆）
- `q.value` への転記・型番の自動入力は**しない**（型番は箱を見て手打ち・誤った値を入れない）
- md5: `ff16881a`（L4/Buyee公開版）→ `24722265`

**2. `lastScanHit` を onDetected から showByJan 内へ移動（手入力経路の穴を塞ぐ）**
- ★Claude Code が公開前に捕捉: 手入力欄(#scanJan)は onDetected を通らず showByJan 直呼びのため、旧実装では「カメラで外す→手入力で当てて閉じる」で**当たっているのにフォーカスが飛ぶ**。
- showByJan の `return true` 直前に `lastScanHit=true`、`return false` 直前に `lastScanHit=false`。onDetected の `lastScanHit=found` を削除（`found` は rAF ループ停止判断に残す）。
- showByJan の出口は true/false が物理的に1箇所ずつ・第3の出口なし → フラグを1箇所に立てれば全経路（カメラ・手入力）が正しくなる。
- md5: `24722265` → `7721f2a2`
- ★**状態は「それを最初に知る場所」で立てる。** 遠くの呼び出し側でコピーすると必ず取りこぼす。

DATA 無傷（const DATA 行 5,647,495 バイト・md5 `4a5364ba` がスキャン作業を通して不変）。構文チェック（vm.Script パース）通過。

### 論理は全経路そろった — 残るは実機だけ

カメラ／手入力どちらで読んでも、当たれば結果表示して閉じる。外れれば正直に「無い」と書き型番検索を案内、Close で検索欄 `q` にフォーカスが移り型番を打つ準備ができた状態に着地。当たり・未スキャンでは何もしない。**「間違った行に飛ばさない」原則を保ったまま、外れが行き止まりでなくなった。**

### 実機(iPhone)で確かめること（机上では詰められない）

1. iOS Safari で **ZXing 経路**がカメラを起動しバーコードを読むか（BarcodeDetector が無いので必ずこの経路）。①が動けば骨格は生存。
2. **closeScan がカメラを止めた後に `q.focus()` が効くか**（iOS はカメラ停止直後のフォーカス移動を無視することがある。今日の実装で唯一「実機でしか分からない」点）。
3. フォーカス移動でソフトキーボードが自動で出るか、一度タップが要るか。

★**公開は実機検証後。**

### 次にやること（この作業とは別）

- 実機検証①〜③（Koha の領域）。
- 独立発見4件（-JPN除去漏れ22行・括弧正規化9行・Ⅶ字形・Famicom -USA 5行）— 依然別作業。
- git 導入・機種別キーワード測定 — 未着手。

## 2026-07-27 — 全角ローマ数字を検索で十進化（FF16/DQ11 が届くように）・公開済み

### 発端: 有名タイトルが空振り（罠#12の顔）
`final fantasy 7` が FF7本編に届かない。原因は DATA が全角「Ⅶ」を持ち、
fold の記号正規化 `\p{P}\p{S}` に**当たらない**こと。全角ローマ数字は
Nl（Number, letter）で、約物でも記号でもないため fold を素通りして Ⅶ のまま残る。

### 測定（実装前）
- row[0]/row[1] に U+2160–217F を含む行 = **97行**（うち FF/DQ の看板 13行）。
- 方式判断: 1対1置換は不可。`ⅩⅥ`(FF16)=Ⅹ+Ⅵ の合成で 106 に化ける。
  → **run単位で整数解釈**（減算則つき）。`ⅩⅥ`→16 / `Ⅶ`→7 / `Ⅰ&Ⅱ`→1&2。
- 97行へ空撃ち目視: 数値誤変換 0件。`ⅠⅠⅠⅠ`→4（Black Ops 4・非標準表記だが
  加算で偶然一致）、`WWⅡ`→WW2 の2件は表示不変なので実害なし。

### 実装（fold 入口の1箇所だけ）
索引(HAY/HAY4/RJ)もクエリ(lo/loc/sq)も**すべて fold 派生**なので、
`fold` の入口に `romanToArabic()` を1回掛ければ索引・クエリ両方に対称に効く
（ユーザー入力は元々半角なのでクエリ側は実質 no-op）。型番系(codeKey)は
fold 非経由なので無傷。→ 629行の str_replace 1つで完結。

### blast radius は 97→**101**（差分4行は思わぬ利）
HAY は8列 [0,1,2,3,5,6,8,9]。row[8]/row[9] 由来で +4行。うち型番2行
(`TFC-BJⅢ`)は CODEKEYS が NFKC(`Ⅲ→III`)経由で不変のまま、HAY側だけ
`bj3` でも拾えるようになった**純増**。副作用でなく利得。

### 検証（全数・DATA無傷）
- DATA行 md5 `4a5364ba` 全工程で不変（末尾改行なしの取り方）。
- 効き目: final fantasy 7 = 9→13 / final fantasy 16 = 0→1 /
  ドラゴンクエスト3 = 3→5 / ドラゴンクエスト11 = 0→1。
- 不変ガード: final fantasy=180 / ドラゴンクエスト=138 / mario=231 / SEKIRO=1（全一致）。

### 私（指示側）のミスと Claude Code の自動修正
「効き目4件は適用前0件のはず」と書いたが、`final fantasy 7`=9・
`ドラゴンクエスト3`=3 は**適用前から当たっていた**（row[9]ローマ字や英題に
半角数字で入る行。例 `Slime Mori Mori Dragon Quest 3`）。純粋な 0→1 は2件のみ。
Claude Code が前後両方を測って自動補正。→ 罠#57（何を数えた値か）の指示側版。

### 公開: 引き算でなく足し算（スキャン変更の同居を回避）
公開時、ローカルは「未検証スキャン導線＋今回のローマ数字」の同居版だった。
同居版からスキャン変更を**引き算**するのは罠#76（revert 版を触る）を踏みやすい。
→ **公開版 `ff16881a`（scanfocus 直前）を起点に、ローマ数字の str_replace だけを
足し直した**。`lastScanHit` grep 0件でスキャン未混入を現物確認してから昇格。
公開版 md5 = `3c12e0aa`。スキャン同居版は `a8f31fc1` として保全（実機検証後に復帰）。

### 罠#106. revert 版は「引き算」でなく「綺麗な起点＋最小差分の足し算」で組む
混在ファイルから一部を取り除くより、目的の変更が入る前の既知の良版を起点に
必要な差分だけ載せ直すほうが安全。「何を引くか」でなく「何を足すか」で考える。
前提（起点にスキャン変更が無いこと）は想像でなく grep 0件で裏取りしてから進める。

### 次にやること
- 実機(iPhone)検証①〜③（Koha の領域）→ 済んだら `a8f31fc1` 系のスキャン導線を公開。
- 独立発見: -JPN除去漏れ22行・括弧正規化9行・**Ⅶ字形の DATA 側修正は別作業**
  （今回は検索のみ・表示は Ⅶ のまま）・Famicom -USA 5行。
- git 導入（今日もバックアップが増えた。優先度は上がり続けている）。
- 機種別キーワード測定。

## 2026-07-27（追記）— 「DATA行 md5」の基準を固定した

過去エントリ間で DATA 行のバイト数がずれて見えた（5,647,495 vs 7,927,042）ため、
現行 index.html（3c12e0aa）で実体を測って定義を確定した。

**基準の定義（今後これで統一する）**:
- 「DATA行 md5」= `const DATA = [...];` の**行全体・行末改行を含めない**バイト列の md5。
  （補足・訂正 2026-08-03）★実態は逆。57個の過去バックアップで両方式を照合した結果、
  旧canonical 2dacc3ca… も新canonical 8bd74741…（後継 d0cb1ccf…）も改行込みの側でのみ
  一致し、改行なしでは1件も一致しなかった。以後は「改行込み」を正とする。3886行も同様。
  （追加補足）3928行付近に「参考（別定義・使わない）: 改行込み=2dacc3ca…」とあり、
  当時は改行なし側(4a5364ba)をcanonicalと定め改行込み側を明示的に却下していた。
  その後の節でcanonicalが却下されたはずの改行込み側に切り替わっており、
  切り替えの記録は残っていない。定義が誤っていたのではなく、運用が定義から
  無記録で乗り換えたというのが正確な経緯。
  （表記統一 2026-08-03）★上記「3928行付近」は行番号参照のため不正確化しうる。
  以後は「同節の『参考（別定義・使わない）』の行」として本文一致で辿ること。
- 現行の基準値: md5 `4a5364ba…` / **7,927,042 バイト** / 609行目 / `const DATA` は全文で1件。
- `data/data_line.js` 全体の md5 とも一致する。
- 参考（別定義・使わない）: 改行込み=`2dacc3ca` (7,927,043B) / 配列リテラルのみ=`af8d9e51` (7,927,028B)。

**結論**: md5 `4a5364ba` は全工程で(a)定義と整合し、DATA は無傷。
過去エントリの `5,647,495` バイトは(a)(b)(c)いずれとも不一致で、
**バイト数だけが別時点/別定義のまま古く残った表示上の誤り**。md5 側が正しい。
既存エントリは承認経路が違うため書き換えず、本追記で基準を上書き固定する。

### 罠#101. 同じ md5 に違うバイト数が併記されていたら、必ず測って定義を確定する
「たぶん誤記」で流すのは罠#12/#57の系統。今回は測った結果 md5 は健全で
傷はバイト数表示1つだけと判明したが、測るまでは「md5計測の範囲がずれている」
可能性（基準そのものの汚染）を否定できなかった。基準値は「何バイトの・行のどの範囲」
を指すかを定義ごと固定する。

## 2026-07-27（続き）— git 導入・バックアップ整理（701M→286M）

ローマ数字対応の公開に続けて、版管理を導入し `.bak` 手管理から卒業した。

### 導入の順序（すべて実地検証してから次へ）
1. リモート現状確認（Claude が API で直接確認）: 既定ブランチ `main`・公開リポジトリ・
   ルートに CNAME(`gamekataban.com`・15B・改行なし)/index.html(3c12e0aa＝今日の公開版と一致)/
   copytest.html/**index_filtered.html(7.9M・想定外の置き土産)**。
2. `git init -b main`。**ホワイトリスト方式の .gitignore**（`/*` で全除外 → `!` で7ファイルだけ許可）。
   理由: 除外リスト列挙だと1本の書き漏れで data/metadata301.ttl(81M) が履歴に焼き付く。
   失敗の向きを安全側に倒す（許可し忘れ＝あとで1行足すだけ）。
3. 初回コミット `6e72a78`（7ファイル・危険物ゼロを ls-files で全数確認）。
4. **復元の実地確認**: 使い捨てファイルを壊す→`git checkout --` で復元、`rm`→復元、
   `git show HEAD:` で過去の中身読み出しを目視。「戻せるはず」を測定に変えてから掃除に入った。

### バックアップ掃除（68本 / 417MB 削除・段階的・全数チェック）
- **重複10本**（同一md5の二重持ち・50.3MB）= 無損失。
- **日付なし7本**（33.1MB）+ **07-25以前の言及なし46本**（295.5MB）+ **07-26中間4本**（30.5MB）。
- 削除基準: 「BIN_DIVER_NOTES.md が md5 で参照している版＝公開の系譜」だけ残す。
  該当は 07-26〜27 の連鎖7本（cb91b493/04d9b04d/2d15edac/ff16881a/24722265/7721f2a2/a8f31fc1）。
  うち `ff16881a`=公開起点・`a8f31fc1`=実機検証後に復帰予定。
- **DELETED_BAKS_LEDGER.md**（台帳・git 追跡）に削除68本を全桁md5で索引化。
  「現物は復元不可・索引だけ残す」。325MB を永久保存する価値はないが索引数KBのコストはゼロ。
- 各削除の前に「同一md5の残す1本が実在」「保護md5と不一致」「ノート言及0」「台帳記載済み」を
  全数チェック。スキップ0で通した。

### 判断: `.md`/`.py` 32本（4.6MB）と data/ 内13本は削除しない
サイズ影響が小さく、掃除の主目的(index.html 462MB)は達成済み。`.py` は書き換えを含み
「消すと失われる過去」が混じるリスクがある。**得るものが小さく下振れがある掃除はやらない。**
台帳の別セクションに索引化して現物は保持。

### 私（指示側）のミスと測定による修正
「git を入れたから既存の .bak は消せる」と最初に言ったが、Claude Code が突き合わせたら
**群A（git が保持済み）は1本だけ**だった。コミットは初回1つで、そこにあるのは今日の最新のみ。
106本はほぼ全部が過去状態で git は未保持。**版管理は「これから作る変更」に効くのであって
過去分に遡らない。** → 罠#57の親戚（「復元可能」の母数を誤った）。

### 罠#102. 「git を入れた＝過去のバックアップは消せる」は誤り
git が守るのはコミット済みの状態だけ。導入時点で既存の .bak を消すには、
(a) 各版をコミットで取り込む（重い・却下）か (b) 台帳に索引化して捨てる、の判断が要る。
遡及効果は無い。`git checkout --` は未コミットの変更を消す点も併せて周知。

### 新しい運用
編集前の `cp index.html index.html.YYYYMMDD-bak-*` は `git add && git commit` に置換。
移行期は不安なら .bak 併用可。**「編集する前にまずコミット」**が新しい習慣。

### 次にやること
- **リモート接続（フェーズ2・別セッション）**: (1)公開リポジトリに BIN_DIVER_NOTES.md /
  build_master.py が載る問題の判断（現在これらは非公開）、(2)git 認証設定。
  ★push で CNAME を消さないこと。ルートの index_filtered.html(想定外)の要否もここで決める。
- 実機(iPhone)検証 → 済めばスキャン導線（a8f31fc1）を公開。
- Ⅶ の DATA 側字形修正・機種別キーワード測定・.md/.py と data/ 内 bak の最終仕分け。

## 2026-07-27（続き2）— 駿河屋 -JPN 除去を公開・codes[0]選択問題は「直さない」と決定

### 実装・公開: 駿河屋型番リンクの -JPN 除去を拡張（55行救済）
- 症状: 型番の -JPN が末尾以外にある行で、現行 `/-JPN$/`（末尾のみ）が削り残し、
  駿河屋「By product code」リンクが -JPN 入り型番で検索して0件。
  検索(HAY/codeKey)は無傷、壊れているのは購入リンクの精度だけ（1030行のみ）。
- 実被害: codes[0]基準で55行。**Wii U が18行（-JPN行の90%）で集中**。
  ゼルダBotW・モンハン3G HD 等の主要タイトルが型番で駿河屋に届かない状態だった。
- 実地検証: 19タイトル×4パターンを Koha が駿河屋で実検索。
  結果、(2)「-JPN以降を接尾辞ごと全除去 `/-JPN.*$/`」が**全機種で最良/同点・
  既存の当たりを壊す例0**。機種別分岐は不要で単一ルールに収束した。
  （身構えた「機種でバラバラ」は測定で否定。sample を19に増やして初めて収束が見えた）
- 実装: 1030行 `/-JPN$/` → `/-JPN.*$/`。全数空撃ちで
  「10,296行中55行のみ変化・-JPN非保有8,511行と既に正常な1,730行は完全不変」を確認。
  括弧つき `RVL-SB4J-JPN(A)`→`RVL-SB4J` も同ルールで救済。
- 公開: md5 `3c12e0aa`（ローマ数字版）→ `6a2b5968`（+ -JPN修正）。GitHub公開済み（Web D&D による手動アップロード。git push ではない・remote未設定）。
  git commit `84f122c`。★git で「本物の変更」を初めて区切った回。

### 調査して「直さない」と決めた: codes[0]選択問題（12行）
発見: 駿河屋リンクは `row[3].split(';')[0]`（先頭コード固定）を使う。先頭に
レーベル/規格品番が来る12行で、タイトル固有のソフト型番を捨てている。
筆頭は idx 3616 ゼルダTPHD: codes[0]=`WUP-006(JPN)`（Wii Uディスク規格品番）、
ソフト型番 `WUP-P-AZAJ-JPN-0` が codes[1]。型番リンクが**別商品に飛ぶ**
（罠#12を超えて「間違った値は無い値より悪い」の実例）。row[7]空で型番が primary。

**codes[1]優先への切替を検討 → 実地測定で却下。**
- 12行の X(品番codes[0]) vs Y(ソフト型番codes[1]に-JPN除去) を Koha が駿河屋で実検索。
- Y は Switch(`LA-H-XXXX`)が10本中9本で0件（駿河屋はSwitchソフトを型番索引していない。
  前回の-JPN調査と整合）。切替は**当たっている9行を0件に壊す**。
- Y が明確に優る（X有害・Y正しい）のはゼルダ1行のみ。らきすたが Y 寄り。残りは X 維持が良い。
- Koha の現場判断: DC-008(90件)/KAD-002(41件)/DOL-006(251件) は別商品が混ざるが
  「サムネで判別可＝0件より良い」。「一意に定まらない＝失敗」は机上の思い込みだった。
- 有害な唯一の行ゼルダTPHD も、**日本語タイトル駿河屋リンクは10件当たる**（救済導線が別にある）。

→ **結論: codes[0]固定を維持（何もしない）。** 切替は9行を壊して実質1行を救う改悪。
その1行もタイトル導線で救済済み。DATA手編集（codes順入替）も MADB由来データ改変＋
build_master.py再生成で戻るため見送り。**measure-first は「直すな」も導く。**
勘で codes[1]優先を入れていたら Switch 9タイトルを黙って壊していた。

### 別種・未解決（codes[0]修正では救えない）
- RVL-006 2行（idx 1941/1945 ドラクエソード）: 代替ソフト型番が無く規格品番のみ。
  しかも -006(USA) は北米版品番。同一タイトルが2行に分裂。要別途調査。

### 罠#103. 「一意に定まらない検索結果＝失敗」は机上の思い込み
海外ユーザーにとって 0件（手がかりゼロ）より、多くても正しい商品を含む結果の方が良い。
サムネ判別は店頭・通販とも自然な行為。件数の多さを一律に「ノイズ＝悪」と数えない。
店頭観察に基づく Koha の判断が、設計側の潔癖な一般化を修正した。

## 2026-07-27（続き3）— ゼルダTPHD の型番リンクをピンポイント修正（公開）

codes[0]選択問題は「一律には直さない」と決めた（続き2）が、明確に有害な1行
＝ゼルダTPHD だけを名指しで直した。前回結論と矛盾しない（一律切替はSwitch7行を壊す・
名指し1行は他10,295行に影響ゼロ）。

### 症状
idx 3616 の row[3]=`WUP-006(JPN);WUP-P-AZAJ-JPN-0`。codes[0]=WUP-006 は
Wii Uディスク規格品番で、駿河屋型番リンクが**別商品2件に飛ぶ**（row[7]空で型番が
primary＝強調位置で外していた）。「間違った値は無い値より悪い」の実例。

### 実測（Koha が駿河屋で実検索）
- WUP-006(JPN) → 無関係2件 / WUP-P-AZAJ-JPN-0;WUP-006 → 0件 /
  2コード空白連結 → 0件 / **WUP-P-AZAJ → 正しいゼルダ3件**。
- 駿河屋は複数コード連結を0件で返す＝渡すのは単一ソフト型番でなければならない。

### 修正の選択（build でなく index.html 側の名指し例外）
- build_master.py に順序補正を入れる案は却下: DATA全再生成→37,855行diff→
  基準md5 4a5364ba 測り直し→公開版差し替え、と連鎖。1行のために基盤を回すのは
  見合わない（TITLE_EN型の手書き辞書の受け皿は将来同種が増えたら検討）。
- 採用: index.html の型番選択(1033行)に1行例外。row[3] 完全一致キーで
  `code==='WUP-006(JPN);WUP-P-AZAJ-JPN-0'` の1行だけ codes[1] を使う。
  判定キーは JAN でなく row[3]（理由と一致・JANは一般に非一意）。
- 全数空撃ち: 37,855行で変化は idx 3616 の1行のみ・kwCodeSg=WUP-P-AZAJ。
  -JPN修正55行・ローマ数字・検索件数すべて不変。DATA 4a5364ba 不変。
- 公開: 6a2b5968 → 0de4590e（足し算・退避は新名 bak-pre-zeldafix）。

### 罠#104. 「一律には直さない」と「特定の1件を直す」は両立する
続き2で codes[0]一律切替を却下したのは正しい（7行を壊す）。だがそれは
「有害な個別行も放置する」を意味しない。母集団への一律操作と、一意キーでの
名指し修正は別物。前者は blast radius が広く、後者は1行に限定できる。
measure-first（全数空撃ちで1行限定を確認）が両立を担保した。

### 別種・未解決（この修正では触らない）
- RVL-006 2行（ドラクエソード）: 代替ソフト型番なし・北米版品番・2行分裂。
- ソニー系5行（PKOS→PLJM等）: -JPN/-P- 判定から漏れるが駿河屋で当たる見込み。
  将来 codes選択ロジックを再検討するなら PLJM/SLPS/ULJM を含めるか要判断。

## 2026-07-27（続き4）— ゼルダ型が他に無いことを全数確認・罠#104 を裏づけ

続き3のゼルダ型番例外について「同じ地雷を踏む行が他にもあるか」を全数測定した。

### 測定: 「同接頭辞のコードを複数持つ行」51行を全走査
- 定義: codes を接頭辞（最初のハイフンまで）で見て、同接頭辞のコードが複数ある行。
  ゼルダ WUP-006 / WUP-P-AZAJ = ["WUP","WUP"] がこの形。
- 群P（ゼルダ型＝codes[0]が規格品番形・後方に同接頭辞のソフト型番）= **1行のみ（ゼルダ自身）**。
  未修正のゼルダ型 = **0行**。
- 群Q（順序が逆で問題なし）= 0。群R（版違い・ディスク番号・表記ゆれ等）= 50。

→ **続き3の1行例外は、現DATAで完結**。名指し修正の対象が本当に1件だったことが
  全数で確定。罠#104（一律には直さない／特定の1件は直す）が測定で裏づけられた。
  「1件だけ直した」が「1件だけで正しかった」に変わった。

### 群R 50行（追加処置は不要）
- KTGS×19（コーエーテクモの版違い）/ DL-DOL×5（GameCube 2枚組の Disc-0/-1）/
  -JPN と -P-…(JPN) の表記ゆれ×15（既に-JPN除去が効く）/ その他11。
- いずれも駿河屋リンクは codes[0] のままで問題ない、またはゼルダと別種。

### 宿題として記録（今日は触らない・実害未測定）
- idx 8463 Gガンダム `SHVC-ACMJ-JPN;SHVC-P-AGMJ(JPN)`: 3文字目が C/G で不一致。
  表記ゆれなら一致するはずで、MADB の誤記疑い。駿河屋は codes[0]=SHVC-ACMJ を使うため
  実害があるかは未確認。
- 全角括弧混在2件（idx 7878/8463 `（JPN)`）: 開きが全角。codeKey は NFKC で吸収・
  駿河屋リンク(codes[0])には無影響。

## 2026-07-27（続き5）— 駿河屋型番の空白をハイフン化（2,030行・今日最大の修正）

### 発端: 「NES-BS -USA の1行」を測ったら氷山の一角だった
Famicom -USA 5行を調べる過程で idx 10265 が型番内スペースで空振りと判明。
「型番内スペースを含む行」を全数測定したら**2,030行**（型番保有10,296の19.7%）。
PS1/PS2/PSP のソニー系が1,895行（93%）で、SLPS 00533 のように
本来ハイフンの箇所がスペースになっていた。検索(codeKey)はNFKCで吸収し無傷、
壊れているのは購入リンクだけ（-JPN・ゼルダと同じ構図）。

### Famicom -USA 5行は「直さない」と確定（実測）
-USA 型番は駿河屋で当たる（NES-XV-USA=4件・DMG-TR-USA=11件等、北米版レトロ市場が実在）。
-USA を削る/リンク抑制は改悪。触らない。唯一の実問題 idx 10265 は「-USA」でなく
「型番内スペース」で、それは下記の一般問題の一部だった。

### 除去 vs ハイフン化 → ハイフン化（案B）が実測で勝ち
27サンプル×3形式を実地検索。(3)ハイフン化=全機種で現行と同点以上・負けゼロ。
(2)除去=8本で激減（ときめきメモリアルGS 33→2・聖剣2 13→2）。駿河屋の商品名は
ハイフン形が主流。単一ルールで全機種OK（機種別分岐不要、-JPNと同じ収束）。

### 実装: 案B = /  *- *| +/g → '-'（二重ハイフンを作らない）
- 単純な / /g→'-'（案A）は NES-BS -USA→NES-BS--USA 等3行で二重ハイフンになり却下。
  案Bは「スペース＋周辺の既存ハイフン」を1個の-に畳むので二重化しない。
- 1034行に -JPN除去の後段として追加。処理順: ゼルダ例外(sgSrc)→-JPN除去→ハイフン化。
- 全数空撃ち: 変化2,030行ちょうど・二重ハイフン0・スペース非含有8,266行はno-op。
  SLPS 00533→SLPS-00533 / NTR P AJRP→NTR-P-AJRP / NES-BS -USA→NES-BS-USA。
  ゼルダ(WUP-P-AZAJ)・-JPN・ローマ数字・検索件数すべて不変。DATA 4a5364ba不変。
- 公開: 0de4590e → 10445ccc。

### 別扱い・宿題（今回触らない）
- 括弧行13行（HAC-P-AB92C (JPN) 等）: 現行も案Bも0件（悪化しないので一緒に出す）。
  救うには (JPN) 除去が要る＝別問題。
- Part No. 系2行（0609 Part No. X15-… 等）: 型番として妥当か疑わしい。
- idx 8463 Gガンダム型番不一致・ソニー系 codes選択・RVL-006 等は既出宿題のまま。

### 罠#105. 「1行の問題」を測ると母集団が桁違いに大きいことがある
NES-BS -USA 1行を直そうとして型番内スペースを全数測定 → 2,030行の購入導線問題を発見。
今日の他修正（101/55/1行）の合計を1修正が超えた。「1件だけ」と決めず母集団を数える
習慣（ゼルダで同型0を確認したのと同じ）が、逆に「実は大量」も掘り当てた。

### 本番反映を現場確認（2026-07-27）
案B公開後、本番 gamekataban.com で聖剣伝説2 Vita(VLJM 38041)の駿河屋型番リンクを実踏。
**13件**表示を確認＝リモートに案Bが反映済み・ローカル/git/本番が 10445ccc で一致。
（この行は「除去案なら2件・ハイフン案なら13件」に分かれた判断の分岐点そのもの。
 本番13件＝実測で案Bを選んだ判断が画面上の結果になった証拠。）
★公開したら「反映されたか」を現場で1本踏んで確かめる。空撃ちは削れた確認、
 現場確認は当たる確認・反映確認を兼ねる。

### 運用メモ: 罠番号は採る前に既存番号を grep する
今日 #101〜#105 は毎回 grep で先行使用を確認したが、「続き」エントリの #100 だけ
確認を怠り、既存の #100(機種の生値2表記)と衝突させた。後で #106 に振り直して解消。
→ 罠を採番するときは必ず `grep "罠#NNN"` で空きを確認してから振る。新番号を立てず
 この運用メモに留めるのは、番号確認の入れ子を避けるため。

## 2026-07-27（続き6）— スキャン導線の focus 補助を公開・「未公開」の実態を訂正

「実機検証後に a8f31fc1 系のスキャン導線を公開」という記述は失効していた。grep で実態を
測った結果、スキャン導線（ZXing・カメラ起動/停止・closeScan()・JAN→行照合・「該当なし」体験）は
すべて公開版 10445ccc に既に入っていた。未公開だったのは lastScanHit フラグ1つ＝
「スキャンが外れたときだけ検索欄へ focus を移す」補助機能のみ。

### 移植（復帰ではなく足し算）
a8f31fc1（スキャン同居版・退避）は今日の駿河屋修正より前の版。「復帰」させると
-JPN/ゼルダ/ハイフン化の3修正が巻き戻るため、a8f31fc1 は戻さず、現行 10445ccc に
lastScanHit だけを移植した（罠#106 の「引き算でなく足し算」と同じ形）。
- 移植は宣言1行＋コメント2行＋代入2行＋openScan初期化1語＋closeScanの1行。
- 全数検証で「a8f31fc1 との残差＝駿河屋ハンク1つだけ」を確認
  （focus完全一致・駿河屋修正保持を同時証明）。DATA 4a5364ba 不変・node --check OK。
- 公開: 10445ccc → b63b5199（git commit be95ae7）。本番で①トップ無崩れ
  ②聖剣伝説2 Vita(VLJM 38041)駿河屋リンク生存 を現場確認。

### 下振れ無しなので実機検証を待たずに公開した
closeScan は `if(lastScanHit === false){ q.focus(); }`。外れたときだけ焦点移動、
当たり/未スキャンは何もしない。iOS が focus() を無視してもカーソルが飛ばないだけで
手動タップ導線が生きる＝下振れ無し。ゆえに iPhone 実機検証を待たずに公開してよいと判断した。

### 罠#107. 「未公開の機能」と思い込む前に、公開版を grep で測る
「スキャン導線は実機検証後に公開」とノートに書いていたが、実態は導線ごと公開済みで
残りは focus 補助1点だけだった。バックアップ名（bak-pre-scan 的な連想）と記述に
引きずられ、公開版の中身を測らずに「未公開」と扱っていた。
**バックアップの存在は「未公開」を意味しない。** 何が既にライブかは、退避ファイルの
名前でなく公開版の grep で確定する。罠#12/#102 の系統（実態を測らず記述・連想で判断）。

### 実機の残点（未検証・公開はブロックしない）
iOS Safari でカメラ停止直後の q.focus() が効くかは依然未検証（周囲に iPhone ユーザーなし）。
ただし効かなくても無害な設計のため公開済み。将来 iPhone が入手できたら
scantest.html の3タイミング（即時/300ms/1000ms）で切り分け可能。

## 2026-07-27（続き7）— 駿河屋の括弧付き型番: HAC-系(Switch)11行のみ除去して公開

括弧 (JPN) 等を含む型番の駿河屋リンクを調査。「13行」の見立ては
母集団115行(除去済み20・残95)、実害(row[7]空でprimary)11行、と測定で修正された(罠#105)。

### 予想を実測が2つとも覆した
当初の見立て「実害大のSwitchは駿河屋が型番索引しないから救えない/効き目はSFC」は両方逆だった。
- **HAC-系(Switch)**: HAC-P-AAACA(JPN)→0件 / HAC-P-AAACA→**8件**。
  ADALA 3件・AR3NA(R型)5件・AB92C 2件も同様(4タイトルで収束・罠#57クリア)。**括弧を消せば当たる**。
  続き2の「駿河屋はSwitchを型番索引しない」は **LA-H- 系の結論**で、HAC- 系には当てはまらなかった。
- **SFC系(SHVC-P-)**: SHVC-P-AVRJ(JPN)→0 / SHVC-P-AVRJ(括弧除去)も**0件**。
  駿河屋の実表記は **SHVC-AVRJ(-P-落ち)で6件**。括弧除去だけでは救えない・別変換が要る
  (Koha が商品ページで実表記を確認・罠#12)。
→ **実害の大きさ(Switch=primary)と修正の効き目(SFC=索引される機種のはずだった)が逆向き。**
  実測しなければ「Switch放置・SFCいじって当たりを壊す」の逆最適に進むところだった。

### 実装: HAC-系限定の括弧除去(11行)
- 範囲は**HAC-系限定(狭い案)**を採用。他機種(N64/GBA等)は未測定のため触らない
  (未測定の母集団に一律操作をかけない)。SFCは別問題として保留。
- 既存チェーン(sgSrc→/-JPN.*$/→/ *- *| +/g→-)を sgBase に受け、最後段に
  /^HAC-/.test() ガード付きで /-?[（(][^）)]*[）)]/g 除去。ハイフン化の後段に置くことで
  idx 3603 HAC-P-AB92C-(JPN)(スペース案B由来の-付き)も - ごと落ちる。
- 全数空撃ち→実装後も全数再検証で一致: 変化11行ちょうど(idx 1834〜3603・全て row[7]空・
  全てSwitch)・HAC-以外0・括弧なしHAC-6行no-op・既存の当たり(ゼルダWUP-P-AZAJ/-JPN 55行/
  スペース2,030行/ローマ数字)全不変・DATA 4a5364ba 不変・node --check OK。
- 公開: b63b5199 → 201aa2b7(git commit 897d826)。本番で HAC-P-ADALA(New マリオU DX)の
  駿河屋型番リンクが**3件表示**を現場確認(空撃ち前は(JPN)付きで0件)。

### 別扱い・宿題(今回触らない)
- SFC系 SHVC-P-…(JPN): 括弧除去に加え -P- の処理(実表記 SHVC-AVRJ)が要る。
  実害は小(row[7]あり=Buyee導線が生きる)。効き目のあるルールが別なので切り離して後回し。
- HAC-系は全体17行(括弧付き11・なし6)のみ。**Switchの型番リンク全般が直ったわけではない**
  (大半は LA-H- で別層)。
- N64/GBA等の他機種の括弧行は未測定。触るなら機種ごとに実表記を実検索してから。
- 既出宿題(idx 8463 Gガンダム型番不一致・ソニー系codes選択・RVL-006・
  ネスト型VW314-J1(SLPM 66364)等)は据え置き。

### 罠#108. 「実害の大きさ」と「修正の効き目」は独立で、しばしば逆向き
直す価値は「その行が壊れているか(実害)」と「直せば当たるか(効き目)」の積。今回は実害大の層
(Switch primary)が効き目も大、と結果的に一致したが、それは実測で確かめて初めて分かった——
事前の予想では両者が逆に割れていた。実害だけ見て直すと効かない行を触り、効き目だけ見て直すと
壊れていない行の当たりを壊す。両方を実地で測ってから範囲を決める。

## 2026-07-27（続き8）— eBay Partner Network 登録・規約の要注意点を記録

EPN に登録・申請。受付確認メールのみ届いた段階（承認は未）。実装は承認後。
以下は合意書の実物（partnernetwork.ebay.com/page/network-agreement・2026-07-27閲覧／
公開日2026-01-22）から、Kataban に効く条項だけを抜いたもの。記憶でなく原文由来。

### 罠#109. 「現物検証＝自分のリンクで買って確かめる」が EPN では規約違反になる
Kataban の URL は毎回「現物で着地を確かめる」方針だが、eBay ではこの習慣が危険。
自分でクリック・購入を発生させる行為（non-bona fide Qualifying Events / self-referral）は
明確に禁止で、アカウント停止・報酬没収の対象。
→ **eBay リンクの検証は「正しい検索結果ページに着地するか」の目視だけ。そこから購入して
   動作確認する導線は絶対に踏まない。** 楽天・駿河屋の「買わずに着地だけ確認」ルールを、
   eBay では規約上の義務として厳格に適用する。

### 規約メモ（EPN・実装時に守る）
- **承認前はリンクを貼らない**。掲出はEPNの承認後に限る（承認前の掲出は規約違反）。
- **アフィリエイト開示を明記**する義務。例: "As an eBay Partner Network Affiliate,
  I earn from qualifying purchases." を広告と分かる形で表示。
- **リンク先は参加対象サイトのみ**。ebay.com / .co.uk / .de 等。**ebay.co.jp は一覧に無い
  ＝報酬対象外**（eBayは日本の国内マーケットから撤退）。海外ゲーマー向けは ebay.com。
- **トラッキングリンク/コードを改変しない**（改変可と明示された範囲以外）。
- **API・データ・画像・価格を取得して表示するなら事前の書面承認が必要**。楽天のように
  APIで画像/価格をカードに出す発想はEPNでは要承認。**素の検索結果リンクだけなら承認不要**。
  → まずはリンクのみで始める。
- **「eBay」表記ルール**: 大小は "eBay"（または "EBAY"）、ロゴ画像を改変しない、一文中に
  ロゴを埋め込まない、単色表示。店舗ボタンは "eBay" の文字だけで足りる。
- **報酬の前提**: 商品価格の数%（Media系≈3%）、クリックから24時間以内の購入が対象の
  ラストクリック方式、最低支払残高$10・月次払い、準拠法は米カリフォルニア州法・英語版が正文。
- **プログラム/レートは3日前通知で変更・終了され得る**。過度に作り込まず「あれば得する
  追加リンク」の位置づけに留める。
- **eBayの取引データをAIの学習・チューニングに使わない**（対象は *eBayから受け取るデータ*。
  普段のClaude併用開発を禁じるものではない）。

## 2026-07-27（続き9）— eBayボタンの確定仕様を先決め（実装はEPN承認後）

EPN承認待ちの間に、eBayボタンの「位置・文言・出し分け」を確定。実装はまだしない。
置き場所そのものは既に正しい（eBayは元から SHIPS WORLDWIDE の素リンク）。今回決めたのは
Buyeeとの並び順・ラベル・但し書き・開示・カード種別の出し分け。前提は罠#109。

| 項目 | 確定 | 根拠 |
|---|---|---|
| グループ | SHIPS WORLDWIDE（既存のまま） | eBayは代行不要で本人に直送・英語決済 |
| 並び順 | **Buyee → eBay** | `order`＝届く可能性の順。Buyeeは日本版そのものに届き、eBayは楽だが版が曖昧（英題引きで北米版が返る・7/25実測）。正しさ優先（罠#27/#12） |
| ラベル | **`eBay`**（混在大小） | 他店ボタンと整合。EPN規約: "eBay" 表記・単色・ロゴ改変不可・descriptive |
| 色 | 非スカーレット＝一般店ボタン扱い | `--signal`（スカーレット）はBuyeeとマッチ強調の専用。eBayをBuyeeより目立たせない＝序列を色でも示す |
| 検索キー | `kwEn`（英題） | eBayは英題検索が命 |

### 但し書き（「採った案C」＝ row[7] 有無の分岐に相乗り）
- Buyeeもある行 → `eBay: ships worldwide, but often non-Japanese versions`
- eBayだけの行（row[7]空・3,630行）→ `eBay only — listings may not be the Japanese release`
- 読めない人向けなので、動詞・比喩を避けた素の短文。

### 開示（EPN・FTC）
- **フッターに1行**、eBay・駿河屋・楽天をまとめて賄う（ボタン毎には出さない）:
  `Kataban earns a commission from some store links (incl. eBay Partner Network) at no extra cost to you.`
- `.cap`/`--muted`。「次にやること」#2（開示行のコントラストを測る・2026-07-25 夕）に乗せる。測り方は未解決#10（`.cap`がスラブ内で暗すぎる件・2026-07-24 解決）と同じ手順。新規部品は不要。

### カード種別ごとの出し分け
- 通常／型番なし → eBay表示。
- **英題なし → eBayを出さない**。弱いクエリを投げるより無い方がよい（罠#12）。
- **ダウンロード専用 → 購入リンク全抑制**（「Show to Staff を出さない」と同じ精神。物理中古なし）。

### 前提（罠#109・厳守）
承認前はリンクを貼らない。検証は「正しい検索結果ページに着地するか」の目視のみ。
自分のリンクからは購入しない（self-referral禁止）。

★実装はEPN承認メール到着後。承認後はこの仕様どおり Claude Code へハンドオフする。

## 2026-07-28（続き10）— eBayリンクの確定テンプレートを実測確定（n=4）

EPNダッシュボードに到達（Active）。Generate Link で実リンクを生成し、4タイトルで型を確認。
4本すべて固定部分が完全一致＝英題だけ差し替えれば自前で量産できる（1本ずつ生成不要）。
検証タイトル: Call of Duty Ghosts / Metroid Prime / Gradius Collection / Dragon Quest VIII。
（罠#57＝n=1は基準にしない、を満たすため4本で確認）

### 確定テンプレート

```
https://www.ebay.com/sch/i.html?_nkw={英題を+区切り}&mkcid=1&mkrid=711-53200-19255-0&siteid=0&campid=5339175838&customid=&toolid=10001&mkevt=1
```

| 部分 | 値 | 扱い |
|---|---|---|
| `_nkw=` | 英題（row[0]）の空白を `+` に置換 | **唯一の可変部**。例: `Metroid Prime`→`Metroid+Prime` |
| `campid=` | `5339175838` | **自分のトラッキングID。全リンク共通。絶対に消さない/変えない** |
| `mkcid` `mkrid` `siteid` `toolid` `mkevt` | `1` / `711-53200-19255-0` / `0` / `10001` / `1` | eBay固定値。そのまま固定 |
| `customid=` | 空 | 将来ページ別計測に使える（今は空） |

### 生成UIの設定（確認済み）
- Link Type = **Simple**（HTML/JS/QRは不要）
- **Shorten Link はオフ**。短縮すると `ebay.us/...` になりトラッキングが隠れ、量産テンプレに使えない

### 罠#110. eBayリンクは _nkw だけ差し替える。固定パラメータを触ると規約違反
- **やってよい**: テンプレの `_nkw=` に英題を差し込んで生成（＝campid付きの正規ディープリンク。検索語だけ変えるので「リンク改変」ではない）
- **やってはいけない**: `campid` を書換/削除、固定パラメータを削る、リンクをcloak（隠蔽）。いずれもEPN規約違反
- **自分のリンクで購入テストしない**（罠#109再掲）。着地確認は素URL（トラッキング無し）で行う
- eBayは英題検索で**主に北米版が返る**（7/25実測、今回も再確認）。これは罠#27の通り。カードの但し書き
  `ships worldwide, but often non-Japanese versions` はこの実データの裏づけ

### 実装時TODO（未解決）
- 英題に `&` `#` `?` 等の記号や非ASCII（例: Pokémon）が含まれる場合、`+`置換だけでは不十分で
  URLエンコードが要る。既存の「非ASCII英題検索（NFKD）」課題と地続き。実装時に確認
- customid に何を入れるか（ページ別計測をするか）は未決。当面は空でよい

★これはリンク「型」の確定。index.htmlへの組み込み・公開はまだ。EPN承認は取得済みだが、
  公開実装は確定仕様（続き9）に沿って別途ハンドオフする。

## 2026-07-28（続き11）— eBayリンクをindex.htmlに実装・公開実装完了（未アップロード）

続き10の型をindex.htmlに実装しコミット（40d0ff3）。L984の1行のみ変更・DATA不変
（md5二基準一致・vm.Scriptパース通過）。Chrome headlessで実href取得＝実測で検証済み。

### 実装した内容
- L984 eBayリンクの href 末尾に固定トラッキングを付与:
  `&mkcid=1&mkrid=711-53200-19255-0&siteid=0&campid=5339175838&customid=&toolid=10001&mkevt=1`
  （HTML文字列内なので `&` は `&amp;` 表記。他店 Mercari/Hard-Off と同じ書式）
- rel を `noopener` → `sponsored nofollow noopener` に変更（Suruga-ya と同一書式）
- 順序・塗りは確定仕様どおり: Buyee(0)→eBay(1)、eBayは class="store"（primaryなし）

### 実測（The Tower of Druaga・DATA実行行）
生成href: `https://www.ebay.com/sch/i.html?_nkw=The%20Tower%20of%20Druaga&…&campid=5339175838&…&mkevt=1`
- パラメータ8個が続き10テンプレと全一致（campid=5339175838 含む）
- 末尾 mkevt=1 まで欠けなし・二重エンコード痕なし・URL()パース成功

### 続き10からの訂正（過去エントリは書き換えず、ここで併記）
- **`_nkw` の生成方式は `+`区切りではなく `encodeURIComponent`（`%20`）を採用**。
  続き10の `{英題を+区切り}` は「EPNのUIが吐いた型」の記録として正しい（Generate Link は
  実際 `+` を返す）が、**実装は現行の `enc()`＝encodeURIComponent を維持**した。
  理由: eBayは `+` でも `%20` でも同結果を返す一方、encodeURIComponent は `&`→`%26`、
  非ASCII（é→`%C3%A9`）に自動で倒れて安全。動いている安全な仕組みを表記合わせのために
  壊さない、という判断。
- **続き10「実装時TODO」1つ目（`&` `#` `?`・非ASCII Pokémon の URLエンコード懸念）は解消。**
  encodeURIComponent 採用により最初から発生しない。以後この項目は未対応扱いにしない。

### まだやっていない（範囲外・未着手）
- **公開アップロード未実施**。GitHub上のindex.htmlは旧版＝実トラッキングはまだ発生しない。
  公開すれば承認済みリンクの掲出（罠#109に非抵触）。アップロード時 CNAME を消さない。
- **英題なし行のeBay抑制（続き9仕様）は未実装**。現行は kwEn が日本語タイトルにフォールバック
  して eBay が出る。別ハンドオフで対応予定。
- 着地の実測は素URL側で既済（CoD Ghosts→北米版）。campid付きの自リンクは自分で叩かない（罠#109）。

（補足・訂正）公開版の impact 認証トークンは既に `41e22ad5…`（7/28に公開ソースを実測確認・
認証通過済み）。「公開版は旧トークン1efd6455のまま」は誤り。追加対応は不要。

## 2026-07-28（続き12）— eBay公開完了／「英題なし→非表示」仕様を実測で撤回

続き11の実装（40d0ff3）をGitHub Pagesへ公開。公開版のeBayリンクにcampid付与を実機確認
（ホバーでhref目視・クリックせず＝罠#109）。Samurai Shodown II / Daikatana でcampid=5339175838
を確認。あわせて row[0] 空の行（戦国無双 KATANA）で日本語が _nkw に入る挙動を現認し、
続き9の「英題なし→eBay非表示」仕様を実測で見直した。

### 公開完了
- 公開アップロード済み（Web D&D、index.htmlのみ差し替え、CNAMEはリポジトリ非追跡＝無傷）
- 実機で campid=5339175838 ほか8パラメータ付与を確認。承認済みリンクの掲出＝罠#109非抵触

### 罠#111. 「日本語ではeBayは当たらない」は思い込みだった（実測で仕様撤回）
続き9で「英題なし→eBay非表示」と決めた根拠は「日本語クエリではeBayでまともに当たらない
だろう」という**未実測の推測**。実測したら覆った。
- row[0]空は 25,143行=66.4%（DATA実測）。全eBayボタンの2/3が日本語クエリ。
- 素URL（campid無し）でブラウザ着地確認（罠#109）。実測3件、いずれも関連ゲームに着地:
  - 戦国無双 KATANA … 14件・ベストマッチあり（日本語/ローマ字とも同一結果＝eBay内部で正規化）
  - ときめきメモリアル … 約3,400件・関連ゲーム＋グッズ
  - がんばれゴエモン … 約844件・ほぼゲーム
- 「KATANA」の英字が効いた例外ではないかを疑い、英字を含まない純日本語2件で再確認済み
  （罠#57＝n=1を基準にしない）。
→ **結論: 「英題なし→eBay非表示」を撤回。現行の日本語フォールバック（en||jp）をそのまま残す。**
  実装変更なし。66%のボタンを消さずに済む。
→ 教訓: 続き9の仕様は「当たらないだろう」という desk 推測に基づいていた。実測で覆るなら
  仕様のほうを直す（罠#12＝想定と違う絶対値は実測を信じる）。仕様は聖域ではない。

### 続き9仕様の該当部分を撤回（過去エントリは書き換えず、ここで無効化を明記）
- 続き9「カード種別ごとの出し分け → 英題なし → eBayを出さない」は**撤回**。
  上記実測により、英題なし行でも日本語フォールバックでeBayを表示する。
- 「ダウンロード専用→購入リンク全抑制」は撤回しない（物理中古が無いという別根拠のため有効）。

### 版の曖昧さについて（未対応でよい）
日本語フォールバックの着地商品に日本版が含まれるかは未検証だが、これはeBay全般の性質
（英題でも北米版が返る＝罠#27で既知）で、日本語固有の欠陥ではない。カードの但し書き
`ships worldwide, but often non-Japanese versions` が両方をカバー。追加対応不要。

### 残タスク
- 但し書き・開示行（フッター1行）・その他 続き9の未実装分（並び順とrelは実装済み）
- W-8BEN 提出（報酬受取の前提）

---

## 2026-07-28（続き13）— バーコードスキャン実機検証：Android実店舗で動作／iPhone未検証・後回し

スキャナ本体（index.html:1395-1508）の机上調査は完了（続き12までの実装は無傷）。
今回は初めて実店舗で実機にかけた。

### Android（ネイティブ経路・BarcodeDetector）
- **実店舗でスキャン→JAN完全一致→検索結果着地を確認。動いた。**
- ただし本数・題の内訳は未記録。★これは「Android は動く」の初回確認どまり
  （罠#57＝n=1 を基準にしない）。複数題・機種横断での再確認は宿題として残す。
  特に「正しい行に着地したか」「空振り題で空振り文言が出たか」は次回まとめて測る。

### iPhone / iOS Safari（ZXing経路）
- **検証用端末が無いため未検証。後回しと決定。**（放置ではなく、端末が無いという明確な理由。罠#52の形）
- 実害が出ない根拠: カメラ不可・ZXingロード失敗時は手入力欄 #scanJan + Look up に落ちる
  二重フォールバックがある（589-592, 1507-1508）。iPhoneユーザーは
  「スキャンが使えない代わりに手打ちでJANを引ける」状態にとどまり、画面は壊れない。
- iPhoneを入手できたら scantest.html（ZXing経路を強制的に通す実機検証ページ）で検証する。

### 到達点
Android のスキャン＋全通販リンク（Buyee/eBay/駿河屋/楽天画像）がそろい、
日本語を読めないユーザーが「通販」でも「店頭ブックオフ」でも購入まで辿り着ける状態になった。
当初の北極星に一旦到達。

---

## 2026-07-28（続き14）— eBay公開後の状態確定／フッター開示にEPN明示／W-8BENは保留

続き11-12の実装を公開済み。フッター開示にEPN明示を追加（40d0ff3の次のコミット）。
W-8BENは入力・署名まで完了したが、提出の最終確定が2回とも通らず保留（TIN検証待ち）。

### 公開・稼働の確定
- eBayリンク（campid=5339175838付き）を公開。実機ホバーでhref確認済み（クリックせず＝罠#109）。
- eBay Partner Network アカウントは Active。リンク・成果計測は稼働中。
- フッター開示に `including as an eBay Partner Network affiliate` を追記（EPN/FTC要件）。
  既存の「product images」「Supported by Rakuten Developers」は保持。カード内L1013の
  `Some store links are affiliate links (PR).` はそのまま（予告として維持）。

### コード変更まとめ（今日）
index.html は3行のみ変更・DATA全工程不変（md5 4a5364ba / 2dacc3ca・7,927,042バイト）:
- L5  impact認証meta（トークン 41e22ad5、公開版と一致・認証通過済み）
- L579 フッター開示にEPN明示
- L984 eBayリンクにEPNトラッキング付与・rel=sponsored nofollow noopener

### W-8BEN 提出は保留（TIN検証待ち・24時間ルール）
- EPN/impact(Comply Exchange)でW-8BENを入力・電子署名まで実施。完了画面(緑チェック)と
  PDF控えのDLまで到達したが、「出口」後にヘルプページへ飛ばされ、ダッシュボード右上の
  W-8提出警告が消えない＝**提出が最終確定していない**。同じ経路で2回発生。
- ヘルプ文の推定原因: ①セッションタイムアウト ②TIN Check Failure ③入力エラー。
  署名まで到達している＝前段の欠落ではなく、②TIN不一致が最有力。
- ヘルプ明記: **TIN不一致でセッション終了した場合、再開まで24時間待機が必要**。
  → 今日は打ち切り。明日以降に再提出。
- 明日の再開手順（確定済みの入力値。※マイナンバーはノートに書かない）:
  - 米国民=いいえ／米国TINタイプ=「米国のTINは適用されません」／外国TIN国=日本＋マイナンバー
  - 住所=5-9-42 / Clair Gotenyama 315 / Kitashinagawa, Shinagawa-ku / Tokyo / 141-0001
  - 市民権6問=生年月日以外すべて「いいえ」／条約請求=はい・Japan／特別料金と条件=いいえ
  - Certification: **「遡及的な声明を添付したい」はチェックを外す**（初回支払いはスキップ、と
    画面赤字で明示。前回チェックしていたのが遠因の可能性）。同意チェックのみ入れて提出
  - 認証コード(署名PIN)は前回入力したものを再入力（実値はノートに書かない。
    分からなくなったら画面の再設定/回復手順から取り直す）
- **明日やること**: マイナンバーが12桁ちょうどか・打ち間違いが無いかを再確認してから再提出。
  正しい番号で再度TIN Failureが続く場合のみ Contact EPN Support。
- 注記: W-8BENは報酬受取前に有効なら足りる。リンクは稼働済みなので急ぎではない。

### 残タスク
- W-8BEN 再提出（TIN確認のうえ・24時間後）
- 続き9の他の未実装分（大半は実装済み。但し書きは実装済み・開示行も対応済み）

---

## 2026-07-28（続き15）— W-8BEN 受理確認（続き14の「保留」を解消）

続き14で「W-8BEN保留・明日再提出」と記録したが、その後ダッシュボードで受理を確認。保留は解消。

- EPNダッシュボード右上の「W-8電子納税書類を提出してください」警告が**消えた**＝税務ステータスが
  提出済み・有効に切替。W-8BENは**受理完了**。
- 2回とも「完了画面(緑)→出口→ヘルプページ」で警告が残り失敗に見えたが、実際はTIN検証が
  **非同期(時間差)**で、検証完了までのラグの間だけ警告が残っていた。ヘルプ画面への遷移は
  セッション終了時の定型表示で、提出失敗を意味しなかった。
- 有効になったのは2回目（**Certificationで「遡及的な声明を添付したい」のチェックを外した版**）。
  初回支払いは遡及声明をスキップ、が正しい入力だった（続き14の手順が正解）。
- 教訓: 「出口後にヘルプへ飛ぶ」「警告が残る」だけで失敗と断定しない。非同期処理は時間差で
  確定することがある。ダッシュボードの実表示で最終判断する（罠#12＝推測を事実にしない）。

→ 続き14の残タスク「W-8BEN 再提出（24時間後）」は**不要**。W-8BEN完了。

### これでEPN一式が完了
申請→承認(Active)→リンク確定テンプレ(n=4)→実装・公開・稼働→フッター開示にEPN明示→
W-8BEN受理、まで一通り完了。index.htmlの累計変更は3行のみ・DATA全工程不変。
残るはeBay固有ではない一般タスク（続き9の細部・棚ナビ等）のみ。

## 2026-07-28 — Buyeeアフィリエイト準備（Indoleads登録まで／オファー申請は温存）

### やったこと
| 項目 | 状態 |
|---|---|
| About/How-to セクションをサイトに追加 | 完了（27行追加・0削除・commit 967e2b4） |
| Buyeeアフィリエイトの経路確認 | 完了（A8.netらしい→**確認済み・Indoleads採用**） |
| Indoleads publisher アカウント作成 | 完了（即時利用可・ランク Rookie） |
| source（自サイト）登録 | 完了（Id 407997・`Websites`・`https://gamekataban.com`） |
| Buyeeオファーへの申請 | **未実施＝意図して温存** |

### About/How-to セクション（審査対策とUXが同一物だった）
審査が嫌う「リンクだけの薄いサイト」対策＝「日本語が読めない海外ユーザーへの説明」で、**要る中身が同じだった**。収益化準備の第一歩が、そのままユーザー体験の改善になった。
- 位置：`#more` と `<footer>` の間（`div.wrap` 内）
- CSS：`border-top` は `var(--line)`（`--hair` は `.card` スコープで参照不可だった）。`font-family:inherit` で本文書体を継承＝ピクセル体にしない
- 表示文修正：下書きの `"Show this to staff"` は実UIと不一致。実ボタンは `Show to shop staff`（index.html:1147）。実物に合わせた
- バックアップ：`index.html.20260728-bak-pre-howto`（8,034,671 bytes・.gitignore で未追跡）

### Buyeeアフィリエイトの経路 — 確認済み（旧「未確認 A8.net」を更新予定：824行は別指示）
- A8.net に今も掲載（第三者集計・2022・アグリゲータ由来）
- ★**新情報：Indoleads が2026-05にBuyee公式アフィリエイトを開始**（英語・全世界・CPS）
- **選択：Indoleads 1本。** 理由＝読者が英語圏／二重申請で一発勝負を無駄打ちしない／worldwide かつ deeplink 可

### Indoleads Buyeeオファー条件（Indoleads公式オファーページ・web_fetch実測）
| 項目 | 値 |
|---|---|
| モデル | CPS・全世界 |
| Cookie | **30日** |
| Hold（成果確定までの保留） | 45日 |
| Deeplink | **可** ← カード別のBuyee検索URLに直接貼れる＝現行方式が活きる |
| Banner / Landing page | 可 |
| XML/CSV feed | **無し**（駿河屋と同じ＝商品ID直リンクの道は無い） |

### Indoleads の関門の作り（本人が画面で実測）
- publisherアカウントは**即時利用可**。審査待ちではなかった（ランク Rookie）
- 関門は**オファー単位**（`Connect to the offer`）にある。アカウント承認ではない
- オファー申請前に **source 登録が必須**。source は **URL＋種別(Website)だけで通り、トラフィック量/PVの必須欄は無かった**
- ★**source を編集・削除すると、オファー申請とトラッキングリンクが作り直しになる**（source Id 407997 は触らない）
- Manager：Support WW（support@indoleads.com）／登録に Messenger 連絡先必須＝Telegram を選択

### 決めた順序（温存）
- 今回：publisherアカウント＋source登録まで
- **温存：`Connect to the offer` は presence が少し出てから。** アカウント/source作成とオファー申請が別段階なので、一発勝負のオファー審査を切らずに済む
- ★**承認前に Buyee リンクへアフィリエイトIDを入れない**（規約違反になり得る）。現行の素リンクは触らない

### 罠#112. 一般集計サイトの数値を、事実として述べた
Claude（指示側）が「Cookie はクリックから24時間」と述べたが、出所は一般集計サイト（linkmydeals）。**Indoleads公式オファーページは30日**だった。罠#48/#12と同型（未確認の値を事実化・出所を見る前に決める）。対策：**数値は一次情報（公式オファーページ）で裏を取ってから書く。**

### 罠#113. 下書きの表示文が実UIと違った
指示側の下書き `"Show this to staff"` が実ボタン `Show to shop staff`（index.html:1147）と不一致。Claude Code が実物に合わせて修正。「表示文とUIをずらさない」の実例。対策：**UIに言及する文面は、実ソースのボタン文字列を grep してから確定する。**

### 未確認（意図して残す）
- Indoleads の Buyeeオファー審査が、ゼロ〜低トラフィックの新規サイトで通るか。**温存中のため未測定。** presence 後の一発勝負
- 最低支払・料率は第三者レビュー由来で不一致あり（$50 PayPal/$100他 という記述と $100 という記述）。**ダッシュボードのBillingで実額未確認**
- eBay 行（3136/3350 の「素リンク＝未回収」）は commit b4f0989/a5cf0f5 と食い違い＝既に古い。**Buyeeとは別作業として、repoのEPN現状を確認してから訂正する**

### 次にやること（更新）
- **presence を作る具体策**（サイト側・宣伝側。Reddit/Discord等）← 次回の起点

## 2026-07-28（続き16）— データ更新の再現性を確定：MADB停止・en横ばいで再取得見送り・ja/pubの穴を記録

「登録データをどう更新するか」を調べ、更新の効く材料と再現性を測って確定した。結論は**当面ほぼ更新不要**。

### MADB：1.2.18 で停止。触らない
Releasesの最新が 1.2.18（前回取り込み版と同一）。年次廃止・順次更新方式なので新リリースが出るまで手当て不要。

### 更新の効く材料は Wikidata en（英題）だけ。手順は再現可能と確定
- クエリ本文（`build_master.py` 21-28行）＋生ダウンロード `data/wikidata_en_new.json`（list・30,800行）＋整形 `_experiments/rebuild_wikidata_en.py` が揃っている。
- 罠#42 の「16,393キー」を現物から段階で再現・完全一致で裏取り：
  30,800（全行）→ 16,602（`^M\d+$` 厳密一致）→ 16,483（ユニークM）→ −90（同一Mに別英題の衝突除外）＝**16,393**。
  よって `wikidata_en_new.json` は**修正後（複数値を取りこぼさない）クエリの出力で確定**。C番号14,197件はノート1508行の14,194件とほぼ一致。
- ★変数名の注意：docstringの原文は `?madb ?enTitle` だが、生ファイルのキーは `madbid / enLabel`。再取得は `?madbid ?enLabel` 版で叩かれ、だから rebuild が改名している。**再取得時は `?madbid ?enLabel` で叩けば rebuild を無改変で使える。**

### 「再取得する価値があるか」を測って、見送りと決めた
WDQS で現行クエリ（`wdt:P7886` + `rdfs:label@en`）を実行：
- **30,538行 / 23,042ms（2026-07-28）。前回 30,800行に対し −262（−0.85%）。**
- 増えていない（微減は項目統合・貼り替え・削除の通常の揺れ）。7-21以降、英題の鉱脈は実質動いていない。
- → **再取得は見送り。** 再ビルド・行差分承認・再公開の実作業に見合う効きが無い。measure-first は「直すな」も導く（7-27 codes[0] と同型）。

### 更新モデル：カレンダーではなく証拠で回す
定期更新はしない。トリガーは**「有名作が英語で出ない」取りこぼし報告が数件溜まったとき**。そのとき今日と同じく WDQS で総行数を測り、前回より明確に多い場合だけ再取得→rebuild→build_master→swap_data に進む。

### ja：復元可能だが docstring と現物が食い違う（未解決）
- クエリは「en の言語フィルタを ja に変えた版」で妥当（実データ側：C込み・キー空間がen_newと同型・値が素の`rdfs:label@ja`）。
- ★docstringの「カタカナ正規タイトル / M直引き」は**誤り**。実物は**Mフィルタ無し**（M 14,887 / C 14,082）で純カタカナは23.8%のみ、漢字・かな・ラテン混在＋アニメ/マンガ混入（MADBはゲーム専用DBではない）。
- 実測：ja の**C側14,082件は `master_final.csv`（全てM番号）に1件も当たらない死票**。当たるのはM側12,578件だけ。
- ja の整形（dict化＋衝突除去）スクリプトは repo に無い。復元時は「原状再現かMのみ改良版か」の分岐が要る。

### pub：クエリ喪失・据え置き
- 中身は**社名対訳表 615件**（`jaLabel→enLabel`）。madb_id と無関係、社名は滅多に変わらない。曖昧3件（Atari/Sony Music/Milestoneが英名2つ）、jaLabelが英字のみ94件（SNK等）。
- P123ベースと推測だが615件という小ささを説明できず復元しきれない。**MADB停止中は更新不要なので、いま逆算しない。** 整形スクリプトも repo に無い。

### 罠#114. docstring が「計画」を「事実」として記録していた（ja）
`build_master.py` の docstring（11行・512行）は wikidata_ja を「madb_id→カタカナ正規タイトル・M直引き」と書くが、現物はMフィルタ無し・カタカナ正規化無しの素の`rdfs:label@ja`だった。罠#19（ノートは計画を事実として記録する）の系統。記憶や散文ではなく**出力JSONの実構造を測って**初めて食い違いが見えた。対策：更新前に docstring を鵜呑みにせず、まず現物ファイルの型・件数・接頭辞内訳を出す。

### 未解決（意図して残す）
- ja の docstring 食い違い（罠#114）と、ja/pub の整形スクリプト未復元。次に ja を固めるならここから。
- pub クエリの完全復元（母集団の絞り・P123/P178・615件の理由）。MADB停止中は優先度低。

---

## Klook（Indoleads アフィリエイト）の器を実装した — リンクは pending・デプロイ禁止（2026-07-29）

**現状**: Indoleads 登録・billing profile（PayPal・Approved）完了。Klook 案件は apply 済みだが status=pending。本番トラッキングリンクが無いため**器だけ**を実装した。

**設計判断**
- Klook は旅行アクティビティのアフィリエイト。**ゲームのカードには載せない**（データ欄に飾りを付けない原則）。`#locbar` にも入れない（sticky 帯は店頭可読性のため最小に保つ）。
- 置き場所＝`.wrap` 内最下部、howto セクションと `<footer>` の間（index.html:612-618・md5 2bb7f370e2f32fd690ae108bfb055abd）。コンテンツ列幅 760px に揃う。
- 出し分け＝`setLoc()` が `document.body.classList.toggle('in-jp', LOC==='jp')` を実行 → CSS `body.in-jp #klookbar{display:block}`。**日本にいる人にだけ出る。** JS 変更は1行。この `body.in-jp` は今後の JP 限定要素にも再利用できるパターン。
- 色＝`--signal`（緋色）不使用。見出し/ボタンは `color:inherit`、副文 `--muted`、枠 `--edge`/`--edge-input`。生hex を足していない。
- 開示＝`.cap`（PR 表記）＋ `rel="sponsored nofollow noopener"`。

**★デプロイ禁止の理由（重要）**: `href="KLOOK_PENDING_DO_NOT_DEPLOY"` はダミーで 404。しかも `body.in-jp` ゲートのため、壊れたリンクが出るのは「日本にいる人」＝狙っている観光客層。無関係な別機能のアップロードに、この working-tree の変更を巻き込んで公開しないこと。承認前は未コミットのまま保持。

**承認後の手順（未実施）**: ①Indoleads で Klook のトラッキングリンク生成（deeplink 欄があれば Klook の日本/英語ページを指定）→ ②実物 URL を live 検証（Klook に着地するか・トラッキング param が残るか・別タブで開くか）→ ③`href` を差し替え → ④デプロイ。live 検証を通すまで公開しない（アフィリエイトURLは実物検証してから、の原則）。

### 未解決に追加
- **未解決#20.** Klook 案件が pending。承認まで器は非公開。承認後に上記4手順。

---

## 2026-07-29 — Buyeeオファーに申請した（7/28「温存」を更新）／status=Pending

### `Connect to the offer` 実施。却下されず Pending
7/28 の「温存（presence が出てから）」判断を更新して申請した。理由：Buyee にとって gamekataban.com は狙いの客層（海外→日本の商品）そのもので、ニッチ広告主の審査はトラフィック量よりトピック一致が効くと判断。JP在住 publisher でも申請は通り Pending に入った。source Id 407997 は触っていない。

### 「Prohibited Country - JP」の意味を一次情報で確定
オファーページ原文 "advertisers ships from Japan to the rest of the world except Japan" により、JP制限は**買い手（トラフィック）の所在地**であり publisher 資格ではないと確定。Geo欄は ALL。→ **Kataban 設計と一致**：Buyee を塗り(primary)にするのは OUTSIDE モードだけ。報酬対象（海外）と主役化する場面が最初から一致。IN JAPAN の買い手は Buyee 対象外で、そこでは主役にしていない。（従来「意図して未確認」としていた点が解消）

### オファー条件（オファーページ実測・2026-07-29）
| 項目 | 値 |
|---|---|
| モデル / Geo | CPS / Geo=ALL（ただし JP は買い手側で prohibited） |
| Payout | 2.10%–4.20%（**カテゴリ別**。Mercari "Games, toys, and goods" = 3.00%） |
| Cookie / Hold | 30日 / 45日 |
| Deeplink | Yes（カード別Buyee検索URLに貼れる＝現行方式が活きる） |
| Banners / Landing / feed | Yes / Yes / XML・CSV=No |
| Traffic制限 | Websites=allow, SEO=allow（Kataban は規約内）。PPC/リタゲ/Doorways=forbidden（未使用） |
| Tracking link | 承認後に表示（"appear once our team confirms…"） |
| 備考 | JD Auctions は tracking 対象外 |

### 承認後にやること（未実施・素リンク維持・デプロイ禁止の原則は継続）
1. 実リンクの対象ソース（buyee.jp/item/search＝主にヤフオク）の**実料率を確認**。上の 3.00% は Mercari 値で、対象が違う可能性（未確認の値を事実化しない・罠#12型）
2. Deeplink でカード別Buyee検索URLにアフィリID付与 → live 検証（着地・param残存・別タブ）→ 差し替え → デプロイ
3. 承認前は Buyee の素リンクに一切手を入れない（規約違反回避）

### 未解決に追加
- **未解決#21.** Buyeeオファー審査（Pending）が低トラフィック新規サイトで通るか結果待ち。Klook（同じサイトで申請・Pending）と合わせ、Indoleads のオファー審査の通り方の実データになる。
- **未解決#22.** 実リンク対象ソースの実料率（buyee.jp/item/search＝ヤフオク検索に Mercari の 3.00% が適用されるか別料率か）未確認。承認後に確認。

### 2026-07-29 — 未解決#22 解決：Buyee 実料率を Payouts CSV（一次情報）で確定
- オファーページ Payouts を「Copy CSV」で出力（全26件）。内訳は **Mercari のカテゴリ別25件 ＋「Other Stores」1件**。**Yahoo Auctions / Rakuten の独立料率表は存在しない。**
- ★指示側の推測「2〜3ページ目に別マーケットの表があるはず」は**誤り**。3ページ表示は同一26件のページ送りだった。CSV が推測を否定（測定が推測に勝った実例）。
- 料率は「最終的にどのマーケットで購入したか」で決まる：
  - Mercari 購入：Games,toys,and goods=**3.0%** / Fashion=3.0% / Hobbies,musical,art=4.2% / 他Mercariカテゴリ=2.1%
  - Mercari 以外（Yahoo Auctions・Rakuten・Yahoo Shopping 等）＝**Other Stores 一律 2.1%**
- Kataban の Buyee リンク（buyee.jp/item/search）は Mercari 限定でない → 多くは Other Stores 2.1%、買い手が Mercari のゲームで確定した時だけ 3.0%。**実務上の期待値＝2.1%（最大3.0%）。**
- Geo 全件 World Wide / Status=Active（オファー全体では買い手 JP は prohibited・既出）。JD Auctions は tracking 対象外（既出）。
- 出典：オファーページ Payouts の CSV 出力（26行）。承認状態に関わらず公開料率なので確定情報として記録。#22 は解決とする。

### 罠#115. 「完全一致＝最良経路」は駿河屋の通販検索では逆だった（2026-07-29）

型番の完全一致(exact match)は同定としては最も正しい。だが「正しい同定」と
「ヒットする検索」は別軸だった。駿河屋の通販検索窓では、型番より日本語タイトルの
方が在庫に当たりやすい。型番検索は0件を返しやすく、日本語を読めないユーザーには
0件が「このゲームは存在しない」という誤ったメッセージになる（実際はtitleで出る）。

→ primary(桃塗り)の当て先を、型番駿河屋から日本語タイトル駿河屋へ移した
   (index.html:1003-1004 の clsCode/clsTitle 条件を交換)。
   ※効くのは jp モード全体と、outside で r[7](Buyeeキー)が空の行のみ。
   outside で Buyeeキーがある行は桃が Buyee のままで、駿河屋2つは両方水色枠
   (index.html:1002 の clsBuyee は無変更)。
   型番駿河屋は消さず水色枠で残す＝精密な完全一致の価値は保持しつつ、
   空振りしやすい方で先導しない。

一般化：「机上で最も正しい経路」と「実地で最もヒットする経路」がずれるとき、
塗り(primary)は後者に当てる。正しさより「押した先に何か出る」を優先。
罠#27（誤った塗りより塗りなし）と同系。

### 罠#116. 型番なし行では primary(桃)がどこにも付かない（塗りゼロの保険が未発動のまま）（2026-07-29）

罠#115 で clsCode/clsTitle を交換した結果、型番なし行の分岐は clsCode 側(===0)へ移った。
だが gCode の <a> は k.codes.length が真のときだけ描画される(index.html 型番リンク生成部)。
つまり型番なし行では clsCode='store primary' になっても、その塗り先の<a>自体が存在しない。
→ 型番なし行は primary がどこにも付かず、カード内の桃がゼロになる。

現行1,389行は型番100%なので未発動＝実害なし。だが将来 MADB 更新で型番なし行が入ると、
そのカードだけ「まず押す1つ」を示す桃が消える。塗りゼロでも枠線があるので壊れて見えないが、
先導が無くなる。対策する場合は、型番なし行のとき clsTitle 側に primary を戻す分岐が要る。

→ ★2026-07-30訂正: この「実害なし」は out モードの fallback 行だけを見た評価だった。jpモードでは物理20,450行で発動していた。罠#123 参照。

罠#27（誤った塗りより塗りなし）とは逆に、ここは「塗りたいのに塗り先が無い」ケース。

## 2026-07-29（午後）— カードUIの視覚調整（一括・CSS/HTMLのみ・DATA/JS/店リンク生成は不変）

一連の見た目調整。**すべての変更の前に、対象クラスの現行CSSとcomputed値をClaude Codeに読み取らせてから指示した**（デザイン案・記憶を現状として書かない＝罠#76/#77/#84/#113の回避を徹底）。各変更は1つずつ実施・スクショ確認。md5: 4111f8a34d5c9c0bdbb37b5602f70926

### 変えたもの（セレクタで辿ること）
| 対象 | 変更 |
|---|---|
| `.card .group` | margin-top 16→32px（3グループ Ships worldwide/By product code/By Japanese title の上を空ける） |
| `.card .spec .v` | font-size 14→22px（PUBLISHER・RELEASEDの値を見出しより大きく） |
| `.card .spec .v.jp` | font-size 18→26px（CONSOLE値・VT323。VT323は同px比で小さく見えるため他値より大きい数字で横に揃う） |
| `.card .romaji` | 13→17px / color #ff8fd8（一段落ち着いた桃・直値）。読み仮名をタイトルの一段下に立たせる |
| `.card .identity > .field + .field` | 新規 margin-top:8px（JAPANESE·ON THE BOXブロックの上だけ。`.field`多用のため`.identity`直下2つ目に限定） |
| `.card .slab` | 桃枠と発光を撤去（★下記） |
| `.card .slab-copy` / `.btn-copy` | 塗り#241c40・枠#4a4278・hover#2f2652に統一（両COPYを「押せる箱」に） |
| `.card .instore` | 背景#171030追加（店頭ゾーンを静かに浮かせる・A案） |
| `.card .instore > .field` | 新規 margin:16px 0 14px（ON THE SHELFブロック上下・shelfNoteの戻り1つだけに当たる） |
| `.card .shelf-kana` | 枠 1px#2a2247 → 2px#e6e2f0（「へ」の四角を太く・白く。純白#fffはShow to shop staffの桃枠を食うため一段下の白） |
| `.card::before`（番号タブ） | left 12→0・font 11→15px・padding 4px10px（大きく・左端へ） |
| `.count`（件数表示） | 12→20px・weight700・color var(--tag)・margin 20px2px16px（大きく・水色太字・上下余白） |
| `#results` | gap 12→24px（カード間隔。番号タブが枠上端に半分はみ出すため広めが安全） |
| `.rak-cap`（楽天リンク文言） | weight700・color var(--tag)追加（青の太字。詳細度注意＝罠#118） |

### 組み替えたHTML（card()内・DATAは不変）
- **PRODUCT CODEブロックを `.slab`形式 → `.field`+`.micro`+`.jp-copy-row`+`.jp-chip` に**。ラベルとCOPYを箱の外へ出し、下のJAPANESE TITLE欄と同一構造に。COPYは`.slab-copy`クラスのまま（コピー動作JSがこのクラスを見るため変えない）。型番文字は`.code`(IBM Plex Mono)のまま。
- **JAPANESE TITLEの注記(.cap)を `.field`の外→内(末尾)へ移動**。上下2つの注記の空きを対称にした。

### ★逆転した設計判断（後で読む人へ・最重要）
**`.slab`の桃枠(border --signal)とbox-shadow(発光)を撤去した。** 結果、現在カード内で桃色の枠を持つのは **`.btn-clerk`(Show to shop staff)だけ**。
これは既存の設計コメント（`.btn-clerk`直前・「★強い色はスラブの桃だけに残す」「そのまま反転すると…一番目立つのが型番スラブではなくこのボタンになってしまう」）と**真逆**。当時＝「桃はスラブ＝型番が主役」。現在＝「桃はShow to shop staff＝店頭アクションが主役」。**コメントだけ古い正典として残っている**（罠#84のコメント版）。
→ 掃除案：`.btn-clerk`直前コメントから「スラブの桃だけに残す」の一文を削除or現状に合わせて書き換え。**別指示で扱う**（今は未変更）。

### 副作用・死にコード（grep対象）
1. **`.slab`/`.reg`/`.slab-main`/`.slab-label`はnoCode分岐でしか使われなくなった。** 現行1,389行は型番100%＝noCode発動ゼロなので実質死にコード。将来型番なし行が入ったときだけ復活。grepで見つけた人が「なぜ表示されない?」と迷う。
2. **四隅「+」の `.card::after, .card .slab::after{…}`（8層グラデーション）を削除。** ルール削除時に直前の設計コメント3行も併せて削除済み＝孤児化なし。
3. **`--slab`変数は完全に未使用に。** 定義は残置。
4. **JAPANESE TITLE注記を`.field`内へ移した結果、noCode分岐では注記が出ない**（罠#116と同型・型番100%の今は未発動）。noCode復活時は`.slab-main`末尾に同文を足す。

### 罠#117. 表示要素を消すと、それを説明するコメントが嘘になる（2026-07-29）
四隅「+」の削除・スラブ桃枠の撤去で、それらを説明する設計コメントが2つ孤児化。罠#62（UI断定文が行追加で嘘になる）・罠#84（ノートが実装より古い）の**コメント版・削除方向**。
→対策：**表示要素を消す変更では、その要素を説明するソース内コメントも同時にgrepして消す/直す。** CSSルールだけ消してコメントを残すと、コメントが「まだあるはず」と次の人を誤誘導する。

### 罠#118. 詳細度の比較は「同じ要素を争っているとき」だけ成立する（2026-07-29）
`.rak-cap{color:var(--tag)}`を足すとき、`.card a{color:inherit}`(0,1,1)が
`.rak-cap`(0,1,0)より詳細度が高いので負ける、と考えた。**これは誤り。**
実測（getComputedStyle）はrgb(34,211,238)＝水色で、`.rak-cap`単独で効いていた。
理由：`.card a`が当たるのは`<a class="rak-img">`（attachRakutenImageが組む親）で、
文言を持つ`<span class="rak-cap">`には当たらない。spanから見れば`.card a`の色は
「継承」でしか届かず、**直接指定は継承に常に勝つ**。詳細度の勝ち負け以前の話だった。
下線は子の明示指定(text-decoration:underline)で親のnoneに負けない（継承ではなく視覚伝播）。
一般化：**詳細度を比べる前に「その2つのセレクタは同じ要素に当たっているか」を先に確かめる。**
違う要素なら勝負は詳細度ではなく「直接指定vs継承」で決まる。DOMを組んでいるのが
テンプレートではなくcreateElementのとき（＝HTMLをgrepしても構造が見えないとき）に
特に間違えやすい。
（この訂正自体が、推測を検証せず書いた記述を実測で直した例。罠#76/#77系の実践。）

## 2026-07-29（夕）— トップページ移植：モック（Kataban ロゴ）を本番 index.html に着せ替え

デザインモック（kataban_top.html）の主要素を本番へ移植。**DATA行（639行目・7,927,042 B）と配線ID・`.pubwrap` には一切触れず**、追加クラスは全て `kb-` 前置で既存（`.jp-chip`／`display:grid`／`.field` 等）と分離。各ステップ1つずつ・grep検証・スクショ目視で確定。

退避（各段階に戻せる）:
- `index_backup_2026-07-29.html` … 移植前の原本。md5 `4111f8a34d5c9c0bdbb37b5602f70926`
- `index_backup_2026-07-29_pre-panel.html` … パネル着せ替え直前。md5 `550a186249e809809cea39c5754e73c0`

実装した順:
1. **ロゴ**：h1テキストロゴ（`.blk`）→ 5×7ビットマップの KATABAN（水色残像＋桃／`kb-word`・`kb-grid`・`kb-on-pink`・`kb-on-cyan`）。点滅は `kb-breathe`（明るさ .97↔1.14 ＋グロー・4秒周期の常時明滅）。reduced-motion時は静止＋点灯。`<h1>` は残し `aria-label="Kataban"`。塗りは末尾の小さな `<script>` で `getElementById` 2箇所。
2. **スタッツ帯**（`kb-stats`）：37,855 / 10,296 / 13,487。数値=VT323（`--f-vt`）、ラベル=Press Start 2P。
3. **SUPPORTSチップ**（`kb-supports`・`kb-chip`）：PS1〜DS の7つ＋「Bare carts too, by code」。
4. **検索パネル**（`kb-panel`）：Title大入力 → Platform/Publisher/Year の3列 → 区切り線 → 大きな桃色「SCAN BARCODE」（インラインSVGのバーコード＋文言）。四隅ティック（桃・水色）。`.scanbtn` は元から桃・Press Start 2P なので拡大のみ。selectは可変長optionの可読性のため Space Mono を維持。
5. **再配置**：スタッツ帯＋SUPPORTS を結果の下（`<footer>` 直前＝フッター扱い）へ移動。`#count`（"N results · showing 60"）をパネル枠の外・直下へ移動。

配線の温存（確認済み）:
- JSは全て `getElementById`／`$('#id')`。`.closest()` は `.pubwrap`／`.pubopt`／`[data-copy]`／`[data-clerk]` のみ。検索行の親子・兄弟構造に非依存（grepで parentNode/sibling 無しを確認）。ボタン文言「Scan」もHTMLのみでJS不変。
- 旧 `<div class="field">` ラッパーはこの検索ブロックからのみ外れ、`.card .field` 系6箇所は無傷。

### 罠#119. 日本語を含むHTMLは授受で化ける（UTF-8→Latin-1誤読）（2026-07-29）
モック配布HTMLを渡した際、`型番`（E5 9E 8B…）が `åž‹…`、`タイトル`→`ã¿ã¤ãã«` に化けた（UTF-8をLatin-1として読む典型）。ファイル内容を貼り付け経路で渡すと起きる。対策：**本番へ新規に挿すHTMLはASCIIだけで書く。見える日本語は数値文字参照**（`型番`=`&#22411;&#30058;`、`タイトル`=`&#12479;&#12452;&#12488;&#12523;`）、**コメントは英語**。非ASCIIを含む既存行（`#q`/`#pubInput` のプレースホルダの `…`/`—`）は**打ち直さず現物を逐語コピー**。保存後は必ずブラウザ実表示で確認（罠#62＝表示文が嘘になる、の予防）。

### 罠#120. `grep -c` は「マッチ行数」で、出現数ではない（2026-07-29）
`kb-chip`（CSS1行＋チップ7個が1行）を「8」と見積もったが `grep -c` は 2 を返す。CSS/HTML/JS に同じ識別子が跨る検証では、期待値を**行ベース**で立てる。個数を数えたい時だけ `grep -o … | wc -l` を使う。対策：検証コマンドの期待値は「何行に出るか」で書く。

### 罠#121. 直書きスタッツはフッターと二重管理になる（2026-07-29）
`kb-stats` の 37,855 / 10,296 / 13,487 はモック同様の直書きで、`<footer>` の同じ数字と**2箇所**に存在。データ更新時に片方だけ直すと不一致になる（罠#62系）。対策：件数更新時は**フッターとスタッツ帯の両方**を直す。将来は build 側で1箇所から差し込む形が望ましい（未解決として保留）。

### 罠#122. NFDで畳んだら、最後にNFCで合成し直す（2026-07-30）
r0(英題)のアクセント畳み込みで `accent = NFD → U+0300–U+036F除去` と定義したが、NFDのままだと**仮名の濁点が U+3099(category Mn)に分離する**。これは `\p{P}\p{S}` に該当せず `fold` で消えないため、accentを掛けたクエリ側だけが分解形になり、NFCのままの r1/r5/r6/r8 索引と符号位置が食い違って**濁点クエリ（ドラゴン等）が全滅**する（`ドラゴン` = `30c9 30e9 30b4 30f3` vs `30c8 3099 30e9 30b3 3099 30f3`）。末尾に `.normalize('NFC')` を足すと仮名は元の符号位置に復帰し、マクロンは分解段階で既に落ちているので r0 の狙いは変わらない。指示側(Claude)が「合成し直す一手」を書き落とし、Claude Code が node で `ドラゴン`/`ゲームス`/`ぎゅっと` を実打して回帰を捕まえて停止。7/19の長音取りこぼしと同型（**机上で組んだ変換が実データで落とす**）。対策：**正規化を分解形で行ったら、比較する相手の正規化形（NFC）に必ず戻してから**索引・クエリに使う。

### 罠#123. 「実害なし」の評価が、母集団を絞りすぎていた（2026-07-30）
罠#116 を「型番なし行で塗り先の `<a>` が消えるが、現行1,389行は型番100%なので未発動・実害なし」と記録した。だがこの評価は **out モードの fallback 行(1,389)だけ**を見ていた。`(jp || noProxy)` は **jpモードでは全行 true** になるため、型番を持たない**物理20,450行(66.5%)**で `clsCode='store primary'` が空振りし、店頭の観光客が主役リンクを1つも見ていない状態が全件で発動していた。「解決済み・実害なし」と書いた所に最大の問題が隠れていた（7/18・7/19と同型）。対策：**「実害なし」と書く前に、その判定が全モード・全母集団で成立するか確かめる。評価した母集団を明記する**（何を数えて実害なしと言ったか）。

### 罠#124. 条件の入れ替えで、意図が反転したまま残った（2026-07-30・原因は#115）
7/29(罠#115)で clsCode/clsTitle を交換した際、**条件(`codes.length` と `codes.length===0`)だけが入れ替わり**、「型番があれば型番リンクを主役」の意図が反転。型番あり1,389行全件で塗りが題名側に乗り、7/26の設計(exact match を主役)と実装が逆になっていた。前ターンで入れた文言「強調リンク＝exact match」が、強調されている実物(和名検索)と噛み合わず、**文言を直したそばから前提が崩れていた**。1144/1145 の条件入れ替えで解消。対策：**class/条件を交換する編集は、交換後に「意図した向きか」を実データ1行**(BLAZBLUE=型番ありで塗りが code 側か)**で必ず確認する**。

## Yahoo!ショッピング商品検索API — 画像の第2ソース化（調査ログ・提携承認待ちで中断 2026-07-30）

**背景**：楽天画像はJAN持ち行（①）しか埋められない。JAN無し行の穴を、別マーケットプレイス（Yahoo）で埋められるかを測った。Wikidata P18・libretro は既に却下済み（道1/道3）なので再検討しない。

### 効果測定（確定）
- 物理版30,746行の3層：① JANあり11,396(37.1%) / ② JAN非13桁だが品番row[3]あり2,583(8.4%) / ③ JANも品番も無い16,767(54.5%)。
- ②のうちSwitch/PS4/PS5（1,751行）で「カテゴリ主軸＋品番補助」を実測：実効16/18。危険群（短題・汎用語）は1.3%(23/1,751)のみで、カテゴリフィルタで誤爆ゼロ（救えないだけ・別商品は出さない＝罠#27を守る）。
- **確定見積り +14%前後**（① 11,396 → 約13,022、物理版 37.1%→42.4%）。前回の+20%は楽観外挿で、危険群と0ヒット率を分離した+14%が確定値に近い。
- ②の旧機種832行はカテゴリ未測定（後回し。旧機種は①楽天と重なりが厚く、新規に足せる余地が新作より小さいため）。

### 実装方針（確定・GO後に使う）
- カテゴリ主軸・品番一段目補助・タイトル補助以下。タイトル一致スコアは短題で飽和し機種を判定できない（Air全滅で実証）。
- カテゴリ・ホワイトリスト：50522(Switch用ソフト パッケージ版)/50521(PS4用ソフト)/50798(PS5用ソフト)。店の誤タグは「取りこぼし」側にしか効かず、誤爆を通さない（実測0件）。
- 「正解カテゴリに入る中の最上位1件」を採る（最上位が誤gidの店があるため単純先頭は不可）。
- 品番照合：両側を英数字だけに畳んで大文字化してから包含判定（SLPS-00533→SLPS00533）。codesのどれか1本一致で可（ゼルダTPHDのcodes[1]特例と整合）。
- Yahoo APIはローカルpythonで通る（楽天と違いブラウザ不要）。画像フィールドは image.medium / image.small。エンドポイント V3/itemSearch、appid一本、jan_code/query、レート1.1秒sleep。

### 権利確認（ここまで）
- アフィリエイト＝**利用可で確定**。API公式パラメータに affiliate_type=vc / affiliate_id。公式ページがバリューコマースを名指しで案内。共通規約 第2章3(3)「対価目的の利用禁止」はAPIソフト自体の転売等を指し、アフィリエイト収益は該当しない。
- クレジット表示＝**必須・確認済み**。規定HTMLをフッターに置き https://developer.yahoo.co.jp/sitemap/ へリンク。改変禁止・最も目立つ要素にしない。楽天の Supported by Rakuten Developers と同じ扱い。英語UIでもクレジット文言だけは規定表記のまま。
- **★未確定（本丸）**：「商品検索APIで取得した商品画像を自サイトに<img>表示してよいか」「アフィリンクと組にする義務」「他社アフィリンク併置の可否」を明示する条文に未到達。楽天・未解決#8の「表示OKの一文」に相当するものがYahoo側で未確認。
- appid（Client ID）は取得済み。生値はノートに書かない（正典はYJDNのアプリ管理画面）。_experiments/yahoo_appid.txt はgit管理外。

### 副収穫（頭の隅）
- 商品参照API（別系統・出店者向け）のレスポンスに CrossBorderAgencyFlag（海外向け購入代行の除外設定・Buyeeなど）がある。将来Buyee導線の出し分けに効く可能性。今日の判断には不要。

### 次にやること（再開時の手順）
1. バリューコマースのメディア審査通過を待つ（**現在ここ・審査中。時間がかかる可能性あり**）。
2. Yahoo!ショッピング（広告主）と提携申請→承認。
3. 承認後、広告主ページの「提携条件／広告素材／MyLink」で上記★の3点を確認。
4. 確認結果を「Yahoo画像 表示可否＋根拠条文」として1枚化し、実装GOか棚上げを最終決定。
5. GOなら実装：カテゴリ主軸フィルタ＋クレジット表示、画像は必ずアフィリンクの<a>で包む・保存しないランタイム取得。「取れる≠表示してよい」を最後まで守る。

### 実験スクリプト（_experiments/ 内・すべて測定専用）
- measure_yahoo_images.py（JAN/NAME穴埋まり率）
- measure_tier2_codematch.py（②品番照合 A群40%/B群20%）
- measure_tier2_category.py（②カテゴリ照合 16/18・誤爆0）
- count_id_tiers.py（3層集計）/ count_tier2_shorttitle.py（危険群1.3%）

## Klook器の「デプロイ禁止href」を解消 — CTAを非活性 span 化（2026-07-30）

### 罠 / 記録：Klook器を「デプロイ禁止href」で置いていた件（2026-07-30 解消）

**背景（旧状態）**
`#klookbar`（日本申告者だけに出る旅行アクティビティのアフィリエイト枠）を、承認前から
`<a href="KLOOK_PENDING_DO_NOT_DEPLOY">` の状態で **本番に載せていた**。
ノートの指示は「承認後に本番トラッキングリンクへ差し替える／差し替えるまでデプロイしない」だったが、
実測すると **live に入っていた**（このセッションのアイコン/OG作業より前から）。

**実害と、その範囲**
- 相対URLなので `https://gamekataban.com/KLOOK_PENDING_DO_NOT_DEPLOY` に解決し、**GitHub Pages の 404**（9,379B）に着地。
- ただし露出は「日本からのアクセス」全員ではない。geo/IP/timezone/`navigator.language` の**自動判定は無い**。
  `#klookbar` が出るのは (1)「I'm in Japan」を押した / (2) `localStorage.kataban_loc='jp'` が残っている / (3) `?loc=jp` で来た、の**自己申告者のみ**。
  → 「値が間違っているのは、値が無いより悪い」(#12) の同型。誤った href は、href 無しより悪い。

**対処（実施済み・729行目1行のみ）**
`<a ... href="KLOOK_PENDING_DO_NOT_DEPLOY">See experiences on Klook →</a>`
→ `<span class="kk-btn" aria-disabled="true">Klook experiences — coming soon</span>`
- `<a>`→`<span>` で**リンク要素そのものを消す**（href が存在しえなくなる）。`target`/`rel` も除去。
- 文言も「押せるのに反応しない矢印」から中立表現へ。**UIテキストの真偽**（新状態で偽になる表示）を実態に合わせた。
- **器・CSS(389–397)・JSゲート(1067–1099) は温存**。承認後は729行目を `<a>` に戻すだけで復活。
  復活元の文言は退避 `index.html.2026-07-30.bak` の729行目に保存。

**検証で確定したこと（手順の型）**
- DATA行md5 `2dacc3ca6e65aec4d7e31ea329fa7eb5` が編集前=後=live で不変（本文データ不可侵）。
- head区間(1..`</head>`)md5 `78179e1d5df28e8ed164b1a3bfc300cf` が不変（head不可侵）。
- `git diff --unified=0` が `@@ -17` と `@@ -729` の**2ハンクのみ**（A=`&amp;`修正 と Y=Klook無効化 を1コミット `a49d31d`）。
- サイズ 8,046,148 → 8,046,095（A +4 / Y −57）。

**残タスク**
- Indoleads の承認リンクが来たら、729行目を
  `<a class="kk-btn" target="_blank" rel="sponsored nofollow noopener" href="＜本番URL＞">See experiences on Klook →</a>` に戻す（＝編集Yの逆）。
- それまで **本番に生きたKlookリンクは無い**。「デプロイ禁止」は *リンクの公開* を指し、*器の存在* は禁じていない、と読み替える。

**（補足・訂正）上の 2026-07-29 の項で失効した記述**
- 「**承認まで器は非公開**」（未解決#20）と「**承認前は未コミットのまま保持**」は**どちらも実態と違っていた**。
  器は live に出ており、未コミット扱いも成立していなかった。未解決#20 は
  「pending なのは *リンク* だけ。器は公開済みで、CTA は非活性 span」に読み替える。
- コミットSHAはローカルと GitHub で別物（remote 未設定・`gh` の contents API 経由で上げているため）。
  上記 `a49d31d` は **GitHub 側**。ローカルは `8a70a1c`。Pages build も `a49d31d`。
- 退避は2本。`index.html.2026-07-30.bak`（head編集後・Y適用前＝**復活元の729行目を含む**）と
  `index.html.2026-07-30b.bak`（Y適用後・live と md5 一致 `73bcacc73134ef303fdcf8ecd7995278`）。

## ファビコン＆OGカードを公開 — ピクセルKと Press Start 2P の OG（2026-07-30）

**やったこと**
サイトに favicon 一式と OGカードを追加。ロゴは頭文字「K」を**ピクセル描画**（SVGの矩形の並びなのでフォント非依存。16pxでも K と読めるが、1セルが 1.67px になるため輪郭は甘い）、桃 `#ff37c7` 本体に水色 `#22d3ee` を右下1ドットずらした色収差、地は角丸の暗紫 `#07040f` チップ。OGは中央に `KATABAN`（Press Start 2P・桃＋水色ずらし）＋英語キャッチ「Find & buy Japanese retro games」「in-store or online · no Japanese required」。隅に型番 `SLPS-00533 → AUBIRDFORCE`（水色 `#22d3ee`）/ `SLPM-86500 → BIOHAZARD`（桃 `#ff37c7`）を置いた（型番＝主役の魂）。
- 隅の型番は**初版では淡色 `#453e66`** だったが、公開版では**高輝度のブランド色**に上げた。「忍ばせた」のは初版の方。
- `--signal` は `#ff37c7` で `--accent` と**同じhex**（155行目）。よって「朱色は色として不使用」とは言えない。守っているのは*意味*の側で、カードの「型番の一致」「Buyee=届く経路」という含意はOGに持ち込んでいない。

**公開ファイル（リポジトリ直下・index.htmlと同階層）**
`favicon.svg` `favicon.ico`(16/32/48) `apple-touch-icon.png`(180) `favicon-32.png` `favicon-16.png` `og.png`(1200×630 RGB)。
`.gitignore` が `/*` 全無視方式なので、6ファイル分の `!` 行を追加して追跡させた。

**head への追記（手書き・再ビルドで消えない）**
`build_master.py` は `data/data_line.js` しか書かず、`swap_data.py` は `const DATA = [` の1行だけを置換する。したがって head 編集はビルドで失われない＝**テンプレート二重管理は不要**。
- 追加：`og:image` / `og:image:type` / `:width`(1200) / `:height`(630) / `:alt`、`<link rel="icon">`×2（.ico + .svg）、`apple-touch-icon`。
- 差し替え：`twitter:card` を `summary` → `summary_large_image`（1200×630を正方形に切らせない）。
- 既存の `og:title` / `og:description` / `og:url` / `og:type` は**再追加しない**（重複禁止）。`twitter:image` は入れない（未指定なら X は `og:image` を流用）。

**罠：書体は見た目でなく字幅で検算する**
OG初版は headless Chrome で焼き、`KATABAN` をセリフ体にしていた（Didot / Bodoni 72 / Baskerville / Times New Roman / Playfair Display を並べて焼き、当時の見本の字幅比に最も近い **Bodoni 72** を意図的に選んだ結果。**無言のフォントフォールバックではない**）。その後ピクセル体（Press Start 2P）版に差し替え、最終版は人手で作成したものを受領して配置した。
判定の決め手は**字幅**：Press Start 2P の "KATABAN" は **564px**（x 313–876）、セリフ版は **383px**（x 408–790）。同じ 1200×630・同じ位置でも書体が違えば幅が 1.5 倍変わる。→ 見た目の印象でなく**数値で書体を検算する**。

**★測り方の罠：PNG は「フィルタを解いてから」測る**
最初の測定で幅 391px・最多色 `#ff00ff` `#ff0000` という値を得たが、これは**偽**だった。og.png はスキャンラインフィルタ 1(Sub)/2(Up)/4(Paeth) を使っており、フィルタを解かずに生バイトを読むと色も幅も別物になる。フィルタ残差はグリフの縁で大きくなるため、**帯の位置だけはもっともらしく出てしまう**のが厄介（＝間違いに気づきにくい）。正しくアンフィルタして 564px / 383px、色は正確に `#ff37c7` / `#22d3ee` を確定した。罠#12「値が間違っているのは値が無いより悪い」の測定側の同型。

**この環境の制約（次回の再開時に効く）**
- **Pillow は使えない**。`/Library/Frameworks/Python.framework` の PIL が x86_64 バイナリで、arm64 の python3 から `ImportError: incompatible architecture`。→ アイコン4点（16/32/180/ico）は stdlib の `zlib` だけで PNG と ICO を自作した。セルはピクセルとの重なり面積で解析的にAA、角丸は符号付き距離から被覆率を出す方式。
- **`rsvg-convert` / ImageMagick(`magick`,`convert`) / `inkscape` / `cairosvg` はいずれも未インストール**。使えるのは `sips` `qlmanage`、そして **headless Chrome**（`/Applications/Google Chrome.app/…/Google Chrome --headless=new --screenshot`）。SVG/HTML→PNG は実質 Chrome 一本。
- Google Fonts は `fonts.googleapis.com` 経由で Chrome に読ませて**成功する**（ネットワークは通る）。フォントファイルを自前で落とす必要はなかった。
- `favicon.ico` は PNG 埋め込みでなく **BMP エントリ**で書いた（16/32/48 の3枚入り・最大互換）。
- **apple-touch-icon だけ全面不透過・角丸枠なし**：iOSが自前で角を丸めるため、48単位で2px内側の枠線を焼くと角で線が割れる。地を全面塗りにして回避。

**検証で確定したこと（手順の型）**
- 配信バイト列＝ローカル：og.png md5 `131773d58f1e14e1854a2509d4b8c6c3` / 93,747B が live と一致。
- head編集は DATA行md5 `2dacc3ca6e65aec4d7e31ea329fa7eb5` を不変に保ったまま実施（本文不可侵）。サイズ +483B、`</head>` 645→653行。
- 重複ゼロ：`og:image"`=1 / `rel="icon"`=2 / `apple-touch-icon`=1 / `content="summary"`=0。

**運用（Claude Codeの作業ではない・人間側）**
X・Facebook・LINE はカードを強くキャッシュするので、公開後に各社のカード検証ツールで**一度 re-scrape** をかける。でないと旧状態を掴んだままになる。

**（補足）コミットSHAの対応**
remote未設定・`gh` の contents API 経由で上げているため、**ローカルと GitHub で常に別SHA**。追跡時は両方を見る。
| 作業 | ローカル | GitHub |
|---|---|---|
| アイコン6ファイル追加 | `bab57fc` | 1ファイル1コミットで6本 |
| og.png 差し替え（ピクセル体） | `2fb9e99` | `66dc10d` |
| live済み未コミット分の記録 | `b521d1d` | （なし・ローカルのみ） |
| head 追加（og:image群/favicon群/summary_large_image） | `60456c6` | `1cb98e2` |
| `og:image:alt` の `&`→`&amp;` ＋ Klook無効化 | `8a70a1c` | `a49d31d` |

`og:image:alt` は初回に素の `&` で入れてしまい、後で `&amp;` に修正した（HTML5パーサは実害なしだがバリデータ警告。厳密には実体参照が正）。この1文字修正は Klook無効化と1コミットに同梱。

### 未解決: 罠番号の重複（2026-07-30 発見）
罠#84 / #85 / #98 が各2回使われている。今日の作業とは無関係の既存状態。本文を読んで統合するか片方を繰り下げるか、別セッションで棚卸しする。総数カウント(現58)も要再計算。

### 開示行コントラスト — 測って据え置き確定（2026-07-30）
FTC "clear and conspicuous" の代理指標としてWCAGコントラスト比を実測。全開示行がAA(4.5:1)超で合格:
footer(--muted×地)5.79:1 / card .cap(--muted×カード面)5.54:1 / howto・Klook(--ink)11.97:1。
→ --ink-2 への引き上げは不要。理由は2つ: (1)最暗でも5.54:1で4.5:1を超える (2)--ink(11.97)まで
上げると楽天/Yahooクレジット規約「最も目立つ要素にしない」に逆行する。--muted は開示として適位置。
放置ではなく「測って変えないと決めた」（未解決#10の手順どおり）。

※副次発見(別件): 730行 #klookbar内の <p class="cap"> は .card スコープ外のため
.card .cap が当たらず、意図(小さく淡い注記)に反して --ink 11.97:1・本文サイズで出る。
コントラストは問題ないが 1198 と見た目が揃わない。Klookが非活性span(8a70a1c)の今は急がないが、
有効化時に #klookbar .cap を足すか .cap を card外にも効くセレクタにする。→ 未解決に追加。

### 未解決: #klookbar の .cap が .card スコープ外（2026-07-30 発見）

## 2026-07-30 — Google Search Console 登録 + sitemap/robots/canonical（SEO土台）

審査待ちのアフィリエイトに触らず進められる作業として着手。検索経由での発見の土台を作った。

### やったこと
| 項目 | 状態 |
|---|---|
| GSC プロパティ登録 | 完了。**ドメイン**プロパティ（URLプレフィックスではない）＝www有無・http/https・全サブドメインをまとめてカバー |
| DNS 認証 | 完了。Cloudflare に TXT レコードを追加して認証。**index.html には一切触っていない**（DATA行リスクなし・再デプロイ不要）。グレークラウド（DNS-only）なのでプロキシによる伝播遅延なし |
| sitemap.xml | 新規作成・公開。1ページサイトなので loc=https://gamekataban.com/ の1エントリのみ |
| robots.txt | 新規作成・公開。User-agent:* / Allow:/ / Sitemap:https://gamekataban.com/sitemap.xml |
| canonical | index.html の <head> に <link rel="canonical" href="https://gamekataban.com/"> を1行追加。?loc=jp 等での重複ページ化を防ぐ |
| サイトマップ送信 | GSC で送信。★ドメインプロパティは**フルURL**で送信する（sitemap.xml だけだとエラー。プレフィックス自動表示はURLプレフィックスプロパティの挙動） |

### 確定した運用
- **TXTレコードは Cloudflare に残す限り認証は無期限維持**。消すと認証が外れる。
- sitemap.xml / robots.txt は favicon/og.png と同じ「Pagesルートに置いて配信する実ファイル」。.gitignore の `/*`（全無視）方式に対し `!/sitemap.xml` `!/robots.txt` を追加して追跡対象にした（31-33行）。favicon群と非対称にしない。
- ★**DATA行は 786 → 787 に繰り下がった**（canonical で head に1行入ったため）。md5 は不変（2dacc3ca…）だが、行番号固定で検証すると別行を掴む。以後 DATA は 787 行目。

### 未確認（時間差で確定するもの）
- サイトマップ送信直後のステータスは「取得できませんでした/保留」でも失敗ではない。Googleが読みに来るまで時間差。数時間〜1日で成功に変わる想定。→ 次セッションで実ステータスを確認。
- GSC のデータ（検索クエリ・インデックス状況）は登録直後は空。数日でクロールされ蓄積される。

### Klook .cap の副次発見（開示行コントラスト節に既記録・再掲なし）
→ #klookbar の .cap が .card スコープ外の件は別項に記録済み。

## 2026-07-31 — 楽天カードに中古相場を追加（取得は availability:0 で全件・表示は2行）

7/23 に入れた楽天画像レイヤーは「在庫あり1件の値段」しか出していなかった。中古の相場（いくらが普通か）が分からないと、目の前の1件が高いのか安いのか判断できない。中央値を足して2行構成にした。index.html への変更は4コミットに分けた。

### やったこと
| 項目 | 状態 |
|---|---|
| 取得 | `_rakFetch` の `hits:'1'→'30'`、`availability:'1'→'0'`（在庫切れ込みで母集団を作る）。`sort:'+itemPrice'`・鍵・`imageFlag` は不変 |
| 中央値 | `_rakMedian` を新規追加（昇順・奇数は中央1つ・偶数は中央2つの平均・空は null）。返り値に `medianPrice` |
| 最安 | 全件の最小値を `minPrice` として返り値に追加。`price`(在庫あり最安・画像用)とは別物 |
| 画像の選択 | `Number(x.Item.availability) === 1` の最初の要素（= 在庫あり最安）。該当なしなら従来どおり全体の先頭にフォールバック |
| 表示・行1 | リンク内 `.rak-cap`。`Buy this one on Rakuten: ¥{price} (PR)` / 価格なしなら `See this listing on Rakuten (PR)` |
| 表示・行2 | リンク外 `.rak-market`（`.rak-slot` 直下）。`Used on Rakuten: ~¥{medianPrice}`。**リンクなし・(PR)なし**（広告ではないため）。中央値が無ければ span 自体を作らない |
| CSS | `.rak-market` を1本追加。Space Mono 14px・`--muted`・下線なし（リンクでないと分かる形）。`.rak-cap` は不変 |
| フッター | 相場注記を1行追加（クラスなし裸テキスト・既存の11px/--muted に乗せる） |
| コミット | `3b707ef`(取得) → `6c10e3d`(minPrice) → `f8aa471`(表示) → `abe15ee`(フッター)。DATA行 md5 `2dacc3ca…` は全コミットで不変 |

### 確定した設計
- **(PR) は行1だけ。** 行1はアフィリンク＝広告なので必要。行2はリンクなしの相場表示なので付けない。7/23の「PR明示と帰属表示は別要件」の延長。
- **`minPrice` は表示に使わない。** 在庫切れ込みの最安は「買えない値段」で、行1（在庫あり最安）より安く並ぶと誤読を招く。`_rakFetch` は返し続けるが表示層は読まない。
- **行2は「相場」であって「価格」ではない。** 文言に `~` を付け、フッターに「median of used Rakuten listings only — a rough guide, not a live price」と明記。
- **切り抜かない・画像に文字を乗せないは維持**（7/23の規約適合条件）。行2は画像の外の別 span。

### 未確認（実測が要る）
- **`availability` キーが実レスポンスに存在するか未実測。** 無い場合 `Number(undefined)` が NaN になり全件フォールバック＝行1が在庫切れ品を指す可能性がある。scratchpad の `rakuten-probe-availability.js` → `rakuten-probe-avail-items.js` で、キーの実在と値の分布（全件同値でないか）を確認する。
  ※ラブプラス1件では実在・値が0:12/1:17に割れることを実測済み、実カードでも在庫あり品が選べている。母集団全般での実在は個別未実測。
- **`hits:'30'` の頭打ち。** `count` が30を超える JAN では中央値が「上位30件の中央値」であって全出品の中央値ではない。人気タイトルほど安い側に偏る。
- デプロイ未実施。上記2点を確認してから公開する。

### 測定用スニペット（scratchpad・リポジトリ外）
`rakuten-probe.js` / `rakuten-probe-measure.js` / `rakuten-probe-stats.js` / `rakuten-probe-median-depth.js` / `rakuten-probe-availability.js` / `rakuten-probe-avail-items.js`。いずれもライブのコンソールに貼る前提で、鍵は直書きせずページの `RAKUTEN` から読む。

### 罠#125. 測定スクリプトが生レスポンスを捨てると、次の問いに答えられない（2026-07-31）
`availability` 比較スニペットは `itemPrice` を数値化した配列だけを `window.__rakAvail` に残し、`Item` オブジェクトを捨てていた。その後「在庫フラグらしきキーはあるか」を調べようとした時点で、**手元のデータでは答えようがない**ことが判明し、30秒の再測定をやり直す羽目になった。測定の時点では「次に何を問うか」は決まっていない。対策：**測定スクリプトは集計値だけでなく生レスポンス（少なくとも配列本体）を必ず残す。** 捨てるのは表示の直前であって、保存の時点ではない。

### 罠#126. 存在しない資材を「渡した」前提で組み立てると、無言で捏造に化ける（2026-07-31）
「上で渡した `_rakMedian` を全文そのまま入れて」という指示を受けたが、その関数は会話のどこにも存在しなかった（中央値の実装は Claude 側が scratchpad に書いた `median` だけ）。ここで「たぶんこれだろう」と黙って埋めると、**本番コードに出所不明の関数が入り、しかも「指定どおり」と報告される**。実際には実在を確認して不在を報告し、代替（scratchpad の `median` を改名）を明示した上で進めた。対策：**「既に渡した/上で書いた」と参照された資材は、使う前に grep で実在を確認する。無ければ推測で埋めず、不在を報告してから代替を明示する。** 罠#114（docstring が計画を事実として記録）と同型で、出所の詐称が起きる方向。

### 罠#127. 母集団を広げると、表示している数字の意味が静かに変わる（2026-07-31）
中央値のサンプルを増やすため `availability:'1'→'0'`（在庫切れ込み）にしたが、これは中央値だけでなく**同じ配列から取る最安値の意味も変えていた**。`minPrice` は「買えない出品を含む最安」になり、行1の「在庫あり最安 ¥1,200」の下に「from ¥380」と並ぶ構図＝安い方はクリックしても買えない、が起こりうる。パラメータ1つの変更が、それを参照する全ての派生値に波及していた。対策：**取得条件（母集団）を変えたら、その配列から作っている派生値を全部列挙して、一つずつ意味が変わっていないか確かめる。** 変わった派生値は表示から外すか、母集団を明記した文言にする。罠#123（評価した母集団を明記する）の実装版。

### 罠#128. 相場の中央値を availability:'1' で計算すると件数が枯れて外れ値に釣られる（2026-07-31）
中央値の母集団を「在庫あり」に絞ると、標本数がそのぶん減る。ラブプラス(4988602143720)の実測では `availability:'0'` の29件に対し在庫ありは17件（0:12 / 1:17）で、4割が落ちる。件数が少ないほど中央値は個々の出品——プレミア価格や送料込みの高値——に引っ張られ、「相場」として出す数字が1〜2件の都合で動く。だから取得は `'0'`（在庫切れ込み）で母集団を広げ、在庫あり判定は画像・リンクに使う1件の選択にだけ効かせた。表示の役割が違えば、必要な母集団も違う。対策：**集計値（中央値・平均）と、クリックさせる1件は、別々の母集団から取る。** 集計は広く、選択は買える条件で絞る。罠#127と対。

## 2026-07-31 — 棚ナビ本丸: 五十音行ロケーター + ジャンル早見表（作品非依存）

海外の非日本語ユーザーが「憧れのブックオフ」で目当てを自力で棚から探せるようにする本丸機能。実装は2本立てで、どちらも「読めない人が棚の日本語札を目で照合する」という同じ動作。機種で中身が変わるだけ。デプロイは未実施（実機確認後にまとめて公開する）。

### 何を作ったか
- **五十音 行ロケーター**（カートリッジ機）: カード内に `この棚を探す → ◯行` を出す。`row[11]`（清音カナ1字）→ 行 の変換で「あ行〜わ行」。表示は店の仕切り札に合わせてひらがな。対象は `SYLLABARY_PLATFORMS = Famicom / Super Famicom / Nintendo 64 / Game Boy / Game Boy Advance` の5機種のみ。対象外機種・`row[11]`空・変換表外の字は **器ごと非表示**（偽の位置を出さない＝配信専用行で店リンクを隠すのと同じ思想）。`shelfGyo()` は既存 `GYO`（ヲ/ン無し・全機種向け文章用）と統合せず別テーブル `GYO_LOC`（ヲ/ン有り）にした。用途が違うものを1本にすると片方の都合で他方が壊れる。
- **ジャンル早見表**（ディスク機）: 五十音表記の下に「ジャンルで探す / Find by genre」リンク→モーダル。中身は **作品に紐付かない静的な英日ジャンル辞書**（15グループ）。英語で自分のジャンルを見つけ→日本語の店札（白地黒字・縦書き）を棚で照合。データは index.html 内 JS定数 `GENRE_GUIDE`（外部ファイルを読まず1ファイル完結）。

### 棚のルール確定（フィールド写真＝一次資料）
店内の並べ方は **メディアの物理形状で決まる**（機種名でなく形状）:
- **カートリッジ／ROMカセット → 五十音（行）順**。店の仕切り札に「スーパーファミコン タイトル順 さ行/た行/は行」と縦書き。対象: Famicom, Super Famicom, N64, Game Boy, GBA, Game Gear, WonderSwan。
- **ディスク → ジャンル順**（ロールプレイング/アクション/シューティング/対戦格闘/恋愛/…の縦書き札）。対象: PS1〜PS4, PSP, Vita, Saturn, Dreamcast, GameCube, Wii, Xbox系, **Nintendo DS**（カードだが店の扱いはディスク側＝ジャンル）, 3DS。
- 店札の実在を写真で確認した13語: 恋愛・シミュレーション・スポーツ・レース・その他・シューティング・アクション・ロールプレイング・テーブル・対戦格闘・パズル・音楽・アドベンチャー（「等」なので非網羅）。**ホラー・アダルトの札は未確認**（早見表には出すが、店に無ければユーザーが別ジャンルを当てる前提）。

### データ実測（この機能に効いた数字）
- `row[11]`→行 変換は5機種で取りこぼし0（不明は全件 `row[11]`空由来、変換表の穴なし）。空率: Famicom 0.2% / Super Famicom 1.5% / GB 1.3% / GBA 1.7% / N64 4.7%。**レトロ棚は98%以上で位置を出せる**。
- 棚あり30,746行のうち `row[11]` 空は961行（3.1%）。中身は819件がラテン英字始まり・139件が数字始まり＝西洋タイトル。機種別では新世代ほど濃い（PS5 29.1% / Switch 14.2% / PS4 10.2%）。**レトロ機ではほぼ0**。`row[8]`(カタカナ)からの初字後埋めは0件成立（英字読みのまま入っており五十音が取れない構造）。
- Wikidata P136（ジャンル）: DATA各行の鍵は **M番号**（`master_final.csv` の madb_id、35機種フィルタで i 行目=DATA i 行目が全37,855行で成立・6列突き合わせ不一致0）。DATA自身は鍵列を持たない。全件取得結果 `data/wikidata_genre.json`（新規）: 37,855件中 Wikidata解決 14,551（38.4%）、うちP136保有 12,910（全体34.1%/解決分88.7%）、ユニークQID 323種。**解決率≒40%が天井**（先頭700件=2026年MADB更新分がWikidata未収録なのが主因。データ側の取りこぼしではない）。
- QID→店札の対応表を作成（`data/genre_qid_labels.json` 日 / `data/genre_qid_labels_en.json` 英 いずれも新規）。上位20QIDで73%をカバー。最終15札に丸めた（ストラテジー系→シミュレーションに合流、題材タグ→その他、非ゲーム誤爆34種→表示しない、BL/百合→英語見出しは残し店札は恋愛、ホラーは独立札を追加＝14→15）。

### 設計の芯（将来の自分が忘れると同じ遠回りをする）
1. **棚ルールはメディア形状で決まる**（→罠#129）。機種で二分でなく、カセット/ディスクで二分。
2. **ジャンルは作品に紐付けず、英日用語辞書として持つ**（→罠#130）。作品カバー率(38%)がこの機能と無関係になり、「35%欠損が不具合に見える」問題が消える。
3. **店の仕切り札を写す**のが棚ナビの原則。UIの都合で行を分割・合併しない（店が「は行」1区画なら我々も「は行」）。表示は店札に合わせひらがな。
4. 早見表の札は**見やすさ最優先で白地黒字**（ブランド配色を使わない）。棚の物理札（白地黒字）と明暗を揃えると照合が速い。Show to staff を白くしているのと同じ思想。

### 未解決・保留（次セッション以降）
- **デプロイ未実施**。行ロケーターとジャンル早見表をまとめて公開する。公開前に実機（Mac/スマホ）で目視（headless Chromeでは拾えないスクロール感・タップ域）。
- **中間世代機の棚ルール未確認**: Sega Saturn / Dreamcast / PC Engine / Mega Drive(CD) の CD 部分がカートリッジ側（五十音）かディスク側（ジャンル）か。Koha証言「ディスクメディアはジャンル別／他のカートリッジは五十音／Neo Geoは在庫を見ず」。**サターン/DCのディスク部分の確定待ち**。
- **Game Gear / WonderSwan を五十音対象に足す**: カセット＝五十音と判明済みだが、`SYLLABARY_PLATFORMS` への追加は measure_tana4 と同じ検算を1度通してから（未測定の機種を勢いで足さない）。
- **DS/Switch のジャンル札**: 早見表は静的辞書なので全機種共通で開ければ足りる。作品→ジャンルの動的表示は解決率40%のため見送り。将来カバー率が上がれば `wikidata_genre.json` + QID対応表がそのまま生きる。
- **ジャンル早見表のトリガーは現状カートリッジ機カードのみ**（行ロケーターと同一条件）。本来はディスク機からこそ開きたい。モーダル本体はカード非依存の単一要素なので、ディスク機カードにもリンクを足すだけで拡張可能。次段。
- モーダルの体裁の最終詰め（タイトル周り・余白・冒頭一文の位置）。

### 罠#129. 中古店の棚ルールは機種でなくメディアの物理形状で決まる（2026-07-31）
「機種ごとに島が分かれ、島の中は五十音」と仮定して五十音ロケーターを2セッション積んだが、ディスク機の棚写真で覆った——ディスク機は**ジャンル順**だった。さらにフィールド写真で確定したのは、境界が「機種の新旧」でも「据置/携帯」でもなく **カートリッジ/ディスクという媒体の形**だということ。Nintendo DS はカードだが店の扱いはディスク側（ジャンル）。五十音は「死んだ」のでなく、カートリッジ機に限れば今も正しいキー（5機種で変換取りこぼし0・98%）。対策：**棚の並べ方を機種でなくメディア形状で二分する。仮定を置く前にフィールド写真で一次確認する**（棚の仕切り札は店自身が答えを書いている＝「タイトル順 さ行」）。

### 罠#130. ジャンルを「作品→棚」で持とうとすると詰む。用語辞書として持て（2026-07-31）
ジャンルを「この作品はどの棚」と1対1で載せようとして、P136のカバー率(38%)・複数ジャンル・代表QIDの選び方に延々詰まった。Koha の指摘で解けた——**1作品は複数ジャンルを持ち、店ごとに置き場が違う**（バイオハザードはアクションにもホラーにも置かれる）。だから我々が代表を1つ選んで「この棚へ」と出すと外れる。ユーザーは自分のゲームのジャンルを既に知っているので、必要なのは**英語のジャンル語→日本語の店札の翻訳**だけ。ジャンルを作品に紐付けず、**静的な英日用語辞書**として持つと、作品カバー率がこの機能と無関係になり、「35%欠損が不具合に見える」問題ごと消える。対策：**「作品→属性」で詰まったら、「属性そのものの辞書」に反転できないか考える。** ユーザーが既に持っている知識（自分のジャンル）は我々が計算し直さない。

### 罠#131. 測定の軸を間違えると、律速でない変数を熱心に測ってしまう（2026-07-31）
ジャンル解決率を「ディスク機/カートリッジ機」で割って測ったが、両群とも35%前後で有意差なし。律速は**メディアでなく「Wikidataに項目があるか」**だった（解決さえすればP136は85%付く）。「効く群に絞って測る」（→旧罠の母集団明記）の実践のつもりが、**割る軸そのものを外していた**。仮説（ディスク機はジャンル棚だから重要）に引きずられて軸を選んだのが原因。対策：**群分けして差が出なかったら、その軸が律速でないサインとして受け取り、別の軸（ここでは"項目の有無"）を疑う。** 差が出ないことも情報。

### 罠#132. CSSカスタムプロパティのスコープ外では font ショートハンドが丸ごと死ぬ（2026-07-31）
モーダル（`.card` の外）で `font: ... var(--f-display)` が全滅していた。`--f-display`/`--f-jp` は `:root` でなく `.card{}` の中だけで定義されており、`.card` 外では未解決→`font` ショートハンドが**丸ごと無効**→ブラウザ既定16pxに落ちる。色トークンは `:root` にあったので色は効き、**「それなりに見える」ため壊れに気づけない**（指定した15/13/11.5/17pxはどれも効いていなかった）。罠#12（値が間違っているのは無いより悪い）の見た目版で、フォールバックが無難だと発覚が遅れる。対策：**トークンに依存するCSSを新しい親要素（モーダル等）に置くとき、そのトークンの定義スコープ（`:root` か特定クラス内か）を必ず確認する。** 直しは当該要素に同値で再定義（`:root` は触らず影響範囲を最小化）。

### 罠#133. 縦書き要素に min-height を使うと空箱ができる。min-inline-size を使え（2026-07-31）
縦書き（`writing-mode: vertical-rl`）の札に `min-height:104px` を付けると、横書きにフォールバックした環境で中身2文字でも高さ104pxの空箱が残る。縦書きではインライン軸が縦なので、`min-inline-size` にすると **縦書き時＝札の長さの下限／横書き時＝札の幅の下限** として両モードで正しく効く。対策：**縦書き要素の寸法下限は物理軸（height/width）でなく論理軸（inline-size/block-size）で指定する。** 1つの指定が書字方向によらず意図どおり振る舞う。

### 罠#134. 同じ「MADB ID」でも C番号（作品/アニメ）と M番号（ゲーム）は別系統（2026-07-31）
P136 の解決率を測るとき、`data/wikidata_ja.json` の C番号の先頭50件で測ったら「82%解決・値はSF/コメディ/ロボットもの」と出た。だがこれは**アニメのジャンル体系**で、C番号は MADB の「作品」ID（先頭は全部アニメ）。ゲームは M番号（P7886）側で、そちらで測り直すとゲームジャンル（RPG/アクション/…）が出た。同じ `wdt:P7886` の「値」でも、C番号（作品・アニメ寄り）と M番号（ゲームパッケージ）は指すものが違う。アニメの82%をゲームの数字と誤読しかけた（罠#12方向）。対策：**「MADB ID」と一括りにせず、C番号=作品/M番号=パッケージの別を意識する。ゲームのジャンルは M番号で引く。** サンプルの中身（先頭50件が全部アニメ）を必ず目視してから率を信じる。

### 補足: 日本語データの授受化けが再発（既出・罠#119）
`genre_guide.json` を渡した際、`jp` の日本語が UTF-8→Latin-1 誤読＋0x80–0x9F欠落の不可逆な壊れ方で届いた（罠#119と同型）。Claude Code 側で「同じ壊し方を逆算して受領文字列とバイト一致するか」で復元候補を確定（14/15完全一致、残1件も 0xA0→0x20 のNBSP正規化1バイト差で確定）。**壊れたデータを推測で埋めず、逆算して裏を取ってから採用した**（罠#126の精神）。対策の再掲：**日本語を含む資材の授受は化ける前提で、受領側で UTF-8 として妥当か・意味の通る語かを確認してから使う。** 渡す側は UTF-8 保存を明示し、可能ならチャット本文へのインライン（コピペ経路は化けにくい）も併用する。

### 【結末】バリューコマース審査 不承認 → Yahoo画像は保留（2026-07-30）
- バリューコマースのメディア審査が**不承認**。理由は**非開示**（「審査結果の理由につきましてはご説明を控えさせていただいております」）。管理画面の提供も終了、バリューポイントクラブも同時見送り。ログイン不可はこの仕様どおりで障害ではない。
- 理由が出ない以上「直して再申請」は検証不能（罠#57：n=1の推測で動かない）。**バリューコマース経由は閉じたものとして扱う**。同一サイトでの再申請は通りにくいのが一般的。
- **重要な切り分け**：閉じたのは「バリューコマース経由でYahoo画像を出す」という収益化＋権利の"入口1本"のみ。**効果測定（+14%）・実装方針（カテゴリ主軸フィルタ）・Yahoo APIがローカルで動く事実は全て有効**。失っていない。
- **判断：Yahoo画像は保留棚へ。今は①楽天側の穴埋めに注力する。**
- 保留を解除する条件（再開時の選択肢。今回は着手しない）：
  - (B) バリューコマース以外でYahoo!ショッピングを扱うASPがあれば、そこで提携を取り直せるか要調査（審査は再度あり）。
  - (C) Web API利用規約・商用利用ページを条文で読み、「アフィリンクなし＋クレジット表示のみ」での商品画像表示が規約上可能かを確定（提携画面が使えないため条文直読が必要。明示が無ければ棚上げ継続）。
- ★未確定の本丸（画像の自サイト表示可否・アフィリンク併置可否・affiliate_type=vc の受理確認）は、(B)か(C)の経路が開くまで確認手段が無いため未解決のまま。
- **【2026-08-03 追記】(B)(C) は事実上クローズ。** 画像の第2ソース化は候補を洗い直した結果すべて閉じ、カバレッジ拡大自体を打ち止めにした。「2026-08-03（続き8）— 画像の第2ソース候補を追加調査 → 全滅」の節を参照。この節の測定値・実装方針は引き続き有効（失っていない）。

### 罠#135. カードの情報配置は「機能のまとまり」でなく「ユーザーの行動順」で決める（2026-08-01）
after 案（Koha が Photoshop で作った完成イメージ）に沿ってカードを再構成する中で確立した設計原則。従来カードは「identity → 型番 → コピー欄 → 店内 → スペック → 購入」と**情報の種類**で並んでいたが、日本語を読めない客の実際の店頭行動は「①タイトルと読みを確認 → ②店内を移動して棚で探す → ③見つからねば店員 → ④通販ボタンで探す → ⑤それでも無ければ型番/タイトルをコピーして他店検索」という**一本の導線**。この順にブロックを並べ替えた：スペック表を英題の上（最初に機種を確認）、店内セクションをローマ字読みの直下（②が①の続きに来る）、型番欄とコピー欄を購入ボタンの下（⑤＝最後の手段の素材）。対策：**画面の並びに迷ったら、要素を種類で群化する前に「この人は上から下に何をする順で読むか」を言語化し、その行動順を並び順に直接マッピングする。** 導線が決まれば配置は自動的に決まる。

### 知見: ジャンル早見は「棚案内」でなく「用語辞書」だから全機種で出す（2026-08-01・罠#130 の実装版）
罠#130 で「ジャンルは作品に紐付けず英日用語辞書として持つ」と決めた。その帰結として実装が変わった：ジャンル早見ボタンは当初、五十音棚ロケーター（カセット5機種限定・shelfGyo）と同じ即時関数に束ねられ、5機種でしか出なかった。だが用途は「棚のどこ」ではなく「自分の探すゲームのジャンルが日本語でどう書かれるか」の確認＝**棚の媒体形状（罠#129）と無関係**。よって shelfGyo の束から分離し、全機種で表示するようにした（ディスク機の PS2 等でも出る）。対策：**機能を別のものに束ねているとき、その機能の"本当の用途"が束ねている条件と無関係なら、条件ごと分離する。** 用途を取り違えると出現条件まで巻き添えで絞られる。

### 知見: テンプレートリテラルの塊移動は「条件分岐ごと動かし、node --check＋全分岐を実機確認」（2026-08-01）
card() 内の4ブロック（スペック表・店内・型番・コピー欄）を大きく並べ替えた。各ブロックは `${codes.length?…}` `${noCode?slab:field}` `${r[12]?…}` という条件分岐を内蔵し、テンプレートリテラルが深く入れ子（コメント内にバッククォートもある＝過去に一度 JS を壊した箇所）。安全に動かせた手順：(1) 各ブロックを**条件式ごと1つの塊**として切り取り、中身は一字も変えず位置だけ動かす。(2) 移動のたびに `node --check` で両 script ブロックの構文を確認（バッククォート対応の崩れを検出）。(3) backup との diff で「削除行に塊の中身が現れない＝純移動」を証明。(4) 実機で**条件分岐の全パターン**（型番あり/配信専用 r[12]/型番なし noCode）を開いて、分岐が移動後も正しく振る舞うか確認。node が通っても分岐の実挙動は実機でしか分からない（配信専用行で「copy to paste into a store」の文言が消える等の細かい出し分けは実機で確認）。対策：**入れ子テンプレートの大改造は「塊単位・中身不変・毎回 node --check・全条件分岐を実機で」の4点セットで進める。**

### 作業サマリ: after リデザイン＋カード行動導線再構成（2026-08-01・6コミット）
店頭ナビの UI を after 案に沿って全面改修。コミット列（古→新）：
- 0da56ac かな棚ロケーター＋ジャンル早見モーダル＋GYO 行名ひらがな化（実店舗の棚札がひらがな・行単位＝フィールド写真で確定）
- 14bc009 棚の札を2枚化（読みの頭＋行）＋上部見出しを小文字英語に統一（.micro.plain 追加）
- 6e7fab5 購入セクション見出しを小文字統一（.group-label .n）
- 78a4113 .instore 再構成（棚を主役・店員を最終手段 "Still can't find it?" に格下げ・ジャンルボタン化・角と日本語ラベル整理）
- c4421a7 上部見出しを「Japanese reading」に変更
- bb0bbdd カード情報配置を行動導線に沿って再構成（罠#135）
GYO ひらがな化は全カセット機カードの棚札照合に効く（画面の行名と実店舗の仕切り札の字形を一致させ、読めない客が照合できる）。店員導線を最終手段に格下げしても情報欠落なし（店員が読む日本語は clerkOpen 画面に全てあり、カードのボタンラベルは英語のみで可）。

### 未掃除メモ（次に index.html を触るとき回収）（2026-08-01）
- デッド CSS `.card .btn-clerk .jp`：`<span class="jp">店員に見せる</span>` を削除したため到達不能セレクタ化。害なし・掃除候補。
- コメント実態ズレ（560行付近）：`.card .instore` の枠（背景・ボーダー）を削除したため、直前コメント「配信専用では card() 側で枠ごと出さない（空枠は残らない）」の"枠"の記述が実態とズレる。挙動は不変。
- BIN_DIVER_NOTES.md にはこの記録の前段として Yahoo 画像調査ログ（未コミット）も含まれる。

### 罠#136. アフィリのAPI status:success は、着地先の正しさを保証しない（罠#109の拡張・実例確定）（2026-08-01）
Buyee(Indoleads offer 21236 / source 407997)で、getOffers も POST /tracking_links も status:success を返し、source紐付け(sharedid=gamekataban.com)も通っていた。にもかかわらず発行スラッグ 6a6994d1e0b3f の終端が Buyee ではなく TVCMall(Impact pid 1398851)に配線されていた。url= deeplink も読まれず、素スラッグと同一着地。原因は先方回答で budget cap（オファー予算上限到達で一時停止）と判明、リフレッシュ後に Buyee へ復帰。教訓: success は「APIが正常応答した」こと以上を意味しない。着地の正否は wheregoes/curl -L の final_url を実際に見るまで確定しない（罠#109）。しかもこれは自分のコードではなくネットワーク側の配線でも起きる。

### 罠#137. Buyeeリンクは「入れっぱなし」にできない — budget cap で終端が別広告主に化ける（運用・恒久監視）（2026-08-01）
罠#136 の budget cap は一度きりの事故ではなく再発しうる状態異常。予算が尽きると同じスラッグが再び別オファー(例:TVCMall)にフォールバックし、Buyeeボタンを押した海外ユーザーが無関係な広告主に飛ぶ。実装時点で正しくても、後日サイレントに壊れる。→ Buyee導線は定期の生存確認（final_urlがbuyee.jpで終わるか）を運用に組み込む。理想は自動監視だが、最低でも「Buyee側で異変report時にまず final_url を踏む」を第一手にする。

### 罠#138. 二段エンコードが正——enc()済みキーワードを含む素URL全体をもう一度enc()で包むと、i0c.xyzの1回デコードと噛み合う（型確定）（2026-08-01）
Buyee deeplinkラッパの正しい型: wrapped = "https://i0c.xyz/6a6994d1e0b3f?url=" + encodeURIComponent(素URL)。素URLは現行ロジックのまま（kwBuyee = enc(jp+' '+r[7]) を含む buyee.jp/item/search/query/…?lang=en）。この素URL全体をもう一度 enc() で包むため、キーワード部は %E3→%25E3、スペースは %20→%2520 の二段になる。これは事故ではなく仕様: i0c.xyz が url= を1回デコードして素URLに戻す前提と噛み合っている。実証は三段（8/1 curl final_url が buyee.jp検索着地 / ブラウザで73件目視 / スーパーリロード後の実クリックで mrc1…utm_content 付き着地）。罠#12回避のため机上一致(node ===)→実ブラウザ の順で確定済み。注意: 素URLの ?lang=en を含め「全体」を包むこと。キーワードだけ再enc しない。素URLの末尾クエリを外に出すと i0c.xyz 側のクエリに食われて壊れる。

（補足・訂正）4929行の「罠#84 / #85 / #98 が各2回衝突」の記録について（2026-08-01 実データ再確認）
見出し(###)定義を数え直した結果、真の番号衝突は 罠#98 のみ（3506「対照実験の対照が測りたいものを測っていなかった」/ 3721「測定サンプルの層化が母集団にない偏りを作った」の別内容2件が同番号）。#84(3)・#85(2)・#76(3) 等の複数出現は「定義1＋後続の運用追記/再発記録」であり衝突ではない。4929の記録は #84/#85 を過大読みしていた。#98衝突の番号付け替えは既存行の改変を伴うため本追記では行わず、未解決として下に登録する。

### 未解決#23. 罠#98 の番号衝突（別内容2件が同番号）— 付け替え要（2026-08-01）
BIN_DIVER_NOTES.md:3506 と 3721 が、内容の異なる別の罠にどちらも「罠#98.」の定義見出しを持つ。付け替えには (a) どちらを繰り下げるか決定、(b) 全ファイルでの 罠#98 参照箇所の洗い出しと更新、が必要で、append-only では扱えない既存行改変を伴う。専用回で処理する。今日のBuyee追記(#136-138)とは独立。

（補足・訂正）3712行の「-JPN 除去漏れ 22行」について（2026-08-02 実データ再確認）
**除去漏れは 0行。この独立発見は誤りで、実装は最初から正しかった。** 実コードは index.html:1369 の
`sgBase = (sgSrc||'').replace(/-JPN.*$/,'')` で、`.*` 付き＝**-JPN 以降を接尾辞ごと全除去**する。
ノートが根拠にした `replace(/-JPN$/,'')`（末尾ちょうどのみ削る形）は index.html に存在しない。
`-JPN` を含む replace は 1369行の1箇所のみ（grep 済み）。ノート記載の「1011行」は旧版の行番号。

実測（DATA 37,855行・型番列=index 3・2026-08-02）:
- 型番列に `-JPN` を含む行: **1,927** → 現行 `/-JPN.*$/` 適用後に残る行 **0**
- 実コード経路は `codes[0]`(`;`区切りの先頭)のみが対象。`codes[0]` に `-JPN` を含む 1,785行 → 適用後 **0**
- 仮に `/-JPN$/` だった場合の残り: 型番列まるごとで 134行 / `codes[0]` のみで 55行。
  **ノートの「22行」はこのどちらとも一致せず、出所不明。** 誤った正規表現を想定したうえに数も合っていない。
- 3713行が挙げた実例の形状内訳（`;`分割後のトークン単位）: 末尾ちょうど `-JPN` 1,879 /
  `-JPN`+ハイフン数字(`WUP-P-AQUJ-JPN-0` `AGB-AAMJ-JPN-1`) 47 / `-JPN-B0` 7 / `-JPN1` 5 / `-JPN(A)` 1。
  形状自体は実在するが、現行正規表現は全て飲み込む。

**罠#84 の実例（実装が先に正しかったのに、ノートだけが「未着手の発見」として残り続けた型）。**
この「除去漏れ22行」は 3771 / 3855 / 3909 行でも「独立発見4件」の1つとして繰り返し引き継がれており、
**誤りが3回転記された。** 実コードを1回 grep すれば止まった。数字を引き継ぐ前に現物を見る。

（補足・訂正）未解決#23 は解決。行番号のズレと、付け替え後の番号（2026-08-04）
- **行番号の訂正**: 上の本文が書く「3721」は**現在 3723行**（その後の追記で2行下がった）。
  片方（3506）は変わらず。以後この衝突を指すときは **3506 / 3723** を使う。
- **決着**: 後から定義された **3723行のほうを 罠#98 → 罠#145 に繰り下げ**、3506行が #98 を保持する。
  3723行の見出し直下にも同趣旨の補足を追記済み。
- **新番号の根拠**: 全ファイル（BIN_DIVER_NOTES.md / index.html / build_master.py / swap_data.py）で
  `罠#[0-9]+` を集計した使用済み最大値 **144** の次。#80/#81 は `###` 見出しではなく箇条書き
  （2726/2729行）で定義されているが、**使用済みとして数えている**（空き番号ではない）。
- **参照の更新は不要だった**: `罠#98` の出現は 3506 / 3723 の定義2件と 5127 / 5130 の記述のみで、
  層化バイアス側を番号で引用している箇所は他ファイルを含め0件（grep 済み）。
- append-only のため既存行（この見出し・本文・3723行の見出し）は一切書き換えていない。

**副次発見（掃除候補・別作業）:** index.html:1364 の `kwCode = enc(codes[0]||'')` は型番を**無加工**で持ち、
1464行の `buyBlock({...kwCode...})` に渡されるが、描画側で参照しているのは 1325行の `kwCodeSg` のみで
`kwCode` はどこからも使われていない**デッドな受け渡し**。出力への影響は無し。削除は別回で。

（補足・訂正）3716行の「Ⅶ/Ⅱ 字形問題（finalfantasy7 が FF本編に届かない）」について（2026-08-02 実データ再確認）
**検索到達は解消済み。** index.html:923-931 の `romanToArabic`（run単位の整数解釈・減算則あり）が
index.html:939 の `fold` の**先頭で必ず走る**。索引側(HAY 944 / HAY4 952 / RJ 959)もクエリ側
(index.html:1517-1520 の `fold(accent(q.value))` / `squash(q.value)`)も同じ関数を通るので**対称**。
実測: fold 適用後に U+2160–217F が残る行は **0**。実例 `Final fantasy Ⅶ remake` は
squash で `finalfantasy7remake` になり、クエリ `finalfantasy7` は L4レーン(index.html:1539・床5文字)で当たる。

**ただしこの件は「実装が先に正しかった」のではなく、同一ノート内で既に記録済みだった。**
3858行以降「2026-07-27 全角ローマ数字を検索で十進化・公開済み」が実装と測定を残しており、
3866行の「97行」は今回の実測と完全一致する。にもかかわらず 3716 の独立発見リストは「未着手」のまま残り、
3772 / 3855 / 3909 行でも「Ⅶ字形」として引き継がれた。**罠#84の変種＝解決済みと未着手が同じノートに併存し、
古い側だけを読むと未着手に見える。** 先の -JPN 除去漏れ（実装が最初から正しかった型）とは原因が違う。

射程の実測（DATA 37,855行・2026-08-02）— 3866行の97行を列ごとに分解:
- 全角ローマ数字を含む行は重複排除で **97行**（3866行の数字と一致）
- うち **row[0](英題)が非空なのは 4行だけ**。row[1](和名)のみが **92行**＝日本語名しか無い行で英語検索の主対象外。
  row[1]のみに含み row[0]は非空(英題側は既に算用数字)が 1行（Xenosaga Episode I / ゼノサーガ エピソード Ⅰ）。
- **仮に未変換のままでも、英題検索への影響は 4行だった。** 字形内訳: Ⅱ39 / Ⅲ21 / Ⅹ10 / Ⅰ10 / Ⅴ7 /
  Ⅳ6 / Ⅵ5 / Ⅶ5 / Ⅸ2 / Ⅷ1 / Ⅺ1。小文字 ⅰ〜ⅹ(U+2170–)と Ⅻ は **0件**（対応不要）。

`strong`(index.html:970)だけは romanToArabic を通さず、NFKC 互換分解で Ⅶ→`vii` / Ⅹ→`x` と**ラテン文字**になる。
ただし用途は AMBIG_EN（英題の曖昧さ注記）専用で検索には非関与。**不整合ではないので直さないこと。**

**副次発見（表示・別作業）:** `Super Momotaro Dentetsu Ⅲ` `Super Momotaro Dentetsu Ⅱ` 等の4行は
**英題が全角ローマ数字なのに和名が算用数字**という列間の字形逆転（例: 英題 Ⅲ / 和名「スーパー桃太郎電鉄 3」）。
検索到達には無影響（foldで両方3になる）だが、カードの英題表示に Ⅲ がそのまま出る。
これは 3909-3910 / 3987 行が既に登録済みの「Ⅶ の DATA 側字形修正＝表示は Ⅶ のまま・別作業」と**同じ枠**。
新規発見として二重登録しないこと。今回の追加情報は「表示側の実対象は英題4行に絞れる」という母数だけ。

---

## 2026-08-02 — 駿河屋の括弧付き型番: d群84行は全滅と判明・中身で分岐が要る（目視測定で確定）

### 3714行「括弧正規化 9行」の母数訂正
**「9行」は母数が誤り。** 実測（DATA 37,855行・型番列=index 3）:
- **codes[0](`;`区切りの先頭)に括弧を含む行 = 97行**
- **型番列全体のどこかに括弧を含む行 = 115行**（2本目以降だけに括弧が18行）
- 括弧の種類: 半角`(` 112行 / 全角`（` 3行（うち2行は `（JPN)` ＝開き全角・閉じ半角の混在）

うち **HAC-系11行は index.html:1371 で既に処理済み**（2026-07-27 実装・4189-4222行に記録あり）。
3714行が「未着手の独立発見」として残っていたのは**罠#84の型**。-JPN除去漏れ・ローマ数字字形に続き3件目。
なお 4192行の「母集団115行」は型番列全体の数と一致しており、そちらは正しかった。

### 97行の行方（index.html:1368-1371 を実際に適用して追跡）
| | 分類 | 行数 |
|---|---|---|
| a | sgBase 到達前に 1369行の `/-JPN.*$/` で括弧ごと消えた | 2 |
| b | HAC- かつ 1371 の括弧除去がカバー | **11** |
| c | HAC- だが括弧が残った | **0** |
| d | **HAC-以外で括弧が残ったまま駿河屋へ送られる** | **84** |

### ★新事実: d群84行は現行リンクで全行0件（全滅）
**目視測定で確定（2026-08-02）。** 括弧を含んだ文字列がそのまま検索語になり、1行も当たっていない。
1369行の空白→ハイフン化は括弧の内側にも効くため `(SLPM 66364)` は `(SLPM-66364)` に変形したうえで
括弧付きのまま送られる。4222行「N64/GBA等の他機種の括弧行は未測定」の宿題が、ここで測定された。

### d群84行の分類と勝ちパターン（すべて目視の到達に基づく・件数ではない）
84 = a群11 + b群72 + 例外1。

- **a群 11行 — 括弧内が別体系の型番**（SLPM 9 / PLJM 1 / ULJM 1）
  → **内側採用で当たる。** 例 `VW314-J1(SLPM 66364)` は `SLPM-66364` が正。
  外側(`VW314-J1` 等)は販売元の独自採番で駿河屋は索引していない。
- **b群 72行 — 括弧内が地域・版マーカー**（JPN 71 / USA 1）
  → **外側除去で当たる。** HAC-系11行で確認済みのパターンと同じ。
- **例外1: `SLPS 02177 (PS-0013)`（LAKE MASTERS PRO 日本縦断黒鱒紀行・PS・ダズ）**
  → **外側採用で当たる。** 外側 `SLPS-02177` が本物のPS1規格品番で、内側 `PS-0013` はダズ社の自社番号。
  **a群と内外が逆向き。** 字面は「英字+数字」でa群と同型に見えるため、形だけで分岐すると誤る。
- **例外2: `RVL-006(USA)`（DRAGON QUEST SWORDS・Wii）**
  → **どの変換でも×。** `RVL-006` はWiiのハード規格品番で特定ソフトを指さない。
  **1368行の `WUP-006(JPN)` と同型**（あちらは codes[1] 切替で回避済み）。
  → **変換対象から除外・現状維持**とする。触ると別商品に飛ぶ側の事故になる。

### 実害の範囲
**d群84行は全行 row[7] あり**（row[7]空は0行）。つまり Buyee 導線が全行生きており、
駿河屋が×でも購入経路は死んでいない。4192行の実害定義（row[7]空でprimary）では**実害0**。
HAC-系11行を直したときとは状況が違う（あちらは row[7]空＝primaryだった）。
**実害は限定的だが、到達改善の価値そのものは有る**（84行は現に全滅している）。

### 回帰対照8行 — 変更前の到達（実測・2026-08-02）
括弧処理を変更したら、この8行が変更前と同じ結果になることを確認する。
| 型番列 | kwCodeSg | 変更前 |
|---|---|---|
| `HAC-P-ADALA(JPN);TRA-HAC-ADALA-JPN` | `HAC-P-ADALA` | ○ |
| `HAC-P-AAACA(JPN);TRA-HAC-AAACA-JPN-1` | `HAC-P-AAACA` | ○ |
| `WUP-006(JPN);WUP-P-AZAJ-JPN-0`（1368行の特例分岐） | `WUP-P-AZAJ` | ○ |
| `SHVC-AO5J-JPN` | `SHVC-AO5J` | ○ |
| `SLPS 00533` | `SLPS-00533` | ○ |
| `AGB-P-BNGJ` | `AGB-P-BNGJ` | ○ |
| `ULJM 08002` | `ULJM-08002` | ○ |
| `CTR-P-AXXJ` | `CTR-P-AXXJ` | **×（健全な型番だが元から未到達）** |

★**CTR-P-AXXJ は実装後も × が正。** 括弧とは無関係の別要因なので、
ここが×のままでも回帰ではない。**逆にここが○に変わったら、意図しない副作用を疑う。**

### 別扱い・宿題（今回スコープ外・未検証）
- **CTR(3DS)の型番が駿河屋で当たらない件。** 対照 `CTR-P-AXXJ` が×。3DS全体の問題か個別かは未測定。
  括弧処理とは独立。触るなら機種ごとに実表記を実検索してから（4222行と同じ作法）。
- **二重ハイフン2行**: `AGB--P-B9AJ`（くにおくん 熱血コレクション 1）/ `DL--DOL-GMSJ-JPN`（スーパーマリオサンシャイン）。
  **DATA原文の時点で `--`**（1369行の変換が作ったものではない。1369行コメントの「二重ハイフン0」は
  空白→ハイフン化が二重を作らないという別の検証で、そちらは正しい）。括弧ともd群とも無関係。

### 罠#139. 「一律除去」は、括弧の中身が2種類あることを見落としていた
括弧付き型番に**一律の括弧除去**をかけると、**a群11行（内側が正）と `SLPS 02177 (PS-0013)`（外側が正）を壊す。**
外側除去が正しいのは b群72行だけで、**中身が「別体系の型番」か「地域マーカー」かで分岐が要る。**
しかも `SLPS 02177 (PS-0013)` は字面がa群と同型（英字+数字）なのに内外が逆＝**形だけの判定では分離できない。**
さらに `RVL-006(USA)` は外側がハード規格品番で、どの変換でも当たらない＝**除外すべき第3の型**。
★**これが見えたのは、件数ではなく到達（1ページ目に目的の物があるか）を目視で測ったから。**
件数だけなら「括弧を消せば非0になる」で一律除去に進み、a群を壊していた。**罠#95の反省が効いた実例。**
罠#87（店ごとに測る）・罠#12（机上一致で終わらせない）と同じ家系。

### 罠#140. 「-P-落ち」をSFC固有と決めつけていた — GBAにも及ぶ（第1段階の実機確認中に発見・2026-08-02）
4199-4200行は SHVC-P-AVRJ(JPN)→0 / 括弧除去も0 / 実表記 SHVC-AVRJ(-P-落ち)で6件 と記録し、
4218行で「**SFC系 SHVC-P-…** は -P- の処理が要る」と**機種固有の宿題**として切り離していた。
b群括弧除去(第1段階)の実機確認中に、**同じ現象がGBAでも起きていることが実測で判明した。**

実測（2026-08-02・生URLを直接取得）:
| 検索語 | HTTP | 件数 | 商品名 |
|---|---|---|---|
| `AGB-P-FSOJ` | **404** | **0** | — |
| `AGB-P-FSOJ-JPN` | 404 | 0 | — |
| `AGB-P-FSOJ(JPN)` | 404 | 0 | — |
| **`AGB-FSOJ`(-P-落ち)** | **200** | **1** | **スターソルジャー** |
| `FSOJ`(コードのみ) | 200 | 1 | スターソルジャー |
| `NUS-NSIJ`(対照) | 200 | 3 | 不思議のダンジョン 風来のシレン2～鬼襲来!シレン城!～ 他 |

DATA row[1] は `STAR SOLDIER`、駿河屋の商品名は `スターソルジャー`＝**同一作品**（罠#93のカタカナ↔ラテン）。
→ **-P-落ちは SHVC 固有ではない。AGB でも同じ。** 「機種ごとに実表記を実検索」(4222行)の作法は
正しかったが、**1機種で見つけた規則を「その機種の問題」と型付けした**のが誤り。
規則そのものは機種横断で効いている可能性がある。**次に測るときは他機種にも同じ形を当ててから型を決める。**

### 副産物: 駿河屋は0件のとき HTTP 404 を返す
ヒット時は200。**件数の代理指標として使える**（0件判定にHTMLを読む必要がない）。
★ただし**代理指標は「0件かどうか」までしか言わない。** 200が返っても目的の物とは限らないので、
到達（1ページ目に目的の物があるか）の判定は従来どおり目視（罠#95・罠#93）。

### af_jump は search_word を変質させない（切り分け済み・疑いを晴らした記録）
`AGB-P-FSOJ` が0件の原因を af_jump の二重エンコード等に疑ったが、**af_jump は無関係だった。**
- af_jump 経由の final_url は `search_word=AGB-P-FSOJ` のまま無傷。302を1回返すだけ。
- **生URLでも同じく404・0件**。af_jump経由(0件)と生URL(0件)が完全一致。
- 対照 `NUS-NSIJ` は af_jump 経由・生URLとも200で、取得できた商品名3件も完全一致。
- 唯一の差異: af_jump は自前UTM4本の後ろに**自分のUTM4本を追記**する（`utm_source=` `utm_content=` は空値）。
  これは #1 #2 の両方で同じに起き、#1は200なので**0件の原因ではない**。
★罠#136（success≠着地正否）を受けて final_url を実際に見た結果、**今回は配線が正常だと確認できた。**
疑いを晴らした記録も残す。次に同じ疑いが出たとき再調査しないため。

### 前回の目視測定の誤差事例（断定はしない）
第1段階の実機確認リストで #2 STAR SOLDIER が「生URLでは当たる」と見えていたが、
再測定では生URLも0件だった。`AGB-FSOJ`(1件・在庫あり)は現在も生きているため**在庫変動では説明できない。**
→ `AGB-P-FSOJ` と `AGB-FSOJ`(-P-落ち) の**取り違えの可能性が高い**。ただし記録が無いので**断定はしない。**
★教訓: 目視測定では**打った検索語そのものを結果と一緒に記録する。** 型番が1文字違いで並ぶと取り違える。

### 宿題: -P-落ちを扱う段階（第1段階のスコープ外）
- **SFC(SHVC)とGBA(AGB)の両方に効く形**で設計する。片方だけ直すと同じ発見をもう一度することになる。
- 他機種（NUS/RVL/NTR/CTR等）にも `-P-` 形があるか、**機種ごとに実表記を実検索してから**型を決める（4222行の作法）。
- 第1段階（括弧除去）とは別レイヤー。**括弧除去は #2 でも正しく動いており、0件→0件で悪化はしていない**
  （`AGB-P-FSOJ(JPN)` も `AGB-P-FSOJ` もどちらも404）。混ぜて評価しないこと。

---

## 2026-08-02（続き）— 括弧付き型番 第2段階完了: a群内側採用 + SLPS-02177 外側特例

### 到達状況
- **b群71行**（第1段階 `0db1e5d`）: 括弧内が地域マーカー `(JPN)` → 外側除去
- **a群11行**（第2段階 `84d725a`）: 括弧内が別体系型番 `SLPM/PLJM/ULJM` → 内側採用
- **SLPS-02177 の1行**（同 `84d725a`）: 外側が本物の規格品番 → 外側採用の1行特例
- → **d群84行のうち83行が到達可能な形になった。** 残る1行 `RVL-006(USA)` は**恒久除外**
  （外側 `RVL-006` はWiiのハード規格品番で特定ソフトを指さない。1368行の `WUP-006` と同型。罠#27）。

### 罠#141. 「触らなければ元の値のまま」ではない — 手打ちの目視値と実装が生成する値は別物
**SLPS-02177 は当初「sgParen で手つかず(`return s`)にすれば外側 `SLPS-02177` になる」と設計したが、誤りだった。**
`sgBase` は 1369行の空白→ハイフン化を**既に通っている**ため、実際の値は `SLPS-02177-(PS-0013)`。
`return s` はこの**404の壊れた形をそのまま返す**。「触らない＝安全」ではなかった。

| | 値 | 駿河屋 |
|---|---|---|
| 目視測定で手打ちした値 | `SLPS-02177` | 200 / 3件・到達○ |
| **実装が実際に生成する値** | **`SLPS-02177-(PS-0013)`** | **404 / 0件** |

→ 明示的な括弧除去の特例が必要だった。**「現状維持」と書いたとき、その"現状"が何の値かを実装で確かめる。**

★**これは #2 STAR SOLDIER と同じ型の2回目。** あちらも「生URLで当たる」と目視した値(`AGB-FSOJ`)と、
実装が送る値(`AGB-P-FSOJ`)が別物だった（罠#140）。**2回とも、目視で打った文字列と実装の生成値がズレていた。**
- 対策: **目視測定の表には「実装が生成する値」を書く。** 手で打ちやすい形を書くと必ずズレる。
- 対策: 空撃ちで `old→new` を出すとき、**old 側が本当に今ライブで送られている値か**を確認する。

### a群の判定を接頭辞ホワイトリストにした理由
採用: `/^(SLPM|PLJM|ULJM)-?\d+$/`（接頭辞の完全一致）
- **字形判定 `/^[A-Z]{2,4}-?\d{4,6}$/` は不採用。** 内側 `PS-0013` を巻き込み、外側が正の
  SLPS-02177行を壊す（実データ13行を全数確認）。罠#90「完全一致で持て」と同じ。
- **`-?` は必須。** `SLPM74414` / `PLJM16159` の2行は DATA原文が最初から空白なしで、
  1369行のハイフン化が働かない。ハイフン必須で書くとこの2行を取りこぼす。

### SLPS特例の判定キーに sgBase を採った理由
`code` 列の `"SLPS 02177 (PS-0013)"` も同じ1行を一意に指す（両方とも実データで一意を確認済み）。
それでも **sgBase 側を採った**のは、`sgParen` を**引数だけで決まる純関数**に保つため。
そうすれば**全数空撃ちで測った挙動が、そのまま本番の挙動になる**。`code` で判定すると
`sgParen` が外側スコープに依存し、関数単体では検証できなくなる。
（1368行が `code` を見ているのは、あの時点で sgBase がまだ無く「どのコードを源にするか」を
選ぶ処理だから。層が違うので揃える必要はない。）

### 内側 PS-0013 を採らなかった判断の裏付け（実測）
`PS-0013` で検索すると **200 / 692件**。ASTRO A20ヘッドセット・LCDモニター・PS3 the Best 各種が並び、
目的の物は見当たらない。**内側採用していたらノイズに沈んでいた。** 字形が a群と同じ「英字+数字」なのに
中身の意味が違う、という判定の難しさが実測で裏付けられた。

### ★未 push・ライブ未反映
第1段階 `0db1e5d` / 第2段階 `84d725a` とも**コミット済みだが push していない。**
**本番サイトにはまだ何も反映されていない。** 現在ライブで動いているのは括弧付きのままの旧挙動。

### 残る宿題
- `-P-` 落ち（SFC/GBA両方に効く形で・機種ごとに実表記を実検索してから。罠#140）
- CTR(3DS)の型番が駿河屋で当たらない件（対照 `CTR-P-AXXJ` が×）
- 二重ハイフン2行 `AGB--P-B9AJ` / `DL--DOL-GMSJ`（DATA原文の時点で `--`）
- a群11行・SLPS-02177 の**実機af_jump経由での到達確認**（生URLでの確認は済み）

（補足・訂正）d0f608a の「★未 push・ライブ未反映」節は失効（2026-08-02 デプロイ完了）
あの節は「第1段階 0db1e5d / 第2段階 84d725a とも push していない・本番は旧挙動」と書いていたが、
**同日中にデプロイしたため失効した。** 現在の本番は括弧処理入りの新挙動。

**デプロイ方式**: `gh` の contents API（`--input` で JSON ペイロードを渡す方式）。
remote 未設定で `git push` は使えないため、これが唯一の経路（4917行の作法どおり）。
8MB の index.html は base64 で 10.7MB になり、引数渡しではシェルの引数長上限に触れる恐れがあるので
`--input /tmp/payload.json` を使った。`sha` は PUT の直前に取得して渡す（衝突検出のため）。
取得した sha は変更直前のローカル blob と一致し、**他者の割り込み更新が無いことを確認してから上げた。**

**反映の検証（すべて実測）**
| 項目 | 値 |
|---|---|
| GitHub commit | `ea7c4a96bda2fddcb3372aae90b0efd90d5db0b5` |
| blob sha | `f1482ae90aba477877fe9f0a308c4478aa64c476`（ローカルHEADと完全一致＝転送は無損失） |
| ライブ md5 | `daad663cb1277894a1051936fd4786dd`（ローカルと一致） |
| ライブ側 DATA行(901) | `2dacc3ca6e65aec4d7e31ea329fa7eb5` **無傷** |
| ライブ側 head区間(1..755) | `0831b69454119c8ae641f50d952b5ee9` **無傷** |
| Pages ビルド | `status: built` / commit `ea7c4a96` / error null |
| CNAME | `gamekataban.com` 健在（index.html のみ PUT なので無傷） |
| 反映までの時間 | **30秒**（30秒間隔のポーリング1回目で一致） |

★**md5 一致だけで合格にせず、ライブ実物を grep して中身を直接確認した**（罠#136: success≠着地正否）。
`sgParen` が4回出現し、`if(s === 'SLPS-02177-(PS-0013)') return s.replace(...)` の特例行も現物に存在。

**本番での実機確認（af_jump 経由・宿題を消化）**
| ゲーム | DATA型番列 | 実装が生成する値 | 到達 |
|---|---|---|---|
| プロ野球スピリッツ3 | `VW314-J1(SLPM 66364)` | **`SLPM-66364`** | ○ |
| LAKE MASTERS PRO 日本縦断黒鱒紀行 | `SLPS 02177 (PS-0013)` | **`SLPS-02177`** | ○ |

★**「実装が生成する値」を表に併記した**（罠#141 の対策そのもの）。手で打ちやすい形を書くとズレる。
上記2値はライブ実物の DATA とロジックから再計算したもので、本番のリンクが実際に送る文字列。
**罠#141（目視値≠実装生成値）に2回当たった案件を、本番の着地まで踏んで閉じた。**

**ローカル / GitHub の SHA 対応**（4917行の作法。remote 未設定のため常に別系統）
| 作業 | ローカル | GitHub |
|---|---|---|
| 第1段階（b群71行 外側除去） | `0db1e5d` | — |
| 第2段階（a群11行 内側採用 + SLPS特例） | `84d725a` | — |
| **公開（両段階まとめて1回）** | HEAD `d0f608a` の index.html | **`ea7c4a96`** |

**結論: 括弧付き型番の到達修正は完結。** d群84行のうち **83行が到達可能**な形になり、
`RVL-006(USA)` の1行のみ**恒久除外で旧挙動を維持**（外側がハード規格品番で特定ソフトを指さないため。罠#27）。
3714行の独立発見「括弧正規化 9行」は、母数訂正（97行/115行）→ 全滅の測定 → 分類 → 2段階実装 → 公開まで到達して閉じた。

**なお残る宿題**（括弧とは別レイヤー・今回未着手）
- `-P-` 落ち（SFC/GBA両方に効く形で。罠#140）
- CTR(3DS)の型番が駿河屋で当たらない件
- 二重ハイフン2行 `AGB--P-B9AJ` / `DL--DOL-GMSJ`（DATA原文の時点で `--`）

## 2026-08-02（続き2）— 型番 `-P-` 落とし: 全数対照255行で回帰0・二重ハイフンガード付きで公開

括弧付き型番の案件（5173行・5301行）で「なお残る宿題」に挙げた `-P-` 落ちを、
**測定 → 目視レビュー → 実装 → 公開 → 本番確認**まで通して閉じた。

### 母集団の確定 = 255行（当初 253 からの訂正）

最初に数えたときは DATA の型番列（行配列 index3）の `codes[0]` **生値**に `/-P-/` を当てて **253行**とした。
実装を入れる直前の全数空撃ちで「値が変わる行 = 254」と出て**数が合わず**、洗ったところ2行の取りこぼしが判明した。

| DATA行 | 機種 | 型番列の原文 | `sgBase` を通った後 |
|---|---|---|---|
| 1894 | Nintendo DS | `NTR P AJRP;NTR-AJRP-UKV` | `NTR-P-AJRP` |
| 10292 | Nintendo 3DS | `CTR P AL8J;LNA-CTR-AL8J-JPN` | `CTR-P-AL8J` |

この2行は**原文が空白区切り**で、`sgBase` の `.replace(/ *- *| +/g,'-')`（空白のハイフン化）を経て
**初めて `-P-` になる「隠れ -P-」**。原文で数えると絶対に引っかからない。

★**母集団は `codes[0]` の生値ではなく `sgBase`（＝実装が実際に持っている値）で数える。**
`sgBase` は `-JPN` 以降の除去と空白のハイフン化を済ませた後の値で、ルールが作用するのはこの値に対してだから。
生値で数えるのは「DATA を見て数えている」であって「実装が触る対象を数えている」ではない。
測定の母集団は、ルールが挿入される場所の**直前の値**で取ること。
（これは罠#141「目視値≠実装生成値」の母集団版だが、**新しい罠番号は付けない**。
罠#84/#85/#98 の重複が未整理（5117行）で、番号付与は重複棚卸しの専用回に回す。ここではプローズで残す。）

### 全数対照（255行・af_jump/UTM 無しの生URL・自己クリック回避）

現行形（`sgParen(sgBase)`＝そのときライブが送っていた文字列）と、そこから `-P-` を落とした提案形を、
1行ずつ2形とも実検索して HTTP ステータスとヒット件数を記録した（2.5秒間隔・ユニーク498リクエスト）。
0件のとき駿河屋は **HTTP 404** を返す。ヒット時は `件数:N件中` から件数を取った。

| 機種 | 行数 | 404→200 | 件数増 | 不変 | 200→404 | 件数減 |
|---|---:|---:|---:|---:|---:|---:|
| Super Famicom | 75 | 15 | 0 | 60 | 0 | 0 |
| Game Boy Advance | 63 | 4 | 1 | 58 | 0 | 0 |
| Nintendo 64 | 28 | 3 | 2 | 23 | 0 | 0 |
| Wii U | 19 | 4 | 6 | 9 | 0 | 0 |
| Nintendo DS | 18+1 | 2 | 0 | 17 | 0 | 0 |
| Wii | 15 | 1 | 0 | 14 | 0 | 0 |
| Nintendo Switch | 14 | 0 | 1 | 13 | 0 | 0 |
| Virtual Boy | 11 | 2 | 0 | 9 | 0 | 0 |
| Nintendo 3DS | 5+1 | 1 | 0 | 5 | 0 | 0 |
| GameCube | 4 | 0 | 1 | 3 | 0 | 0 |
| Game Boy | 1 | 0 | 0 | 1 | 0 | 0 |
| **合計** | **255** | **32** | **10** | **212** | **0** | **0** |

（「+1」は上記の隠れ -P- 2行。件数増10行は二重ハイフンガード対象の1行を除いた数。）

★**回帰は0行**。200→404 も件数減も、255行のどこにも出なかった。

**件数増10行**は「現行でも当たっているが件数が増える」＝目的の物が1ページ目から押し出されるリスクがある側なので、
増分の大きい順に並べた目視レビュー表を作って現行形・提案形の両URLを開き比べた
（`_experiments/pdash_review_20260802.html`・404→200 の32行と合わせて41行）。
増分が最大の4行 **ムジュラ 3→7件 / ドンキーコンガ 2→6件 / マリオオデッセイ 3→4件 / どうぶつの森 1→4件** を含め、
**増えた分はすべて目的タイトルの状態違い**（箱説の有無・版・同梱違い等）で、無関係商品の混入は無いことを目視で確認した。

### 二重ハイフンガード — 特定型番のハードコードにしない

```js
const sgPdash = /--/.test(sgBase) ? sgBase : sgBase.replace(/-P-/g,'-');
```

`AGB--P-B9AJ`（くにおくん 熱血コレクション1・DATA原文の時点で `--`）だけは、
`-P-` を落とすと **`AGB--B9AJ`** という壊れたトークンになり **116件 → 1320件** に膨張した。
増えた分は無関係な商品で、**誤った値より無いほうがまし**（罠#27）。件数増の他10行が +1〜+4件なのと性質がまるで違う。

★**`s === 'AGB--P-B9AJ'` のハードコードにしなかった。** `--` の有無で判定すれば、
将来 DATA に二重ハイフン行が増えても**自動で安全側（現状維持）に倒れる**。
全数空撃ちで **`--` を含む行は2行・そのうち値が変わった行は0行**を確認（もう1行は `DL--DOL-GMSJ`、
`-P-` を含まないので元から対象外）。

### 測定は sgParen の「後」・実装は「前」— 順序差を全数で潰した

測定スクリプトは `sgParen(sgBase).replace(/-P-/g,'-')`（**後置**）で提案形を作ったが、
実装は `sgParen(sgPdash)`（**前置**）。この順序差で結果が変わりうる。
括弧処理が絡む行（例 `HAC-P-ADALA(JPN)`）では、`-P-` を落とすタイミングで
`SG_INNER` / `SG_REGION` の判定に入る文字列が変わる可能性があるため。

★**全37,855行で前置と後置の結果を突き合わせ、不一致0行**を確認してから実装位置を決めた。
「測ったのと違う順序で実装した」まま出さない。

### 発見: 3DS の宿題が全滅ではなかった

5405行の宿題「CTR(3DS)の型番が駿河屋で当たらない件」は、**全滅ではない**と切り分けが進んだ。

```
CTR-P-AL8J  404/0  →  CTR-AL8J  200/1   （NINTENDO 3DS Guide LOUVRE）
```

3DSで `-P-` を含むのは6行（隠れ -P- 1行を含む）で、そのうち1行がこれで救えた。
**「CTRが当たらない」の一部は `-P-` 由来だった**＝ここは今回の実装で解消済み。
残る CTR の当たらなさは別レイヤー（型番体系そのものが駿河屋の実表記と違う可能性）で、宿題としては継続。

### 実装位置とハンク

`sgBase` が確定した直後・`sgParen` に渡る前に `sgPdash` を1箇所だけ定義し、
`kwCodeSg = enc(sgParen(sgPdash))` に変更。**18 insertions / 2 deletions・4ハンク**。

`sgBase` の使用箇所は `enc(sgParen(sgBase))` の1箇所だけと grep で確認済み（他はコメント）。
そこから先は 1325行の `sgAf(k.kwCodeSg)`（コード検索リンク）に流れるだけで、
タイトル検索側の `kwKana` 系には一切影響しない。

SLPS-02177 特例のコメント「★判定は sgBase の完全一致」は、`sgParen` の引数が `sgBase` から `sgPdash` に
変わったため**連動して修正**（`SLPS-02177-(PS-0013)` にはそもそも `-P-` が無いので値は不変＝挙動は変わらない）。
コメントだけ古い前提のまま残すと、次に読む人が誤読する。

### デプロイ検証値

前回（ea7c4a96）と同じ `gh` の contents API 経路。PUT 直前に現在の sha を取得し、
それがローカル HEAD~1 の blob `f1482ae9…` と一致すること＝**他者の割り込み更新が無いこと**を確認してから上げた。
ペイロードは `--input /tmp/payload.json`（10,763,159 bytes）。`index.html` のみ PUT・CNAME には触れていない。

| 項目 | 値 |
|---|---|
| ローカル commit | `69cf5e6` |
| GitHub commit | `9db1efe979cd703855dd2f8af66b3e28a45e7e45` |
| blob sha | `48bfb08fa81726f5f91f578209bb4f4e3609e21b`（ローカルHEADと完全一致＝転送無損失） |
| ライブ md5 | `b9456d6bf707c0374721000b3bfd37cd`（ローカルと一致） |
| ライブ側 DATA行(901) | `2dacc3ca6e65aec4d7e31ea329fa7eb5` **無傷** |
| ライブ側 head区間(1..755) | `0831b69454119c8ae641f50d952b5ee9` **無傷** |
| Pages ビルド | `status: built` / commit `9db1efe9` / error null |
| CNAME | `gamekataban.com` 健在 |
| 反映までの時間 | 20秒間隔ポーリングの1回目 |

★**CNAME の確認方法**: `https://gamekataban.com/CNAME` を直叩きすると **404** が返るが、これは異常ではない。
Pages は CNAME を**設定として消費し、ファイルとしては配信しない**。健在の確認は
`gh api repos/…/pages`（`cname: gamekataban.com` / `status: built`）とリポジトリ内 CNAME ファイル（sha `11f5f0eb…`）で取る。
404 を見て「CNAME が消えた」と誤判定しないこと。

★**md5 一致だけで合格にしていない**（罠#136）。ライブ実物を grep して
`const sgPdash = /--/.test(sgBase) ? sgBase : sgBase.replace(/-P-/g,'-');` が 1383行に存在することを確認し、
さらに**ライブの index.html から実処理を切り出して実行**して送信値を再計算した。

| ゲーム | DATA型番列 | ライブが実際に送る値 |
|---|---|---|
| ムジュラの仮面 | `NUS-P-NZSJ` | **`NUS-NZSJ`** |
| New SUPER MARIO BROS.U DELUXE | `HAC-P-ADALA(JPN);TRA-HAC-ADALA-JPN` | **`HAC-ADALA`** |
| NINTENDO 3DS Guide LOUVRE | `CTR P AL8J;LNA-CTR-AL8J-JPN` | **`CTR-AL8J`** |
| くにおくん 熱血コレクション1 | `AGB--P-B9AJ` | **`AGB--P-B9AJ`**（ガード作動・不変） |

ライブで送る値に `-P-` が残る行は**1行のみ**（ガード対象のくにおくん）。ライブ DATA も 37,855行で無傷。

**本番実機で押される完全アフィリンク**（ムジュラ・af_jump + UTM + `NUS-NZSJ`）:

```
https://affiliate.suruga-ya.jp/modules/af/af_jump.php?user_id=5357&goods_url=https%3A%2F%2Fwww.suruga-ya.jp%2Fsearch%3Fcategory%3D2%26search_word%3DNUS-NZSJ%26utm_medium%3Daffiliate%26utm_source%3Dgamekataban.com%26utm_content%3D%252F%26utm_campaign%3Dafid5357
```

★この文字列は**ライブ実物の DATA とロジックから再計算したもの**（罠#141 の対策。手打ちの目視値は使わない）。
到達確認は**実機でのホバー確認**であり、自分でクリックはしていない（罠#109・自己クリック回避）。

### ★この節で `-P-` 落ち宿題は解決

5352行・5404行の宿題「`-P-` 落ち（SFC/GBA両方に効く形で・機種ごとに実表記を実検索してから。罠#140）」は
本節で解決した。SFC/GBA だけでなく **N64・WiiU・DS・Wii・Switch・VB・GC・3DS・GB を含む10機種255行の全数対照**で
機種別に実表記を測ってから実装しており、罠#140 の「SFC固有と決めつける」条件も満たしている。
（既存の宿題行への打ち消し線は**この追記では入れない**。追記と既存行の変更は承認経路が違うため、別作業とする。）

## 2026-08-02（続き3）— 二重ハイフン2行: 同型の壊れ方だが remedy は正反対・DATA原文を初めて修正

5354行・5406行の宿題「二重ハイフン2行 `AGB--P-B9AJ` / `DL--DOL-GMSJ`」に着手した。
机上調査 → 候補測定 → 片方だけ実装・公開まで進み、**もう片方は別の族の問題だと判明して移管**した。

### ★DATA 行 md5 の基準を更新（旧基準は失効ではなく「ここまで」）

| 区間 | 旧基準 | 新基準（2026-08-02 以後） |
|---|---|---|
| **DATA 行(901)** | `2dacc3ca6e65aec4d7e31ea329fa7eb5` | **`8bd747412be22b2ec93b692185a62551`** |
| head 区間(1..755) | `0831b69454119c8ae641f50d952b5ee9` | 同左（**不変**） |
| ロジック区間(902..末尾) | `1194016a2654dfdc4c03963b10497524` | 同左（**不変**） |

★**以後の DATA 検証はこの新基準 `8bd74741…` を使う。**
3764 / 4456 / 4858 / 4910 / 4962 / 4986 / 5373 / 5526 行などに書かれた `2dacc3ca…` は
**その時点までの記録として正しい**（消さない・append-only）。今回の1文字修正でここが切り替わった。

★**これは DATA 原文を修正した最初の事例。** 括弧付き型番（5173行・5301行）も `-P-` 落とし（5408行）も、
**DATA は不可侵のままロジック側で吸収**する形だったので DATA 行 md5 は `2dacc3ca…` で動かなかった。
今回初めて「DATA そのものが間違っている」ケースに当たり、md5 が動く系統に入った。
以後、`2dacc3ca…` を見て「無傷」と判定するチェックは**誤判定になる**ので注意。

md5 が変わったのが意図した1文字由来であることは、次の3点で裏取りした（md5 の一致/不一致だけを見ない）:
1. 旧新 DATA を**全数突合**（37,855行 × 全13列）してセル差分が **1箇所のみ**（行1891 index3）
2. **ロジック区間(902..末尾)の md5 が完全一致**＝ロジックには一切触れていない
3. ファイルサイズ差が **ちょうど 1 バイト**（8,072,234 → 8,072,233）

### 教訓: 同型の壊れ方でも、背後の族が到達しているかで直し方が変わる

2行は机上では**完全に同型**だった。どちらも「正規接頭辞の直後にハイフンが1本多い」だけ。

| 行 | 現状 | 同機種の正規形 | 正規形の要素数 | 机上で推定した正しい原文 |
|---|---|---|---|---|
| 1891 | `AGB--P-B9AJ` | `AGB-P-XXXX` | 62 | `AGB-P-B9AJ` |
| 5375 | `DL--DOL-GMSJ-JPN` | `DL-DOL-XXXX-JPN` | 90要素/84行 | `DL-DOL-GMSJ-JPN` |

同型なので「同じ直し方（ハイフン1本削る）で両方直る」と考えたが、**実測すると remedy は正反対だった。**

```
AGB-P-B9AJ   → 200/1件   ★机上の推定どおり当たる
DL-DOL-GMSJ  → 404/0件   ★机上の推定どおりに直すと 0件になる
```

原因は**背後の型番族が駿河屋に到達しているかどうかが違った**こと。
`AGB-P-` 族は元から到達している（`-P-` 落とし案件で全数測定済み）ので、誤記1文字を直せば族に合流できる。
`DL-DOL-` 族は**族ごと到達していない**（下記）ので、誤記を直しても到達しない族に正しく合流するだけ。

★**「壊れ方が同型」は「直し方が同じ」を意味しない。** 直す前に、
**直した先の形が実際に当たっているか**（＝その族が到達しているか）を測ること。
机上の「正しい原文」は、正しさの基準が DATA の内部整合性であって、駿河屋の実表記ではない。
（罠番号は付けない。罠#84/#85/#98 の重複が未整理（5117行）のため、番号付与は棚卸しの専用回に回す。）

### 行5375 スーパーマリオサンシャインは 84行 `DL-DOL-` 族へ再分類

`DL-` は 90要素すべてで必ず `DOL-` を伴い、単独使用は0（当該行を除く）。
つまり `DL-DOL-XXXX-JPN` は GameCube の正規な複合形であって、`DL-` が余分なのではない。
**余分なのはハイフン1本だけ** ——という机上の結論は正しかったが、族が当たらなかった。

**対照（ライブが実際に送っている `-JPN` 落ち形・生URL・行番号昇順の等間隔サンプル5本）**

| 行 | タイトル | 送信文字列 | HTTP | 件数 |
|---|---|---|---:|---:|
| 1824 | PAC-MAN VS. | `DL-DOL-PRJJ` | 404 | 0 |
| 3006 | コロッケ! バン王の危機を救え | `DL-DOL-GK6J` | 404 | 0 |
| 3188 | ファミリースタジアム 2003 | `DL-DOL-GFMJ` | 404 | 0 |
| 5238 | キン肉マン2世 新世代超人vs 伝説超人 | `DL-DOL-GKNJ` | 404 | 0 |
| 32394 | 実況パワフルプロ野球11 | `DL-DOL-GJPJ` | 404 | 0 |

**5/5 全滅。** `DL-DOL-` 系84行は**族ごと駿河屋の実表記と不整合**。

**パイロット（サンシャインで `DL-` を落とした形）**

| 送信文字列 | HTTP | 件数 | 到達 |
|---|---:|---:|---|
| `DL--DOL-GMSJ`（現状） | 200 | 360 | ×（壊れたトークンのノイズ） |
| `DL-DOL-GMSJ`（ハイフン修正のみ） | 404 | 0 | × |
| `DOL-GMSJ` | 200 | 4 | **○** |
| `DOL-P-GMSJ` | 200 | 4 | **○**（`DOL-GMSJ` と完全に同じ4商品） |

`DOL-GMSJ` が返した4件は**全て「スーパーマリオサンシャイン」(NGCソフト・任天堂) の状態違い**:
`157000033` スーパーマリオサンシャイン ¥1,610 ／ `157000636` (状態：パッケージ・説明書状態難) ¥1,450 ／
`157000607` (状態：スリーブ状態難) ¥880 ／ `257000033` (箱説なし) ¥660。無関係商品の混入なし。

★**(B) の宿題**: `DL-DOL-XXXX` → `DOL-XXXX` が**族全体の正解か**を 84行の全数対照で測る。
パイロット1件が当たっただけでは族に一般化できない（`-P-` 案件と同じ構造。母集団を測ってから実装する）。
測定時の母集団は `codes[0]` の生値ではなく **`sgBase`** で取ること（5408行の節で確定した作法）。

★**二重ハイフンガードは残置する。** 行5375 がまだ `--` を持っており、ガードを外すと
`DL--DOL-GMSJ` に `-P-` ルールが当たる余地が生まれる（現状 `-P-` は含まないが、族の変換を足すときに壊れうる）。
くにおくんが抜けたことで、ライブ DATA で `codes[0]` に `--` を持つ行は**サンシャイン1行のみ**になった。

### 行1891 の測定・実装・デプロイ

**3形の実測**（2026-08-02・af_jump/UTM 無しの生URL・自己クリック回避）

| 形 | 送信文字列 | HTTP | 件数 | 到達 |
|---|---|---:|---:|---|
| 現状（対照） | `AGB--P-B9AJ` | 200 | 116 | ×（壊れたトークンで無関係商品が大量混入） |
| ハイフン修正のみ | `AGB-P-B9AJ` | 200 | 1 | **○** |
| 修正＋現行 `-P-` ルール | `AGB-B9AJ` | 200 | 1 | **○** |

修正2形は**同一商品**に到達: `175000822`「くにおくん熱血コレクション1 ドッジボール部＆すとりーとバスケット」
**GBAソフト** [発売日：2005/08/25] [アトラス]。DATA の当該行（Game Boy Advance）と一致。

**DATA 修正**: `AGB--P-B9AJ` → `AGB-P-B9AJ`（ハイフン1本削除のみ・11文字→10文字）。
置換はスクリプトで DATA 行のみを対象にし、**「行長の差がちょうど -1」「ファイル全体の長さ差もちょうど -1」を
アサートしてから書き込んだ**。ファイル内には同一文字列がもう1箇所ある（1375行＝ガードの根拠コメント）ので、
素朴な全文置換だとそちらも巻き込む。巻き込めば長さ差が -2 になって停止する設計にした。

**全数突合**: セル差分1箇所・送信値が変わった行1行・**前回 `-P-` ルールが作用した254行のうち今回動いた行は0**。

**デプロイ検証値**

| 項目 | 値 |
|---|---|
| ローカル commit | `d662ee0` |
| GitHub commit | `2d38f90e83980a3102762e446c9388cb1453994c` |
| blob sha | `6c87d76090d2034f5d65fc9b0b9b6a0aba0f0cd4`（ローカルHEADと完全一致） |
| ライブ md5 | `61eaad8c37529515abb63392426265a7`（ローカルと一致） |
| ライブ側 DATA行(901) | **`8bd747412be22b2ec93b692185a62551`**（新基準に一致） |
| ライブ側 head区間(1..755) | `0831b69454119c8ae641f50d952b5ee9` **不変** |
| ライブ側 ロジック区間(902..末尾) | `1194016a2654dfdc4c03963b10497524` **不変** |
| Pages ビルド | `status: built` / commit `2d38f90e` / error null |
| CNAME | `gamekataban.com` 健在（sha `11f5f0eb…`） |

★**新基準に一致しただけで満足せず、旧基準が不在であることも確認した。**
ライブ DATA 行を直接 grep して `AGB--P-B9AJ` の出現 **0回** / `AGB-P-B9AJ` の出現 **1回**。
「新しい値になった」と「古い値が消えた」は別の主張で、後者を測らないと差し替え漏れを見逃す。

**ライブ実物から再計算した完全アフィリンク**（行1891・af_jump + `search_word=AGB-B9AJ` + UTM4本）:

```
https://affiliate.suruga-ya.jp/modules/af/af_jump.php?user_id=5357&goods_url=https%3A%2F%2Fwww.suruga-ya.jp%2Fsearch%3Fcategory%3D2%26search_word%3DAGB-B9AJ%26utm_medium%3Daffiliate%26utm_source%3Dgamekataban.com%26utm_content%3D%252F%26utm_campaign%3Dafid5357
```

分解: ラッパー `affiliate.suruga-ya.jp/modules/af/af_jump.php` / `user_id=5357` /
goods_url `www.suruga-ya.jp/search` / `category=2` / **`search_word=AGB-B9AJ`** /
`utm_medium=affiliate` / `utm_source=gamekataban.com` / `utm_content=/` / `utm_campaign=afid5357`。

★この文字列は**ライブ実物の `SG_AF`/`SG_UTM`/`sgAf` の定義行と `sgSrc`〜`kwCodeSg` 区間を
そのまま抜き出して評価したもの**（罠#141。手打ちの目視値は使わない）。href の生成箇所は
ライブ1325行の `href="${sgAf(k.kwCodeSg)}"` で、この式そのものを評価している。
リンクへのリクエストは一度も送っていない（罠#109・自己クリック回避）。

### ★行1891 は解決。行5375 は (B) 84行族へ移管

5354行・5406行の宿題「二重ハイフン2行 `AGB--P-B9AJ` / `DL--DOL-GMSJ`」のうち、
**`AGB--P-B9AJ`（行1891）は本節で解決**（DATA修正・公開・UI着地まで完了）。
**`DL--DOL-GMSJ`（行5375）は二重ハイフンの問題ではなく `DL-DOL-` 族84行の問題**と判明したため、
(B)「84行 `DL-DOL-` 族の全数対照測定」へ移管する。二重ハイフンという括りはここで解体された。
（既存の宿題行への打ち消し線は**この追記では入れない**。追記と既存行の変更は承認経路が違うため別作業とする。）

## 2026-08-02（続き4）— `DL-DOL-` 族87行: 族ごと未到達だったものを JAN 優先で到達化

5405行の宿題「`DL-DOL-` 族」に着手した。5566行（続き3）で「二重ハイフンの問題ではなく族の問題」と
切り分けたものの本体。**族が丸ごと未到達**という前提から、全数対照 → 実装 → 公開 → 着地まで通した。

### 前提として先に片付けた独立案件: 動物番長の誤着地（★この節が唯一の記録）

族の衝突チェックで見つかった、**族とは独立した現在進行形のバグ**。先に単独で修正・公開済み。
※ この案件には専用の節が無く、本サブセクションが唯一の記録。実装・公開は済んでいる。

DATA の動物番長(行5558)の型番 `DOL-GKDJ-JPN` は**誤り**で、`GKDJ` は巨人のドシン(行5520)の型番。
実測: `DOL-GKDJ` → 200/5件が**全て巨人のドシン**(`157000022` ほか)、動物番長は1件も出ない。
**行5558 のカードは本番で別ゲームに着地していた。**

正しい型番は確定できなかった。駿河屋の**商品ページは Cloudflare のボット判定で 403**
(`Just a moment...`)、検索結果ページに型番の記載は無い。外部からの推測値を DATA に書かない(罠#141)。
代わりに **JAN 検索へ逃がした**。実測: JAN `4902370505900` → 200/2件が全て動物番長
(`157000013` / `157000608`)。対照でカービィのエアライドの JAN `4902370506594` → 200/6件が全て
カービィで、型番 `DOL-GKYJ` の5件より広く拾った。**この2件が「JAN は型番の代替になる」実例**で、
のちの族の設計判断の土台になった。

実装は `code === 'DOL-GKDJ-JPN'` の完全一致で1行を指し、JAN の生値は直書きせず `r[10]` から読む。
★完全一致にした理由: 巨人のドシンの `DL-DOL-GKDJ-JPN` が `DOL-GKDJ-JPN` を**部分文字列として含む**。
部分一致だと巨人のドシンまで JAN 化して壊す(罠#90)。
★値を列から読む理由: DATA が正典で、JAN 値をコードに二重持ちすると将来ズレる(罠#19の系統)。
判定だけ型番一致・値は列から取る。`&& r[10]` により JAN 列が空になれば通常パイプラインに戻る。
★実装位置は `kwCodeSg` 生成時。`WUP-006` の源差し替え(`sgSrc`)より1段あとに置いて、型番用の変換
(`sgParen` / `-P-` / `SG_REGION`)をバイパスする。今日の JAN は数字だけなので通しても無傷だが、
型番ルールが今後増えたときに数字列を壊されない位置に置く。

検証: 全37,855行で検索語が変わった行は1行のみ。DATA 不変。
デプロイ: ローカル `882bc25` / GitHub `0575396d51689b1e0cbb0a532489dd06739c77fa` /
blob `5c12a8f5…` / ライブ md5 `f051d7bee43b7ebf66fcdc24379f3d20`。

### 族が丸ごと未到達だった

現送信形 `DL-DOL-XXXX` は、**測った17パターンすべてが 404/0**（層1の正常形12行＋変則＋パイロット）。
個別行の誤記ではなく族の問題。GameCube の複合型番 `DL-DOL-XXXX-JPN` が駿河屋の実表記と噛み合わない。

**母集団は `codes` 全要素走査 `/DL-+DOL-/` で 87行。**
前方一致 `/^DL-DOL-/` だと次の3変則を取りこぼす:

| 行 | 型番列 | 前方一致で漏れる理由 |
|---|---|---|
| 3193 GIFTPIA | `GS-DL-DOL-GGFJ-JPN` | 前に `GS-` が付く(GameCube 行でこれ1行のみ) |
| 5751 SUPER MONKEY BALL | `684-00001;DL-DOL-GMBJ-JPN-01` | `codes[0]` が別体系で、族要素は2番目 |
| 5375 スーパーマリオサンシャイン | `DL--DOL-GMSJ-JPN` | 二重ハイフンで `DL-DOL-` に一致しない |

ハイフンを `-+` にして全要素を走査すれば、この3種が1つの式で拾える。JAN 保有は **82/87 (94.3%)**。

### 全数対照 87行 ×((a) 変換 + JAN)

| 形 | 到達 | 備考 |
|---|---|---|
| 現送信 `DL-DOL-XXXX` | 0 | 測った全パターンが 404/0 |
| **(a) `DOL-XXXX`** | **82/86 (95.3%)** | 測定バグ訂正後の値(下記) |
| **JAN** | **82/82 (100%)** | 欠測なし |
| (a) だけが到達する行 | **0 行** | ★決め手 |
| どちらも未到達 | 2 行 | PAC-MAN VS. / ドルアーガの塔 |

★**JAN 到達率100%** と ★**(a) 単独優位が0行** の2点が JAN 優先の決め手。
「JAN が使える行では JAN が常に当たる」ので、JAN を先に見て損をする行が存在しない。

### 実装: JAN 優先・(a) フォールバック — 全行一律にした理由

```js
族の行 かつ JAN(r[10]) 非空 → JAN をそのまま送る(型番パイプライン非通過)
族の行 かつ JAN 空          → (a) 変換 elem.replace(/^.*DL-+DOL-/,'DOL-')
族でない                    → 現行パイプライン sgParen(sgPdash)
```
内訳は **JAN 82行 / (a) 3行(FE蒼炎・FFCC・ピクミン) / リンク非表示 2行**。

★**「(a) が広い18行だけ (a) を使う」という折衷を採らなかった。**
その18行という**境界自体が測定時の在庫スナップショットで決まる**。同梱版が売れて消えれば
(a) の優位は消え、新たに入荷すれば別の行が優位になる。**正しさが在庫に依存するルールは将来腐る**
(罠#84 系)。一様ルールは判定が「族か・JAN があるか」の2つだけで在庫に依存しない。
77行は (a) でも到達していたのを JAN で上書きするが、**到達は失われない**
(JANのみ到達0・どちらも未到達2で確定済み)。行ごとの最適化より一様ルールの保守性を取る。

### ★版の網羅性トレードオフ（捨てた側の記録）

(a) は**同梱版・廉価版まで広く拾い**、JAN は**その商品そのものに絞る**。差が出るのは18行。

```
ドンキーコングジャングルビート(2934)
  (a) DOL-GYBJ  5件 = タルコンガセット同梱版3件 + 単品2件
  JAN           2件 = 単品のみ            ← 同梱版が落ちる
```

これは**「目的の物に着けない」のではなく「その版も選べたのに」という機会の話**。
ユーザーの第一目的(自分のゲームを買う)は JAN で達成される。到達確実性を優先した。

★**将来 missing-report(「同梱版しか無い時期に JAN で0件に見えた」等)が実際に観測されたら、
そのとき折衷を再考する。** 今は仮想リスク。**観測に基づいて動く**という今日の原則どおり、
仮想リスクのために保守性を捨てない。この段落は再考の条件を残すために書いている。

### 族検出を全要素走査にした理由（測定バグを実装で踏まない）

全数測定の候補を**前方一致 `/^DL-DOL-/` で機械生成したため、3193 と 5751 の2行が変換されないまま
測られた**。GIFTPIA は `GS-DL-DOL-GGFJ` のまま投げて 404、モンキーボールは `684-00001` のまま
投げて 19件の無関係商品。集計上は「(a) 未到達6行」に見えたが、**真の未到達は4行**だった
(層2で `DOL-GGFJ` 200/3件・`DOL-GMBJ` 200/4件を確認済み)。

★**検出も変換も全要素走査で持つ。** 実装後、走査版が生成する (a) の値と全数測定で実際に投入した語を
87行分突き合わせ、**食い違い0行**で裏取りした。測定の道具と実装が同じ式を使っていれば、この種の
取りこぼしは起きない。**候補生成のロジックが本番のロジックより弱いと、測っていないものを測ったと
思い込む。**（罠番号は付けない。#84/#85/#98 の重複が未整理(5117行)のため棚卸しの専用回に回す。）

### 救えない2行は駿河屋コードリンクを出さない

| 行 | タイトル | 型番列 | (a) | JAN |
|---|---|---|---|---|
| 1824 | PAC-MAN VS. | `DL-DOL-PRJJ-JPN` | 404/0 | なし |
| 1892 | ドルアーガの塔 | `DL-DOL-PKBJ-JPN` | 404/0 | なし |

型番も JAN も無く**恒久的に未到達**。この2行は `gCode` ブロックごと出さない。
★誤った値ではなく**空振り**だが、**日本語の0件ページはユーザーの迷いを生む**。
このサイトの利用者には言語の壁がある相手が含まれ、空ページは「探し方が悪いのか/在庫が無いのか」の
区別がつかない混乱源になる。出さないほうがまし(罠#27 と同じ物差しを、誤着地ではなく空振りに当てた)。
★**消すのは駿河屋のコード検索リンク1本だけ。** 他導線(Buyee / eBay / Mercari / Book-Off / Hard-Off /
タイトル検索)は残す＝到達手段を消さない。`buyBlock` を実物から切り出して実行し、出力HTMLで確認した。
★判定は型番の完全一致。両文字列とも DATA でその行だけ・部分一致の巻き込み無しを確認済み(罠#90)。

### 衝突の全数チェック

族87行の (a) 変換値を DATA 全37,855行の現送信値と型番列の全要素に突き合わせた。**誤着地は2件のみ**。

| 変換値 | 衝突 | 判定 |
|---|---|---|
| `DOL-GKDJ` | 動物番長(5558) ↔ 巨人のドシン(5520) | **DATA の誤り**。上のサブセクションで解消済み |
| `DOL-GPOJ` | PSO プラス(3109) ↔ モデムセット(5308) | **許容**。DATA の誤りではない |

`GPOJ` は駿河屋側が Plus と無印に同じ型番を割り当てている状態で、両者の目的商品が同じ5件に
含まれる。**誤着地ではなく絞り込みが効かないだけ**なので触らない。
族の内部でも別タイトル同士の衝突は0(`DL-DOL-GPXJ` のポケモンボックス2行・`DOL-GEAJ` の
エターナルアルカディア2行は同名の重複行)。

### デプロイ検証値

| 項目 | 値 |
|---|---|
| ローカル commit | `ccd513d` |
| GitHub commit | `2ff083a673d7449784ba94278fe96a41ee9f734d` |
| blob sha | `ed370522464402ca89c47d1e890d96fe5e6dbb53`（ローカルHEADと完全一致） |
| ライブ md5 | `1e52a1afa357b6e54fe6368b61e3d6bb` |
| ライブ側 DATA行(901) | `8bd747412be22b2ec93b692185a62551` **無傷**（今回 DATA 非変更） |
| ライブ側 head区間(1..755) | `0831b69454119c8ae641f50d952b5ee9` **不変** |
| Pages ビルド | `status: built` / commit `2ff083a6` / error null |
| CNAME | `gamekataban.com` 健在 |

ライブ実物から実処理を切り出して再計算し、次を確認(罠#141・grep より一段強い):

```
[5520] 巨人のドシン    DL-DOL-GKDJ-JPN      → 4902370505917
[5375] サンシャイン    DL--DOL-GMSJ-JPN     → 4902370506068   (二重ハイフン)
[3193] GIFTPIA       GS-DL-DOL-GGFJ-JPN   → 4902370506464   (GS- 前置)
[5751] モンキーボール  684-00001;DL-DOL-…   → 4974365870003   (2要素目に隠れ)
[5711] ピクミン       DL-DOL-GPIJ-JPN(JAN空) → DOL-GPIJ      ((a) フォールバック)
[1824] PAC-MAN VS.   noCode=true → "By product code" ブロック非出力・他導線は全て残存
[5558] 動物番長       DOL-GKDJ-JPN         → 4902370505900   (既存特例のまま・族に非干渉)
```

分岐順は `kwCodeSg = enc(sgJan || sgFam || sgParen(sgPdash))` で動物番長特例が先だが、
`DOL-GKDJ-JPN` は `/DL-+DOL-/` に当たらないので順序に依存しない(実装後にも確認済み)。

**結果: 族87行のうち85行(97.7%)が到達可能になった。元は丸ごと未到達だったもの。**

### ★`DL--DOL-GMSJ`（サンシャイン）はこの族作業に吸収されて解決

5354行・5406行の宿題「二重ハイフン2行 `AGB--P-B9AJ` / `DL--DOL-GMSJ`」は**これで両方完結**。
`AGB--P-B9AJ`(行1891) は続き3(5566行)で DATA 修正、`DL--DOL-GMSJ`(行5375) は本節で族として処理。
サンシャインは DATA の `--` を直さないまま解決した——**族検出を `/DL-+DOL-/` にしたので二重ハイフンの
ままでも拾える**ため。二重ハイフンガードも残置のままで矛盾しない(ガードは `-P-` 落としに対するもので、
族の JAN 化はガードの後段で効く)。
（既存の宿題行への打ち消し線は**この追記では入れない**。追記と既存行の変更は承認経路が違うため別作業。）

### 未解決に追加

- **未解決#24.** `index.html:891` Genre guide 見出しの `<span class="jp">ジャンル早見表</span>` が
  画面表示される日本語のまま。英語表記に統一する必要あり（**未着手**）。
  発見の経緯: 2026-08-02 の Suruga-ya ラベル英語統一（「日本語表記を画面に残さない」）の作業中に、
  静的HTML本文(756-899行)を日本語文字で全走査して見つけた。ラベル側の指示スコープ外だったので触っていない。
  なお `駿河屋` / `型番` の出現は**すべて JS/CSS のコメント内**で画面には出ない。画面に出る日本語で
  DATA 由来でないものは、現時点でこの1箇所のみ。

---

## 2026-08-02（続き5）— Mercari 導線を廃止・Suruga-ya ラベルを (code)/(title) に統一（表示層のみ・DATA不変）

続き1〜4がすべて「型番を駿河屋に当てる」DATA/ロジックの話だったのに対し、本節は**表示層だけ**の作業。
ロジックの判定・URL生成・DATA には一切触れていない。

### やったこと（コミット4本）

| コミット | 内容 | 差分 |
|---|---|---|
| `0c435fd` | Mercari リンクを非表示化 | 1行削除 + 経緯コメント4行 |
| `f8082b9` | Suruga-ya ラベルを `Suruga-ya (code)` / `Suruga-ya (title)` に統一 | 2行（`<a>` のテキストのみ） |
| `3eee46a` | 上2件で実態とずれたコメントを修正（357行 `.store` 一覧 / 1367行 `kwName`） | 2行・コメントのみ |
| `b269762` | ノート追記（未解決#24） | 9行・追記のみ |

- **Mercari**: `gTitle` の1本がファイル内で唯一の `mercari.com` リンクだった。生成をやめただけで
  他店の導線・並び順・URL 生成は不変。
- **ラベル**: `By product code` 側と `By Japanese title` 側がどちらも `Suruga-ya` で同名だったため区別した。
  変更は `<a>` のテキストノードのみ。`SG_AF` / `SG_UTM` / `sgAf()`（1273-1275行）は**バックアップと diff で完全一致**を確認済み。
  af_jump ラップ・検索クエリ・UTM は1バイトも変えていない。サイズ差 +15B = `" (code)"`7 + `" (title)"`8 でちょうど一致。

### ★head 区間 md5 の基準を更新（`0831b694…` → `1217086677aaaae1c033e0d0f89fc90c`）

| 区間 | 作業前 | 作業後 |
|---|---|---|
| head 区間(1..755) | `0831b69454119c8ae641f50d952b5ee9` | **`1217086677aaaae1c033e0d0f89fc90c`** |
| **DATA 行(901)** | `8bd747412be22b2ec93b692185a62551` | **同左（不変）** |
| ロジック区間(902..末尾) | `667327a5eb205dff56913d4df4ea8303` | `55a12b86452c52bf8b123e71384f3b5e` |

**head が動いた理由**: `.store` のフォント指定を説明する CSS コメント（357行）が head 区間内にあり、
そこから Mercari の記述を1つ消したため。**CSS 規則そのものは1バイトも変えていない**（`/* */` の中だけ）。
つまり**挙動は完全に不変のまま head 区間の md5 だけが動いた**形で、「md5 が動いた＝挙動が変わった」ではない。
★**以後の head 検証は `1217086677aaaae1c033e0d0f89fc90c` を使う。**
ロジック区間も同様に動いているが、これは表示ラベルとコメントの変更（1327/1338/1367行）によるもの。

### 罠#142. 行番号で固定した検証手順があるファイルでは、コメント追記も「行数を増やす変更」

357行のコメントを最初**2行に分けて**書いたところ、CSS 区間が1行増えて **DATA 行が 901 → 902 に繰り下がった**。
ノートの DATA 検証は `sed -n '901p'` と行番号固定なので、そのままなら以後の検証は**別の行を掴む**。
実際 902 に落ちた状態で 901 を測ると `77bf7c17…` という無関係な値が出て、
「DATA が壊れた」ようにしか見えない（4962行で canonical 追加時に踏んだのと同じ形の再発）。

**対処**: コメントを1行に収めて書き直し、DATA を 901 行目に戻した。
**教訓**: DATA より**上**を触るときは、内容が無害（コメント・空白）でも**行数が増減しないか**を必ず見る。
無害な変更ほど「行数」の観点が抜ける。md5 の不一致を見て DATA を疑う前に、まず行番号のずれを疑うこと。

### 指示と実態がずれていた2件（そのまま実行せず、実態に合わせた）

1. **`.store` コメント（357行）**: 「現状は Buyee / eBay / Suruga-ya のみ」という前提で指示が来たが、
   Book-Off / Hard-Off も `class="store"` を持っている（1339-1340行）。Mercari だけ消し、両者は残した。
2. **`kwName` コメント（1367行）**: 「`— Suruga-ya` のみに」という指示だったが、**Suruga-ya が使うのは `kwKana`**
   （1480行 `sgAf(k.kwKana)`）で `kwName` ではない。`kwName` の唯一の利用者が Mercari リンクだったため、
   廃止後は `buyBlock` に渡すだけの**未参照の値**になっている。`— Suruga-ya` と書くと新しい嘘になるので
   `— no consumer now (passed to buyBlock only)` とした。罠#12（間違った値は無いより悪い）のコメント版。
   → `kwName` 自体を消すかは**未着手**（下の未解決#25）。

### 公開と実機確認

`gh api -X PUT repos/english-tadashi/bin-diver/contents/index.html`（contents API・index.html 1ファイルのみ）。

| 項目 | 値 |
|---|---|
| Pages コミット | `b4c393470328e567942bc3425bffa14095f64715` / build `built`(39秒) |
| blob sha | `ed370522…` → `0ae843bb…` / 8,077,072 バイト |
| **CNAME** | sha `11f5f0eb…` / 15B / `gamekataban.com` — **PUT 前後で一致・無傷** |
| ライブ md5 | `16758a48ba513bc4bbe5bb770e711a18`（ローカルと一致） |
| ライブ側 DATA行(901) | `8bd747412be22b2ec93b692185a62551` **無傷**（今回 DATA 非変更） |

★**上書き前に「live の md5 = 作業前バックアップの md5(`1e52a1af…`)」を確認した。**
live が変更前の状態そのものだと確かめてから PUT したので、他所からの未知の変更を潰していない。
`KLOOK_PENDING_DO_NOT_DEPLOY` の混入も0件。

★**md5 一致だけで合格にしていない**（罠#136）。headless Chrome で live URL を実際に描画し、
検索を打って**描画後の DOM の `<a>` 要素を実測**した。5クエリ・計226カード・1,136リンク:

| クエリ | カード | a要素 | mercariリンク | 本文のmercari | (code) | (title) | 旧`Suruga-ya` |
|---|---|---|---|---|---|---|---|
| Pokemon | 60 | 261 | 0 | false | 30 | 46 | 0 |
| Final Fantasy | 60 | 360 | 0 | false | 59 | 60 | 0 |
| Chrono Trigger | 6 | 29 | 0 | false | 3 | 5 | 0 |
| Zelda | 40 | 165 | 0 | false | 24 | 28 | 0 |
| Dragon Quest | 60 | 321 | 0 | false | 45 | 55 | 0 |

リンク先も実測: `By product code` → `af_jump.php?user_id=5357&goods_url=…search_word=SLPS02430`、
`By Japanese title` → 同ラップで `search_word=クロノ・トリガー`。Buyee(`i0c.xyz` ラップ)・eBay(campid=5339175838) も従来どおり。
**ソースの grep だけでは不十分**だった点に注意: 初期ロードの DOM にはカードが1枚も無く（検索して初めて描画される）、
`--dump-dom` を素で grep すると `<script>` 内のテンプレート文字列を「描画結果」と読み違える。
CDP で検索を打ってから DOM を読むところまでやって初めて実測になる。

### 未解決に追加

- **未解決#25.** `kwName`（1367行）が未参照の値のまま残っている。Mercari 廃止で利用者が消えたが、
  `buyBlock` への引数（1557行）としては渡し続けている。消すか残すか未判断（**未着手**）。

### バックアップ

`index.html.20260802-bak-pre-mercari-hide`(作業前・live旧と同一 `1e52a1af…`) /
`.20260802-bak-pre-sgya-label` / `.20260802-bak-pre-storecomment` / `.20260802-bak-pre-kwname-comment`、
ノートは `.20260802-bak-pre-genrejp-note` / `.20260802-bak-pre-mercari-worknote`。

---

## 2026-08-02（続き6）— Suruga-ya (title) をピンク化・全幅化（IN JAPANでBook-Off/Hard-Offと並んでいた問題の解消）

**発端**: IN JAPAN モードで `Suruga-ya (title)` が Book-Off / Hard-Off と同じ `.stores` グリッドに入っており、
3等分の1つ（219.33px）として埋もれて表示されていた。`Suruga-ya (code)`（ピンク・全幅）と同じ扱いにしたい、というユーザー指摘。

### 実施した変更（実コード4行・コメント除く）

| # | 箇所 | 変更 |
|---|---|---|
| 1 | `index.html:1309` | `clsTitle` を Suruga-ya (title) 専用の変数として独立させ、無条件で `'store primary'` に |
| 2 | `index.html:1354-1357` | `div.stores` を **Suruga-ya (title) 単独** と **Book-Off / Hard-Off のペア**（`class="stores pair"`）に分割 |
| 3 | `index.html:548` | CSS の2カラム指定を `:last-child` 依存から明示クラス `.card .stores.pair` に変更 |

`clsTitle` は同日に2段階で動かした: `((jp||noProxy) && k.codes.length===0)`（型番なし行だけ塗る）→ `(jp||noProxy)`（海外は水色のまま）→ **無条件**。
`clsCode` / `clsBuyee` は一切触っていない。DATA・店URL・店名・グループの並び順（`order`）も不変。

### 罠として記録: `:last-child` は「並び順が動く」設計と噛み合わない

旧 CSS は `.card .group:last-child .stores{ grid-template-columns:1fr 1fr; }` で、
「買う枠内の最後の `.group` ＝ 日本語タイトル群」という前提だった。だが**最後がどのグループかは LOC で入れ替わる**
（`order` は jp: gCode→gTitle→gWorld / out: gWorld→gCode→gTitle）。実際には

- **jp モード**: 最後は `gWorld` なので、2カラム指定は Buyee / eBay に当たっていた（title 群には当たっていない）
- **out モード**: 最後は `gTitle`。今回 `.stores` を2つに割った結果、**単独の Suruga-ya (title) にも 1fr 1fr が当たり半分幅（333px）** になった

＝ HTML を分割した瞬間に、触っていない CSS が別の要素へ当たり方を変える形のバグ。

**対策**: 複数要素に同じ CSS を当てたいときは、`:last-child` のような**相対位置セレクタを使わず明示的なクラス名で名指しする**。
`.stores.pair` に変えたことで、`gWorld`（Buyee/eBay）は `auto-fit minmax(130px,1fr)` に戻ったが、
実測幅は 900px 幅で 333/333、390px 幅で 195.5/195.5 と**変更前と完全に一致**（見た目の変化なし）。

### 方針変更: 「ピンクは常に1つだけ」を撤回

`index.html` の `buyBlock` 冒頭コメントにあった **「塗り(primary)は常に1つだけ。蛍光灯下で最初に押す店を1つ示すのが塗りの役目」**
という方針を撤回し、**「押しやすい入口を複数示してよい（最大2つ）」**に変更した（2026-08-02）。コメントも実態に合わせて書き直した。

| モード | ピンク | 例外 |
|---|---|---|
| IN JAPAN | `Suruga-ya (code)` ＋ `Suruga-ya (title)` の2つ | 型番なし行 / `sgNoCode` 行は `gCode` 自体が出ないので (title) の1つ |
| OUTSIDE | `Buyee` ＋ `Suruga-ya (title)` の2つ | `row[7]` 空(noProxy)の行は Buyee を塗らず `Suruga-ya (code)` ＋ (title) の2つ |

3つ以上は塗らない。Book-Off / Hard-Off / eBay は常に枠線のまま（「全部塗ると、まずどれを押すかが伝わらない」という元の理由はここでは生きている）。

### 実測（headless Chrome・`getBoundingClientRect` / `getComputedStyle`）

`index.html` のコピーに計測スクリプトを注入し、`sekiro` を検索して描画後の DOM を測った（本体には計測コードを入れていない）。
罠#136（md5一致だけで合格にしない）と同じ理由で、grep では済ませていない。

| 要素 | jp モード | out モード |
|---|---|---|
| Suruga-ya (code) | 674px・ピンク `rgb(255,55,199)` | 674px・水色 |
| **Suruga-ya (title)** | **674px・ピンク** | **674px・ピンク** |
| Book-Off / Hard-Off | 333 / 333・水色 | 333 / 333・水色 |
| Buyee / eBay | 333 / 333・水色 | Buyee ピンク / eBay 水色 |

修正前の値との差: jp の Book-Off / Hard-Off は 219.33px → 333px（3等分→2等分＝分割の直接の結果）。
out の Suruga-ya (title) は 333px（半分幅バグ）→ 674px。390px 幅でも同じ形（399 全幅 / 195.5・195.5）。

### DATA の無事と行番号

**DATA行 md5: `8bd747412be22b2ec93b692185a62551`（一連の作業を通して不変）**。
ただし CSS コメントを5行足したため **DATA の行番号が 901 → 906 に移動**した（続き5で 901 に戻した直後の移動）。
`awk 'NR==901'` で測ると別物の md5 が出る。**照合は必ず `grep -n '^const DATA = \[\['` で位置を取り直してから**行うこと（続き5の教訓の再確認）。

### バックアップ

`index.html.20260802-bak-pre-sgya-title-split` / `.20260802-bak-pre-laschild-fix` / `.20260802-bak-pre-clstitle-alwayspink`、
ノートは `BIN_DIVER_NOTES.md.20260802-bak-pre-sgya-pink-note`。

### 公開状態

**未公開**。ローカルコミットまで。GitHub Pages への PUT（contents API）は次の作業で行う。

#### 公開状態の追記（2026-08-02・上の「未公開」を上書きする最新情報）

**上の「公開状態: 未公開」は、この追記の時点で解消済み。** 同日中に公開した。既存行は残し、事実だけここに足す
（罠#107「バックアップの存在は未公開を意味しない」と同じ理由で、古い記述を消さずに新しい行で上書きする）。

| 項目 | 値 |
|---|---|
| Pages コミット | `e5c5fb545c6b4ab3b7f9409b17c26def4fec8e48`（2026-08-02） |
| ローカルコミット | `0145045681c3d6eefe2a9876d1ca9f7ecf349007`（index.html + ノート） |
| blob sha | `0ae843bb…` → `206e5532522d48caebb90e94e42e714340fb42bc`（ローカル git の blob と一致） |
| **live md5（公開後）** | **`957c5dc8269540dcc188adf960ea9f9a` / 8,078,850 バイト**（ローカル現行と一致） |
| **live DATA行(906) md5** | **`8bd747412be22b2ec93b692185a62551`**（無傷。live 側でも `grep -n '^const DATA = \[\['` で位置を取り直して照合） |
| CNAME | sha `11f5f0eb…` / 15B — PUT 前後で一致・**無傷** |
| Pages ビルド | `built` / 35,972ms / error なし |

★**上書き前に「live の md5 = 作業前バックアップ `index.html.20260802-bak-pre-sgya-title-split` の md5（`16758a48ba513bc4bbe5bb770e711a18`）」を確認してから PUT した。**
live が変更前の状態そのものだと確かめた上での上書きなので、他所からの未知の変更を潰していない。公開したのは `index.html` 1ファイルのみ（ノートは Pages 対象外）。

★**md5 一致だけで合格にしていない**（罠#136）。headless Chrome を `--remote-debugging-port` で起動し、
**live URL を実際に開いて検索を打ち、描画後の DOM を `getBoundingClientRect` / `getComputedStyle` で実測**した
（依存を増やさないため CDP の WebSocket は Python 標準ライブラリだけで最小実装した。スクリプトは使い捨て・リポジトリには入れていない）。

sekiro・900px 幅での実測値:

| モード | Suruga-ya (title) | Book-Off / Hard-Off | もう1つの塗り |
|---|---|---|---|
| jp | **674px 全幅・`rgb(255,55,199)` ピンク** | 333 / 333・水色 | Suruga-ya (code) 674px ピンク |
| out | **674px 全幅・`rgb(255,55,199)` ピンク** | 333 / 333・水色 | Buyee 333px ピンク |

単独 `.stores` の `grid-template-columns` が live でも `674px 0px 0px 0px`（＝`1fr 1fr` が当たっていない）、
ペア側に `class="stores pair"` が乗っていることも確認した。

5クエリ・**計226カード ×2モード**の網羅確認（`By Japanese title` グループを持つカードが分母）:

| クエリ | カード | (title) ピンク | (title) 全幅 | pair 2等分 | mercari |
|---|---|---|---|---|---|
| Pokemon | 60 | 46/46 | 46/46 | 46/46 | 0 |
| Final Fantasy | 60 | 60/60 | 60/60 | 60/60 | 0 |
| Chrono Trigger | 6 | 5/5 | 5/5 | 5/5 | 0 |
| Zelda | 40 | 28/28 | 28/28 | 28/28 | 0 |
| Dragon Quest | 60 | 55/55 | 55/55 | 55/55 | 0 |

jp / out とも全数で意図どおり。カード内の塗りの数も方針どおりで、**out は全カードちょうど2つ**、
**jp は2つ（型番あり）または1つ（型番なし・`sgNoCode`）**、3つ以上のカードは0件だった。

ノートのバックアップ: `BIN_DIVER_NOTES.md.20260802-bak-pre-sgya-publish-note`。

## 2026-08-03 — howto に Buyee 代行購入の流れを明記（住所=倉庫 / 決済 / 手数料）・3番目の li との重複を解消

英語圏ユーザー向けの `<section class="howto">` の "Buying online, from outside Japan" の `<ol>` に、
Buyee が何をする業者なのかを 2 行足した。**表示層の文言のみ。CSS も JS も DATA も触っていない。**

### 発端: 「代行購入の仕組み」がページのどこにも書かれていなかった

追記前、Buyee に言及していたのは `<ol>` の3番目の li ただ1行だけだった:

> Follow a store link to the listing. Buyee ships worldwide: it buys the item inside Japan for you and forwards it overseas.

これは「国内で買って海外へ転送する」という**結果**しか書いておらず、初めて代行を使う人が知りたい3点、
すなわち **(a) 荷物はまず Buyee の日本国内倉庫に届く / (b) 支払先は店ではなく Buyee / (c) 代行手数料・
国際送料・関税が別に乗る** が、ページ全体を検索しても一切なかった。
`proxy` / `warehouse` / `forwarding fee` / `customs` / `import duty` は HTML マークアップ部に0件。
手数料に近い唯一の語は footer の "Kataban may earn a commission" だが、これは Kataban 自身の
アフィリエイト報酬の開示であって Buyee の手数料の話ではない。**別物を「書いてある」と誤読しないこと。**

### 罠#143 — `#more` は howto ではない。`Load more` ボタンの容れ物

作業開始時、「howto の説明文は `#more` の中」という前提で入ったが、これは**誤り**だった。

```
811:  <div id="more"></div>          ← 空。JS が Load more ボタンだけを差し込む
813:  <section class="howto" …>      ← 説明文の本体はこっち
```

`#more` は `paint()` が `more.innerHTML = '<button class="moreBtn">Load more · N left</button>'` を
書き込むページネーション用のコンテナで（1619〜1625行）、howto とは無関係。名前が近い（`more` と
「もっと詳しい説明」）だけで隣接しているため取り違えやすい。**編集対象は id ではなく、実際に
その文字列が入っている要素を grep で確定してから決めること。**

### 2段階で入れた。1段階目で重複が発生し、2段階目で潰した

**1段階目**（`.20260803-bak-pre-howto-proxy`）— 3番目の li の直後に2行追加:

```html
<li>Buyee buys the item in Japan on your behalf and holds it at their Japan warehouse — you don't deal with the seller directly.</li>
<li>You pay Buyee, not the store: item price plus Buyee's service fee. International shipping is added when they ship to you, and customs duties may apply depending on your country.</li>
```

**罠#144 — 足した瞬間、既存の3番目の li と意味が二重になった。** 3番目の
"it buys the item inside Japan for you and forwards it overseas" と、新しい4番目の
"buys the item in Japan on your behalf" が同じことを言っている。追記は既存文の内容を確認してから
書いたつもりでも、**「既存文が要約として述べていたことを、後続行が詳細として展開する」形になると重複する**。
足す前ではなく**足した後の並びを通しで読み直す**工程が要る。

**2段階目**（`.20260803-bak-pre-howto-dedupe`）— 3番目を要約役に徹させて短縮:

```html
<li>Follow a store link to the listing on Buyee — they ship worldwide.</li>
```

結果、3番目=リンクを踏む / 4番目=代行購入と倉庫 / 5番目=支払いと手数料、と役割が分かれた。

### DATA行 md5（今回も不変）

**`8bd747412be22b2ec93b692185a62551`（一連の作業を通して不変）。**
ただし2行追加により **DATA の行番号が 906 → 908 に移動**した（続き6で 901 → 906 に動いた続き）。
2段階目は1行置換なので 908 のまま。今回も `grep -n '^const DATA = \[\['` で位置を取り直してから照合した
（続き5・続き6の教訓の再々確認）。`node --check` は `<script>` 2ブロック（908〜2132 / 2135〜2154）を
切り出して実行、いずれも OK。今回は HTML のみの変更なので当然だが、念のため。

サイズの推移: 8,078,850 →（2行追加）8,079,185 →（3番目を短縮）8,079,131 バイト。

### バックアップ

`index.html.20260803-bak-pre-howto-proxy`（proxy 2行追加**前** / 8,078,850B）、
`index.html.20260803-bak-pre-howto-dedupe`（追加**後**・重複解消**前** / 8,079,185B）、
ノートは `BIN_DIVER_NOTES.md.20260803-bak-pre-howto-proxy-note`。

### 公開状態

**公開済み**（2026-08-03）。

| 項目 | 値 |
|---|---|
| ローカルコミット | `b64af93510d478abbc253bbb50d74e115dc11266`（index.html のみ・1 file changed, 3 insertions(+), 1 deletion(-)） |
| Pages コミット | `e85f020ae5d5df233a0cb1e357f99830042bcc97` |
| blob sha | `206e5532…` → `3d1c832466b4da2b4e44418a3aa88862c1e23807`（ローカル git の blob と一致） |
| **live md5（公開後）** | **`9cf1bd2e7e1c7f83323e0a1fb8a25e90` / 8,079,131 バイト**（ローカル現行と一致） |
| **live DATA行(908) md5** | **`8bd747412be22b2ec93b692185a62551`**（無傷） |
| CNAME | `gamekataban.com` — PUT 前後で健在 |
| Pages ビルド | `built` / 44,849ms / error なし |

★**上書き前に「live の md5 = 作業前バックアップ `index.html.20260803-bak-pre-howto-proxy` の md5
（`957c5dc8269540dcc188adf960ea9f9a`）」を確認してから PUT した。** 加えて **remote の blob sha
`206e5532…` と `git hash-object` した作業前バックアップの sha が一致**することも確認している。
md5 より blob sha の照合の方が直接的で確実（GitHub 側が持っているものと同じ土俵で比べられる）。
live が変更前の状態そのものだと確かめた上での上書きなので、他所からの未知の変更を潰していない。
公開したのは `index.html` 1ファイルのみ。KLOOK バー（`差し替えるまでデプロイしない` のコメント付き）は
**公開前の live に既に存在していた**もので、今回の PUT で新たに出したものではない。

★**index.html 自体を contents API で PUT できることを、今回初めて実地で確認した。**
`deploy-via-gh-contents-api` のメモは「index.html には触れない（＝他ファイルだけ PUT する）」運用だったが、
これは working tree の未承認の変更を巻き込む事故を避けるための方針であって、API 側の制約ではない。
8MB → base64 約10.8MB の JSON payload を GitHub は受け付ける。手順:
`base64 -i index.html | tr -d '\n'` → `jq -n --rawfile c … '{message,content,sha,branch:"main"}'` →
`gh api -X PUT … --input payload.json`。
なお **zsh では URL の `?` を必ずクォートする**（`contents/index.html?ref=main` が glob 展開されて
`no matches found` になる）。

### 検証の程度について（正直に書いておく）

続き6のような **headless Chrome での DOM 実測はしていない。** 今回の確認は
(1) live 全体の md5 とローカルの一致、(2) 実際に配信されている HTML の当該 `<section>` を
`curl` で取得して全文目視、の2点まで。CSS もセレクタも JS も触らない純粋な文言追加で、
`<ol>` 内に既存と同じ `<li>` を並べただけなので、レイアウトが崩れる経路が無いと判断した。
**この判断が妥当なのは「表示層のうち文言だけ」の変更に限る。** クラスや構造をいじったときは
罠#136 のとおり md5 一致だけで合格にせず、実描画を測ること。

---

## 2026-08-03（続き2）— Buyee の費用内訳注記を "Ships worldwide" に追加（海外モードのみ表示）

`card()` の `gWorld` に `.buyee-note` を1つ足した。**表示層のみ。DATA も検索ロジックも触っていない。**
CSS 1ルール（4行）＋ JS 8行の計 **12 insertions(+), 0 deletions**。

### 何を出したか

```js
const buyeeNote = !jp
  ? `<div class="buyee-note">Buyee adds a flat ¥500 service fee, plus Japan domestic shipping (¥150&ndash;1,500) and international shipping &mdash; the international cost is shown once your item reaches their warehouse.</div>`
  : '';
```

分岐は `!jp` の1本だけ。国内モードでは出さない——国内の買い手は Buyee を経由しないので代行手数料の話が要らない。
`worldDesc`（1327〜1331行）が `jp` × `noProxy` の**4パターン**を持つのに対し、こちらは **2パターン**であることに注意。
同じ `gWorld` の中に粒度の違う2つの分岐が並んでいる。

続き1（howto への3行）が「代行とは何か」を `<ol>` で説明したのに対し、これは**カード上の買う導線のすぐ横**に
費用を出す。howto を読まずにボタンを押す人に届く位置に置いたのが今回の主眼。

### 置き場所: `.stores` の**中ではなく** `.group` 直下

```html
<div class="stores"> …Buyee / eBay ボタン… </div>
${buyeeNote}          ← ここ（1350行）
</div>
```

★`.stores` は `grid-template-columns:repeat(auto-fit,minmax(130px,1fr))`（312行）。
**中に `<span>` や `<div>` を入れると、それがグリッドセルを1つ占有してボタンと横並びになり、ボタン幅が崩れる。**
「ボタンの直下に出したい」からといって `.stores` の内側に入れてはいけない（どうしても入れるなら `grid-column:1/-1` が必須）。
`.card .group` は `display:flex; flex-direction:column; gap:8px`（308行）なので、`.group` 直下に足せば
**間隔は親が持ってくれる＝子側にレイアウトプロパティを一切書かずに済む。**

副作用として、この注記は視覚上 Buyee と eBay の**両方**を含むグループの下に付く。文中に "Buyee" と
明記してあるので誤読は起きにくいと判断したが、Buyee 専用の注記ではない位置にあることは意識しておくこと。

### CSS は文字サイズと色だけ。値は `.rak-market` ではなく `.group-label .d` に揃えた

```css
.card .buyee-note{font:400 11.5px/1.4 var(--f-display); color:var(--faint);}
```

指示は「`.rak-market`（466行付近）を参考に」だったが、`.rak-market` は `.card` スコープ**外**で
Space Mono 14px / `var(--muted)`。同じカード内で同じ役割（小さい説明文）を担っているのは
`.group-label .d`（311行）なので、そちらの値を採った。`.rak-market` から受け継いだのは
**「文字サイズと色だけを書き、`display`/`margin`/`padding` を書かない」という書き方**の方。

### 設計判断: 総額計算機ではなく「内訳の説明」に留めた

実額を足し上げて `total ≈ ¥X` を出す方向は**採らなかった**。**国際送料が倉庫到着まで確定しない**ため、
総額を出せば必ず外れる数字を出すことになる。だから文末を
"the international cost is shown once your item reaches their warehouse" にして、
**「いつ分かるか」を答えにした**（いくらかは答えられないので）。

これは 2195〜2196行の **landed cost 計算機**の議論と同じ判断軸。あそこで為替表示を見送った理由
「正確なレートを持っていない／固定値は必ずいつか嘘になる（罠#19）／目的が違う」がそのまま効く。
**内訳の説明は今の1ファイル完結で書ける。総額計算は書けない。** 精密計算をやるなら Buyee の料金表と
重量・配送方法を持つ必要があり、それは landed cost 計算機（プレミアム候補）側の仕事。

### ★金額の出どころについて（正直に書いておく）

`¥500 固定` / `国内発送 ¥150〜1,500` / `国際送料は倉庫到着まで確定しない` の3点は、
**今回のセッションでは指示として与えられた文言をそのまま入れたものであり、この作業の中で
Buyee の料金表に当たって実測・照合はしていない。** live に載ったこと（md5一致）は確認済みだが、
**数字そのものの正しさは今回の検証範囲外。**

さらに、**ハードコードした金額は必ずいつか嘘になる**（罠#19 と同じ構造）。Buyee が手数料を改定しても
このページは気付かない。将来の対策候補: (a) 金額を落として "a service fee plus domestic and
international shipping" と質的表現に留める、(b) 料金表の確認日を併記する、(c) 定期確認をタスク化する。
**次に Buyee 周りを触る人は、まず現行の料金表と突き合わせること。**

### 検証

| 項目 | 結果 |
|---|---|
| DATA行 md5 | `8bd747412be22b2ec93b692185a62551` — **不変**。ただし CSS 4行追加で**行番号が 908 → 912 に移動** |
| `node --check` | `<script>` 2ブロック（**912〜2144 / 2147〜2166**）を切り出して実行、いずれも OK |
| 注記文字列 | 指示文字列と `diff` で完全一致。`&ndash;` / `&mdash;` はエンティティのまま |
| diff の範囲 | 意図した3ハンク（CSS 1・const 1・挿入 1）以外に差分なし |

サイズ: 8,079,131 →（12行追加）**8,080,102 バイト**。
DATA の行番号は続き6で 901→906、続き1で 906→908、今回 908→912 と動き続けている。
**毎回 `grep -n '^const DATA = \[\['` で取り直すこと**（行番号を覚えない）。

### バックアップ

`index.html.20260803-bak-pre-buyee-note`（8,079,131B / md5 `9cf1bd2e7e1c7f83323e0a1fb8a25e90`）、
ノートは `BIN_DIVER_NOTES.md.20260803-bak-pre-buyee-note`。

### 公開状態

**公開済み**（2026-08-03）。

| 項目 | 値 |
|---|---|
| ローカルコミット | `02bad8d8174a40896a64ece7d510570b1d0680ee`（index.html のみ・1 file changed, 12 insertions(+)） |
| Pages コミット | `9045f44569c789f1177a363f676433c7fbf7720c` |
| blob sha | `3d1c8324…` → `0cd927b444a60833e3b89eb58cd08e0c5c0a67f5`（ローカル git の blob と一致） |
| **live md5（公開後）** | **`fe73abb27347df0375a4dc627463ac99` / 8,080,102 バイト**（ローカル現行と `diff` で IDENTICAL） |
| **live DATA行(912) md5** | **`8bd747412be22b2ec93b692185a62551`**（無傷） |
| CNAME | `gamekataban.com` — PUT 前後で健在 |
| Pages ビルド | `built` / 36,692ms |

★**上書き前のドリフト確認**: remote blob sha `3d1c8324…` と、作業前バックアップを
`git hash-object` した sha が**一致**することを確認してから PUT した（続き1で確立した手順の再実行）。
live が編集前の状態そのものだと確かめた上での上書きなので、他所からの未知の変更を潰していない。
続き1で初実施した「index.html 自体を contents API で PUT」は今回も問題なく通り、**再現性を確認**した。

### 検証の程度について（正直に書いておく）

続き1と同じく **headless Chrome での DOM 実測はしていない。** 確認したのは
(1) live 全体の md5 とローカルの完全一致、(2) live の HTML を `curl` で取得して該当3箇所を grep 目視、
(3) `node --check`、の3点。すなわち**「コードが本番に乗っていること」は証明したが、
「海外モードで実際に描画されること」「jp モードで消えること」「ボタン幅が崩れないこと」は未確認。**

★続き1は「CSS もセレクタも触らない純粋な文言追加」だったので md5 一致で合格にしたが、
**今回は CSS セレクタを1つ新設し、DOM ノードを1つ増やしている。** 罠#136 の基準では
**実描画を測るべき変更に該当する。** `.stores` の外に置いたので理屈上グリッドには影響しないはず、
という**推論で止まっている**ことを明記しておく。次に触る人は LOC トグルを実機で往復させて確認すること。

---

## 2026-08-03（続き3・補足訂正）— Buyee の3金額を公式ページで裏取りした。続き2の「検証範囲外」を撤回する

続き2の「★金額の出どころについて（正直に書いておく）」で
**「数字そのものの正しさは今回の検証範囲外」**と書いたが、**その後（同日）実際に Buyee 公式に当たり、
3点とも裏が取れた。当該記述は撤回する。** 3つの金額は根拠のない指示文言ではなく、**公式の記載と一致する値**である。

**時系列は正確に書いておく: 注記を書いて deploy した時点では未照合で、照合はこの追記の時点（後追い）。**
値が正しいことと、正しいと確かめてから書いたことは別。続き2の記述は「当時の状態」としては誤っていない。

### 出典と、そこに実際に書かれている文言

| 金額 | 出典 URL | 公式の記載（原文） |
|---|---|---|
| 手数料 **¥500 固定** | `https://bc.help.buyee.jp/en/fee/` | **"500 yen per order"**（Purchase Fee。注文時の初回支払いで課金される定額） |
| 国内発送 **¥150〜1,500** | `https://bc.help.buyee.jp/en/shipping-fees` | **"Typically, the price falls within the range of 150 yen to 1,500 yen."** |
| 国際送料は**倉庫到着まで確定不可** | 同上 | **"International shipping fees are calculated based on weight and size, so we are unable to provide customers with information on international shipping fees until the item arrives at the warehouse and the weight and size are precisely determined."** |

**確認日: 2026-08-03。** 続き2で「将来の対策候補(b) 料金表の確認日を併記する」と書いた件は、この行をもって満たす。
次に Buyee 周りを触る人は、**この日付を起点に再確認**すること。

### `buyee.jp/helpcenter/guide/fees` は取得できなかった（403）

想定していた `https://buyee.jp/helpcenter/guide/fees`（`?lang=en` 付きでも）は **HTTP 403 で本文を取れなかった**。
bot ブロックと思われ、**ブラウザでは開ける可能性が高い**が、自動取得の裏取り先としては使えない。
**裏取りは `bc.help.buyee.jp`（Buyee Cart のヘルプセンター）側で行った。** 同じ Buyee 公式だが別ホスト。
将来スクリプトで料金を監視するなら、**`bc.help.buyee.jp` を参照先にすること**（`buyee.jp/helpcenter` は 403 で詰む）。

### 裏取りで分かった精度の話（注記の文言は妥当だった）

★**¥500 は「1注文あたり」であって「1商品あたり」ではない。** 同一ストアからの複数購入は、
点数によらず定額 ¥500。現行の注記は "a flat ¥500 service fee" と書いており、
**per item とは主張していないので誤りではない**が、**"flat" が「注文単位で定額」の意味だと
読み手に伝わるかは別問題。** 複数点買う人には有利な条件なので、将来 "flat ¥500 per order" と
明示する余地はある（文字数は増える）。

★**注記は費用を網羅していない。** 公式が挙げる項目は他に
**補償プラン（最大 ¥500・任意）／ GST 等の税（国により実費）／ オプション（同梱・保護梱包・写真）**がある。
注記は "adds a flat ¥500 service fee, plus …" と**加算の一部を示す形**で、網羅を主張していないので誤りではない。
関税については続き1で howto の `<li>` 側に "customs duties may apply" として既に書いてある
（**カード上の注記と howto で分担している**——両方に書くと続き1の罠#144 と同じ重複になる）。

### 続き2のどの記述が生きていて、どれが死んだか

| 続き2の記述 | 現状 |
|---|---|
| 「Buyee の料金表に当たって実測・照合はしていない」 | **撤回**（この追記で照合済み） |
| 「数字そのものの正しさは今回の検証範囲外」 | **撤回**（3点とも公式記載と一致） |
| 「ハードコードした金額は必ずいつか嘘になる（罠#19 と同じ構造）」 | **有効**。裏が取れても改定リスクは消えない |
| 「Buyee が改定してもこのページは気付かない」 | **有効**。監視の仕組みは無いまま |
| 対策候補(b)「確認日を併記」 | **実施**（確認日 2026-08-03 を上の表に記録） |
| 対策候補(a)「金額を落として質的表現に留める」/(c)「定期確認をタスク化」 | **未実施**。候補として残す |

### index.html は変更していない

**この追記はノートのみ。`index.html` は1バイトも触っていない。** 注記の文言は続き2で deploy した
`02bad8d` / Pages `9045f445` のまま（live md5 `fe73abb27347df0375a4dc627463ac99`）。
裏取りの結果、**文言を直す必要が無かった**ため再デプロイもしていない。

### バックアップ

`BIN_DIVER_NOTES.md.20260803-bak-pre-buyee-fee-verify`（6,358行時点）。

---

## 2026-08-03（続き4）— 実機確認済み。続き2の「実描画は未確認」は解消

続き2の末尾「検証の程度について（正直に書いておく）」で**未確認として残した3点**について、
**Koha が本番デプロイ後に実機（ブラウザ）で gamekataban.com を直接開いて確認し、3点とも問題なし**だった。

| 続き2で未確認だった点 | 実機での結果 |
|---|---|
| 海外モードで実際に描画されるか | **表示される**。Buyee ボタンの下に注記が出る |
| jp モードで消えるか | **消える**。国内モードに切り替えると注記は出ない |
| `.stores` のボタン幅が崩れないか | **崩れていない**。ボタンの幅・並びとも従来どおり |

よって続き2の **「`.stores` の外に置いたので理屈上グリッドには影響しないはず、という推論で止まっている」
という留保は解消。推論どおりだったことが実機で裏付けられた。**

### この確認で実証されたこと

★**`.group` 直下・`.stores` の外に置く判断は正しかった。** `.stores` は
`grid-template-columns:repeat(auto-fit,minmax(130px,1fr))`（312行）なので、
中に入れればセルを1つ占有してボタン幅が崩れる——という続き2の設計判断が、
**実際にボタンが崩れていないことで裏側から確認できた**（崩れる方の経路は踏んでいないので直接の証明ではないが、
少なくとも選んだ方の経路が安全であることは確定）。

★**`!jp` の分岐が両方向で効いている。** 片方向（出る）だけでなく**消える側も確認済み**。
表示分岐は「出ること」だけ見て合格にすると、消えるべき場面で出ているのを見落とす。
**LOC トグルを往復させて両方見るのが正しい確認手順**（今回はそれをやっている）。

### 確認方法の記録（後から精度を判断できるように）

**方法: 人手によるブラウザ実機の目視。** 続き6でやったような **headless Chrome での DOM 実測
（要素の getBoundingClientRect 等を数値で取る）ではない。** 罠#136 が要求する「実描画を測る」に対して、
今回は**「実描画を見る」**まで。ただし今回の判定対象は
**(a) ノードが出るか消えるか / (b) ボタンが横に潰れていないか**という、
**目視で確実に判別できる種類の差**なので、この方法で十分と判断する。
ピクセル単位の寸法や配色コントラストを問題にするときは、目視ではなく実測が要る。

### これで続き2〜続き4の未確認事項は一通り閉じた

| 論点 | 状態 |
|---|---|
| コードが本番に乗っているか | **確認済**（続き2・live md5 一致 / `diff` IDENTICAL） |
| 金額3点が公式と一致するか | **確認済**（続き3・出典URL付き / 確認日 2026-08-03） |
| 実際に描画されるか・消えるか・崩れないか | **確認済**（続き4・実機目視） |
| Buyee が料金改定したとき気付けるか | **未解決**。監視の仕組みは無いまま（続き2の指摘は生きている） |

**残るリスクは金額の陳腐化のみ**で、実装・表示側の懸念は現時点で無い。

### index.html は変更していない

**この追記もノートのみ。** 実機で問題が出なかったので修正は不要、再デプロイもなし。
live は `02bad8d` / Pages `9045f445` / md5 `fe73abb27347df0375a4dc627463ac99` のまま。

### バックアップ

`BIN_DIVER_NOTES.md.20260803-bak-pre-buyee-visual-check`（6,422行時点）。

## 2026-08-03（続き5）— コンディション表記モーダル。公式で裏取りしたら「ゲームソフト5項目」の前提が崩れた

ジャンル早見モーダルにコンディション表記の表を相乗りさせ、コミット・デプロイまで完了。
**ただし、追記の直前に出典を実際に叩いたところ、前提にしていた「ゲームソフトは5項目」が公式ページの記述と合っていなかった。**
実装済み・本番反映済みの内容と、公式が実際に書いていることの差をここに残す。

### 出典と確認方法（2026-08-03）

`https://www.suruga-ya.jp/man/customer_guide/used_item_policy.html`

WebFetch は 403。ブラウザ UA を付けた `curl` で HTTP 200 / 105,791 bytes を取得し、
タグを除去した本文に対して節見出し（`〜の取扱について`）の位置とラベル語の出現位置を突き合わせた。
節の帰属は「ラベルの文字位置が、直前の節見出しより後・次の節見出しより前か」で判定している。

### 公式ページが実際に定めていること

| ラベル | 定義されている節 | 適用範囲 |
|---|---|---|
| 【Sランク】【Bランク】【Cランク】【Dランク】 | `トレーディングカードの取扱について` | **トレカ専用**。節の内側にのみ出現 |
| 【未開封品】【帯付き】【美品】 | 冒頭の `コンディション表記について` | **全商品共通**。カテゴリ別ではない |
| 「ジャンク商品」 | 冒頭の共通記述 | 全商品共通。ノークレーム・ノーリターン |
| 「ランクＢ」 | カテゴリごとに個別記述。`PCソフト・ゲームソフトの取扱について` にもある | 「パッケージ、ソフト本体、説明書等に…傷み・汚れ…**欠品はございません**」 |

`PCソフト・ゲームソフトの取扱について` 節の全文を読んだが、**この節が定義するコンディション表記は「ランクＢ」ただ1つ**。
節内の出現回数は ランクＢ=1 / ジャンク=0 / 未開封=0 / 美品=0 / 帯付=0。

### 指示された要約との食い違い（4点）

作業指示は「S/A/B/C/Dランクはトレーディングカード専用、ゲームソフトは表記なし/ランクB/ジャンク/未開封品/美品の5項目のみ」だったが、公式ページと照合すると：

1. **「Aランク」は公式ページに存在しない。** `Aランク` `Ａランク` とも 0 回。実際は **S/B/C/D の4段階**。
   「トレカ専用」は正しいが、段階の並び自体が第三者情報のまま混入していた。
2. **「5項目」はゲームソフト固有の体系ではない。** 未開封品/帯付き/美品は全商品共通節、ジャンクも共通、
   ランクＢだけがゲームソフト節にある。カテゴリで括れる集合ではない。
3. **【帯付き】が5項目から漏れている。** 共通節は 未開封品・帯付き・美品の**3つ**を並べており、帯付きだけ落ちている。
   ゲームソフトにも帯は存在する（同節に「帯、応募はがき…特に記載が無い場合につきましては付属致しません」）。
4. **「表記なし」は公式ページ上のラベルではない。** 本文中に 0 回。
   「記載ないものについては対象外となります」から概念としては導けるが、駿河屋が掲げている表記名ではない。

### 罠#144. 出典を叩く前に「公式で確認済み」の体裁で要約を受け取り、そのまま実装・デプロイした

CONDITION_GUIDE の5項目は、出典URLを一度も開かないまま実装・本番反映まで進んだ。
URL を実際に叩いたのは**デプロイ後・ノート追記の直前**で、そこで初めて上記4点の食い違いが出た。
罠#112（一般集計サイトの数値を事実として述べた）と同型だが、今回は**ノートに「公式で確認した5項目」と書き込む一歩手前**だった。
ノートに入れば以後の作業はそれを一次情報として扱うので、誤りが固定化する。
対策：**出典URLが付いた事実は、実装前に自分で叩く。403 なら UA を付けて取り直す。ノート追記時ではなく実装前に。**

補足（提供元の記録）：最初の CONDITION_GUIDE は、指示文が「上記のCONDITION_GUIDE配列」と参照していたが
会話にもリポジトリにも実体が無く（`grep -rn CONDITION_GUIDE .` で0件）、**Claude が中古ゲームの一般語彙から12件を起こした**もの。
その後 5件に差し替えられた。つまりこの表は最初から最後まで一次情報を経由していない。

### 実装：既存のジャンル早見モーダルに相乗りさせた

モーダルを新設せず、`#genre-modal` の中に2つ目の表として足した。器・開閉ロジック・Escape/オーバーレイの閉じ方は既存のまま。

- `CONDITION_GUIDE` は `GENRE_GUIDE` と同じ `{en, terms[], jp}` 形。**行テンプレートを `guideRow` に切り出して2つの表で共用**（片方だけ直して食い違うのを防ぐ）。
- 描画は読み込み時に1回だけ。`GENRE_GUIDE.map(guideRow)` → 見出し `.genre-section-h` → `CONDITION_GUIDE.map(guideRow)` の順で連結。
- 全値 `esc()` 経由。`en` の em ダッシュ（`No notation — standard`）は `&`/`<`/`>` のみ対象なので素通りする。
- `.genre-row` は 15 + 5 = **20行**。

### 横書き併記（.genre-tag-h / .genre-jp-h）と、配置の試行錯誤

縦書き棚札と同じ見た目（白地#fff・黒字#111・1px solid #111・padding 11px 9px）で `writing-mode` だけ外した札を併記した。到達までに3段階かかっている。

1. **`.genre-tag` の直後に兄弟として追加 → 右隣に並んだ。** `.genre-row{display:flex}` なので「すぐ下」にはならない。
   縦札120px＋横札135pxで、左の英語列を約270px圧迫した。
2. **`.genre-tags`（`flex-direction:column`）で囲んで縦積み。** 右端の占有幅は横札の最長分（約135px）に収まった。
3. **横書き札を `.genre-main` の中へ移動。** `h3.genre-en` の直後・`.genre-terms` の直前。
   右端は縦札だけになり、`.genre-tags` は1枚しか包まないが「行の右端の置き場所」を持つ役として残した。

移動に伴う調整：
- `.genre-tag-h` は `flex:0 0 auto; display:flex` → **`display:inline-block; margin:0 0 8px`**。ブロックのままだと幅が親いっぱいに伸び、「美品」2字が横長の白帯になる。margin は `h3.genre-en` の `margin:0 0 8px` と同値。
- **`.genre-jp-h` から `min-inline-size:120px` を外した。** 幅の下限は子側が持っていたので、ラッパだけ inline-block にしても短い札は120px幅のまま変わらず、「内容幅」にならなかった。
- モバイル `@media (max-width:560px)` を分割：`.genre-jp, .genre-jp-h{font-size:14px; padding:9px 7px;}` と `.genre-jp{min-inline-size:100px;}`。字と余白は両方縮め、長さの下限は縦札だけに残す。

### 見出し帯のデザイン（.genre-section-h / .genre-bar）

`.genre-bar` に合わせたかったが、**`.genre-bar` は `background` を宣言していなかった**。
見えていた色は親 `.genre-in{background:var(--surface)}` が透けていただけで、`#genre-modal-body` の地も同じ `--surface`。
つまり「同じ値」を入れても帯にならない（実際に一度 `var(--surface)` を入れて無効果を確認した）。上部バーの区切りは背景ではなく `border-bottom` が作っていた。

最終形：

```css
.genre-section-h{font:500 16px/1.3 var(--f-display); color:var(--ink);
  background:var(--field); margin:0 -18px; padding:10px 18px;
  border-top:1px solid var(--line); border-bottom:1px solid var(--line);
  border-left:3px solid var(--accent);}
.genre-bar{… padding:16px 18px; background:var(--field);
  border-bottom:1px solid var(--line); border-left:3px solid var(--accent);}
```

- `--field` #140d24 は `--surface` #0b0714 より1段明るい（入力欄・チップと同じ地）。これで面として浮く。
- `margin:0 -18px` が `#genre-modal-body{padding:18px}` を打ち消し、帯が左右端まで伸びる。padding も同じ18pxなので字の左端は動かない。
- `.genre-bar` に `border-top` は付けない。`.genre-in` の `border-radius:12px` の内側に接しており、角が濁る。
- `--accent` は `:root` にあるのでモーダルから解決できる（`--f-display` が `.card{}` 内定義で未解決になった件とは別）。
- ★未確認：`.genre-bar` の左3pxは `.genre-in` の `border-radius:12px` ＋ `overflow:hidden` で**左上が丸く切れる**。実機目視はしていない。

### コミット・デプロイ

| 項目 | 値 |
|---|---|
| ローカルコミット | `570cd90`（`modal: add condition term guide alongside genre guide` / index.html のみ・76 insertions, 10 deletions） |
| リモートコミット | `5218b4e267da48acddc60cfb7962db713d15658d`（gh contents API） |
| blob sha | `5c1a0f6da2a124907069c8c7025a4cf28a860b86`（ローカル `git hash-object` と一致） |
| Pages | `built` / error なし（約42秒） |
| live md5 | `6bee13fdee00343fca3250eb67ded255`（ローカルと一致） |
| DATA行 md5 | `8bd747412be22b2ec93b692185a62551`（canonical 不変。live 側でも確認） |
| CNAME | `gamekataban.com` 変更なし |

デプロイ前に「live == 編集前ローカル」を確認した（remote blob sha `0cd927b4…` = 編集前バックアップの `git hash-object` = `HEAD:index.html`）。drift なし。

### 未解決（意図して残す）

- **本番の CONDITION_GUIDE 5項目は、上記4点の食い違いを抱えたまま公開されている。** index.html は未修正。
  直すなら候補は「帯付き」の追加、「表記なし」の扱い（公式ラベルではない旨か、文言変更）、
  ランクB以外を「全商品共通の表記」として括り直すこと。**どれも未着手。**
- モーダルの実機目視が未了。帯・縦横2枚の札・角丸で切れる桃線は静的検証のみ。

### バックアップ

`BIN_DIVER_NOTES.md.20260803-bak-pre-condition-note`（6,479行時点）。
index.html 側は `index.html.20260803-bak-pre-condition-guide`（作業開始時）と `index.html.20260803-bak-pre-genre-jp-h`（横書き併記の着手前）。

## 2026-08-03（続き6）— CONDITION_GUIDE を公式ラベルに合わせて修正。続き5の「食い違いを抱えたまま公開中」は解消

続き5で洗い出した公式ページとの食い違いのうち、**表示に出る2点を直して本番に反映した。**
続き5の「未解決（意図して残す）」に書いた **「本番の CONDITION_GUIDE 5項目は、上記4点の食い違いを抱えたまま
公開されている。index.html は未修正」という記述は、この修正で解消**。以下がその置き換え後の状態。

### 修正内容：「表記なし」を落として「帯付き」を入れた

```js
const CONDITION_GUIDE = [
  {en:'Rank B — visible wear', terms:['case or manual damage','plays fine, nothing missing'], jp:'ランクB'},
  {en:'Junk — as-is', terms:['no returns','not guaranteed to work'], jp:'ジャンク'},
  {en:'Unopened', terms:['sealed','shrink wrap intact'], jp:'未開封品'},
  {en:'Near-mint (used)', terms:['opened but like new','little to no wear'], jp:'美品'},
  {en:'With obi strip', terms:['original obi band included'], jp:'帯付き'}
];
```

配列の中身だけの変更（2 insertions, 2 deletions）。`guideRow`・CSS・見出し・ボタン文言には触れていない。
件数は5のままなので `.genre-row` は 15 + 5 = **20行**で不変。

**これで5件すべてが駿河屋の実在ラベルに対応した：**

| 表示 | 公式ページでの位置づけ |
|---|---|
| ランクB | `PCソフト・ゲームソフトの取扱について` 節の「ランクＢ」。「欠品はございません」 |
| ジャンク | 冒頭共通の「ジャンク商品」。ノークレーム・ノーリターン |
| 未開封品 | 冒頭 `コンディション表記について` の【未開封品】 |
| 美品 | 同【美品】 |
| 帯付き | 同【帯付き】 |

### 続き5の食い違い4点：どれが片付いたか

| # | 続き5で挙げた食い違い | 現状 |
|---|---|---|
| 1 | 「Aランク」は公式に存在しない（実際は S/B/C/D の4段階） | **表示には影響しない**。モーダルはトレカランクを出していないため、ノート上の記録事項として残る |
| 2 | 「5項目」はゲームソフト固有の体系ではない（ランクBのみがゲームソフト節、他4つは全商品共通節） | **未解決**。表示は正しいが「ゲームソフトの5段階評価」と読まれる余地は残っている。`.genre-intro` 相当の一文を足す案は**未着手** |
| 3 | 【帯付き】が漏れている | **解消**（今回追加） |
| 4 | 「表記なし」は公式ラベルではない | **解消**（今回削除。本番ファイル全体を検索して0件） |

### コミット・デプロイ

| 項目 | 値 |
|---|---|
| ローカルコミット | `d065b68`（`modal: fix condition guide terms to match official Suruga-ya labels` / index.html のみ） |
| リモートコミット | `a7dd874cc7abfc9d4fa9846a0aade2f2ca819af0`（gh contents API） |
| blob sha | `2242604c6b03929f566ec8d067d597573d89e982`（ローカル `git hash-object` と一致） |
| Pages | `built` / error なし |
| **live md5** | **`a8627f3c6b614a880222f3470f21e7c1`**（ローカルと一致・`cmp` で IDENTICAL。前回の `6bee13fd…` から入れ替わり） |
| DATA行 md5 | `8bd747412be22b2ec93b692185a62551`（canonical 不変。live 側でも確認） |
| CNAME | `gamekataban.com` 変更なし |

デプロイ前の drift 確認：live blob sha が `HEAD:index.html` および前回デプロイ記録 `5c1a0f6d…` と一致。巻き込みなし。

本番ファイルに対する検証：**「表記なし」0件**、`CONDITION_GUIDE` 定義は上記のとおり、
本番JSを Node で実行して `.genre-row` 20件・縦札20・横札20、見出し以降の札は
`ランクB / ジャンク / 未開封品 / 美品 / 帯付き`。

### 実機確認

**Koha が本番デプロイ後に実機（ブラウザ）で gamekataban.com を直接開いて目視確認済み。**
これにより続き5の「モーダルの実機目視が未了。帯・縦横2枚の札・角丸で切れる桃線は静的検証のみ」も解消。
続き5で ★未確認 として残した **`.genre-bar` の左3pxが `.genre-in` の `border-radius:12px` ＋ `overflow:hidden` で
左上が丸く切れる件**も、実機で問題として上がっていない。

### 続き5からの状態遷移まとめ

| 論点 | 続き5時点 | 現在 |
|---|---|---|
| 帯付きの欠落 | 未修正のまま公開中 | **解消**（`d065b68` / `a7dd874`） |
| 「表記なし」が公式ラベルでない | 未修正のまま公開中 | **解消**（本番から0件） |
| Aランクの誤り | ノート記録のみ | 変わらず（表示に出ない） |
| 「5項目＝ゲームソフト体系」の読まれ方 | 未解決 | **未解決のまま** |
| モーダルの実機目視 | 未了 | **解消**（Koha 実機確認） |

### バックアップ

`BIN_DIVER_NOTES.md.20260803-bak-pre-condition-fix-note`（6,603行時点）。

---

## 2026-08-03（続き7）— howto に「ブックオフのカード決済は全店ではない」注記を追加。現金推奨の根拠を公式で裏取り

### 追加した文言

`<section class="howto">` の `Inside a store in Japan (Book-Off, Hard-Off, Suruga-ya…)` の `<ol>` に
**4項目目**として1行追加（既存3項目・他セクションには一切触れていない）。

```html
<li>Most Book-Off stores take major credit cards, but not all — bring cash as backup.</li>
```

`index.html:880`。インデントは既存の兄弟 `<li>` と同じ6スペース。
指定文字列との一致は `cmp` でバイト単位確認済み（em dash `—` 含め完全一致）。

### 公式ページでの裏取り

出典：`https://www.bookoff.co.jp/qa/card.html`（ブックオフ公式 Q&A）。本セッションで実際に取得して確認した。

| 支払い手段 | 公式の記載 |
|---|---|
| クレジットカード | 「こちらのサービスは現在**一部の店舗への導入**となっております。サービス利用可能店舗は順次拡大していきます。」 |
| 電子マネー | 同上（同一文面） |
| バーコード決済 | 同上（同一文面） |

3手段すべてが同一文面で「一部の店舗への導入」。**全店舗導入ではない**ことが公式に明記されている。
よって `but not all` は公式記載に直接対応する。

### ★現金についてだけは公式の裏取りではない（区別して記録する）

**当該ページに現金に関する記載は無い。** 「現金は常に使える」は、
現金が特記を要する導入サービスとして列挙されていない＝既定の支払い手段である、という**推定**であって、
このページが明示している事実ではない。

したがって `bring cash as backup` の根拠は：
- 「カード類が一部店舗のみ」＝**公式明記**（裏取り済み）
- 「だから現金を持て」＝**上記からの実務的な帰結**（現金可の明文根拠はこのページには無い）

文言自体は「カードが使えない店がありうるから現金も持て」としか言っておらず、
「全店で現金が使える」とは主張していないため、**表示上の問題は無い**。
ただし将来この一文の根拠を問われたときに「公式で現金可を確認した」と誤って引き継がないよう、ここに区別を残す。

### コミット・デプロイ

| 項目 | 値 |
|---|---|
| ローカルコミット | `bedb3a4`（`howto: note that Book-Off card acceptance varies by store` / index.html のみ・1 insertion） |
| リモートコミット | `39172b9b6aa972403fa1d926e0178b3887286bd4`（gh contents API） |
| アップロード後 blob sha | `10b74a1d055503c7266130d1aa6e41d06cdac38a`（ローカル `git hash-object` と一致） |
| **live md5** | **`ffe8e1986e318a86ee6c09e057b5f95f`**（ローカルと一致。続き6の `a8627f3c…` から入れ替わり） |
| DATA行 md5 | `8bd747412be22b2ec93b692185a62551`（canonical 不変） |

デプロイ前の drift 確認：live blob sha `2242604c…` が編集前バックアップ
`index.html.20260803-bak-pre-payment-note` の `git hash-object` と一致。巻き込みなし。
（`2242604c…` は続き6のデプロイ記録と同一 ＝ 続き6以降 live は誰にも触られていない。）

反映確認：キャッシュバスター付き curl を15秒間隔でポーリング、**4回目（約45秒）で live md5 が一致**。
live を再取得して `Inside a store in Japan` 直後の `<ol>` が4項目になっていること、
880行目が指定文字列であることを本文で確認済み。

### 実機確認

**Koha が本番デプロイ後に実機で gamekataban.com を直接開いて目視確認済み。**

### node --check についての手順メモ（次回のため）

`node --check index.html` は `ERR_UNKNOWN_FILE_EXTENSION` で**通らない**（`.html` を JS として扱えない）。
インライン script を行範囲で抜き出して個別に検査する必要がある。今回の範囲は
`<script>` が 958行目・2213行目、`</script>` が 2212行目・2234行目なので、
959–2211行 と 2214–2233行 を `.js` に切り出して `node --check`。両方 OK。
（行番号は今回の1行追加後のもの。次回は `grep -n "<script\|</script>"` で取り直すこと。）

### KLOOK コメントについて

`index.html` の `<!-- ★KLOOK: 承認後、本番トラッキングリンクに差し替える。差し替えるまでデプロイしない。 -->` は
今回のデプロイ対象に含まれるが、Klook 部分は現状リンクではなく無効化スタブ
（`<span class="kk-btn" aria-disabled="true">Klook experiences — coming soon</span>`）であり、
かつデプロイ前の live と完全一致していた。**今回のデプロイで新たに公開されたものではなく、
承認前トラッキングリンクが本番に出た事実は無い。**

### バックアップ

- `index.html.20260803-bak-pre-payment-note`（8,085,628 bytes / 編集前）
- `BIN_DIVER_NOTES.md.20260803-bak-pre-payment-note`（6,682行時点）

## 2026-08-03（続き8）— 画像の第2ソース候補を追加調査 → 全滅。Yahoo直接問い合わせも見送り、画像カバレッジ拡大は打ち止め

**背景**：バリューコマース審査 不承認（2026-07-30・上記「【結末】」節）を受け、Yahoo以外の画像ソースを洗い直した。既存却下分（Wikidata P18 / libretro / IGDB / LaunchBox / Wikipedia / Google画像検索）は再検討せず、**今回新たに調べた3本**についての記録。

### 追加で調べた道

| 道 | 結果 | 理由 |
|---|---|---|
| 駿河屋（自社アフィリエイト） | **却下** | 公式に画像掲載は許可されている（`affiliate.suruga-ya.jp` に明記）。だが取得方法が**商品詳細ページのボタンを1点ずつ手動クリックする方式のみ**で、JAN/型番からの一括取得APIが無い。既知の「XML/CSVフィード無し」と一致。37,855行への適用は不可能。 |
| Amazonアソシエイト（PA-API） | **却下** | **鶏と卵構造**。PA-API（画像取得可）を使うには先に通常リンクで売上実績を作り審査を通す必要があり、通過後も**過去30日以内に売上が無いとアクセスを失う**。新規サイトが今すぐ乗れる設計ではない。 |
| Yahoo!デベロッパーネットワークへ直接問い合わせ | **着手見送り** | 公式ヘルプ（H000011080）が「**非商用目的のみ許可、商用利用は個別相談**」と明記。バリューコマース不承認と同種の**審査依存構造**になる可能性が高いと判断し、今回は問い合わせを実施しないことにした。 |

★駿河屋は「権利はクリーンなのに規模で落ちた」唯一の例。**却下理由が権利ではなく"APIが無い"であること**を混同しないよう区別して残す（将来フィードが提供されれば復活しうる唯一の候補）。

### 確定した全体像

画像の第2ソース化は、調査した**全ての道が「権利」「規模（API無し）」「審査依存」のいずれか**で閉じた。

| 落ち方 | 該当 |
|---|---|
| 権利 | libretro（ライセンス不在）、Wikipedia/Google画像検索、LaunchBox |
| 規模（API無し／薄い） | 駿河屋（一括取得API無し）、Wikidata P18（4.8%＋大半が非ゲーム） |
| 審査依存 | Yahoo（バリューコマース不承認）、Amazon PA-API（売上実績が前提） |

**楽天単独（物理版 37.1% → 実装方針込みで見込み 42.4%）が現状の到達点。**

### 決定

画像カバレッジ拡大は**ここで打ち止め**とし、他の優先事項（Buyee審査結果・型番正規化の残タスク等）へ注力する。**再開条件は設けない。**
上記「【結末】バリューコマース審査 不承認 → Yahoo画像は保留（2026-07-30）」に置いた**再開の道(B)(C)は、本件の判断により事実上クローズ**とする。
（(B)＝他ASP経由での提携取り直し、(C)＝Web API利用規約の条文直読。どちらも今回の「審査依存の道には乗らない」という決定の内側に入る。）

★この節は**調査の記録であって、権利判断の撤回ではない**。既存却下分の理由（特に libretro の権利不在）は変わらず有効。

### 本セッションでの変更

**コード・データの変更は無し。** `index.html` / DATA 行 / `build_master.py` はいずれも未編集で、デプロイも行っていない。本節はノート追記のみ。

### バックアップ

- `BIN_DIVER_NOTES.md.20260803-bak-pre-image-2ndsource-note`（566,983 bytes / 6,768行時点）

## 2026-08-03（続き9）— Xbox 360 / Xbox / Wii U / 64DD のキーワード測定を実装。252行に Buyee リンクが復活

3554行・3640行に「★測定」として積んであった残タスクを消化した。測定結果と、
実装のときに出てきた**測定していない判断が混ざりこむ構造**の切り離しを記録する。

### 測定（実ブラウザ・シークレットウィンドウ）

| 機種 | 行数 | 採用キーワード | 結果 |
|---|---|---|---|
| Xbox 360 | 144 | `Xbox360` | BIOHAZARD 5 → 65件（1件目が目的物）/ GEARS OF WAR 3 → 36件。合格 |
| Xbox | 82 | `Xbox` | DOA3 → 95件 / Panzer Dragoon Orta → 7件。Xbox360・One版の混入はあるが目的物は1ページ目。合格 |
| Wii U | 20 | `Wii U` | マリオカート8 → 287件（Switch版混入）/ ベヨネッタ2 → 21件。同上で合格 |
| 64DD | 6 | `64DD` | 6行中4行が非0件。合格 |

★**測り方**：curl / WebFetch / ヘッドレスChrome は AWS WAF に遮断されて測定できない
（`x-amzn-waf-action: challenge` が返る）。ここを機械で測ろうとしないこと。
また**通常ウィンドウでは、こちらが付けていない未知のクエリパラメータが自動で付加され、
キーワードが落ちる**現象を観測した（原因未特定・シークレットでは再現せず。付加される
具体的なパラメータ名は実測時のURLを見ること ―― ここには残さない）。
「0件だった」と言う前に、まずURLにキーワードが残っているかを見ること。

★**マイナス検索は効かない**。Xbox の混入を `-360 -One` で削ろうとしたが 95→74件にしか
ならず、Buyee 側が除外演算子を解釈していないと判った。ネオジオ（罠#87）と同型。
混入を許して1ページ目到達で合格、という判断はネオジオのときと同じ。

★**64DD の残り2行**（MARIO ARTIST ペイントスタジオ / ポリゴンスタジオ）は
キーワード付きでもタイトル単体でも0件。キーワードが悪いのではなく**出品が実在しない**（罠#27）。
機種としては通っているので 64DD は入れた。

### 実装で出てきた分岐 ―― BUYEE_OK に足すと、収録範囲まで広がる

`build_master.py:558` は `KNOWN_PLATFORMS = set(PLATFORM_EN) | set(BUYEE_OK)` で、
**「取り込む機種」と「リンクを出す機種」が同じ集合から作られていた**。
この4機種は `PLATFORM_EN` に和名を持たないため、`BUYEE_OK` に足すだけで
**型番の無い行まで新たに取り込まれる**。実測して 2,344行：

| 機種 | 型番あり（既に出ている） | 型番なし（新たに入ってしまう） |
|---|---|---|
| Xbox 360 | 144 | 1,316（物理 946 / 配信 370） |
| Xbox | 82 | 188（物理 187 / 配信 1） |
| Wii U | 20 | 840（物理 115 / 配信 725） |
| 64DD | 6 | 0 |
| 計 | **252** | **2,344**（物理 1,248 / 配信 1,096） |

今回測ったのは「キーワードが Buyee に通るか」だけであって、収録範囲を広げるかは別の判断。
**測っていない判断を、測った判断の副作用として通さない**ため、切り離した：

```python
BUYEE_LINK_ONLY = {"Xbox 360", "Xbox", "Wii U", "64DD"}
KNOWN_PLATFORMS = set(PLATFORM_EN) | (set(BUYEE_OK) - BUYEE_LINK_ONLY)
```

引くのは `BUYEE_OK` 側だけ。`PLATFORM_EN` に和名が入った機種は「そのとき収録対象になった」
という意味なので消さない。収録範囲を広げると決めたら、`BUYEE_LINK_ONLY` から消せばそうなる。

★**556行のコメントの前提が変わった**。「Xbox系・Wii U は何も書かなくても自動的に外れる」は
もう成り立たない（`BUYEE_LINK_ONLY` が明示的に外している）。コメントも書き換えた。

### 途中で見つかった別件 ―― 手で直した型番が、再生成で戻る状態だった

`swap_data.py` が [0]〜[6] のガードで止まった。原因は**今回の変更とは無関係**で、
2026-08-02 のコミット `d662ee0`（`AGB--P-B9AJ` → `AGB-P-B9AJ`・二重ハイフン誤記）が
**index.html だけを手で直し、`build_master.py` に入れていなかった**こと。
つまり**次に再生成した瞬間に元の誤記へ戻る**状態だった。ガードが機能して見つかった。

`PRODUCT_CODE_TYPO` を作り、名指し1件だけ置いた。**パターン置換（`--`→`-` を畳む等）はしない**
――二重ハイフンが正しい型番が将来出てきたときに静かに壊すため。

★**未処理として残した**：MADB にはもう1件、二重ハイフンの型番がある。
`DL--DOL-GMSJ-JPN`（スーパーマリオサンシャイン / GameCube）。
index.html も今この値のままなので触っていない。直すなら `PRODUCT_CODE_TYPO` に足すこと。

★教訓：**index.html を手で直したら、必ず `build_master.py` にも書く。** 書かないと次の再生成で消える。

### 検証

- `master_final.csv` 39,782行 → 39,782行（**増減なし**。madb_id 単位で照合して追加・削除ゼロ）
- 変わったのは `buyee_kw` 列だけ・**252行ちょうど**（Xbox 360 144 / Xbox 82 / Wii U 20 / 64DD 6）
- `data_line.js` 37,855行 → 37,855行。差分は index 7 のみ・252行（4機種とも `KEEP_PLATFORMS` に既存）
- `platform_en` は `PLATFORM_EN` 未登録のため生値が素通しになるが、4機種とも元から英字表記なので
  `Xbox 360` / `Xbox` / `Wii U` / `64DD` で正しく出る（和名の追加は不要）
- `swap_data.py` v3：[0]〜[6] 照合で **消失0行 / 増加0行** を確認して注入
- キーワードを持つ行：36,466 → **36,718行**

### 本セッションでの変更

- `build_master.py` — `BUYEE_KEYWORD` に `Xbox 360`→`Xbox360`、`BUYEE_OK` に4機種、
  `BUYEE_LINK_ONLY` 新設、`KNOWN_PLATFORMS` の式、`PRODUCT_CODE_TYPO` 新設、
  および 312行付近・556行付近のコメント更新
- `data/master_final.csv` / `data/data_line.js` — 再生成
- `index.html` — DATA 行のみ差し替え（`swap_data.py`）

**デプロイは未実施。**

### バックアップ

- `build_master.py.20260803-bak-pre-buyee4`
- `data/master_final.csv.20260803-bak-pre-buyee4`
- `data/data_line.js.20260803-bak-pre-buyee4`
- `index.html.20260803-1410.bak`（swap_data.py が自動作成）
- `BIN_DIVER_NOTES.md.20260803-bak-pre-buyee4-impl`（571,441 bytes / 6,813行時点）

## 行番号相互参照の注意（2026-08-03）

追記の積み重ねにより、行番号を指す既存の相互参照
（3764 / 4456 / 4858 / 4910 / 4962 / 4986 / 5373 / 5526行 等）は
既に実際の行番号からずれている。ずれ幅は一定でなく、追記のたびに
変わるためここには記さない。ノート内で行番号を参照する記述は、
今後は本文のキーワード一致で辿ること（罠#142と同型）。

## 配信専用行のJapanese title・COPY復元（2026-08-03）

2667行（"配信行(row[12]==1)で日本語タイトルのCopyと店案内を出さない" の項）は、
実機視覚確認で見つかった別の問題（COPYボタン・注記そのものが配信専用行で
一律非表示になっていた）を機に見直した。

当時の意図（現物が無いのに実店舗の検索欄に貼れと言わない）は残しつつ、
COPY機能自体は復元。見出し文言を row[12] で分岐させ、配信専用行は
「copy to search the Japanese store」、物理行は従来の
「copy to paste into a store」とした。.instore（棚ナビ・ジャンル・店員ボタン）
と購入ブロックのr[12]ゲートは変更していない。

★2667行（同項）の記述はこの変更で一部古くなった（「Copyを出さない」が現状と不一致）。
既存行への打ち消し線は別作業とする。

## DL--DOL-GMSJ-JPN（スーパーマリオサンシャイン）のタイポ修正（2026-08-03）

6893行付近で「未処理として残した」としていた二重ハイフン型番を、AGB--P-B9AJ と同じ
PRODUCT_CODE_TYPO 方式で修正した。MADB原文(metadata301.ttl)が既に`--`（1件のみ）で、
配信版接頭辞ではないことを確認済み（DL-は全90要素でDOL-必須・単独使用0、当該行の
online フラグは空、配信行7,109件は全行product_code空）。

修正前から FAM_RE = /DL-+DOL-/ がハイフン1本・2本を両方拾っていたため、Suruga-ya
ルーティング（sgFam）は修正前後で完全不変（全数空撃ちで変化0行）。直ったのは画面表示の
型番文字列のみ。DATA全体に残る `--` 型番は0件になった。

swap_data.pyの[0]-[6]列ガードで一度中止（想定どおり・AGBと同型）。ノート5579節の手順
（全数突合・区間md5・サイズ差の確認）で正規の迂回により注入し、再実行で0行差分の通過を
確認済み。

新canonical: DATA行md5 29e5636d9aa33664ad1e1daf037f1594（改行込み）。

## SFC型番 SHVC-P- 71行の全数対照測定（2026-08-04）

2026-08-02 の255行全数対照（「全数対照（255行・af_jump/UTM 無しの生URL・自己クリック回避）」の項）
のうち、SFC分を独立に切り出して再測定した。**新しい実装は入れていない。測定のみ。**

### 母数の確定

対象は「codes（型番列）に SHVC-P- を含む行」だが、数えるのは DATA の生値ではなく
**実装が実際に送信している値の直前＝`sgBase`**（-JPN以降の除去と空白のハイフン化を済ませた値）。
これは255行測定のときと同じ数え方（「母集団は codes[0] の生値ではなく sgBase で数える」の項）。

| 定義 | 行数 |
|---|---:|
| codes 生値のいずれかに `SHVC-P-` を含む | 80 |
| **うち sgBase（＝送信の元）に `SHVC-P-` を含む＝母数** | **71** |
| 差分（codes[1] 以降だけが `SHVC-P-`・送信値には出ない） | 9 |

除外した9行は `SHVC-AH8J-JPN;SHVC-P-AH8J（JPN)` 型で、送信に使う codes[0] が最初から -P- 無し
（桃鉄HAPPY / DBZ HYPER DIMENSION / パーラー2 / 実戦バスフィッシング / キャラバンSHOOTING /
魔法陣グルグル / Gガンダム / SUPER TETRIS 3 / キャプテン翼5）。
**生値で数えると80行になり、9行を余分に測ることになる。** 母集団は必ず sgBase で取ること。

71行のうち二重ハイフン（`--` ガード）該当は0行、JAN差し替え・DL-DOL族に横取りされる行も0行。
純粋に `sgParen(sgPdash)` だけを通る。sgParen が効くのは `(JPN)` 付きの19行
（例: `SHVC-P-ZDBJ (JPN)` → 送信は `SHVC-ZDBJ`）。

★機種別集計の「SFC 75行」とは数が違う。あちらは **platform列 == Super Famicom** で数えた値で、
差の4行は型番の綴りが `SHVC-` ではない: 8062 `AHVC-P-AN5J`（西陣パチンコ物語2）/
8223 `SHCV-P-AYQJ`（商人よ、大志を抱け!!）/ 8371 `SHCV-P-AEFJ`（エルファリア2）/
8382 `SHCV-P-AKBJ`（スーパー競馬2）。SHCV は SHVC の綴り違い、AHVC も誤記の可能性が高いが
**今回は測定対象外・未着手のまま**（型番誤記の扱いは PRODUCT_CODE_TYPO 側の話で、-P- 落としとは別件）。

### 測定（2026-08-04・af_jump/UTM 無しの生URL・自己クリック回避）

71行 × 2形（-P-あり＝旧形 / -P-なし＝現行送信値）＝ユニーク142リクエスト・2.5秒間隔。
0件のとき駿河屋は HTTP 404。ヒット時は `件数:N件中` から件数を取った。通信エラー・件数抽出失敗は0。

| 判定 | 行数 |
|---|---:|
| 同点（両形とも200・件数一致） | 55 |
| 回復（404/0件 → 200） | 15 |
| 両方0件（404→404） | 1 |
| **悪化（200→404 / 件数減）** | **0** |
| 件数増（200→200 で増加） | 0 |

回復した15行: 7940 マーヴェラス / 8007 ナンバーズパラダイス / 8082 サウンドノベルツクール /
8120 それ行け♥エビス丸 / 8203 ざくろの味 / 8204 3×3EYES / 8205 天外魔境ZERO /
8212 SUPER FIRE PROWRESTLING X / 8230 遥かなるオーガスタ3 / 8330 悪魔城ドラキュラXX /
8417 Winning Post 2 / 8418 第4次スーパーロボット大戦 / 8568 DBZ超武闘伝3 /
10203 機動戦士Zガンダム / 10206 Romancing Sa・Ga 3。

★**回復15行・件数増0・回帰0** は 2026-08-02 の SFC 行（15 / 0 / 0）と完全一致した。
2日空けた再測定で1件もずれていない＝在庫変動によるノイズは今回の判定に混ざっていない。

### 実装は既に入っていた（2026-08-02 / 69cf5e6）

測定に入る前、「-P- 落としはまだ未実装」という前提で母数を取りに行ったが、
`index.html` の `sgPdash`（`const sgPdash = /--/.test(sgBase) ? sgBase : sgBase.replace(/-P-/g,'-')`）は
コミット 69cf5e6「実装: 型番 -P- 落とし(任天堂255行)+二重ハイフンガード」で
**255行すべて（SFC 71行を含む）に対して既に適用済み**だった。
よって「実装が実際に送信している値」は `sgBase` ではなく `sgParen(sgPdash)`＝-P- が落ちた形。
今回の測定は新規実装の事前評価ではなく、**既存実装の事後検証**にあたる。

★教訓（罠番号は付けない）: 母数を取る前に、その変換が既にコードに入っていないかを
`git log -S` で確認すること。「これから入れる値」と「今送っている値」を取り違えると、
測定の解釈（事前評価か事後検証か）がまるごと反転する。

### 未解決: idx 7892 スーパー人生ゲーム3（`SHVC-P-A3XJ`）

71行で唯一、**両形とも 404/0件**。-P- 落としでは救えない1行。

- `SHVC-P-A3XJ` → 404/0件、`SHVC-A3XJ` → 404/0件（2026-08-04 実測）
- 原因は2択で、どちらかは未確定: **(a) DATA の型番自体が誤り** / **(b) 駿河屋に該当商品が無い**
- DATA行: `["","スーパー人生ゲーム3","Super Famicom","SHVC-P-A3XJ","1996","タカラ","Takara Tomy",…]`
- **JAN逃がしの余地はある**: r[10] に `4904880134120` が入っており、動物番長（`DOL-GKDJ-JPN`）と
  同じ「型番を JAN に逃がす」手が形の上では使える。ただし**このJANは未測定**。
- ★**今回は着手しない**。動物番長＋カービィエアライドで JAN が型番の代替として機能した実例は
  n=2 しかなく、そこに n=1 を足して族に一般化するには根拠が足りない（罠#57・未測定の母集団に
  一律操作をかけない、と同じ判断）。着手するなら先に JAN 側を実測してから。

生データ（idx / withP / status / 件数 / noP / status / 件数 / title）はセッションの
scratchpad に `shvc_measure.tsv` として残したが、**恒久保存はしていない**。
再現が要るときは同じ手順（生URL・2.5秒間隔・`件数:N件中` から抽出）で測り直すこと。

### バックアップ

- `BIN_DIVER_NOTES.md.20260804-bak-pre-shvc-pdash-measure`（581,742 bytes / 6,965行時点）

**コード変更・デプロイは無し（測定と記録のみ）。**

## idx 8463 機動武闘伝Gガンダムの型番誤記を修正（2026-08-04）

4108行に「宿題として記録（今日は触らない・実害未測定）」として残していた
`SHVC-ACMJ-JPN;SHVC-P-AGMJ(JPN)` の 3文字目 C/G 不一致を、実測して決着させ修正した。
2026-07-27 に宿題として書いてから、この行は一度も触られていない
（`git log -S "SHVC-ACMJ"` は宿題を記録した 56ab99d と初回コミット 6e72a78 の2件のみ）。

### MADB 原文の状態

`metadata301.ttl` の M726043（機動武闘伝Gガンダム / バンダイ / 1994-12-27）は
**同一レコードが productID を2つ持ち、その中で食い違っている**:

```
schema:productID "SHVC-ACMJ-JPN",
        "SHVC-P-AGMJ（JPN)" ;
```

SFC の型番は `SHVC-P-XXXJ` と `SHVC-XXXJ-JPN` が同じ4文字を共有するので、片方が誤記。
TTL 全体で `AGMJ` は1件・`ACMJ` は1件のみで、多数決では決まらない。

### 実測で決着（2026-08-04・af_jump/UTM 無しの生URL・自己クリック回避）

| 検索語 | HTTP | 件数 | ヒット内容 |
|---|---|---:|---|
| `SHVC-ACMJ`（当時の実送信値） | 404 | 0 | なし。HTMLに「ガンダム」の語が1度も出ない |
| `SHVC-AGMJ` | 200 | 2 | 2件とも「機動武闘伝Gガンダム スーパーファミコンソフト [発売日:1994/12/27][バンダイ]」（商品ID 167000882 / 167900502） |

★**`AGMJ` が実在の型番で、codes[0] の `ACMJ` が誤記。** 無関係商品の混入は0件。

★5606行の教訓（「壊れ方が同型でも直し方は同じとは限らない。直した先が実際に当たるかを先に測れ」）
に従い、**直す前に直した先の形を測ってある**。DL-DOL-GMSJ のように「机上では正しいが0件」に
なる形には直していない。

★罠#141（外部からの推測値を DATA に書かない）には抵触しない。**書いた値は MADB が
同じレコード内に持っているもう一方の productID** を正規形（`-P-` 落とし・全角括弧の
`（JPN)` を `-JPN` に）へ揃えただけで、外部サイトから拾った値ではない。

### 修正内容

`build_master.py` の `PRODUCT_CODE_TYPO` に1件追加した（AGB--P-B9AJ / DL--DOL-GMSJ-JPN と同じ方式）。

```python
"SHVC-ACMJ-JPN": "SHVC-AGMJ-JPN",
```

★これは上2件と型が違う。あちらは「ハイフンが1本多い」、こちらは**英字1文字の誤記**。
それでも同じ表に入れるのは、この表が「見て確かめた1件だけを名指しで置く」ための場所だから。
パターン置換（C→G の一般化など）は絶対にしない。

★codes[1] の `SHVC-P-AGMJ（JPN)` は触っていない。開き括弧が全角だが codeKey は NFKC で吸収し、
駿河屋リンクは codes[0] しか見ないので無影響（2026-07-27 の全角括弧2件の調査どおり）。

### 検証

再生成（`build_master.py` → `master_final.csv` / `data_line.js`）後、以下を確認した。

1. **全数突合**（37,855行 × 全13列）でセル差分が **1箇所のみ**: 行8463 index3
   `SHVC-ACMJ-JPN;SHVC-P-AGMJ（JPN)` → `SHVC-AGMJ-JPN;SHVC-P-AGMJ（JPN)`
2. `master_final.csv` の差分も **1行のみ**（M726043）
3. **空撃ち**（旧DATA/新DATA の全37,855行で駿河屋への実送信値を再計算して突合）:
   **変化1行のみ**（8463: `SHVC-ACMJ` → `SHVC-AGMJ`）。他37,854行は完全不変
4. **巻き込みなし**: 新DATA に `SHVC-ACMJ` を含む行は0、`SHVC-AGMJ` を含む行は8463のみ。
   `SHVC-AGMJ` を送る行も8463のみ（他行との衝突なし）
5. `swap_data.py` は `[0]〜[6]` ガードで**一度中止**（想定どおり・AGB/GMSJ と同型。型番の
   書き換えは必ずここで止まる設計）。全数突合・区間md5・サイズ差の確認を経て正規の迂回で注入し、
   再実行で **0行差分**の通過を確認
6. **区間md5**: head区間（1..958行）`9018bded4e3ccdce791de22a4e21bfb5` **不変** /
   ロジック区間（960..末尾）`5a7996a1dbe55667e6caf8ede05fb26d` **不変**
7. **ファイルサイズ差 0 バイト**（8,087,710 → 8,087,710）。C→G は同バイト長なので当然だが、
   「1文字だけ動いた」ことの裏取りとして記録する

### ★DATA 行 md5 の基準を更新

| 区間 | 旧基準 | 新基準（2026-08-04 以後） |
|---|---|---|
| **DATA 行(959)** | `29e5636d9aa33664ad1e1daf037f1594` | **`b8ed393cd53a38c105270857c3dce772`** |

`29e5636d…`（2026-08-03 のサンシャイン修正時点）は**そこまでの記録として正しい**。消さない。
DATA 原文を修正した**3例目**で、md5 が動く系統の3代目にあたる。

### ライブ実物からの再計算

注入後の `index.html`（data_line.js ではなく現物）から DATA 行と定数を読み直して再計算:

```
codes     : ["SHVC-AGMJ-JPN","SHVC-P-AGMJ（JPN)"]
sgSrc     : SHVC-AGMJ-JPN
sgBase    : SHVC-AGMJ
sgPdash   : SHVC-AGMJ        （-P- を含まないので素通り）
実送信値  : SHVC-AGMJ        ← 実測で 200/2件・Gガンダム着地を確認済みの形
```

完全アフィリンク（記録のみ。**クリックはしない**＝自己クリック回避）:

```
https://affiliate.suruga-ya.jp/modules/af/af_jump.php?user_id=5357&goods_url=https%3A%2F%2Fwww.suruga-ya.jp%2Fsearch%3Fcategory%3D2%26search_word%3DSHVC-AGMJ%26utm_medium%3Daffiliate%26utm_source%3Dgamekataban.com%26utm_content%3D%252F%26utm_campaign%3Dafid5357
```

### この修正で 2026-07-27 の宿題が1つ減る

4108行の宿題2件のうち、**「idx 8463 の型番不一致」は解決**。
残る「全角括弧混在2件（idx 7878/8463 `（JPN)`）」は**未解決のまま**（無影響と確認済みで放置）。
4146行・4235行の「既出宿題」リストにある Gガンダムの項も、この節をもって解決とする
（既存行は append-only のため書き換えない）。

### バックアップ

- `build_master.py.20260804-bak-pre-acmj`
- `data/master_final.csv.20260804-bak-pre-acmj`
- `data/data_line.js.20260804-bak-pre-acmj`
- `index.html.20260804-bak-pre-acmj` / `index.html.20260804-0824.bak`（swap_data.py が自動作成）
- `BIN_DIVER_NOTES.md.20260804-bak-pre-acmj-note`

**デプロイは未実施。**

### デプロイ（2026-08-04・idx8463 型番誤記修正）

| 項目 | 値 |
|---|---|
| ローカルコミット | `09fd9b3`（実装・build_master.py + index.html） |
| リモートコミット | `f925bab84b28f8cbda239035a0081222ad7ede81`（gh contents API・index.html 1ファイルのみ） |
| アップロード後 blob sha | `f4bfc6f6fff500796928f708c109f885b65edaf5`（ローカル `git rev-parse HEAD:index.html` と一致） |
| Pages ビルド | `built` / 36,131ms / error null / commit `f925bab8…` |
| **live md5（全体）** | **`ef376c16cb65e7e2970281ece4c29184`**（ローカルと一致） |
| live DATA行 md5 | `b8ed393cd53a38c105270857c3dce772`（新canonical と一致） |
| live head区間（1..958行） | `9018bded4e3ccdce791de22a4e21bfb5`（不変） |
| live ロジック区間（960..末尾） | `5a7996a1dbe55667e6caf8ede05fb26d`（不変） |
| CNAME | `gamekataban.com` / sha `11f5f0eb198f73ad578c7daf41cd3cbb4a05379b`（不変）/ 証明書 approved |

デプロイ前の drift 確認: live blob sha `c70e57d0422e902821460e725374e9485594c4c4` が編集前バックアップ
`index.html.20260804-bak-pre-acmj` の `git hash-object` と一致。**巻き込みなし**
（＝2026-08-03 のデプロイ以降、live は誰にも触られていない）。

反映確認: キャッシュバスター付き curl で **1回目（ビルド完了直後）に live md5 が一致**。
さらに live を再取得して行8463を読み直し、`SHVC-AGMJ-JPN;SHVC-P-AGMJ（JPN)` であること、
そこから再計算した駿河屋への実送信値が `SHVC-AGMJ`（実測 200/2件・Gガンダム着地の形）
であること、live 全37,855行に `SHVC-ACMJ` が **0行**であることを確認した。

★リモートコミットの files は `index.html` の1ファイルのみ。`build_master.py` は
GitHub 側には上げていない（Pages の配信物ではないため。ローカル git の `09fd9b3` が台帳）。
remote 未設定でローカルとリモートは常に別SHA、という従来どおりの状態は変わっていない。

## RVL-006 の2行に SG_DEAD を適用（2026-08-04）

4108行・4146行・4235行に「既出宿題」として残っていた RVL-006 の件を決着させた。
対象は ドラゴンクエストソード(Wii) の海外版2行 ―― **idx 1941**（M718953・`RVL-006(USA)`）と
**idx 1945**（M718987・`RVL-006`）。

### 現状の確定

| | idx 1941 | idx 1945 |
|---|---|---|
| row[0] 英題 | `Dragon Quest Swords` | `Dragon Quest Swords` |
| row[3] 型番列 | `RVL-006(USA)` | `RVL-006` |
| codes 全要素 | `["RVL-006(USA)"]`（**1要素**） | `["RVL-006"]`（**1要素**） |
| row[7] Buyeeキーワード | `"Wii"`（あり） | `"Wii"`（あり） |
| row[10] JAN | 空 | `5060121822719` |
| row[8] 正規タイトル | `ドラゴンクエストソード 仮面の女王と鏡の塔` | 同左 |
| **実送信値**（sgBase通過後） | **`RVL-006(USA)`** | **`RVL-006`** |

実測（2026-08-04・af_jump/UTM 無しの生URL・自己クリック回避）:

| 送信値 | HTTP | 件数 | 中身 |
|---|---|---:|---|
| `RVL-006(USA)` | 404 | 0 | なし（**空振り**） |
| `RVL-006` | 200 | **80** | Wiiリモコン[RVL-003] / ACアダプタ[RVL-002] / ヌンチャク[RVL-004] / LANアダプタ / D端子ケーブル / スーパーペーパーマリオ / METROID:Other M ほか。**1ページ20件を全数確認し「ソード」「SWORDS」「剣神」は0回＝完全な誤着地** |
| JAN `5060121822719`（idx1945） | 404 | 0 | JAN逃がしも効かない（idx1941 は JAN 自体が空） |

### ★1483行の予測が、別の行で既に現実になっていた

`sgParen` の恒久除外コメント（1483行付近）は **「(USA) を外側除去すると 0件だったものが
別商品に化ける」** と予測して idx1941 を除外していた。予測は正しかったが、**idx1945 は
DATA の値が最初から裸の `RVL-006`** なので、除去するまでもなく既にその状態だった。
除外判断は「その1行を触らない」ことしか守れず、**同じ害を持つ隣の行は素通りしていた。**

★教訓（罠番号は付けない）: 「この形は危ないから触らない」で止めると、**最初からその形で
入っている行**を見逃す。危険な形を特定したら、`触らない` だけでなく **その形が DATA に
何行あるかを全数で数える** ところまでやること。今回は 2行のうち1行しか塞げていなかった。

### primary としての実害 ―― 「row[7] が空でなければ安全」ではない

`noProxy = !k.kwBuyee` なので、両行とも row[7]=`"Wii"` が入っている＝ **noProxy は false**。

| モード | primary の割り当て | 型番リンク | 実害 |
|---|---|---|---|
| OUTSIDE | Buyee ＋ Suruga-ya (title) | 塗られない | なし |
| **IN JAPAN** | Suruga-ya (code) ＋ (title) | **塗られる**（`clsCode` は `(jp \|\| noProxy)` で jp なら無条件に primary） | **idx1945 は塗りボタンが80件の無関係商品に着地していた** |

★2026-07-26 に入れた「row[7] が空なら primary を型番へ移す」ルールを根拠に
「row[7] が埋まっていれば型番リンクは主役でない＝実害は小さい」と考えるのは**誤り**。
jp モードでは row[7] と無関係に型番リンクが primary になる。

### ゼルダTPHD と同型だが remedy は使えない

| | ゼルダTPHD | RVL-006 の2行 |
|---|---|---|
| codes[0] がハード規格品番 | ○ `WUP-006` | ○ `RVL-006` |
| 誤着地する | ○ 無関係2件 | ○ idx1945 が無関係80件 |
| **codes[1] に本物のソフト型番** | **○ `WUP-P-AZAJ`**（1行特例で差し替え） | **✗ codes が1要素のみ＝差し替え先が無い** |

JAN 逃がし（動物番長方式）も上記のとおり 404/0件で不可。残る手は
**PAC-MAN VS. / ドルアーガの塔と同じ `SG_DEAD` 方式**（コード検索リンクを出さない）だけだった。

### 実装

`SG_DEAD` に2件追加（判定は型番の**完全一致**・1540行付近）。

```js
const SG_DEAD = new Set(['DL-DOL-PRJJ-JPN','DL-DOL-PKBJ-JPN',   // 型番もJANも無く到達不能な2行
                         'RVL-006','RVL-006(USA)']);            // ハード規格品番で誤着地する2行
```

`sgNoCode` の説明コメント（1400行付近）も、入っているものが2種類（**空振り** と **誤着地**）に
なったので実態に合わせて書き直した。

★消したのは **コード検索リンク1本だけ**。Buyee / eBay / Suruga-ya (title) はそのまま出る。
両行とも row[8] に `ドラゴンクエストソード 仮面の女王と鏡の塔` を持ち、**常に primary の
Suruga-ya (title) はこの正しい日本語タイトルを送っている**＝到達手段は消していない。

### 検証

| 項目 | 結果 |
|---|---|
| 全数空撃ち（37,855行） | **挙動が変わった行は2行のみ**（1941 / 1945 とも gCode「出す→出さない」）。他37,853行は不変 |
| 巻き込み | 型番列に `RVL-006` を**含む**行 = `[1941,1945]` / **完全一致**する行 = `[1941,1945]`。両者が一致＝部分一致でも巻き込みは無いが、判定は完全一致で持つ（罠#90） |
| sgNoCode の総数 | 2行 → 4行 |
| 他導線の残存 | 2行とも Buyee（row[7]="Wii"）・eBay（row[0]あり）・Suruga-ya(title)（row[8]）が出る。row[12] は空＝配信専用行ではない |
| **DATA行 md5** | `b8ed393cd53a38c105270857c3dce772` **不変**（canonical 維持・DATA は触っていない） |
| head区間（1..958行） | `9018bded4e3ccdce791de22a4e21bfb5` **不変** |
| ロジック区間（960..末尾） | `5a7996a1dbe55667e6caf8ede05fb26d` → `891ecd43281fc06d2f76890f1e2d1cd1`（**ここだけ変化＝意図どおり**） |
| 全体md5 | `ef376c16cb65e7e2970281ece4c29184` → `5b703cafdc7acd3042529abef0f98440` |
| 行数 / サイズ | 2,240 → 2,263行 / 8,087,710 → 8,090,177 bytes（増分は全てコメント） |
| `node --check` | インライン script 2本（959–2238行 / 2241–2260行）とも **OK** |

★`<script>` の行番号は今回の23行追加で動いた（旧: 958/2213行 → 新: 958/2240行）。
次回は `grep -n "<script\|</script>"` で取り直すこと（6770行付近の手順メモと同じ注意）。

### バックアップ

- `index.html.20260804-bak-pre-rvl-dead`（8,087,710 bytes / md5 `ef376c16…`）
- `BIN_DIVER_NOTES.md.20260804-bak-pre-rvl-note`

**ローカルコミット `eb38169` まで。デプロイは未実施**（検証結果を見てから判断する、との指示のため）。
実機での目視確認も未実施。

### デプロイ（2026-08-04・RVL-006 の SG_DEAD 適用）

| 項目 | 値 |
|---|---|
| ローカルコミット | `eb38169`（index.html のみ） |
| リモートコミット | `86e222d1444de02a567b7839fecde99bba6ea34a`（gh contents API・index.html 1ファイルのみ） |
| アップロード後 blob sha | `c86a1bc12377adf27aeb7cb939b167cf1142e0c9`（ローカル `git rev-parse HEAD:index.html` と一致） |
| Pages ビルド | `built` / 53,485ms / error null / commit `86e222d1…` |
| **live md5（全体）** | **`5b703cafdc7acd3042529abef0f98440`**（期待値と一致。前回 `ef376c16…` から入れ替わり） |
| live DATA行 md5 | `b8ed393cd53a38c105270857c3dce772`（**canonical 不変**） |
| live head区間（1..958行） | `9018bded4e3ccdce791de22a4e21bfb5`（**不変**） |
| live ロジック区間（960..末尾） | `891ecd43281fc06d2f76890f1e2d1cd1`（変化＝意図どおり） |
| CNAME | `gamekataban.com` / sha `11f5f0eb198f73ad578c7daf41cd3cbb4a05379b`（不変）/ 証明書 approved |

デプロイ前の drift 確認: live blob sha `f4bfc6f6fff500796928f708c109f885b65edaf5` が編集前バックアップ
`index.html.20260804-bak-pre-rvl-dead` の `git hash-object` と一致。**巻き込みなし**
（この blob は同日の idx8463 デプロイで上げたもの＝それ以降 live は誰にも触られていない）。

反映確認: キャッシュバスター付き curl で **1回目（ビルド完了直後）に live md5 が一致**。
さらに live から `SG_DEAD` を読み直して挙動を確認した:

```
live の SG_DEAD : ['DL-DOL-PRJJ-JPN','DL-DOL-PKBJ-JPN','RVL-006','RVL-006(USA)']
idx1941 RVL-006(USA) → sgNoCode=true / gCode 出さない / Buyee="Wii" / title=ドラゴンクエストソード 仮面の女王と鏡の塔
idx1945 RVL-006      → sgNoCode=true / gCode 出さない / Buyee="Wii" / title=同上
live で sgNoCode=true になる行 : [1824, 1892, 1941, 1945]  ← 4行のみ（既存2＋今回2）
```

★**実機での目視確認は未実施。** 該当2行のカードで `By product code` ブロックが消えていること、
他の導線（Buyee / eBay / Suruga-ya (title)）が残っていることは、Koha の実機確認待ち。

---

## idx7892 スーパー人生ゲーム3 — JAN逃がし（2026-08-04）

### 何が起きていたか

`SHVC-P-A3XJ`（idx7892 / スーパー人生ゲーム3 / SFC / タカラ / 1996）は、2026-08-02 の
**-P- 落とし**を経て `SHVC-A3XJ` として送られていた。この形は駿河屋に該当表記が無く、
**404 / 0件**。誤着地はしないが到達もしない＝コード検索リンクが空振りしていた。

### 実測（2026-08-04・af_jump/UTM 無しの生URL・自己クリック回避）

| 検索語 | HTTP | 件数 | 中身 |
|---|---|---|---|
| `SHVC-A3XJ`（現行送信形） | **404** | **0件**（「検索結果は０件です。条件を変更して下さい。」） | — |
| JAN `4904880134120` | **200** | **2件**（該当件数:2件中 1-2件） | `スーパー人生ゲーム3 (ボード)` 167001390（中古 ￥450〜￥1,100）/ `ランクB)スーパー人生ゲーム3 (ボード)` 167900025（品切れ） |

目視判定＝**到達○**。2件は同一タイトルの状態違いで、どちらもカテゴリ「スーパーファミコンソフト」・
発売日 1996/11/29・タカラ・定価 ￥6,578 で、DATA の `SHVC-P-A3XJ` 行と一致する。
★商品名末尾の `(ボード)` は**駿河屋側のジャンル注記**であって、ボードゲーム実物ではない。
SFC ソフトに正しく着地している（誤着地・別作品・別ハードの混入は0件）。

★`SHVC-A3XJ` の 0件は在庫変動ではない。今回あらためて叩き直して 404/0件を再確認済み。

### 実装

動物番長（2026-08-02）と**同じ段階・同じ方式**。型番用の変換（`sgParen` / `-P-` / `SG_REGION`）を
バイパスする位置に置く＝将来それらのルールが増えても JAN の数字列が壊されない。

単発の `code === 'DOL-GKDJ-JPN'` を Set に変え、2行目を足した（`SG_DEAD` と同じ持ち方に揃えた）:

```js
const SG_JAN = new Set(['DOL-GKDJ-JPN',    // 動物番長(型番が巨人のドシンのもので誤着地)
                        'SHVC-P-A3XJ']);   // スーパー人生ゲーム3(型番が空振り・JANは2件とも本物)
const sgJan = (SG_JAN.has(code) && r[10]) ? r[10] : '';
```

★**JAN の生値をコードに直書きしない。** 値は `r[10]`（JAN列）から読む。DATA が正典で、
値を二重に持つと将来ズレる。判定だけ型番の完全一致で持つ。

★**`&& r[10]` を条件に残す。** JAN列が空になれば `sgJan` が `''` に落ち、
`sgFam || sgParen(sgPdash)` の通常パイプラインへ**自動的に戻る**（安全側に倒れる）。

★**判定は `code` 列（DATA原文）の完全一致。** `SHVC-P-A3XJ` は全37,855行でこの1行のみ、
他行の型番に**部分文字列として含まれる例も0件**を確認済み（罠#90）。動物番長側は
`DL-DOL-GKDJ-JPN` が `DOL-GKDJ-JPN` を含むので、完全一致でないと巨人のドシンを壊す。

★**族には一般化しない。** 動物番長・カービィのエアライドに続く3例目だが、
「型番が空振りする行は JAN に逃がす」を全体ルールにするには未測定の母集団が大きすぎる
（罠#57 と同じ判断）。**実測で当たった行だけを完全一致で1行ずつ足す。**

### 検証

`card()` 本体の `codes` 〜 `kwCodeSg` 区間を**原文のまま切り出して eval** する空撃ちハーネスを使った
（ロジックを書き写すと実装との乖離が起きるため）。

| 項目 | 結果 |
|---|---|
| 全数空撃ち（37,855行） | **検索語が変わった行は1行のみ**：idx7892 `SHVC-A3XJ` → `4904880134120`。他37,854行は完全一致 |
| 巻き込み | 型番列に `SHVC-P-A3XJ` を**含む**行 = `[7892]` / **完全一致**する行 = `[7892]`。両者一致＝巻き込み無し |
| 既存 JAN逃がしの回帰 | 動物番長 idx5558 は前後とも `4902370505900`＝無傷 |
| **DATA行 md5** | `b8ed393cd53a38c105270857c3dce772` **不変**（canonical 維持・DATA は触っていない） |
| head区間（1..958行） | `6d60251d74639414e96cb781bf5b8be4` **不変** |
| ロジック区間（960..末尾） | `891ecd43281fc06d2f76890f1e2d1cd1` → `c71eb2c4b852ce1903e7bb84db98baec`（**ここだけ変化＝意図どおり**） |
| 全体md5 | `5b703cafdc7acd3042529abef0f98440` → `3170badb06b0c261a96d83a15474077d` |
| 行数 / サイズ | 2,262 → 2,278行（`wc -l`）/ 8,090,177 → 8,092,017 bytes（+1,840・増分は大半がコメント） |
| `node --check` | インライン script 2本とも **OK** |

★head区間 md5 の基準が前回メモの `9018bded…` から `6d60251d…` に変わって見えるのは、
**区間の取り方が違うだけ**（`sed -n '1,958p'` は 958行目の `<script>` 開始タグを含む）。
DATA 行（959）は両方式とも区間外で、canonical `b8ed393c…` は不変。次回もどちらの取り方をしたか
明記すること。

★`<script>` の行番号は今回の16行追加で動いた（旧: 958/2241行 → 新: 958/2256行）。
次回は `grep -n "<script\|</script>"` で取り直すこと。

### バックアップ

- `index.html.20260804-bak-pre-shvc-jan`（8,090,177 bytes / md5 `5b703cafdc7acd3042529abef0f98440`）
- `BIN_DIVER_NOTES.md.20260804-bak-pre-shvc-jan-note`（md5 `da9a5b54ced699649cf8dc82858a51a3` / 7,353行）

### デプロイ（2026-08-04）

| 項目 | 値 |
|---|---|
| ローカルコミット | `39de819`（index.html のみ） |
| リモートコミット | `eaf379466f9c196158b66b6992aea59f5d5adc8b`（gh contents API・index.html 1ファイルのみ） |
| アップロード後 blob sha | `93f4a1ed4a730852ee9c2b4111bd824afaf0047e`（ローカル `git rev-parse HEAD:index.html` と**一致**） |
| Pages ビルド | `built` / 42,548ms / **error null** / commit `eaf37946…` |
| **live md5（全体）** | **`3170badb06b0c261a96d83a15474077d`**（期待値と一致。前回 `5b703caf…` から入れ替わり） |
| live DATA行 md5 | `b8ed393cd53a38c105270857c3dce772`（**canonical 不変**） |
| live head区間（1..958行） | `6d60251d74639414e96cb781bf5b8be4`（**不変**） |
| live ロジック区間（960..末尾） | `c71eb2c4b852ce1903e7bb84db98baec`（変化＝意図どおり） |
| live サイズ / `cmp` | 8,092,017 bytes / ローカル `index.html` と **IDENTICAL** |
| CNAME | `gamekataban.com` / sha `11f5f0eb198f73ad578c7daf41cd3cbb4a05379b`（不変）/ 証明書 approved |

デプロイ前の drift 確認: live blob sha `c86a1bc12377adf27aeb7cb939b167cf1142e0c9` が編集前バックアップ
`index.html.20260804-bak-pre-shvc-jan` の `git hash-object` と一致。**巻き込みなし**
（この blob は同日の RVL-006 デプロイで上げたもの＝それ以降 live は誰にも触られていない）。

反映確認: キャッシュバスター付き curl で **1回目（ビルド完了直後）に live md5 が一致**。
さらに **live の実物から `SG_JAN` を読み直し**、同じ空撃ちハーネスを live に対して回した:

```
live 1540-1542行:
  const SG_JAN = new Set(['DOL-GKDJ-JPN',    // 動物番長(型番が巨人のドシンのもので誤着地)
                          'SHVC-P-A3XJ']);   // スーパー人生ゲーム3(型番が空振り・JANは2件とも本物)
  const sgJan = (SG_JAN.has(code) && r[10]) ? r[10] : '';

live 全数空撃ち(37,855行) → ローカル期待値と 0行差分
  idx7892 → 4904880134120   ← JAN に切り替わっている
  idx5558 → 4902370505900   ← 動物番長は無傷
```

★**実機での目視確認は未実施。** idx7892 のカードで `By product code` が JAN を送り、
駿河屋で「スーパー人生ゲーム3」2件に着地することの実機確認は Koha 待ち。

---

## SFC 型番接頭辞の誤記7件（AHVC- / SHCV- → SHVC-）(2026-08-04)

`PRODUCT_CODE_TYPO` 方式（AGB / GMSJ / ACMJ に続く4例目）で、MADB の SFC 規格品番の
接頭辞誤記を7件まとめて直した。**DATA 原文を修正した4例目**にあたる。

| idx | タイトル | MADB 原文 | 修正後 |
|---|---|---|---|
| 8033 | シムシティジュニア | `AHVC-ALIJ-JPN` | `SHVC-ALIJ-JPN` |
| 8062 | 西陣パチンコ物語 2 | `AHVC-P-AN5J` | `SHVC-P-AN5J` |
| 8223 | 商人よ、大志を抱け!! | `SHCV-P-AYQJ(JPN)` | `SHVC-P-AYQJ(JPN)` |
| 8371 | エルファリア2 | `SHCV-P-AEFJ(JPN)` | `SHVC-P-AEFJ(JPN)` |
| 8382 | スーパー競馬 2 | `SHCV-P-AKBJ(JPN)` | `SHVC-P-AKBJ(JPN)` |
| 9031 | スーパーテトリス 2 +ボンブリス | `SHCV-T2` | `SHVC-T2` |
| 9207 | オセロワールド | `SHCV-TO` | `SHVC-TO` |

`SHCV` は `SHVC` の**並べ替え**、`AHVC` は**1文字違い**。4文字（2文字）部分は一切触っていない。

### ★根拠の種類が ACMJ とは違う（重要）

7082行の `SHVC-ACMJ` は「**同一レコードのもう一方の productID**」から正しい値が取れた
＝レコード内で自己完結していた。今回の7件は **どのレコードも `schema:productID` を1つしか
持たない**ので、同じ手は使えない。**修正後の値（`SHVC-ALIJ` 等）は TTL のどこにも存在しない。**

「同じ綴りが原文の別の場所にある」という直接証拠は無い。代わりに次の4点を根拠にした。
いずれも MADB 原文の中で完結しており、**外部サイトから値を持ってきてはいない**（罠#141）。

1. **接頭辞の分布**（同 TTL・platform にスーパーファミコンを含むレコードの productID・計655件）:

   ```
   SHVC 615 / DMG 16 / SVHC 6 / SHCV 5 / SFT 2 / AHVC 2 / DMJ 2 / CLV 1
   ```

   `AHVC-`（2件）と `SHCV-`（5件）は **この7件で全部**。TTL 全体で見ても同数。

2. **直した先の書式はすべて同 TTL に実在する**:
   `SHVC-####-JPN` 142件 / `SHVC-P-####` 49件 / `SHVC-P-####(JPN)` 18件 /
   `SHVC-`+2文字（`SHVC-T8, -TD, -TE, -TF, -TI, -TK, -TM, -TQ, -TY` …）多数。
   `SHVC-T2` / `SHVC-TO` はこの T系列の**欠番**にちょうど収まる。

3. **4文字（2文字）部分は不変**。動かしたのは接頭辞だけで、新しい文字を外から入れていない
   ＝**原文の正規形化**に留まる。

4. **直した先が実際に当たることを先に測ってある**（下記）。

★つまり (1)〜(3) は「同じ綴りが他所にある」という直接証拠ではなく、**接頭辞の分布と書式の一致**
による正規形化。だから (4) の実測を必須にしている。**測らずに直すことはしない**
（「壊れ方が同型でも直し方は同じとは限らない」＝5606行の教訓）。

### 実測（2026-08-04・af_jump/UTM 無しの生URL・自己クリック回避）

左が修正前の実送信値（`sgBase` → `-P-` 落とし → `(JPN)` 除去を通過した後の値）。

| idx | 修正前 | HTTP | 件数 | 修正後 | HTTP | 件数 | ヒット商品（目視） |
|---|---|---|---|---|---|---|---|
| 8033 | `AHVC-ALIJ` | 404 | 0 | `SHVC-ALIJ` | 200 | **1** | シムシティJr [1996/07/25][イマジニア] 167001351 |
| 8062 | `AHVC-AN5J` | 404 | 0 | `SHVC-AN5J` | 200 | **1** | 西陣パチンコ物語2 [1996/06/28][KSS] |
| 8223 | `SHCV-AYQJ` | 404 | 0 | `SHVC-AYQJ` | 200 | **1** | 商人よ、大志を抱け!!(ETC) [1995/12/15][バンダイ] 167001219 |
| 8371 | `SHCV-AEFJ` | 404 | 0 | `SHVC-AEFJ` | 200 | **1** | エルファリア2 [1995/06/09][ハドソン] 167001032 |
| 8382 | `SHCV-AKBJ` | 404 | 0 | `SHVC-AKBJ` | 200 | **1** | スーパー競馬 2 [1995/05/19][アイマックス] |
| 9031 | `SHCV-T2` | 404 | 0 | `SHVC-T2` | 200 | 3 | 2件が スーパーテトリス2+BOMBLISS [1992/12/18][BPS] 167000215 と ランクB版 |
| 9207 | `SHCV-TO` | 404 | 0 | `SHVC-TO` | 200 | 9 | 6番目に オセロワールド [1992/04/05][ツクダオリジナル] 167000080 |

発売日・発売元はいずれも DATA 側の値と一致＝**正しい商品に着地している**。
修正前は7件とも `404 / 0件`（「検索結果は０件です。条件を変更して下さい。」）で、
**誤着地はしないが到達もしない**状態だった。

★`SHVC-ALIJ` は駿河屋の表記が「シムシティ**Jr**」で DATA の「シムシティ**ジュニア**」と字面が違う。
**型番でしか当たらない行**＝この修正の価値が最も高いケース。

### ★T2 / TO のノイズを許容した判断

`SHVC-T2` と `SHVC-TO` は短いトークンなので、駿河屋の検索が**英字タイトルの一部にも部分一致**する。

```
SHVC-TO → 9件。1〜5番目と7〜9番目はノイズ:
  キャプテン翼J THE WAY TO WORLD YOUTH（外箱欠品 / 箱状態難の2件）
  機動戦士Zガンダム AWAY TO THE NEWTYPE / 下野正希のFishing to Bassing
  TOKORO'Sマージャン / 新・熱血硬派くにおたちの挽歌[レプリカパッケージ]
  在宅投票システム SPAT4 カセット（2件）
  ★6番目に オセロワールド 167000080  ← 目的の商品
SHVC-T2 → 3件。1番目 T2ザ・アーケードゲーム がノイズ、2〜3番目が目的の商品
```

**それでも直す。** 直す前は `404 / 0件` で**到達手段が無い**のに対し、直した後は件数内に
**必ず目的の商品が居る**。罠#27 の「誤った値より無いほうがまし」は **0件 と 誤着地** の比較であって、
ここは **0件 と ノイズ混じりだが当たる** の比較。後者が上。

★1件ピンポイントの5行（A群）と、ノイズ混じりの2行（B群）を**同じ扱いにした**。
B群だけ JAN 逃がし（7892行の方式）に回す選択肢もあったが、7件とも JAN は埋まっているものの
**JAN 側は未測定**なので、測っていない方式には倒さない。将来 B群を JAN に寄せるなら先に測ること。

### ★SVHC- 6件は保留（触っていない）

同じ TTL に `SVHC-` が **6件**ある。これも `SHVC` の並べ替えに見えるが、**今回は触らない。**
**未測定だから**で、他意はない。測ってから足すこと。この節の (4) の原則をそのまま適用する。

> **★追記（2026-08-04・同日）: 測って足した。** 上の記述は**消さない** ―― 「未測定だから保留 →
> 測ってから足す」という順序を踏んだこと自体が記録なので、結論だけに書き換えると
> その順序が消える。実測と実装は末尾の
> 「SFC 接頭辞の誤記 `SVHC-` 6件（2026-08-04）」節にある。コミットは `7a65ca1` / `365e613`、
> デプロイは同日 `21e78c9c`。

### 実装

`build_master.py` の `PRODUCT_CODE_TYPO` に7件を**1件ずつ名指しで**追加（3件 → 10件）。

```python
"AHVC-ALIJ-JPN": "SHVC-ALIJ-JPN",        # シムシティジュニア
"AHVC-P-AN5J": "SHVC-P-AN5J",            # 西陣パチンコ物語 2
"SHCV-P-AYQJ(JPN)": "SHVC-P-AYQJ(JPN)",  # 商人よ、大志を抱け!!
"SHCV-P-AEFJ(JPN)": "SHVC-P-AEFJ(JPN)",  # エルファリア2
"SHCV-P-AKBJ(JPN)": "SHVC-P-AKBJ(JPN)",  # スーパー競馬 2
"SHCV-T2": "SHVC-T2",                    # スーパーテトリス 2 +ボンブリス
"SHCV-TO": "SHVC-TO",                    # オセロワールド
```

★`SHCV` → `SHVC` の**パターン置換にはしない**。将来 `SHCV` が正しい何かの接頭辞として
現れたときに静かに壊すため（このブロック冒頭の原則どおり）。

### 検証

再生成（`build_master.py` → `master_final.csv` / `data_line.js`）後、以下を確認した。

| 項目 | 結果 |
|---|---|
| **全数突合**（37,855行 × 全13列） | **セル差分は7箇所のみ**。差分のあった列番号の集合 = `[3]`（型番列のみ）、行番号 = `[8033, 8062, 8223, 8371, 8382, 9031, 9207]` |
| `master_final.csv` の差分 | **7行のみ**（M725602 / M725631 / M725798 / M725946 / M725957 / M726623 / M726799）。変わったのは product_code 列だけ |
| **空撃ち**（旧/新 DATA で実送信値を再計算して突合） | **変化7行のみ**。全部 `SHVC-` 系に変わり、他37,848行は完全不変 |
| **巻き込みなし** | 新DATA に `AHVC` を含む行 **0** / `SHCV` を含む行 **0**。7件の新送信値は**いずれもその行だけが送る**（他行との衝突0） |
| **DATA行 md5** | `b8ed393cd53a38c105270857c3dce772` → **`0bee046fc459ac6eb1ba36eb4a3d0333`** |
| head区間（1..958行） | `6d60251d74639414e96cb781bf5b8be4` **不変** |
| ロジック区間（960..末尾） | `c71eb2c4b852ce1903e7bb84db98baec` **不変** |
| 全体md5 | `3170badb06b0c261a96d83a15474077d` → `06defa7c11faa33f58c56311e5da33ac` |
| ファイルサイズ差 | **0 バイト**（8,092,017 → 8,092,017）。接頭辞の置換は同バイト長なので当然だが、「型番だけが動いた」ことの裏取りとして記録する |
| `node --check` | インライン script 2本とも **OK** |
| `swap_data.py` | `[0]〜[6]` ガードで**一度中止**（想定どおり・AGB / GMSJ / ACMJ と同型）。上記の全数突合・区間md5・サイズ差を確認したうえで**正規の迂回**で注入し、再実行で **0行差分**の通過を確認 |

### ★DATA 行 md5 の基準を更新

| 区間 | 旧基準 | 新基準（2026-08-04・この節以後） |
|---|---|---|
| **DATA 行(959)** | `b8ed393cd53a38c105270857c3dce772` | **`0bee046fc459ac6eb1ba36eb4a3d0333`** |
| head 区間(1..958) | `6d60251d74639414e96cb781bf5b8be4` | 同左（**不変**） |
| ロジック区間(960..末尾) | `c71eb2c4b852ce1903e7bb84db98baec` | 同左（**不変**） |

`b8ed393c…`（同日の ACMJ 修正時点）は**そこまでの記録として正しい**。消さない。
DATA 原文を修正した**4例目**で、md5 が動く系統の**4代目**にあたる。

★head 区間 md5 が 7145行の `9018bded…` と違うのは**区間の取り方の差**（`sed -n '1,958p'` は
958行目の `<script>` 開始タグを含む）。7250行付近に書いたとおり、どちらの取り方をしたか明記すること。

### バックアップ

- `build_master.py.20260804-bak-pre-shcv`（md5 `ebb43b27f39d2b542c5054e8938dac64`）
- `data/master_final.csv.20260804-bak-pre-shcv`（md5 `a364d59fa48b7280aea0d808771d199d`）
- `data/data_line.js.20260804-bak-pre-shcv`（md5 `d257474de9b0bd75204df5cc360d6804`）
- `index.html.20260804-bak-pre-shcv`（8,092,017 bytes / md5 `3170badb06b0c261a96d83a15474077d`）← **注入前の現物**
- `BIN_DIVER_NOTES.md.20260804-bak-pre-shcv-note`（md5 `932c559c58b780e5529d1b764c36a437` / 7,470行）

★**`index.html.20260804-0920.bak`（swap_data.py の自動バックアップ）は注入後の内容で上書きされた。**
swap_data.py の bak 名は**分単位**（`%Y%m%d-%H%M`）なので、同じ分内に2回走らせると
1回目に取ったバックアップを2回目が潰す。今回は注入前の現物が `…-bak-pre-shcv` に残っていたので
実害ゼロだったが、**swap_data.py の自動バックアップだけに頼らない**こと。
作業前の手動バックアップ（`…-bak-pre-XXX`）が本命で、自動バックアップは保険。

### コミット（段階的）

- `e2d7369` — `build_master.py`（辞書に7件追加）
- `ae6d149` — `index.html`（再生成した DATA 行の注入）

### デプロイ（2026-08-04・SFC 接頭辞誤記7件）

| 項目 | 値 |
|---|---|
| ローカルコミット | `ae6d149`（index.html のみ。実装は `e2d7369`） |
| リモートコミット | `8a83a205ac0cb98e6cea814b889c9c5bfa16270c`（gh contents API・index.html 1ファイルのみ） |
| アップロード後 blob sha | `c1bcfd0bf9767db195c531e7b36872830ceb0dca`（ローカル `git rev-parse HEAD:index.html` と**一致**・working tree とも一致） |
| Pages ビルド | `built` / 36,779ms / **error null** / commit `8a83a205…` |
| **live md5（全体）** | **`06defa7c11faa33f58c56311e5da33ac`**（期待値と一致。前回 `3170badb…` から入れ替わり） |
| **live DATA行 md5** | **`0bee046fc459ac6eb1ba36eb4a3d0333`**（新 canonical と一致） |
| live head区間（1..958行） | `6d60251d74639414e96cb781bf5b8be4`（**不変**） |
| live ロジック区間（960..末尾） | `c71eb2c4b852ce1903e7bb84db98baec`（**不変**） |
| live サイズ / `cmp` | 8,092,017 bytes / ローカル `index.html` と **IDENTICAL** |
| CNAME | `gamekataban.com` / sha `11f5f0eb198f73ad578c7daf41cd3cbb4a05379b`（不変）/ 証明書 approved |

デプロイ前の drift 確認: live blob sha `93f4a1ed4a730852ee9c2b4111bd824afaf0047e` が編集前バックアップ
`index.html.20260804-bak-pre-shcv` の `git hash-object` と一致。**巻き込みなし**
（この blob は同日の idx7892 JAN逃がしデプロイで上げたもの＝それ以降 live は誰にも触られていない）。

★ビルドに **36.8秒**（ポーリング4回目）かかった。従来の記録（1回目で一致・約45秒）より
待ちが長く、`building` が3回続いた。異常ではないが「1回目で一致する」を前提にしないこと。

### ライブ実物からの再計算

live の `index.html`（`?cb=2` で取得した現物）から DATA 行とロジックを読み直し、
全37,855行の実送信値を再計算した:

```
live idx8033 code='SHVC-ALIJ-JPN'     → 送信値 SHVC-ALIJ
live idx8062 code='SHVC-P-AN5J'       → 送信値 SHVC-AN5J
live idx8223 code='SHVC-P-AYQJ(JPN)'  → 送信値 SHVC-AYQJ
live idx8371 code='SHVC-P-AEFJ(JPN)'  → 送信値 SHVC-AEFJ
live idx8382 code='SHVC-P-AKBJ(JPN)'  → 送信値 SHVC-AKBJ
live idx9031 code='SHVC-T2'           → 送信値 SHVC-T2
live idx9207 code='SHVC-TO'           → 送信値 SHVC-TO

live で型番列に AHVC を含む行: []   /  SHCV を含む行: []
live 全37,855行 vs ローカル期待値: 0行差分
```

**7行とも `SHVC-` 系**＝実測で 200 / 着地を確認済みの形が本番に乗っている。

★**実機での目視確認は未実施。** 7行のカードで `By product code` が新しい型番を送り、
駿河屋で目的の商品に着地することの実機確認は Koha 待ち。特に B群（idx9031 / idx9207）は
ノイズ混じりなので、実機で「目的の商品が一覧内に見える」ことを確かめてほしい。

---

## 引き継ぎ: `.genre-link` 直前にも同種の古いコメントが残っている（2026-08-04）

4753行の掃除案（「`.btn-clerk` 直前コメントを現状に合わせて書き換え。**別指示で扱う**」）は
コミット `0614544` で片付けた（コメント書き換え＋デッドセレクタ `.card .btn-clerk .jp` の削除）。
その作業中に、**掃除案として挙げられてすらいなかった同型のものをもう1つ見つけた**ので記録する。

**場所**: `index.html` の `.card .genre-link` 直前（**689行付近**・掃除後の行番号）。

```
/* 行ロケーターの直下に出る枠線ボタン。行ロケーターと同じ条件でしか描かれない。
   塗りは載せない（.btn-clerk が既にカード内で一番強い。ここを同じ強さにすると競る）。
   枠線＋普通体で「押せるが主役ではない」段に置く。 */
```

**何が古いか**: 「`.btn-clerk` が既にカード内で一番強い」が厳密でない。
`.btn-clerk` が持つのは桃の**枠**だが、購入ブロックの `.card .store.primary`（掃除後 328行）は
`border-color` と `background` の両方が `--signal` ＝桃の**塗り**。カード内で最も強い面はそちら。

**★今回は指示範囲外なので触っていない。** 指示は「`.btn-clerk` 直前の設計コメント」と
「デッドセレクタ `.card .btn-clerk .jp`」の2点で、ここは含まれていなかった。
見つけたからといって勝手に広げない（指示された範囲だけを直す）。

**次にやるなら**: 文言だけの問題であり、`.genre-link` に塗りを載せないという**判断自体は今も妥当**。
店頭ゾーン内での強弱の話としては筋が通っている。直すなら「カード内で一番強い」を
「この店頭ゾーンでは `.btn-clerk` が一番強い」程度に狭めるのが最小の修正。
CSS ルールには手を入れない＝表示は変わらない。

★行番号は編集のたびに動く。`0614544` の掃除で `</style>` は 809 → **817行**、`const DATA` は
959 → **967行**、`<script>` は 958/2256 → **966/2264行** に動いた。
次に触るときは行番号を信じず `grep -n "genre-link"` で引き直すこと（罠#21 の再発防止）。

★これは罠#117（表示要素を消すと、それを説明するコメントが嘘になる）の**残り**にあたる。
4762行の罠#117 は「四隅の+」と「スラブ桃枠」の2件を数えていたが、**孤児化したコメントは
その2件で全部ではなかった**。`.slab` の桃枠撤去は `.btn-clerk` 直前だけでなく、
`.btn-clerk` を基準点として参照している**別のコメント**も同時に古くしていた。
→ 一般化: 表示要素を消したら、その要素を説明するコメントだけでなく、
**その要素を「基準」として言及している他のコメント**も grep すること。
今回で言えば `grep -n "btn-clerk" index.html` を撤去時に打っていれば 689行も同時に見つかった。

---

## SFC 接頭辞の誤記 `SVHC-` 6件（2026-08-04）

7565行の「★SVHC- 6件は保留（触っていない）」の**続き**。同日の `AHVC-`/`SHCV-` 7件を直したとき、
`SVHC-` 6件は「未測定だから触らない」で保留した。それを測って足した回の記録。

### 対象6行

| idx | row[0] 英題 | row[1] 日本語タイトル | row[3] 型番 | 当時の実送信値 |
|---|---|---|---|---|
| 7996 | （空） | ペブルビーチの波濤 NEW トーナメント・エディション | `SVHC-AONJ-JPN` | `SVHC-AONJ` |
| 8407 | Old Testament Megami-Tensei | 旧約・女神転生 | `SVHC-AKMJ-JPN` | `SVHC-AKMJ` |
| 8621 | Sansara Naga 2 | サンサーラ・ナーガ 2 | `SVHC-IV` | `SVHC-IV` |
| 8630 | Dolucky's A-League Soccer | ドラッキーのAりーぐさっかー | `SVHC-Z7` | `SVHC-Z7` |
| 8905 | GP-1 | GP-1 | `SVHC-G7` | `SVHC-G7` |
| 8997 | （空） | ナムコットオープン | `SVHC-NO` | `SVHC-NO` |

★row[3] はいずれも `;` 区切りの要素が**1つだけ**＝`codes[0]` がそのまま `sgSrc`。
★`sgBase` 以降のパイプライン（`sgBase` → `sgPdash`(-P-落とし) → `sgParen`）は、この6行では
`-JPN` 以降の除去しか働かない（`-P-` なし・括弧なし・`SG_JAN`/`SG_DEAD`/`DL-DOL-` 族いずれも非該当、
row[10]=JAN は6行とも空）。だから実送信値＝接頭辞の誤記がそのまま出ていた。
★**row[7] は6行とも埋まっている**（全て `スーパーファミコン`）。Buyee キーワード（row[1] + row[7]）は
6行とも出ており、`kwBuyeeFallback` は使われていない＝駿河屋以外の到達手段は元から生きていた。
7258行の RVL-006 の教訓（「row[7] が空でなければ安全」ではない）の逆で、ここは row[7] が
埋まっていることが**実際に効いている**側の例。

### 根拠（MADB 原文の中で完結・外部サイトから値は持ってきていない・罠#141）

7474行の節（`AHVC-`/`SHCV-` 7件）と**同じ作り方**をした。

1. **同一 TTL の接頭辞分布**（platform にスーパーファミコンを含むレコードの productID・計655件）:
   `SHVC 615 / DMG 16 / SVHC 6 / SHCV 5 / SFT 2 / AHVC 2 / DMJ 2 / CLV 1`。
   `SVHC-` は TTL 全体でも**この6件で全部**。
2. **直した先の書式はすべて同 TTL に実在する**: `SHVC-####-JPN` **145件**（→ AONJ / AKMJ の2件が
   この形）/ `SHVC-2文字` **約350件**（`SHVC-G3`, `SHVC-G8`, `SHVC-I2`, `SHVC-I7`, `SHVC-IA`,
   `SHVC-NA`, `SHVC-NJ`, `SHVC-Z2`, `SHVC-ZE` …）。**`IV` / `Z7` / `G7` / `NO` はいずれも
   この2文字系列の欠番に収まる**（`SHVC-G3` と `SHVC-G8` は在るのに `SHVC-G7` が無い、など）。
3. **4文字（2文字）部分は一切触っていない。** 動かしたのは接頭辞だけ＝原文の正規形化に留まる。
4. **直した先の6値は TTL のどこにも存在しない**＝既存レコードとの衝突が無い。
   （7件のときは「修正後の値は TTL のどこにも無い」を*弱点*として書いたが、
   **衝突が無いことの確認**でもある。両面から見ること。）
5. **直した先が実際に当たることを先に測ってある**（下記）。測らずに直すことはしない。

### 実測（2026-08-04・af_jump/UTM 無しの生URL・自己クリック回避）

左が当時の実送信値。URL は `https://www.suruga-ya.jp/search?category=2&search_word=…`。

| 旧 | 結果 | 新 | 結果 | ヒット内容 |
|---|---|---|---|---|
| `SVHC-AONJ` | 404 / 0件 | `SHVC-AONJ` | 200 / **3件** | 全件「ペブルビーチの波濤 new トーナメントエディション」の状態違い（無印 / 箱説なし / ランクB） |
| `SVHC-AKMJ` | 404 / 0件 | `SHVC-AKMJ` | 200 / **2件** | 「旧約・女神転生」と「旧約・女神転生 (箱説なし)」 |
| `SVHC-Z7` | 404 / 0件 | `SHVC-Z7` | 200 / **3件** | 全件「ドラッキーのAりーぐさっかー」の状態違い |
| `SVHC-NO` | 404 / 0件 | `SHVC-NO` | 200 / **4件** | うち2件が「ナムコットオープン」。残り2件は「糸井重里 バス釣りNo1」＝`No1` への部分一致ノイズ |
| `SVHC-IV` | 404 / 0件 | `SHVC-IV` | 200 / **59件** | 目的の「サンサーラナーガ2」は **49位・52位・59位** |
| `SVHC-G7` | 404 / 0件 | `SHVC-G7` | 200 / **200件** | 目的の「GP-1」は **146位・165位** |

★**6行とも旧値は 404/0件**。誤着地はしておらず、全行が空振りだった
（動物番長 5758行・RVL-006 7221行のような「誤った商品に着地する」型ではない）。
★順位は**全ページを走査して確定させた**。1ページ24件で、`SHVC-IV` は3ページ・`SHVC-G7` は9ページ。
`&page=N` でページを取れる（**`page=1` は 301 リダイレクト**なので、1ページ目はパラメータ無しの
URL で取ること。ここを間違えて最初 0件に見えた）。

### ★IV / G7 の2件は「到達はするが下位に埋没する」―― それでも直す

`IV` はローマ数字の4として **ファイナルファンタジー IV・イースIV・キャプテン翼IV・
ウィザードリィ外伝IV** 等に、`G7` は駿河屋側で広いトークンとして拾われ、
それぞれ **59件 / 200件** に膨張する。1ページ24件なので **IV は3ページ目・G7 は7ページ目**で、
実用上は「リンクを踏んでもすぐには見つからない」。`SHVC-G7` の1ページ目は
**24件すべてが無関係の「(箱説なし)」品**で、目的の GP-1 は影も形も無い。

**それでも直す。** 直す前は `404 / 0件` で**到達手段が無い**のに対し、直した後は
件数内に**必ず目的の商品が居る**（順位まで確定させてある）。罠#27 の「誤った値より無いほうがまし」は
**0件 と 誤着地** の比較であって、ここは **0件 と 埋没するが当たる** の比較。後者が上。
7557行の `SHCV-T2` / `SHCV-TO` で同じ判断をしており、**そこと矛盾させない**。

★ただし**埋没の度合いは T2/TO（3件・9件）より一段悪い**。T2/TO は「ノイズ混じりだが1ページ目に居る」、
IV/G7 は「ページを繰らないと出てこない」。同じ判断に倒したが、質は違う。ここを同列に書かない。
★将来 JAN 逃がし（`SG_JAN`・5758行の動物番長 / 7357行のスーパー人生ゲーム3の方式）や別の絞り込みで改善する余地はある。
ただし**それは別件**で、この回では接頭辞の誤記だけを直した。7562行で B群を JAN に寄せなかったのと
同じ理由 ―― **JAN 側は未測定**で、測っていない方式には倒さない。やるなら先に測ること。
★残り4件（AONJ / AKMJ / Z7 / NO）は上位が全部目的の商品で、素直に当たる。

### 実装

`build_master.py` の `PRODUCT_CODE_TYPO` に6件を**1件ずつ名指しで**追加（10件 → 16件）。

```python
"SVHC-AONJ-JPN": "SHVC-AONJ-JPN",        # ペブルビーチの波濤 NEW トーナメント・エディション
"SVHC-AKMJ-JPN": "SHVC-AKMJ-JPN",        # 旧約・女神転生
"SVHC-IV": "SHVC-IV",                    # サンサーラ・ナーガ 2（当たるが 49位・下位に埋没）
"SVHC-Z7": "SHVC-Z7",                    # ドラッキーのAりーぐさっかー
"SVHC-G7": "SHVC-G7",                    # GP-1（当たるが 146位・下位に埋没）
"SVHC-NO": "SHVC-NO",                    # ナムコットオープン
```

★`SVHC` → `SHVC` の**パターン置換にはしない**。将来 `SVHC` が正しい何かの接頭辞として
現れたときに静かに壊すため（このブロック冒頭の原則どおり）。
★埋没する2件のコメントに「（当たるが 49位・下位に埋没）」と**辞書の行に直接**書いた。
別の場所の説明文だけに書くと、辞書だけ見た人が「素直に当たる4件」と同じものだと思う。

### 検証

再生成（`build_master.py` → `master_final.csv` / `data_line.js`）後、以下を確認した。

| 項目 | 結果 |
|---|---|
| **全数突合**（37,855行 × 全13列） | **セル差分は6箇所のみ**。差分のあった列番号の集合 = `[3]`（型番列のみ）、行番号 = `[7996, 8407, 8621, 8630, 8905, 8997]` |
| `master_final.csv` の差分 | **6行のみ**（M725565 / M725982 / M726201 / M726210 / M726493 / M726585）。変わったのは product_code 列だけ |
| **空撃ち**（旧/新 DATA を同じ実処理に通して実送信値を突合） | **変化6行のみ**。全部 `SHVC-` 系に変わり、**他37,849行は完全不変** |
| **巻き込みなし** | 新DATA に `SVHC-` を含むセル **0** / 実送信値に `SVHC-` を含む行 **0**。6件の新送信値は**いずれもその行だけが送る**（他行との衝突0） |
| `sgNoCode`（駿河屋コード検索リンクの非表示判定） | 変化した行 **0** |
| **DATA行(967) md5** | `0bee046fc459ac6eb1ba36eb4a3d0333` → **`381d91452d3e5701482bfb4f922d5733`** |
| head区間（1..966行） | `d06eba881367a6b539c54bba418380fe` **不変** |
| ロジック区間（968..末尾） | `c71eb2c4b852ce1903e7bb84db98baec` **不変** |
| 全体md5 | `528b1c3ddf614f7206468e0aa2073e10` → `2ba98b6917f321bf32f539a39c5ae3f7` |
| ファイルサイズ差 | **0 バイト**（8,092,963 → 8,092,963）。接頭辞の置換は同バイト長 |
| `node --check` | インライン script 2本とも **OK** |
| `swap_data.py` | `[0]〜[6]` ガードで**一度中止**（想定どおり・AGB / GMSJ / ACMJ / SHCV と同型）。上記を確認したうえで**正規の迂回**で注入し、再実行で **0行差分**の通過を確認 |

★**中止時に swap_data.py が列挙した6行が、対象6行と完全一致した。**
ガードは「止めるための仕掛け」だが、止まったときに何を列挙するかが**独立した裏取り**になる。
自分の全数突合と、ガードの `[0]〜[6]` 多重集合照合という**別経路の2つが同じ6行を指した**。

★**区間の取り方が 7604行（SHCV の回の検証表）の記録と違う。** DATA 行が **959 → 967** に動いたため
（`0614544` の CSS 掃除でコメントが増えた・末尾の引き継ぎ節に記録あり）。今回は
head = `sed -n '1,966p'`・DATA = `sed -n '967p'`・ロジック = `sed -n '968,$p'` で取った。
**ロジック区間 md5 が前回と同値**なのは、間の変更が全部 head 側に入ったことの裏取りになっている。
7424行 / 7622行に書いたとおり、**どの区間の取り方をしたかを必ず明記すること**。

### ★DATA 行 md5 の基準を更新

| 区間 | 旧基準 | 新基準（2026-08-04・この節以後） |
|---|---|---|
| **DATA 行(967)** | `0bee046fc459ac6eb1ba36eb4a3d0333` | **`381d91452d3e5701482bfb4f922d5733`** |
| head 区間(1..966) | `d06eba881367a6b539c54bba418380fe` | 同左（**不変**） |
| ロジック区間(968..末尾) | `c71eb2c4b852ce1903e7bb84db98baec` | 同左（**不変**） |

`0bee046f…`（同日の SHCV 修正時点）は**そこまでの記録として正しい**。消さない。
DATA 原文を修正した**5例目**で、md5 が動く系統の**5代目**にあたる。

### バックアップ

- `build_master.py.20260804-bak-pre-svhc`
- `data/master_final.csv.20260804-bak-pre-svhc`
- `data/data_line.js.20260804-bak-pre-svhc`（md5 `62f87b791314cfcf967718bb16df5ec0`）
- `index.html.20260804-bak-pre-svhc`（8,092,963 bytes / md5 `528b1c3ddf614f7206468e0aa2073e10`
  / `git hash-object` `53d4576c957b3bc6e45845f4c1c55a829c222b22`）← **注入前の現物・drift 確認の基準**
- `BIN_DIVER_NOTES.md.20260804-bak-pre-svhc-note`

★swap_data.py の自動バックアップは `index.html.20260804-0956.bak`（**注入後**の内容）。
7634行に書いたとおり bak 名は分単位なので、これは保険にしかならない。
**本命は作業前の手動バックアップ**（`…-bak-pre-svhc`）で、今回は**それが drift 確認の基準値そのもの**
になった（下記）。手動バックアップは「戻すため」だけでなく「live と突き合わせるため」にも要る。

### コミット（段階的）

- `7a65ca1` — `build_master.py`（辞書に6件追加）
- `365e613` — `index.html`（再生成した DATA 行の注入）

### デプロイ（2026-08-04・SFC 接頭辞誤記6件）

`gh` contents API で `index.html` 1ファイルのみ（5997行の方式・6254行で初実施）。

| 項目 | 値 |
|---|---|
| **drift 確認（上げる前）** | live blob sha `53d4576c957b3bc6e45845f4c1c55a829c222b22` == `git hash-object index.html.20260804-bak-pre-svhc` → **一致＝drift なし**。live は「自分が編集する前のローカル」そのものだった |
| 上げる前の一致確認 | `git hash-object index.html` == `git rev-parse HEAD:index.html` = `cd66d3cbd3cc21ae7b4dc5008c67cac84254f931` → working tree == HEAD(`365e613`) |
| 方式 | `base64 -i index.html | tr -d '\n'` → `jq -n --rawfile c … '{message,content,sha,branch:"main"}'` → `gh api -X PUT repos/english-tadashi/bin-diver/contents/index.html --input payload.json` |
| payload | 10,790,827 bytes（base64 本体 10,790,620） |
| リモートコミット | **`21e78c9ca83b80054eceeaf3e99fa6bdf3881806`** |
| 返った blob sha | `cd66d3cbd3cc21ae7b4dc5008c67cac84254f931` = **ローカル HEAD と一致** |
| Pages ビルド | `status: built` / commit `21e78c9c…` / duration **37,387ms**（約37秒。ポーリング3回・15秒間隔） |
| live 取得 | `https://gamekataban.com/index.html?cb=svhc20260804` → HTTP **200** / **8,092,963 bytes**（ローカルと同値） |
| **live 全体md5** | **`2ba98b6917f321bf32f539a39c5ae3f7`** ＝ローカルと一致 |
| **live DATA行(967) md5** | **`381d91452d3e5701482bfb4f922d5733`** ＝**5代目・新canonical** と一致 |
| **live head区間(1..966) md5** | **`d06eba881367a6b539c54bba418380fe`** ＝**不変** |
| **live ロジック区間(968..末尾) md5** | **`c71eb2c4b852ce1903e7bb84db98baec`** ＝**不変** |
| live 実物の `git hash-object` | `cd66d3cbd3cc21ae7b4dc5008c67cac84254f931` ＝ローカル HEAD blob と一致 |
| **live 実物からの実送信値再計算** | 6行とも `SHVC-AONJ` / `SHVC-AKMJ` / `SHVC-IV` / `SHVC-Z7` / `SHVC-G7` / `SHVC-NO`。`sgNoCode` は6行とも `false`（リンクは出る）。**live 全37,855行で実送信値に `SVHC-` を含む行 0 / DATA 全セルに `SVHC-` を含む行 0** |
| CNAME | `gamekataban.com`（Pages 設定・`status: built`）/ リポジトリ内 CNAME sha `11f5f0eb198f73ad578c7daf41cd3cbb4a05379b`（15 bytes）＝**無傷** |

★**実送信値の再計算は「live から落とした実物だけ」を読んで行った。**ローカルの `index.html` は
一切参照していない（`_experiments/verify_live_svhc_20260804.js` に第1引数で live のパスを渡す形）。
md5 が一致していれば中身は同じ**だが**、「配信されているものを実際にパイプラインへ通したら
何が出るか」は md5 とは別の確認。片方だけでは「md5 は合っているがリンクは壊れている」型の
事故（`sgNoCode` の変化・`sgParen` の副作用など）を拾えない。

★**drift 確認は md5 ではなく `git hash-object` で行う**（6626行で確立した方式）。remote が返すのは
git の blob sha なので、同じ計算式で突き合わせるのが最も直接的。md5 だと remote 側の値が
無いので、一度落としてから比べる手間が増えるうえ、比べているものが1段間接になる。

### この回で確認できたこと（一般化できる分だけ）

- **「未測定だから保留」は、あとで測って足すためのメモとして機能した。**7565行の保留節が
  無ければ、この6件は忘れられていた。**保留するときは「何を・なぜ保留したか」を書いて残す。**
  「触らなかった」だけでは、次に見た人が「検討済みで不要」と読む。
- **ガードに止められることは失敗ではない。**`swap_data.py` の中止出力が、自前の全数突合とは
  別経路の裏取りになった。**止まったときに何を列挙するかまで読むこと。**
- **検索順位まで見ないと「当たる」の質が分からない。**件数が 200件 でも 1ページ目に無ければ
  実用上は届かない。`&page=N` で全ページ走査して順位を出す（`page=1` は 301。パラメータ無しで取る）。

---

## `.genre-link` 直前コメントの修正（2026-08-04）

7692行の引き継ぎ節（「`.genre-link` 直前にも同種の古いコメントが残っている」）の**実施回**。
そこに書いた「次にやるなら」＝文言だけを狭める最小修正を、別指示として受けて実行した。

**場所**: `index.html` の `.card .genre-link` 直前。修正後は **688〜692行**（コメント5行）、
ルール本体は **693行**から。引き継ぎ節には「689行付近」と書いたが、これは修正前の
`塗りは載せない…` の行を指していた。★行番号は毎回 `grep -n "genre-link" index.html` で引き直す。

### 変更内容（コメントのみ・CSS ルールは無変更）

修正前（3行）:

```
/* 行ロケーターの直下に出る枠線ボタン。行ロケーターと同じ条件でしか描かれない。
   塗りは載せない（.btn-clerk が既にカード内で一番強い。ここを同じ強さにすると競る）。
   枠線＋普通体で「押せるが主役ではない」段に置く。 */
```

修正後（5行・688〜692行の全文）:

```
/* 行ロケーターの直下に出る枠線ボタン。行ロケーターと同じ条件でしか描かれない。
   この店頭ゾーンでは塗りは載せない（.btn-clerk がゾーン内で一番強い。ここを同じ強さに
   すると競る）。カード全体で一番強いのはこのゾーンではなく、購入ブロックの
   `.store.primary`（328行）の桃**塗り**。
   枠線＋普通体で「押せるが主役ではない」段に置く。 */
```

**直したのは主語の範囲だけ。**「カード内で一番強い」→「この店頭ゾーンでは／ゾーン内で」に狭め、
カード全体の最強面が `.card .store.primary`（328行・`border-color` と `background` の両方が
`--signal` ＝桃の**塗り**）であることを明記した。行番号の書き方は `.btn-clerk` 直前コメント
（266〜267行）が `.store.primary`（328行）を参照している形に揃えた。

**`.genre-link` に塗りを載せないという判断自体は変更していない。** 引き継ぎ節に書いたとおり
店頭ゾーン内の強弱としては筋が通っており、`.card .genre-link{}` の宣言ブロックは1バイトも
触っていない（差分はコメント3行の入れ替えのみ・+163 bytes）。

### 検証（`</style>` 以降の byte 一致）

`.btn-clerk` 修正時と同じ担保。コメントは `<style>` 内なので、`</style>` 以降が完全一致なら
DATA 行・ロジック・DOM は無傷＝表示と挙動に影響が無いことが言い切れる。

| 項目 | 値 |
|---|---|
| 手動バックアップ | `index.html.20260804-bak-pre-genrelink-comment` |
| 編集前 `</style>` オフセット | 63,217（817行）/ 以降 **8,029,746 bytes** |
| 編集後 `</style>` オフセット | 63,380（**820行**・コメント2行増）/ 以降 **8,029,746 bytes** |
| **`</style>` 以降の md5** | 編集前 = 編集後 = **`64d68c8fafd899f539b724f7dcf4a0ab`** ＝**不変** |
| 差分 | 全体 8,092,963 → 8,093,126 bytes（**+163**）。増分はすべて `</style>` より前 |
| `git diff` | `index.html | 4 +++-`（3 insertions / 1 deletion）＝コメント行のみ |

★**`</style>` の行番号はまた動いた**（817 → **820行**）。`0614544` の掃除で 809 → 817 に
動いたばかりの値で、7719行にそう書いてある。**コメントを足すだけでも動く。**
7719行の数字は `0614544` 時点のスナップショットとして読むこと（今回で `const DATA` は 967 → **970行**、
`<script>` は 966/2264 → **969/2267行**）。

### コミット

- `810302b` — `index.html`（`.genre-link` 直前コメント3行 → 5行）

### デプロイ（2026-08-04・`.genre-link` コメント修正）

`gh` contents API で `index.html` 1ファイルのみ（5997行の方式）。

| 項目 | 値 |
|---|---|
| **drift 確認（上げる前）** | live blob sha `cd66d3cbd3cc21ae7b4dc5008c67cac84254f931` == `git hash-object index.html.20260804-bak-pre-genrelink-comment` → **一致＝drift なし**。この blob は同日の SVHC- 6件デプロイ（`21e78c9c`）で上げたもの＝それ以降 live は誰にも触られていない |
| 上げる前の一致確認 | `git rev-parse HEAD:index.html` = `3275782aa778f55ee934145d7823c0a5e4c0ba2e`（working tree == HEAD `810302b`） |
| payload | 10,791,023 bytes（base64 本体 10,790,836） |
| リモートコミット | **`9b7aa93fb16363f696973b0bf8d2fecae4c1d914`** |
| 返った blob sha | `3275782aa778f55ee934145d7823c0a5e4c0ba2e` = **ローカル HEAD と一致** |
| Pages ビルド | `status: built` / commit `9b7aa93f…` / duration **33,307ms** / error null |
| live 取得 | `https://gamekataban.com/index.html?cb=…` → **8,093,126 bytes**（ローカルと同値） |
| **live 全体md5** | **`5f2ed7a748430d2cae56d47181d8ba8e`** ＝ローカルと一致 |
| **live の `</style>` 以降 md5** | **`64d68c8fafd899f539b724f7dcf4a0ab`** / 8,029,746 bytes ＝**デプロイ前と不変** |
| live 実物の byte 比較 | `live == ローカル index.html` が **8,093,126 bytes 全体で完全一致**（部分md5ではなく全バイト比較） |
| live 実物の文言確認 | 新コメント（`この店頭ゾーンでは塗りは載せない`）**あり** / 旧コメント（`塗りは載せない（.btn-clerk が既にカード内で一番強い`）**なし** |
| live blob sha | `3275782aa778f55ee934145d7823c0a5e4c0ba2e` ＝ローカル HEAD blob と一致 |

★**今回は「全体 byte 完全一致」まで取れた。**データ変更を伴う回（SVHC- 等）は DATA 行が
変わるので区間ごとの md5 で見るしかないが、**コメントだけの回は live とローカルを丸ごと
`==` で比べられる**。区間 md5 より強い担保なので、取れるときは取る。

★引き継ぎ節（7692行）の「次にやるなら」は**これで消化済み**。同節の罠#117 の一般化
（「表示要素を消したら、その要素を基準として言及している他のコメントも grep する」）は残る。

---

## 本日のまとめ（2026-08-04）

この日に扱った9件。**詳細は各節に書いてあるので、ここでは重複させず参照だけ置く。**

1. **SFC型番 `SHVC-P-` 71行の全数対照測定** — 71行すべてを実測し、対照表を確定。コード変更・デプロイ無し。→ 6989行「## SFC型番 SHVC-P- 71行の全数対照測定（2026-08-04）」／ノートのみ `efc6df7`
2. **idx8463 機動武闘伝Gガンダムの型番誤記** — `SHVC-ACMJ` → `SHVC-AGMJ` を `PRODUCT_CODE_TYPO` で修正。→ 7079行「## idx 8463 機動武闘伝Gガンダムの型番誤記を修正（2026-08-04）」／コミット `09fd9b3`・デプロイ `f925bab8`
3. **RVL-006 の2行に `SG_DEAD` を適用** — 誤着地するコード検索リンクを非表示に。→ 7221行「## RVL-006 の2行に SG_DEAD を適用（2026-08-04）」／コミット `eb38169`・デプロイ `86e222d1`
4. **idx7892 スーパー人生ゲーム3 の JAN逃がし** — 型番 `SHVC-A3XJ` が空振り（404/0件）のため JAN `4904880134120` に逃がす。→ 7357行「## idx7892 スーパー人生ゲーム3 — JAN逃がし（2026-08-04）」／コミット `39de819`・デプロイ `eaf37946`
5. **罠#98 の番号衝突を解消** — 後から定義された3723行を **罠#145** に付け替え、3506行が #98 を保持。→ 3730行「（補足・訂正）この罠の番号を #98 → 罠#145 に付け替える（2026-08-04）」／ノートのみ `d133f97`
6. **SFC 型番接頭辞の誤記7件（`AHVC-` / `SHCV-` → `SHVC-`）** — 辞書方式で一括修正。→ 7474行「## SFC 型番接頭辞の誤記7件（AHVC- / SHCV- → SHVC-）(2026-08-04)」／コミット `e2d7369`＋`ae6d149`・デプロイ `8a83a205`
7. **`.btn-clerk` 直前コメントの書き換え＋デッドセレクタ削除** — 4753行の掃除案を消化。`.card .btn-clerk .jp` も削除。→ 4751〜4753行の掃除案と、7692行「## 引き継ぎ: `.genre-link` 直前にも同種の古いコメントが残っている（2026-08-04）」冒頭に経緯あり／コミット `0614544`・デプロイ **`94bb729a`**
8. **SFC 接頭辞の誤記 `SVHC-` 6件** — 7565行の「未測定だから保留」を測って消化。→ 7733行「## SFC 接頭辞の誤記 `SVHC-` 6件（2026-08-04）」／コミット `7a65ca1`＋`365e613`・デプロイ `21e78c9c`
9. **`.genre-link` 直前コメントの修正** — 「カード内で一番強い」を店頭ゾーン基準に狭め、カード全体の最強面が `.store.primary` であることを明記。CSS ルールは無変更。→ 引き継ぎは 7692行、実施はこの直前の節「## `.genre-link` 直前コメントの修正（2026-08-04）」／引き継ぎ記録 `cbfbae4`・コミット `810302b`・デプロイ `9b7aa93f`

**この日のデプロイは6回。**リモートコミットと、その時点の `index.html` blob sha
（`gh api "repos/english-tadashi/bin-diver/contents/index.html?ref=<commit>" --jq .sha` で実測）:

| # | 対象 | リモートコミット | その時点の blob sha |
|---|---|---|---|
| 2 | idx8463 | `f925bab8` | `f4bfc6f6fff500796928f708c109f885b65edaf5` |
| 3 | RVL-006 | `86e222d1` | `c86a1bc12377adf27aeb7cb939b167cf1142e0c9` |
| 4 | idx7892 | `eaf37946` | `93f4a1ed4a730852ee9c2b4111bd824afaf0047e` |
| 6 | AHVC-/SHCV- 7件 | `8a83a205` | `c1bcfd0bf9767db195c531e7b36872830ceb0dca` |
| 7 | `.btn-clerk` | **`94bb729a`** | **`53d4576c957b3bc6e45845f4c1c55a829c222b22`** |
| 8 | SVHC- 6件 | `21e78c9c` | `cd66d3cbd3cc21ae7b4dc5008c67cac84254f931` |
| 9 | `.genre-link` | `9b7aa93f` | `3275782aa778f55ee934145d7823c0a5e4c0ba2e` |

★**7番（`.btn-clerk`・`94bb729a`）だけ専用のデプロイ記録節が無い**（記録漏れ。この表が唯一の記録）。
他の5回は各節に個別のデプロイ節がある。**デプロイしたら必ずその回のノートにデプロイ節を作る。**
コメント修正のような「小さい」回ほど節を立てずに済ませたくなるが、live に上がっている以上は
台帳に残っていないと drift 確認の基準値が辿れなくなる（次回の drift 確認は「前回上げた blob」を
基準にするため）。実際、SVHC- 6件の drift 基準値 `53d4576c…` は**この記録漏れ回で上げた blob** で、
7897行はそれを「live は自分が編集する前のローカルそのものだった」とだけ書いており、
**どのデプロイで上がった blob なのかは辿れなくなっていた**。

★**live の連鎖は途切れていない。**各回の drift 確認で「基準にした blob == 前回上げた blob」が
毎回成立している（前日の `c70e57d0` → `f4bfc6f6` → `c86a1bc1` → `93f4a1ed` → `c1bcfd0b` →
`53d4576c` → `cd66d3cb` → `3275782a`）。上の表の blob 列と各節の drift 記録が鎖として繋がるので、
**この日 live に触ったのは自分だけ**と言い切れる。

---

## 駿河屋.com（英語・海外発送）の検索リンクを OUTSIDE JAPAN に追加（2026-08-04）

★**この日の10件目。**上の「本日のまとめ（2026-08-04）」は9件で締めてある。まとめ表は
append-only の原則で書き換えていないので、**この節はまとめに載っていない**。次に日次まとめを
作るときはここを拾うこと。この日のデプロイ回数も、まとめ本文の「6回」＋この回＝7回になる。

### 何を測ったか

駿河屋には日本語サイト `suruga-ya.jp`（国内発送のみ）とは別に、英語サイト
`suruga-ya.com/en`（海外発送に対応）がある。後者に英題で当たるなら、OUTSIDE JAPAN の
買い手を代理購入業者を通さずに直接店へ送れる。そこで**当たるのかどうか**を先に測った。

| 項目 | 値 |
|---|---|
| 母集団 | `data/master_final.csv` 全 39,782 行のうち **`title_en` が非空の 13,711 行** |
| 抽出 | `random.seed(42)` で **30 件** |
| 生成スクリプト | `_experiments/gen_surugaya_com_check_20260804.py`（gitignore 対象） |
| チェックシート | `_experiments/suruga_com_accuracy_check.md`（同上） |
| 検索式 | `https://www.suruga-ya.com/en/products?category=2&keyword={英題}+{機種名英語}` |
| 判定 | Koha が30本すべてを実際に開いて目視 |

★**母集団を「英題あり」に絞った。**全行から引くと 26,071/39,782＝約66%が英題空で、
検索語が作れない行ばかりが標本に入る。測りたいのは「英題があるとき当たるか」なので、
英題が無い行は母集団ではなくノイズ。**この絞り込みは実装側の出し分け条件とも一致している**
（下の「英題が空の行は出さない」）。

★**自動アクセスはしていない。**スクリプトは URL を組み立てるだけ。`suruga-ya.com` の
`robots.txt` は `User-agent: ClaudeBot` を `Disallow: /` にしており、`Content-Signal` も
`ai-train=no` / `use=reference`。**判定は人間が自分のブラウザで開いて行う。**
機械で叩いて件数を数える方式は、この店に対しては採らない。

### 結果

| 判定 | 件数 | 割合 |
|---|---|---|
| 1ページ目に対象**あり** | **17** | **56.7%** |
| 1ページ目に対象**なし**（空振り） | 13 | 43.3% |
| **誤表示**（別物がヒットする） | **0** | **0%** |

★**移植版2件（Wii / 3DS）は、原盤ではなく「検索した版そのもの」が正しく出た。**
標本作成時に「バーチャルコンソール等は原盤（スーファミ版など）が先に出て不当に『なし』判定に
なるのでは」と懸念したが、**外れた**。駿河屋側が版を区別して持っている。

★**この2つの数字の意味は非対称で、そこが採否の決め手になった。**
- **当たれば実物が確実に手に入る**（誤表示0＝出てきた物は探している物）
- **外れても実害が無い**（「検索結果が薄い」だけで、別物を掴ませない＝罠#27 の害が発生しない）

つまり 56.7% は「43.3% の確率で害が出る」ではなく「43.3% の確率で何も起きない」。
**空振りのコストと誤着地のコストを同じ土俵で扱わない。**この道具では一貫して
「誤った値より無いほうがまし」（罠#27）で判断してきており、今回はその基準に照らして通った。

### 実装

`index.html` のみ。**IN JAPAN モードは一切変更していない**（下の検証を見ること）。

| 変更 | 内容 |
|---|---|
| 定数 | `SG_COM` / `sgCom(en,pf)` を `sgAf` の直後に追加 |
| 新グループ | **`By English title`** を `gWorld` の直後・`gCode`/`gTitle` の**前**に新設 |
| `.jp` 注記 | `gCode` と `gTitle` の**両方**に `jpOnlyNote` を追加 |
| CSS | `.card .store.wide`（折り返し許可）・`.card .note-link`（注記内リンクの下線と色） |
| 受け渡し | `card()` で `sgComUrl` を作り `buyBlock()` へ渡す |

★**独立したグループにした。**既存の `gCode`（By product code）／`gTitle`（By Japanese title）に
混ぜると見出しの意味が壊れる ―― これは**英題**での検索で、型番でも和名でもない。

★**`gCode` ではなく独立させたもう一つの理由: 型番の有無に左右されないため。**
`gCode` は `codes.length && !sgNoCode` が条件で、型番の無い行（物理20,450行）ではグループごと
消える。そこに相乗りすると、型番が無いという**この導線とは無関係な理由**で .com リンクが消える。

★**塗り（primary）にはしない。**海外モードの塗りは Buyee ＋ Suruga-ya (title) の2本のままで、
`buyBlock` 冒頭の方針コメント「3つ以上は塗らない」を維持している。加えて **56.7% は
「蛍光灯の下で最初に押す1本」として出せる数字ではない。**枠線（`--edge-input`）でも導線は死なない。

★**`af_jump` を通していない。**アフィリエイトではない直リンクなので、通すと .jp の
アフィリエイト経由に見せかけることになる。`sgAf` とは別系統の関数にしてあるのはそのため。

★**英題が空の行では、このリンク自体を出さない。**`card()` の `sgComUrl = en ? sgCom(en,pf) : ''`
で空にし、`buyBlock` 側が `(!jp && k.sgComUrl)` で判定する。英題が空だと検索語が
「機種名だけ」になり、**その機種の商品を無差別に並べる**＝探している物と無関係な結果を
主導線として見せることになる。空振りより誤誘導のほうが害が大きい（罠#27）。

★**`.jp` の注記は code / title の両方に付けた。**片方だけだと「型番のほうなら送れる」と
読める余地が残る。両リンクとも同じ .jp で、**制約はリンクの種類によらない**。
文面は `Suruga-ya.jp (Japan domestic shipping only) — Can't ship to you directly, use Buyee to
receive it overseas` で、"Buyee" は既存の代理購入リンク（`buyeeWrapped`）へ繋ぐ。
`row[7]` も `row[1]` も空で `buyeeWrapped` が作れない行では**プレーンテキストに落とす**
＝リンク切れを作らない。

### URL エンコード形式を測定時に合わせた

実装当初は `encodeURIComponent` そのままで、空白が `%20` になっていた。
**測定に使った30本は `+` 区切り**（Python の `quote_plus`）で、30件すべてが文字列不一致だった。
`.replace(/%20/g,'+')` を足して**不一致 30件 → 1件**にした。

★**残る1件は英題にアポストロフィを含む行**（標本では #22 Kirby's Dream Course）。
`quote_plus` は `%27`、`encodeURIComponent` は素の `'` を送る。**実測で駿河屋.com 側の挙動に
影響しないことを確認済み**（両形とも同じ結果）。記号エンコードには手を付けていない。

★**「測ったURLと本番のURLが違う」は、それ自体が事故の形。**的中率という数字は
測ったURL形式に紐づいており、別形式を本番に置くとその数字が担保にならなくなる。
**測定値を根拠に実装するときは、実装が出す値そのものを測定値と突き合わせること。**
今回は30本の生成URLを実装の `sgCom()` で作り直して1本ずつ比較して見つけた。

### 検証

★**`buyBlock` を変更前後の `index.html` からそれぞれ切り出し、node で実行して突き合わせた。**
4パターン（英題あり型番あり / 英題なし / 英題あり型番なし / `buyeeWrapped` 空）× 2モード。

- **IN JAPAN は変更前の出力と「バイト単位で」完全一致**（4パターンすべて）
- OUTSIDE: `.com` リンクの有無が英題の有無と一致・`af_jump` 非経由・`target=_blank rel=noopener`
- OUTSIDE: `.jp` 注記が code/title 両方に出る（型番なし行は `gCode` ごと無いので1つ）
- OUTSIDE: `buyeeWrapped` 空の行で "Buyee" がプレーンテキストに落ちる

★**当初は IN JAPAN がバイト一致しなかった。**`${jpOnlyNote}` を独立行に置いたため、
jp モード（空文字）で**空白だけの行が2つ残っていた**。DOM 上は無変化だが、
`</div>${jpOnlyNote}` と同じ行に寄せて完全一致にした。既存の `${buyeeNote}` は同じ空白行を
出しており、**この書き方の差が「変更なし」の強度を変える。**「見た目が同じ」より
「バイトが同じ」のほうが検証として強く、取れるときは取る（8016行と同じ判断）。

### md5 / 差分

| 項目 | 値 |
|---|---|
| 編集前バックアップ | `index.html.20260804-bak-pre-surugacom`（8,093,126 bytes） |
| **DATA行(978) md5** | `381d91452d3e5701482bfb4f922d5733` **不変**（**5代目canonical 維持**・DATA は触っていない） |
| `git diff --unified=0` hunk数 | **8**（CSS 1・定数 1・`buyBlock` 5・`card` 2） |
| 差分規模 | +68 / -4 |

★DATA 行の行番号は 967 → **978** に動いた（`<style>` に7行足したため）。**md5 は不変。**

### 新しい罠番号は採番していない

既存の最大は **罠#145**（`grep -o "罠#[0-9]\+"` で全数確認）。この回で新しく足した知見は
既存の罠#27（誤った値より無いほうがまし）の適用例であって、新しい落とし穴ではないので
番号は振っていない。**採番するときは必ず grep で最大値と重複を確認すること**
（罠#98 が実際に衝突していた・3730行の付け替えを見よ）。

### デプロイ（2026-08-04）

`gh` contents API で `index.html` 1ファイルのみ（5997行の方式・6254行で初実施）。

| 項目 | 値 |
|---|---|
| **drift 確認（上げる前）** | live blob sha `3275782aa778f55ee934145d7823c0a5e4c0ba2e` == `git hash-object index.html.20260804-bak-pre-surugacom` → **一致＝drift なし**。前回（`.genre-link`・`9b7aa93f`）で上げた blob と鎖が繋がっている |
| 上げる前の一致確認 | `git hash-object index.html` == `git rev-parse HEAD:index.html` = `6d096fc9fe33c59e7f56f16a8b5984162c728da5` → working tree == HEAD |
| ローカルコミット | `ada5166d`（index.html）＋ `3e0bf6c8`（ノート） |
| 方式 | `base64 -i index.html \| tr -d '\n'` → `jq -n --rawfile c … '{message,content,sha,branch:"main"}'` → `gh api -X PUT repos/english-tadashi/bin-diver/contents/index.html --input payload.json` |
| payload | 10,799,712 bytes（base64 本体 10,799,504） |
| **リモートコミット** | **`2c7fec9e2a6977faf8fb91cc2140a8da237f2df6`** |
| 返った blob sha | `6d096fc9fe33c59e7f56f16a8b5984162c728da5` = **ローカル HEAD と一致** |
| Pages ビルド | `status: built` / duration **39,143ms**（約39秒） |
| live 取得 | `https://gamekataban.com/index.html?cb=surugacom20260804` → HTTP **200** / **8,099,627 bytes**（ローカルと同値） |
| **live 全体md5** | **`067aa98b5968215828c0ca80ac39a5b3`** ＝ローカルと一致 |
| live 実物の `git hash-object` | `6d096fc9fe33c59e7f56f16a8b5984162c728da5` ＝ローカル HEAD blob と一致 |
| **live DATA行(978) md5** | **`381d91452d3e5701482bfb4f922d5733`** ＝**5代目canonical** と一致 |
| live head区間(1..977) md5 | `fc255ea9e31329ab37a192e77dc6c12d`（CSS を足したので前回から変化・想定どおり） |
| live ロジック区間(979..末尾) md5 | `ec51c0c3efad8f8e5f97745931ca6ce5`（同上） |
| CNAME | `gamekataban.com`（Pages 設定・`status: built`）/ リポジトリ内 CNAME sha `11f5f0eb198f73ad578c7daf41cd3cbb4a05379b`（15 bytes）＝**無傷** |

★**Pages の builds API がコミット sha を前回値（`9b7aa93f`）で返した。**ビルド作成時刻
`04:36:51Z` が今回のコミット `04:36:49Z` の2秒後なので、これが今回のビルドである。
**builds API の `commit` フィールドを当てにしない。**判定は live の実物を落として
`git hash-object` / md5 で突き合わせるほうが直接的（8039行と同じ方針）。

★**実送信値の再計算は「live から落とした実物だけ」を読んで行った。**
`_experiments/verify_live_surugacom_20260804.js` に第1引数で live のパスを渡す形で、
ローカルの `index.html` は一切参照していない。

| live 実物からの再計算 | 結果 |
|---|---|
| live DATA 行数 | 37,855 |
| 英題あり＝ .com リンクが出る行 | **13,487 / 37,855（35.6%）** |
| 実送信URLの異常（接頭辞不正・`af_jump` 混入・`%20` 残存・検索語が空） | **0 件**（英題ありの全13,487行を走査） |
| OUTSIDE: 群の並び | `Ships worldwide` → `By English title` → `By product code` → `By Japanese title` |
| OUTSIDE: `.com` リンクが塗りでない | OK（`class="store wide"`・`primary` 無し） |
| OUTSIDE: 国内発送注記 | code / title の**2箇所**・Buyee は `note-link` で代理購入リンクへ |
| IN JAPAN: `.com` リンク／`By English title` 群／国内発送注記 | **いずれも出ない**・群は従来どおり**3つ** |

★**ブラウザでの目視確認はしていない。**上は live の実物を読んでの機械的検証まで。
`_experiments/preview_surugacom_20260804.html`（実CSS込み・両モード×4パターン）を
別途書き出してあるので、見た目の確認はそちらか本番URLで人間が行うこと。

---

## 店リンク3本のラベル修正（2026-08-04）

上の駿河屋.com 追加の直後に、ボタン文言だけを3箇所直した。**ラベル文字列のみ**で、
URL・出し分け条件・CSS・DATA には触れていない。

| # | 場所 | 変更前 | 変更後 |
|---|---|---|---|
| 1 | `gEnTitle`(1465行) | `🔍 Search Suruga-ya.com (English · ships overseas)` | **`Suruga-ya.com (English · ships overseas)`** |
| 2 | `gCode`(1476行) | `Suruga-ya (code)` | **`Suruga-ya.jp (code)`** |
| 3 | `gTitle`(1487行) | `Suruga-ya (title)` | **`Suruga-ya.jp (title)`** |

★**1: 絵文字と "Search " を落とした。**他の店ボタン（`Buyee` / `eBay` / `Book-Off` /
`Hard-Off`）はどれも**店名だけ**で、動詞も絵文字も付いていない。ここだけ
「🔍 Search 〜」だと、同じ `.stores` グリッドに並ぶボタンの中で1本だけ書式が違う
＝**押す前に読む手がかりが増えるのではなく、揃っていないぶん読みにくくなる。**
何をするボタンかは `.group-label` の見出し（`By English title`）と説明文が担っている。

★**2・3: `.jp` を明示した。**`.com` を足したことで、ラベルが `Suruga-ya` のままだと
**どちらのサイトへ行くのか読み取れない**。3本が同じカードに並ぶ以上、
店名だけでは区別として不足する。直前に足した「Japan domestic shipping only」の注記とも
語が揃う（注記の主語も `Suruga-ya.jp`）。

★**2・3 は IN JAPAN モードにも及ぶ。**`gCode` / `gTitle` は両モード共通で、
`.com` リンクのような `!jp` の出し分けを持たない。直前の回まで「IN JAPAN は変更なし」を
検証条件にしてきたので、**ここで初めて IN JAPAN の表示が変わった**ことを明記しておく。
害は無い（IN JAPAN では `.jp` しか出ないので `.jp` と書いて誤読の余地が増えない）うえ、
`.com` を知った利用者が後で IN JAPAN に切り替えたときに区別がつく。

★**`.store.wide`（折り返し許可）は残した。**`🔍 Search ` が消えて9文字短くなったが、
`Suruga-ya.com (English · ships overseas)` は依然40文字弱あり、420px 以下では
折り返しが要る。**ラベルが短くなったからといって、それ用に足した CSS を反射的に消さない。**

### md5 / 差分

| 項目 | 値 |
|---|---|
| 編集前バックアップ | `index.html.20260804-bak-pre-label-fix`（md5 `067aa98b5968215828c0ca80ac39a5b3` ＝当時の live と同値） |
| **DATA行(978) md5** | `381d91452d3e5701482bfb4f922d5733` **不変**（5代目canonical 維持） |
| `git diff --unified=0` hunk数 | **3**（+3 / -3・すべてラベル文字列の1行置換） |
| 旧ラベルの残存 | `&#128269;` / `>Search Suruga-ya` / `>Suruga-ya (code)<` / `>Suruga-ya (title)<` を grep して **0件** |

### デプロイ（2026-08-04・この日7回目）

`gh` contents API で `index.html` 1ファイルのみ。

| 項目 | 値 |
|---|---|
| **drift 確認（上げる前）** | live blob `6d096fc9fe33c59e7f56f16a8b5984162c728da5` == `git hash-object index.html.20260804-bak-pre-label-fix` → **一致＝drift なし**。前回（駿河屋.com 追加・`2c7fec9e`）で上げた blob と鎖が繋がる |
| 上げる前の一致確認 | `git hash-object index.html` == `git rev-parse HEAD:index.html` = `c5d2afb5eaa7070886da16c0b20f4658da843409` |
| ローカルコミット | `951928e`（index.html）＋ `f4e9929`（ノート） |
| **リモートコミット** | **`d94f8b1f25811fa679b4265e1474e0751de0870f`** |
| 返った blob sha | `c5d2afb5eaa7070886da16c0b20f4658da843409` = **ローカル HEAD と一致** |
| Pages ビルド | `status: built` / commit `d94f8b1f` / duration **31,817ms** |
| live 取得 | `https://gamekataban.com/index.html?cb=labelfix20260804` → HTTP **200** / **8,099,616 bytes** |
| **live 全体md5** | **`ab940601bf4c1587ab220e7e8b773bdf`** ＝ローカルと一致 |
| **live 実物の `git hash-object`** | **`c5d2afb5…`** ＝ローカル HEAD / PUT の戻り値 / contents API の**4者すべて一致** |
| **live DATA行(978) md5** | **`381d91452d3e5701482bfb4f922d5733`** ＝**5代目canonical 不変** |
| live のラベル3本 | `Suruga-ya.com (English · ships overseas)` / `Suruga-ya.jp (code)` / `Suruga-ya.jp (title)` |
| live の旧ラベル残存 | **0件** |
| live 実物からの再検証 | `_experiments/verify_live_surugacom_20260804.js` を live に対して再実行 → OUTSIDE 7項目・IN JAPAN 4項目すべて OK |
| CNAME | `gamekataban.com` / リポジトリ内 CNAME sha `11f5f0eb198f73ad578c7daf41cd3cbb4a05379b`（15 bytes）＝**無傷** |

★**今回は Pages の builds API が正しいコミット（`d94f8b1f`）を返した。**前回は前回値を
返していた（8230行付近）。**同じ API が回によって当たったり外れたりする＝当てにしない**
という前回の判断は変えない。判定は live の実物の `hash-object` で行う。

★**この日のデプロイは通算7回。**「本日のまとめ（2026-08-04）」本文は6回・9件で締めており、
駿河屋.com 追加（10件目・`2c7fec9e`）とこのラベル修正（11件目・`d94f8b1f`）は
まとめ表に載っていない。

| # | 対象 | リモートコミット | その時点の blob sha |
|---|---|---|---|
| 10 | 駿河屋.com 追加 | `2c7fec9e` | `6d096fc9fe33c59e7f56f16a8b5984162c728da5` |
| 11 | ラベル修正3本 | **`d94f8b1f`** | **`c5d2afb5eaa7070886da16c0b20f4658da843409`** |

★**ブラウザでの目視確認は、この2回とも未実施。**live の実物を読んでの機械的検証まで。
`_experiments/preview_surugacom_20260804.html`（ラベル変更を反映して再生成済み）か
本番URLで、人間が見ること。

---

## OUTSIDE JAPAN の群の並びを変更（noProxy 行は元のまま）（2026-08-04）

`buyBlock` の `order` 配列のみ。**IN JAPAN の `[gCode, gTitle, gWorld]` には触れていない。**

```js
const order = jp
  ? [gCode, gTitle, gWorld]
  : noProxy
    ? [gWorld, gEnTitle, gCode, gTitle]   // 元の順序を維持
    : [gWorld, gEnTitle, gTitle, gCode];  // 通常行は gTitle が先
```

### 通常行: gTitle を gCode より前に出す

海外モードでは `Ships worldwide` → `By English title` → **`By Japanese title`** →
**`By product code`** の順になった。`gTitle` の内側に Book-Off / Hard-Off があるので、
**結果として `By product code` が Book-Off / Hard-Off の下へ移動する。**

理由: Buyee で受け取れる行では和名検索のほうが実際に使う主経路で、型番の exact match は
「念のため」の位置づけになる。塗り（primary）も `Suruga-ya.jp (title)` に付いており、
**塗りの位置と並びの順序が一致する形になった。**

### noProxy 行(row[7] 空・1,389行)だけは元の順序を維持した

★**これは並べ替えを実装した直後に気づいて戻した。**一律に入れ替えると、
**noProxy 行で塗りボタンが最下部に落ちる。**

`clsCode` は `(jp || noProxy) && codes.length` で primary になる。つまり noProxy 行では
海外モードでも `By product code` が塗られる ―― これは 2026-07-26 の判断で、
**「届く可能性」ではなく「正しい物である可能性」で塗りを決めた**結果（row[7] が空の行に
eBay を英題で当てると北米版が返るため、型番の exact match を主経路にした）。
その塗りボタンを Book-Off / Hard-Off より下へ動かすと、**塗りで示した主経路が
一番最後に来て、塗りの意味が消える。**

★**順序の変更は、塗りの位置とセットで考えること。**塗りは「まずこれを押せ」の合図で、
順序は「上から読む」前提。**この2つがずれると、どちらも弱くなる。**
今回は `order` 配列だけを見て入れ替えたので、`clsCode` の条件分岐を見落としかけた。
**`order` を触るときは `clsBuyee` / `clsCode` / `clsTitle` の3つを必ず一緒に読むこと。**

| | 群の並び（OUTSIDE） | 塗り |
|---|---|---|
| 通常行(row[7] あり) | worldwide → English → **Japanese title** → **product code** | Buyee ＋ Suruga-ya.jp (title) |
| noProxy行(row[7] 空) | worldwide → English → **product code** → **Japanese title** | Suruga-ya.jp (code) ＋ (title) |

★**塗りの数はどちらも2つ**で、`buyBlock` 冒頭の方針「最大2つ・3つ以上は塗らない」は不変。

### 触っていないもの

- **IN JAPAN の順序** `[gCode, gTitle, gWorld]`（両パターンでレンダリング確認済み）
- **Book-Off / Hard-Off** は `gTitle` の内側なので、`Suruga-ya.jp (title)` の直後という
  位置関係は両パターンとも不変
- **下部のコピー欄**（Product code / Japanese title）は `card()` 側で `buyBlock()` の外

### md5 / 差分

| 項目 | 値 |
|---|---|
| 編集前バックアップ | `index.html.20260804-bak-pre-order-swap`（md5 `ab940601bf4c1587ab220e7e8b773bdf` ＝当時の live と同値） |
| **DATA行(978) md5** | `381d91452d3e5701482bfb4f922d5733` **不変**（5代目canonical 維持） |
| `git diff --unified=0` hunk数 | **1**（+14 / -2・実コードは `order` の1式のみ、残りはコメント） |

### デプロイ（2026-08-04・この日8回目）

`gh` contents API で `index.html` 1ファイルのみ。

| 項目 | 値 |
|---|---|
| **drift 確認（上げる前）** | live blob `c5d2afb5eaa7070886da16c0b20f4658da843409` == `git hash-object index.html.20260804-bak-pre-order-swap` → **一致＝drift なし**。前回（ラベル修正・`d94f8b1f`）で上げた blob と鎖が繋がる |
| ローカルコミット | `89c7e7a`（index.html）＋ `febebd9`（ノート） |
| **リモートコミット** | **`396f0c7dc02bfc7cf974544fa64957a969d3681e`** |
| Pages ビルド | `status: built` / commit `396f0c7d` / duration **36,855ms** |
| live 取得 | `https://gamekataban.com/index.html?cb=orderswap20260804` → HTTP **200** / **8,100,565 bytes** |
| **live 全体md5** | **`075076739b4bd7f9de4fa5fa465fd2bb`** ＝ローカルと一致 |
| **blob sha 4者一致** | live 実物の `git hash-object` / ローカル HEAD / PUT の戻り値 / contents API＝すべて **`e7e43a9448660d9af7ff23700613afed31e65e14`** |
| **live DATA行(978) md5** | **`381d91452d3e5701482bfb4f922d5733`** ＝**5代目canonical 不変** |
| CNAME | `gamekataban.com` / CNAME sha `11f5f0eb198f73ad578c7daf41cd3cbb4a05379b`（15 bytes）＝**無傷** |

**live 実物からの再計算**（ローカルの `index.html` は不参照）:

| パターン | 群の並び | 塗り |
|---|---|---|
| 通常行 / OUTSIDE | worldwide[塗り] → English → **Japanese title[塗り]** → **product code** | 2 |
| noProxy行 / OUTSIDE | worldwide → English → **product code[塗り]** → **Japanese title[塗り]** | 2 |
| 通常行 / IN JAPAN | product code[塗り] → Japanese title[塗り] → worldwide | 2 |
| noProxy行 / IN JAPAN | product code[塗り] → Japanese title[塗り] → worldwide | 2 |

★**この日のデプロイは通算8回。**「本日のまとめ（2026-08-04）」本文（6回・9件）に対し、
10件目=駿河屋.com 追加(`2c7fec9e`)・11件目=ラベル修正(`d94f8b1f`)・
12件目=この並び変更(`396f0c7d`)がまとめ表に未収録。

| # | 対象 | リモートコミット | その時点の blob sha |
|---|---|---|---|
| 12 | 群の並び変更 | **`396f0c7d`** | **`e7e43a9448660d9af7ff23700613afed31e65e14`** |

★**ブラウザでの目視確認は、10〜12件目のいずれも未実施。**
`_experiments/preview_surugacom_20260804.html` は通常行と noProxy 行を並べて出すよう
作り直してあるので、順序の違いはそこで一目で見える。

---

## グループ見出し行を太字に統一（2026-08-04）

`buyBlock` の4グループの見出し行（`.n`）と説明文（`.d`）を、**どちらも `font-weight:700`** にした。

| セレクタ | 役割 | 変更前 | 変更後 |
|---|---|---|---|
| `.card .group-label .n` | 見出し（`Ships worldwide` 等） | **500** | **700** |
| `.card .group-label .d` | 説明文（`proxy & international — …` 等） | **400** | **700** |

宣言はどちらも `font:` ショートハンドの先頭値。**太さ以外（サイズ・行間・フォント・
letter-spacing・色）は変えていない。**

対象の4グループ＝`Ships worldwide` / `By English title` / `By product code` /
`By Japanese title`。**`.d` も太くしたので、見出し行が1つの塊として立つ。**

### 影響範囲の確認（先にやること）

★**`class="n"` / `class="d"` はファイル全体で各4箇所のみ**で、すべて `.group-label` の内側。
CSS セレクタも `.card .group-label .n` と親で限定されているので、**他セクションには波及しない。**
グレップで実数を数えてから触った（4 = buyBlock の4グループと完全に一致）。

★**他セクションのラベルは別系統**で、いずれも無変更:

| ルール | 用途 | weight |
|---|---|---|
| `.card .micro` | Product code 欄・ENGLISH / CONSOLE / PUBLISHER 等 | 600（不変） |
| `.card .slab-label` | slab 内ラベル | 600（不変） |
| `.card .cap` | 注意書き | 400（不変） |
| `.card .buyee-note` | Buyee 費用注記・.jp 国内発送注記 | 400（不変） |

★**`.n` / `.d` のような1文字クラスは、影響範囲をグレップの実数で確かめてから触ること。**
名前が短いほど「どこかで使い回されているのでは」という不安が残るが、
**数えれば確定する。**推測で範囲を広げても狭めても間違える。

### `.buyee-note` のコメントを直した（ルールは無変更）

`.card .buyee-note` の直前コメントが「値は同じ役割の `.group-label .d` と揃える」と
書いていた。**`.d` だけを 700 にしたことで、この記述が事実と食い違う状態になった。**
CSS ルールには触れず、**コメント文だけ**を現状に合わせた（サイズ 11.5px と色 `var(--faint)` は
今も共通・太さだけ違う、と明記）。参照行番号も 308/311 → 316 に更新。

★**あるルールの値を変えたら、そのルールを「基準にしている」と書いた別のコメントを探すこと。**
値をコピーしている箇所ではなく、**値の由来を説明している箇所**が壊れる。
`grep` で `.group-label .d` を引けば1件で見つかった。

### md5 / 差分

| 項目 | 値 |
|---|---|
| 編集前バックアップ | `index.html.20260804-bak-pre-label-bold`（md5 `075076739b4bd7f9de4fa5fa465fd2bb` ＝当時の live と同値） |
| **DATA行 md5** | `381d91452d3e5701482bfb4f922d5733` **不変**（5代目canonical 維持） |
| DATA行の行番号 | 978 → **986**（CSS にコメントを8行足したため。md5 は不変） |
| `git diff --unified=0` hunk数 | **2**（+13 / -5・実 CSS 宣言の変更は2行、残りはコメント） |

### デプロイ（2026-08-04・この日9回目）

`gh` contents API で `index.html` 1ファイルのみ。

| 項目 | 値 |
|---|---|
| **drift 確認（上げる前）** | live blob `e7e43a9448660d9af7ff23700613afed31e65e14` == `git hash-object index.html.20260804-bak-pre-label-bold` → **一致＝drift なし**。前回（並び変更・`396f0c7d`）で上げた blob と鎖が繋がる |
| ローカルコミット | `0b0af43`（index.html）＋ `0519ebb`（ノート） |
| **リモートコミット** | **`3138be44d3c2bb7bf14c7738c13bf7d12e4524ad`** |
| Pages ビルド | `status: built` / duration **54,258ms** |
| live 取得 | `https://gamekataban.com/index.html?cb=bold20260804` → HTTP **200** / **8,101,497 bytes** |
| **live 全体md5** | **`9d4752bd720fcf46f08db1f3ed104bed`** ＝ローカルと一致 |
| **blob sha 4者一致** | live 実物の `git hash-object` / ローカル HEAD / PUT の戻り値 / contents API＝すべて **`96bf0028994437e16f055969f23518e30f640eda`** |
| **live DATA行(986) md5** | **`381d91452d3e5701482bfb4f922d5733`** ＝**5代目canonical 不変** |
| live の該当CSS | `.group-label .n{font:700` / `.group-label .d{font:700` |
| live の他ラベル系 | `.micro{font:600` / `.slab-label{font:600` / `.cap{font:400` / `.buyee-note{font:400` ＝**すべて無変更** |
| live 実物からの再検証 | 4パターン（通常/noProxy × OUTSIDE/IN JAPAN）の並び・塗り＝**8項目すべて OK** |
| CNAME | `gamekataban.com` / CNAME sha `11f5f0eb198f73ad578c7daf41cd3cbb4a05379b`（15 bytes）＝**無傷** |

★**Pages の builds API がまた前回値（`396f0c7d`）を返した。**これで3回中2回外している。
**この API の `commit` は当てにしない**という方針を再確認（判定は live 実物の `hash-object`）。

★**この日のデプロイは通算9回。**「本日のまとめ（2026-08-04）」本文（6回・9件）に対し、
10〜13件目がまとめ表に未収録。

| # | 対象 | リモートコミット | その時点の blob sha |
|---|---|---|---|
| 10 | 駿河屋.com 追加 | `2c7fec9e` | `6d096fc9fe33c59e7f56f16a8b5984162c728da5` |
| 11 | ラベル修正3本 | `d94f8b1f` | `c5d2afb5eaa7070886da16c0b20f4658da843409` |
| 12 | 群の並び変更 | `396f0c7d` | `e7e43a9448660d9af7ff23700613afed31e65e14` |
| 13 | 見出し行の太字 | **`3138be44`** | **`96bf0028994437e16f055969f23518e30f640eda`** |

★**ブラウザでの目視確認は、10〜13件目のいずれも未実施。**今回は太さという見た目そのものの
変更なので、`_experiments/preview_surugacom_20260804.html` か本番URLで人間が見ること。

---

### 罠#146. VS Code の markdown プレビュー経由のリンク検証が、実際には存在しないバグを誤検出させた（2026-08-04）

Buyee の検索キーに含まれる `/` が `i0c.xyz` ラッパ経由で処理を拒否される問題（`%2F`）を
`jpBuyee` の置換で直した直後、「連続スペースでも同じように壊れる」ように見える現象が出た。
`.hack//G.U.` や `SUPER MARIO BROS. / DUCK HUNT` のように `/` を空白に置換した結果
空白が2〜3連になる行（実測41行）が、ブラウザで開くと期待どおりに動かなかった。

**原因は連続スペースではなかった。** 検証に使っていたチェックシートが markdown の表で、
リンクを `[裸](https://…)` の記法で書いていた。VS Code の markdown プレビュー上で
そのリンクをクリック・コピーする過程で、閉じ括弧 `)` などの文字がURLに混入していた。
壊れていたのは**対象のURLではなく、URLの運び方**だった。

リンク記法を一切使わないプレーンテキスト（`_experiments/slash_double_space_urls_plain.txt`・
「タイトル行 → URL行 → 空行」だけの形）に同じURLを書き出して開き直したところ、
**正常に動作することを確認**した。連続スペース由来のバグは存在しなかった＝誤検出。

★**符合していた傍証を先に持っていたのに、そちらを信じ切れなかった。** 修正の前に
node で A（`SUPER MARIO BROS. / DUCK HUNT`）と B（正常行）の各段階を突き合わせており、
`enc()` 適用後の `%25` 出現回数は**両方 0**、`url=` を1回復号すると両方とも `%25` が 0 に戻り
`?lang=en` が素のクエリとして現れる＝**エンコード経路に差は無い**と測れていた。
「measure したのに、目視の再現に引きずられて対象側を疑い続けた」形になっている。

### 対策

- **URLをブラウザで直接検証するときは、markdown のリンク記法やレンダリング経由を避ける。**
  コピー元は必ずプレーンテキスト（`.txt` 等）にする。目視チェックシートを作る側の既定を
  これにする（表で見せたいときも、開いて叩く用の `.txt` を別に出す）。
- **挙動が再現しないときは、対象そのものより先に「検証経路の汚染」を疑う。** 経路が
  1段でも増えていれば（プレビュー・レンダラ・クリップボード・ラッパ）、そこが容疑者。
- 罠#141（「手打ちの目視値と実装が生成する値は別物」）と同じ構図。あちらは**入力**が
  別物になっていた話で、こちらは**出力の運搬**が別物になっていた話。
  実装が吐く値を機械的に取り出して、そのまま叩くのが両方への答えになる。

★この回で入れた「連続空白を1個に畳む」修正（`.replace(/\s+/g,' ').trim()`）は、
**この誤検出を前提に足したもの**である。畳み込み自体は害が無く、全角スペース(U+3000)231行と
NBSP(U+00A0)46行を普通の `%20` に寄せる副次的な利点もあるが、**「連続スペースが壊れるから
入れた」という理由づけは誤り**である。残すか戻すかは、この記録を読んだうえで判断すること。

---

## デプロイ（2026-08-04）— Buyee検索キーの `/` 置換＋空白畳み込み

`gh` contents API で `index.html` 1ファイルのみ。remote 未設定のため通常の push は使えない
（5997行の方式）。

| 項目 | 値 |
|---|---|
| **drift 確認（上げる前）** | live blob `96bf0028994437e16f055969f23518e30f640eda` == `git hash-object index.html.20260804-bak-pre-slash-fix` → **一致＝drift なし**。前回（見出し行の太字・`3138be44`）で上げた blob と鎖が繋がる |
| 上げる前の一致確認 | `git hash-object index.html` == `git rev-parse HEAD:index.html` = `5d290c322caf6b9d2d8d4c9c2975d2f635f4212b` → working tree == HEAD |
| 編集前バックアップ | `index.html.20260804-bak-pre-slash-fix`（`/` 置換の前）／`index.html.20260804-bak-pre-space-collapse`（畳み込みの前） |
| ローカルコミット | `88cc9cb`（index.html）＋ `7743232`（ノート） |
| 方式 | `base64 -i index.html \| tr -d '\n'` → `jq -n --rawfile c … '{message,content,sha,branch:"main"}'` → `gh api -X PUT repos/english-tadashi/bin-diver/contents/index.html --input payload.json` |
| payload | 10,804,346 bytes |
| **リモートコミット** | **`a34e6e93`**（`a34e6e933740c32975e687f20bcaf54eeb435d10`） |
| **blob sha 4者一致** | live 実物の `git hash-object` / ローカル HEAD / PUT の戻り値 / contents API＝すべて **`5d290c322caf6b9d2d8d4c9c2975d2f635f4212b`** |
| **DATA行(986) md5** | `0dd350850ed89c074dfbe87cec146c97` **不変**（DATA は触っていない・live 実物から再計算して一致） |
| 反映時刻 | PUT 直後は旧 blob。**16:41:00 に新 blob へ切り替わり**（PUT から約20秒・15秒間隔のポーリングで検出） |
| 差分規模 | +18 / -2（`jpBuyee` 1行＋理由コメント） |

### live 実物での確認

live（`https://gamekataban.com/index.html`）を落として、その**実物のソース行**を取り出して実行した。

- `.hack//G.U. last recode`（Nintendo Switch・`row[7]` 空＝`kwBuyeeFallback` 経路）
  `jpBuyee` = `.hack G.U. last recode` → キー `.hack%20G.U.%20last%20recode`
  → `%2F` なし・`%20%20` なし。`row[12]` も空なので buyBlock が呼ばれ、ボタンは出る。
- live 全 **37,855行**で `%2F` 残存 **0** / `%20%20` 残存 **0** / キーが空になった行 **0**。

★**Koha が本番実機で `.hack//G.U. last recode` の Buyee ボタンをクリックし、正しく着地することを
確認済み（2026-08-04）。** コード上の生成結果だけでなく、実機のクリックで着地まで見ている。

---

## 総括 — タイトル中の `/` で Buyee リンクが機能しなかった問題（2026-08-04・解消）

### 何が起きていたか

Buyee の検索は**キーを URL のパスの一部に載せる**形式（`buyee.jp/item/search/query/{キー}`）。
タイトルに `/` があると `enc()` で `%2F` になり、さらに `i0c.xyz` ラッパの二重エンコードを経て
`%252F` としてパス位置に入る。ラッパ側でこれが処理拒否され、**Buyee リンクが機能しなかった。**

### 対象行（実測・2026-08-04）

`master_final.csv` 全 39,782行 → index.html の DATA に載るのは 37,855行。その中で:

| 段階 | 行数 |
|---|---:|
| `title_en` か `title_ja` に `/` を含む | 238 |
| − `/` が `title_en` にしか無い（キーは `title_ja` 由来なので影響しない） | 12 |
| = `title_ja` に `/` を持つ | 226 |
| − `row[12]`（配信専売）でボタン自体が出ない | 37 |
| **= 実際にこのURLを吐き、壊れていた行** | **189**（`row[7]` あり 179 / なし 10） |

上位は PS2 39・PSP 29・PS4 18・PSVita 18・DS 14・PS 12。

### どう直したか

`jpBuyee`（`index.html:1691`）で `/` を半角スペースに置換した。**Buyee のキーだけ**が対象で、
`jp` 本体は触っていない＝画面表示・駿河屋・eBay は無変更。

```js
const jpBuyee = jp.replace(/\//g, ' ').replace(/\s+/g, ' ').trim();
```

後半の畳み込み＋trim は罠#146 の誤検出を前提に足したもの（経緯は罠#146 を見よ）。
畳み込み自体に害は無く、全角スペース(U+3000) 231行と NBSP(U+00A0) 46行が普通の `%20` に
寄る副次的な利点もあるため残した。

★`title_en` 側の `/` は Buyee のキーに入らない（キーは `title_ja` 由来）ので今回の対象外。
英題の `/` が効くのは eBay の `kwEn`。そちらは変更していない。

### 未解決#16 との関係 — これは別件

**今回の `/` 問題と未解決#16（2974行・`row[7]` が空の行）は別件である。** 混同しないこと。

| | 今回の `/` 問題 | 未解決#16 |
|---|---|---|
| 何の話か | キーに含まれる `/` がラッパで処理拒否される | `row[7]`（Buyee検索キー）が**空**の行を埋められるか |
| 影響 | リンクは出るが**着地しない** | `kwBuyeeFallback`（和名のみ）に落ち、海外モードで塗りが型番へ移る |
| 今回の状態 | **解消**（189行） | **手つかず＝今回は1行も動かしていない** |

★**未解決#16 自体はまだ片付いていない。** ただし「完全に未着手」ではなく、3562行で
「機種で決まっていた・73%を埋めた」と答えが出ており、`index.html` の `noProxy` コメントには
**5,099行 → 1,389行**（2026-07-26・PS3/Vita/3DS 追加時点）と残数が記録されている。
**残りの 1,389行が未解決のまま**という状態。今回の `/` 修正はこの数字を1行も動かしていない。
（2974行の未解決#16 の記述そのものへの打ち消し線は、3563行の判断どおり**別作業**のまま。）

---

## 未解決#16 の追記情報 — `row[7]` 空を再測定したら 1,389 → **1,137行** だった（2026-08-04）

**既存の未解決#16 の記述（2974行・3562行）は変更していない。この節は append のみ。**
数字が古くなっていただけで、未解決#16 が解決したわけではない。

### 再測定の方法

`index.html:986` の `const DATA` を**読み取りのみ**でパースして数えた（index.html は無変更）。
出力は `_experiments/row7_empty_platform_breakdown.txt`（`.gitignore:17` の `/_experiments/` で無視対象）。

★DATA に `platform_ja` は入っていない。`build_master.py:994` の `data.append` は
`PLATFORM_RENAME` で名寄せした **`platform_en` だけ**を `row[2]` に入れる。
よって以下の機種別は全て `row[2]`（名寄せ後 platform_en）別である。**何を数えたかを明記する**（罠#116の対策）。

### 結果 — 総数 37,855 は一致、空は 1,137行（3.00%）

3483行に記録した 2026-07-26 の内訳と突き合わせる:

| 機種（row[2]） | 2026-07-26 | 2026-08-04 | 差 |
|---|---:|---:|---:|
| Nintendo Switch | 845 | 845 | ±0 |
| PlayStation 5 | 258 | 258 | ±0 |
| Xbox 360 | 144 | **0** | −144 |
| Xbox | 82 | **0** | −82 |
| Xbox One | 29 | 29 | ±0 |
| Wii U | 20 | **0** | −20 |
| 64DD | 6 | **0** | −6 |
| Famicom | 5 | 5 | ±0 |
| **合計** | **1,389** | **1,137** | **−252** |

### 差 252行は 2026-08-03 の `BUYEE_OK` 追加で**解消済み**

**4機種は DATA から消えたのではない。行はそのままで `row[7]` が埋まった。**

| 機種 | DATA内の行数 | `row[7]` が空 | 入った `row[7]` の値 |
|---|---:|---:|---|
| Xbox 360 | 144 | 0 | `Xbox360` |
| Xbox | 82 | 0 | `Xbox` |
| Wii U | 20 | 0 | `Wii U` |
| 64DD | 6 | 0 | `64DD` |

いずれも当該機種の**全行**が埋まっている。原因は `build_master.py:324-347` に記録した
2026-08-03 の実測（シークレットウィンドウで生URLを目視）で、この4機種を `BUYEE_OK` に追加したこと。
**144+82+20+6 = 252 がちょうど差分に一致する。**

★つまり 1,389 という数字は 7/26 時点として正しく、**8/3 の作業が数字を動かしたのに
それを記録した節（3483行・8646行・`index.html` の `noProxy` コメント）を更新していなかった。**
「解消済みの分が未解決の数に混ざったまま残る」形。**母集団を変える変更をしたら、
その母集団を引用している既存の記述を洗い出すこと。**

### 残る 1,137行の性格

| 機種 | 空行数 | 機種内総数 | 機種内比率 | 型番あり | title_en あり |
|---|---:|---:|---:|---:|---:|
| Nintendo Switch | 845 | 845 | 100.0% | 845 (100%) | 3 (0.4%) |
| PlayStation 5 | 258 | 258 | 100.0% | 258 (100%) | 0 (0.0%) |
| Xbox One | 29 | 29 | 100.0% | 29 (100%) | 20 (69.0%) |
| Famicom | 5 | 1,257 | 0.4% | 5 (100%) | 3 (60.0%) |
| **合計** | **1,137** | **37,855** | **3.00%** | **1,137 (100.00%)** | **26 (2.29%)** |

- **型番（codes）充足率 100.00%。** 3579行・3623行の 2026-07-26 の記録（1,389行が型番100%）は
  **1,137行になった現時点でも成立している。** `codes[0]`（駿河屋 primary が使う先頭型番）も
  1,137行すべてで非空。よって 4716行・4756行の「noCode 分岐は現行では未発動」も引き続き有効。
- **title_en 充足率は 2.29%（26行）と極端に低い。** しかも偏っており、
  **Switch 3/845・PS5 0/258 で、この2機種 1,103行は事実上まるごと英題なし。**
  Xbox One だけ 20/29（69.0%）と高いのは洋ゲー中心だから。
  ★「型番は100%あるが英題はほぼ無い」＝ **この母集団に英語で当てに行く経路は無く、
  型番の exact match だけが頼り**という構造。3579行で primary を型番リンクへ移した判断は、
  1,137行になった今も（むしろ一層）正しい。

### Famicom の5行は row[7] の話ではない — 切り分けること

**Famicom 5行は `-USA` 型番の北米版混入（罠#16）であって、`row[7]` が空である原因が他の3機種と違う。**
`build_master.py:320` に既にそう書いてある通り。

| | Switch / PS5 / Xbox One（1,132行） | Famicom（5行） |
|---|---|---|
| なぜ `row[7]` が空か | `BUYEE_OK` に**入れていない**＝現行機で未実測。意図的な除外 | 機種としては `BUYEE_OK` に**入っている**（ファミリーコンピュータ 1,252行は埋まっている） |
| 直し方 | Buyee でキーワードを実測して 0件でなければ `BUYEE_OK` に足す | データ側の混入を取り除く。Buyee のキーワードの問題ではない |
| 未解決#16 の範囲か | **そう** | **違う。罠#16（外国版型番の混入）の残骸** |

★**「1,137行」を1つの束として扱わないこと。** 1,132 + 5 で原因が別。
3555-3560行で「`-USA` / `BLUS` を持つ行を数える」を宿題に挙げているのはこちら側の話であり、
`BUYEE_OK` を足しても Famicom の5行は減らない。

### 未解決#16 の現状

**未解決#16 はまだ片付いていない。** 残数が 1,389 → 1,137 に減っただけで、
残る Switch 845 / PS5 258 / Xbox One 29 は **7/18 の除外理由「現行機で、英語圏の収集家が
この道具を必要としない」がそのまま当たっている3機種**＝ 8646行の「未実測はこの3機種だけになった」の状態。
今回の再測定は数字を更新しただけで、1行も埋めていない。

★`index.html` の `noProxy` コメントに残る **1,389 は古い**（現在 1,137）。
ただし今回は index.html に触れていないので、**コメントの修正は別作業**とする
（3563行の判断と同じく、追記と既存記述の変更は承認経路が違う）。

---

## 未解決#16 の追記情報2 — 「型番100%」の理由と、`BUYEE_OK` 追加の副作用（2026-08-04）

**既存行は変更していない。この節は append のみ。** 直前の追記節（8649行）に対する補足2点。

### 測り方（両方ともこの1回の測定から出ている）

`data/metadata301.ttl` を `class:GamePackage` ブロック単位で読み、
`schema:gamePlatform` 別に `schema:productID` の有無を数えた（読み取りのみ・index.html 不関与）。

★**この数え方は検算済み。** 同じスクリプトが Xbox 360 1,316 / Wii U 840 / Xbox 188 / 64DD 0 を出し、
合計 2,344 が `build_master.py:770-776`（`BUYEE_LINK_ONLY` のコメント）に 2026-08-03 に記録された
実測値と**完全一致**した。独立に測った2経路が同じことを言っている。

| 機種 | 型番あり（＝現在 DATA に居る） | 型番なし（＝現在落ちている） | MADB 計 |
|---|---:|---:|---:|
| Nintendo Switch | 845 | **318** | 1,163 |
| Xbox One | 29 | **683** | 712 |
| PlayStation 5 | 258 | **0** | 258 |

### 1. 3626行の「MADB 完備だから型番100%」は PS5 でしか成立しない

3626行はこう書いている ――
「残った1,389行は、データベース中で最も同定精度が高い行の集まりだった（現行機・準現行機なので
MADB のメタデータが完備している）」。

| 機種 | 3626行の説明は成立するか | 実際 |
|---|---|---|
| PlayStation 5 | **成立する** | MADB 258件中、型番なしは **0件**。本当に完備している |
| Nintendo Switch | **成立しない** | MADB 1,163件中 **318件（27.3%）が型番なし**。完備していない |
| Xbox One | **成立しない**（さらに極端） | MADB 712件中 **683件（95.9%）が型番なし** |

**Switch / Xbox One の「型番100%」は、カタログが完備している結果ではなく、収録条件の選抜の結果である。**
この2機種は `PLATFORM_EN` に無く、`BUYEE_OK` にも入っていないので `KNOWN_PLATFORMS` に**入っていない**。
よって `build_master.py:758` の「型番がある **or** 機種が対象集合」を、
**「型番がある」でしか通れない。型番なし行は最初から DATA に入れない。**
＝ 母集団が「型番を持つ行」に絞り込まれた後で充足率を測れば、100% になるのは当たり前。

★**罠#116 と同型**（「実害なし」と書く前に、その判定が全モード・全母集団で成立するか確かめる／
評価した母集団を明記する）。今回は「完備している」という**原因の説明**が、
実は**母集団の作り方**だった。**充足率を見たら、その母集団が何で絞られているかを先に見ること。**
100% は「良い数字」ではなく「絞り込みの疑い」から入るのが正しい。

★ただし 3579行の判断（primary を型番リンクへ移す）は**揺らがない。**
DATA に載っている 1,137行が型番を100%持っていることは事実であり、
画面が扱うのはその1,137行だけだから。**崩れたのは説明であって、結論ではない。**

### 2. Switch / Xbox One を `BUYEE_OK` に足すと、型番なし1,001行が新規流入する

`KNOWN_PLATFORMS = set(PLATFORM_EN) | (set(BUYEE_OK) - BUYEE_LINK_ONLY)`（`build_master.py:747`）。
**`BUYEE_OK` に足すことは、収録範囲を広げることでもある。**

| 機種 | `PLATFORM_EN` | `BUYEE_OK` に足すと | 新規流入（型番なし） |
|---|---|---|---:|
| Nintendo Switch | **無い** | `KNOWN_PLATFORMS` に新規で入る | **318行** |
| Xbox One | **無い** | 同上 | **683行** |
| PlayStation 5 | **有る**（`プレイステーション5`） | 既に入っているので**変化なし** | **0行** |
| | | **計** | **1,001行** |

★**これは 2026-08-03 に `BUYEE_LINK_ONLY` を作って回避したのと、まったく同じ構造の罠。**
そのときは Xbox 360 / Xbox / Wii U / 64DD で 2,344行が流入するところだった。
`build_master.py:770-776` の言葉をそのまま持ってくる ――
「今回測ったのは**キーワードが Buyee に通るか**だけであって、**収録範囲を広げるかどうかは別の判断**。
測っていない判断を、測った判断の副作用として混ぜないために、ここで切り離す。」

→ **対策も同じ。Switch と Xbox One は `BUYEE_LINK_ONLY` にも入れる。**
そうすれば「既に出ている 874行（Switch 845 + Xbox One 29）にリンクが付く」だけで済む。
収録範囲を広げると決めたときに、この集合から消せばそうなる。

→ **PS5 だけは `BUYEE_LINK_ONLY` に入れる必要が無い**（`PLATFORM_EN` 経由で既に
`KNOWN_PLATFORMS` 内。足しても収録範囲は1行も動かない）。**3機種を1つの束として扱わないこと。**
罠#99（除外の理由を1つの束にすると、理由の届かないものが混ざる）の裏返しで、
今度は**追加**の側で束にすると、副作用の有無が違うものが混ざる。

★なお **`BUYEE_OK` に足す前に、Buyee でキーワードを実測すること**（`build_master.py:294`
「実測して0件でないことを確認した機種だけ。未実測の機種にはリンクを出さない」）。
上の 1,001行の話は**実測して合格した後に踏む段取り**であって、実測を省略してよい理由ではない。
測り方は `build_master.py:326-333`（実ブラウザのシークレットウィンドウ・
curl / WebFetch / ヘッドレスChrome は AWS WAF に遮断される）。

---

### 罠#147. ターミナルの Cmd+クリックで URL を開くと、アポストロフィでコマンドが切れて途中までしか開かない（罠#146 と同型・2026-08-04）

`_experiments/buyee_switch_ps5_xboxone_urls_plain.txt` の URL を VS Code の統合ターミナル上で
Cmd+クリックして開いていたとき、`King's bounty Ⅱ`（Nintendo Switch・`row[7]` 空＝
`kwBuyeeFallback` 経路）だけが期待どおりに開かなかった。

**原因は URL ではなく、開き方だった。** URL 中の**アポストロフィ `'`** が
シェルのクォート開始と解釈され、**そこでコマンドが切断**されて、実際には
`'` の手前までの URL しか開かれていなかった。`enc()`（＝`encodeURIComponent`）は
`'` を**エスケープしない**（`!*'()` は非エスケープ集合）ので、`'` は素のまま URL に載る。

```
検索キー   Nintendo Switch King's bounty Ⅱ
enc 後     Nintendo%20Switch%20King's%20bounty%20%E2%85%A1
                                   ^ ここが素の ' のまま残る
```

★**.txt の中身は正しかった。** ファイルに書かれた URL は完全な文字列であり、
壊れていたのは**そこから先の運び方**（ターミナル → シェル → ブラウザ）である。

### 対策

- **URL の目視検証では、ターミナル経由のクリックを使わない。**
  `.txt` から URL を選択してコピーし、**ブラウザのアドレスバーに直接ペースト**して開く。
- とくに**アポストロフィ `'`・引用符 `"`・バッククォート `` ` ``・`$`・`\`・`;`・`&`** など
  **シェルで特殊な意味を持つ文字を含むタイトル**の検証時は要注意。
  `enc()` が通してしまう `!*'()` は、そのままシェルに渡ると意味を持つ側の文字を含む。
- 一覧を眺めるとき（`.md`）と、実際に叩くとき（`.txt` → アドレスバー）を分けたままにする。
  `.txt` を作った目的は「レンダラを挟まないこと」であって、
  **「ターミナルを挟んでよいこと」ではない。**

★**罠#146 と完全に同型のパターン**である。あちらは markdown プレビュー（レンダラ＋
クリップボード）が URL に `)` を混入させ、こちらはターミナル（シェル）が `'` で URL を切断した。
**どちらも対象そのものは無傷で、誤検出を生んだのは「検証経路」の側**だった。
罠#146 の対策として `.txt` を用意したのに、その `.txt` を**ターミナルから開いた**ことで
新しい経路をもう1段挟んでしまった――**経路を減らしたつもりで、別の経路を足していた。**

→ **一般化: 挙動が再現しないときは、対象より先に「値が通ってきた経路」を数えること。**
経路が1段でも増えていれば（プレビュー・レンダラ・クリップボード・**シェル**・ラッパ）、
そこが容疑者。最短経路は「実装が吐いた文字列を、そのままアドレスバーに置く」こと。

---

## 未解決#16 の追記情報 — Switch / PlayStation 5 / Xbox One を実測して埋めた。残 **5行**（2026-08-04）

**既存の未解決#16 の記述（2974行・3562行・8634行の節）は変更していない。この節は append のみ。**
未解決#16 が完全に解決したとは書かない ―― 残り5行は性質の違う別問題（下記）。

### 残数の推移

| 時点 | `row[7]` が空の行 | 何をしたか |
|---|---:|---|
| 2026-07-26（PS3/Vita/3DS 追加時点） | **1,389** | `index.html` の `noProxy` コメントに記録された数字 |
| 2026-08-03（Xbox 360 / Xbox / Wii U / 64DD） | **1,137** | 252行を実測して埋めた（1,389 − 252） |
| **2026-08-04（今回・Switch / PS5 / Xbox One）** | **5** | **1,132行**を実測して埋めた（1,137 − 1,132） |

内訳の消え方（7/26 の 1,389行の内訳＝3483行）:

```
Switch 845 ┐                    PS5 258 ┐                Xbox One 29 ┐
Xbox 360 144 ├ 8/3 に 252行を消化       │                            ├ 今回 1,132行を消化
Xbox 82      │                          └────────────────────────────┘
Wii U 20     │
64DD 6      ┘
Famicom 5 ───────────────────────────────────────── 残る。対象外（下記）
```

★**残った5行は Famicom の `-USA` 型番（北米版混入・罠#16）で、機種の実測とは別問題。**
`SNS-F4-USA` / `NES-MH-USA` / `NES-XV-USA` / `NES-WH-USA` / `NES-BS -USA` の5行。
機種の生値が `Nintendo Entertainment System` で、`PLATFORM_RENAME` で `Famicom` に寄る一方
`BUYEE_OK` には入っていないため `row[7]` が空になる。**今回は触っていない。**
機種の実測で埋まる類のものではないので、まとめて直さないこと。
＝**「未実測の機種」に由来する空行は 0 になった**が、未解決#16 の行数はまだ 0 ではない。

### 実装 — `BUYEE_OK` と `BUYEE_LINK_ONLY` の**両方**に追加した

`build_master.py`（+44 / −4行）。8/3 の Xbox 360 / Xbox / Wii U / 64DD と同じ仕組み。

| 集合 | 追加した値 | なぜ必要か |
|---|---|---|
| `BUYEE_OK` | `Nintendo Switch` / `プレイステーション5` / `Xbox One` | `row[7]` を埋めているのは `buyee_keyword()` で、見ているのは**こちら**。ここに入れないと `row[7]` は空のまま |
| `BUYEE_LINK_ONLY` | `Nintendo Switch` / `Xbox One` | `KNOWN_PLATFORMS = set(PLATFORM_EN) \| (set(BUYEE_OK) − BUYEE_LINK_ONLY)` から引いて、収録範囲を広げない |

★**片方だけでは足りない。** 「`BUYEE_OK` ではなく `BUYEE_LINK_ONLY` に足す」と読むと `row[7]` が
埋まらない。`BUYEE_LINK_ONLY` は `BUYEE_OK` を**打ち消す**ための集合であって、
`BUYEE_OK` の代わりではない。8/3 のコメント「この4つは `BUYEE_LINK_ONLY` にも入っている」の
**「にも」**がそれを言っている。

★**プレイステーション5 は `BUYEE_LINK_ONLY` に入れていない。** `PLATFORM_EN` に
`プレイステーション5` が既に在り、`KNOWN_PLATFORMS` は `PLATFORM_EN` との**和**で作るので、
入れても引かれない＝新規流入は元から **0行**。入れると「収録範囲から外す」という
**効きもしない意図**を書くことになり、次に読む人を誤らせる。
3機種を1つの束として扱わないこと ―― 罠#99 の裏返しで、**追加**の側で束にすると
副作用の有無が違うものが混ざる。

新規流入を止めた実効: **1,001行**（Switch 318 / Xbox One 683）。
これを止めなければ、型番を持たない行が収録範囲に紛れ込んでいた。

### ローカル build の確認結果

```bash
python3 build_master.py data/metadata301.ttl data/master_final.csv \
        data/wikidata_en.json data/wikidata_pub.json data/wikidata_ja.json
python3 swap_data.py
```

| 確認項目 | 結果 |
|---|---|
| **DATA 総行数** | **37,855 → 37,855（±0）**。型番なし行の紛れ込み 0 |
| `KNOWN_PLATFORMS` の差分 | **空集合**（収録範囲が1行も動いていないことを集合レベルでも確認） |
| `[0]`〜`[6]` 照合（swap_data.py） | 旧にあって新に無い **0行** / 新しく増えた **0行** |
| 変化した列 | **`row[7]` のみ**・**1,132行**（空→値あり。既存値の書き換え **0**） |
| Nintendo Switch | 845行 → `row[7] = 'Nintendo Switch'` 845行（型番なし 0） |
| PlayStation 5 | 258行 → `row[7] = 'プレイステーション5'` 258行（型番なし 0） |
| Xbox One | 29行 → `row[7] = 'Xbox One'` 29行（型番なし 0） |
| **Famicom `-USA` 5行** | 旧 DATA と**13列すべて完全一致**・`row[7]` は空のまま＝**触れていない** |
| `master_final.csv` | 39,782行のまま／相違列は `buyee_kw` のみ／埋まり 36,718 → 37,850 |
| `index.html` | 2,387行のまま・**相違は986行目（DATA行）のみ** |

### ★Buyee の検索は語順に依存しない — 11件の実測で確定

**危なかった点。** 目視チェックシートを作ったときの語順は「機種名 → タイトル」
（`Nintendo Switch King's bounty Ⅱ`）だったが、`index.html:1692` が実際に組むのは

```js
const kwBuyee = r[7] ? enc(jpBuyee + ' ' + r[7]) : '';   // ＝ タイトル → 機種名
```

で**語順が逆**（`King's bounty Ⅱ Nintendo Switch`）。**測った文字列と本番が吐く文字列が
別物**のままデプロイしかけた ―― 罠#141 と同じ構図。

→ 本番の語順で `_experiments/buyee_word_order_check_20260804.txt` を作り直して測り直した。
**Switch 4件 / PS5 4件 / Xbox One 3件の計11件で、旧語順と新語順の検索結果件数が完全一致。**
＝**Buyee の検索は語順に依存しない**と確定。よって機種名前置での実測結果（Switch 30件 /
PS5 30件 / Xbox One 29件＝全件）はそのまま有効で、`BUYEE_OK` 追加の根拠は崩れていない。

- 11件には `King's bounty Ⅱ`（`'` 入り）・`.hack//G.U. last recode` と `Fate/Samurai Remnant`
  （`/` 入り）を含めた。`/` は空白に畳まれて `%2F` は残らない（罠#146 の修正が効いている）。
- URLは書き写しではなく、**`index.html:986` の DATA から `row[1]`/`row[7]` の実物を読んで**組んだ。
  さらに **node で index.html の実物の JS 式**（`enc()`/`jpBuyee`/`kwBuyee`/`buyeeWrapped`）を
  実行し、生成した `.txt` の中に同じURLが文字列として在ることを照合している。
- 叩くときは markdown プレビュー経由もターミナルの Cmd+クリックも使っていない（罠#146・罠#147）。

★**教訓の形は「測ったこと自体は正しかったが、測った対象が本番のものと1箇所ずれていた」。**
罠#141・罠#146・罠#147 と同じ族。**実装が吐く値を機械的に取り出して、そのまま叩く**のが
毎回の答えになる。今回はそれを node での照合まで含めてやった。

### この変更が画面に与える影響

`row[7]` が埋まった 1,132行は `kwBuyeeFallback`（和名だけ）から `kwBuyee`（和名＋機種名）に
経路が変わる。海外モードで `noProxy`（`index.html:1400`）が false になるため、
**primary が型番リンクから戻る**行がこの 1,132行分ある。
＝8354行「`noProxy` 行だけは元の順序を維持した」の対象も同じ数だけ減る。

---

## 罠#16 の残骸5行を DATA から除外した（2026-08-04）

**既存の未解決#16 の記述（2974行・3562行・8634行の節・8652行の節・8874行の節）は
一切変更していない。この節は append のみ。** 数字の更新は下記の「未解決#16 との関係」で行う。

### 何をしたか

`build_master.py` に `EXCLUDE_MADB_ID`（madb_id 名指しの除外 dict）を新設し、
罠#16 該当の**北米版5行だけ**を `data_line.js` の生成対象から外した。
`master_final.csv`（＝完全な記録）は**素通しのまま**で、5行とも残っている。

★**機種や `platform_ja` での一律フィルタにはしていない。** `KEEP_PLATFORMS` は機種単位の
道具で、こちらは行単位の道具。別々に持つ理由は、`Nintendo Entertainment System` を
機種ごと切ると、将来 MADB が日本版をこの生値で登録したときに巻き込むため。
切るべきは機種ではなく、この5行だけである。

### 除外した5行

| madb_id | title_ja | 型番 | 対応する日本版 |
|---|---|---|---|
| M727104 | FINAL FANTASY 2 | `SNS-F4-USA` | **M726991 / Super Famicom / `SHVC-F4`**（ゲームID `F4` が一致する日米版） |
| M727504 | SUPER MARIO BROS. / DUCK HUNT | `NES-MH-USA` | 無し（本体同梱2in1・日本未発売）。日本は2本別売り＝M727790 `HVC-SM` / M727853 `HVC-DH` |
| M728043 | XEVIOUS | `NES-XV-USA` | **M727847 / Famicom / `NXV-4900`**（北米はバンダイ発売・日本はナムコ） |
| M728046 | SUPER MARIO BROS / DUCK HUNT / WORLD CLASS TRACK MEET | `NES-WH-USA` | 無し（本体同梱3in1・日本未発売） |
| M728047 | MAJOR LEAGUE BASEBALL | `NES-BS -USA` | 無し（日本未発売）。★同名の M878432「メジャーリーグ」は**アイレムの別ゲーム**（原典 `©1989 IREM CORP.`）。突き合わせないこと |

★**落としても日本のソフトは1本も減らない。**5行のうち2行は日本版が別行として既に出ており、
残り3行は日本で発売されていない商品だから。これが「型番を直す」ではなく「落とす」を選んだ理由。

★型番の傷は3種類に分かれる。**「-USA だから二重の誤り」と一括りにしないこと。**
`SNS-F4-USA` だけが機種違い（`SNS-` は北米SNESの接頭辞）＋地域違いの**二重**。
他4行は機種の族（NES）は合っており地域違いのみ。`NES-BS -USA` は加えて
型番文字列に半角空白が混入している（MADB 原典の `schema:productID` が既にこの形）。

### 除外の根拠 — 3つとも実測

調査記録は `_experiments/famicom_5rows_investigation.txt` と
`_experiments/famicom_5rows_collision_check.txt`（どちらも `_experiments/` なので履歴には残らない）。
検索・カードの実測は **`index.html` の `run()` と `card()` を逐語のまま node に移植して実行**した
（書き換え・簡略化なし）。以下は本番のコードが返した文字列そのもの。

**(1) `regionNote` が事実と逆の断定を出していた**

`index.html:1739` の `Match · Japan / NTSC-J` は**分岐の無い固定文字列**で全行に出る。
さらに `REGION`（`index.html:1180`）由来で、この5行にこう出ていた:

```
Japanese release — needs a Japanese Famicom:
the 60-pin cartridge does not fit an NES. The game's text is Japanese.
```

★**主張が丸ごと逆。** 実物は NES の72ピンカートリッジで**日本のファミコンには挿さらず**、
中身も英語。`index.html:1171` のコメントが「買う前に一番効く情報なので、間違えてはいけない」と
書いている、まさにその1文が嘘になっていた。
加えて棚案内（`この棚を探す: は行` 等）と `Show to shop staff` も通常どおり出ていた
＝日本の店の棚と店員へ、日本に無い商品の客を送っていた。

★**画面上に「北米版だ」と読み取れる要素は1つも無かった。** 型番文字列の `USA` だけが
唯一の手がかりだが、ラベルは `Product code · exact match`、注記は
`Latin letters — type it into any store yourself, or copy.` ＝**そのまま店に打てという指示**の
文脈で提示されており、むしろ北米型番での検索へ誘導していた。
`AMBIG_EN` の注記（M727104 のみ）も文面は「複数の**日本の**リリースに付く英題」で、
正真正銘の日本版（`SQF-FY`・`SHVC-F4`）にも出るので目印にならない。

**(2) 検索衝突 — 英語で打つ利用者だけが当たっていた**

`run()`（`index.html:1840-1876`）は `for(i=0;i<DATA.length;i++){ … res.push(r) }` と
**DATA の添字順に積むだけでスコアも sort も無い。** 順位は DATA 上の位置だけで決まる。

| クエリ | 除外前 | 除外後 |
|---|---|---|
| `duck hunt` | **2件が両方とも北米版**（日本版 `HVC-DH` は出ない） | **0件（No match）** |
| `SUPER MARIO BROS. / DUCK HUNT` | **2件とも北米版** | 0件 |
| `world class track meet` | **1件＝北米版のみ**（対照が画面に無い） | 0件 |
| `XEVIOUS` | 6件（日本版 `NXV-4900` 5位・北米版 6位。表示は両方 `Famicom` / `XEVIOUS` で**完全同一**） | **5件・日本版のみ** |
| `Final Fantasy II` | 12件（北米SNES版4位・**日本版ファミコンFFII `SQF-FY` 6位**。中身の違う2本が同じ `Famicom` 表示で並ぶ） | **11件・日本版のみ** |
| `MAJOR LEAGUE BASEBALL` | 29件（北米版5位） | 28件 |
| `ゼビウス`／`ダックハント`／`メジャーリーグ` | 北米版は**出ない** | 変化なし |

★日本語で打つと北米版は出ない。5行とも `title_ja` がラテン文字で、`r[8]`(カナ)も `r[9]`(ローマ字)も
カナを持たず、`HAY`（`index.html:1029`）に日本語の索引語が1つも入らないため。
＝**被害を受けるのは英語で打つ利用者だけ**＝この道具の主対象そのものだった。

★`XEVIOUS` で日本版が先に出ていたのは DATA 上 10109 < 10261 という**位置の偶然**であって、
日本版を優先するロジックがあったわけではない。

**(3) 購入リンクが北米の値を国内店へ送っていた**

`card()` が実際に生成していた URL（M727104 の例）:

| リンク | 送っていた値 |
|---|---|
| Suruga-ya.jp (code) | `search_word=SNS-F4-USA` ← 国内店に北米型番 |
| Suruga-ya.com (EN) | `keyword=Final+Fantasy+II+Famicom` ← 実物は SNES なので存在しない組合せ |
| Book-Off / Hard-Off | `ファイナルファンタジーII` ← `r[8]` 由来。**北米ナンバリングのカナ**（日本版は IV） |
| Buyee | `FINAL FANTASY 2`（`row[7]` 空の fallback＝機種名が付かない） |
| eBay | `Final Fantasy II` ← **ここだけは実物に当たる** |

★英題を持たない M728043 / M728046 は `sgComUrl` が空になり、
`By English title / Suruga-ya.com` の**ブロックごと出ていなかった**（`index.html:1544`）
＝海外発送できる唯一の店リンクが消えた状態だった。

### ローカル build の確認結果

```bash
python3 build_master.py data/metadata301.ttl data/master_final.csv \
        data/wikidata_en.json data/wikidata_pub.json data/wikidata_ja.json
python3 swap_data.py --approve-removed 5
```

★**編集前に、変更なしの状態でスクラッチパッドへ再ビルドし、
現行 `index.html` の DATA と完全一致（37,855行）することを先に確認した。**
差分をこの変更だけに帰属させるため。ベースラインを取らずに「差分ゼロ」と言わないこと。

| 確認項目 | 結果 |
|---|---|
| **DATA 総行数** | **37,855 → 37,850（−5）** |
| **Famicom 行数** | **1,257 → 1,252** |
| 除外5行の残存 | **0件**（master 内で対象型番は各1件＝madb_id と1対1で対応することも確認） |
| **他の 37,850行** | **`[0]`〜`[12]` で1セルも差分なし**（旧DATAから5行を抜いた配列と新DATAが完全一致） |
| 対応する日本版行 | `SHVC-F4` / `HVC-SM` / `HVC-DH` / `NXV-4900` いずれも残存 |
| `master_final.csv` | **39,782行のまま・完全一致**（素通し。除外5件も残っている） |
| `swap_data.py` | `--approve-removed 5` で通過。「削除5行のみ・追加/改変/改名なし」を `[0]`〜`[6]` 照合で確認 |
| `index.html` | 2,386行のまま・**相違は986行目（DATA行）のみ** |
| 差分規模 | `build_master.py` **+73 / −0**（既存行の変更ゼロ） |

★**パブリッシャー綴りの多数決パス1（`build_master.py:1053`）でも除外している。**
`data_line.js` に出ない行が、見えないところで綴りの多数派に票を入れている状態を避けるため。
実測でこの5票は結果を動かさない（他 37,850行の `[0]`〜`[12]` が完全一致）。

★**除外はパターンで書いていない。** `-USA` で括ると、master 全体を走査したとき
M1104128（Game Boy / `DMG-TR-USA` / TETRIS）まで一緒に消える。あちらは `platform_ja` が
「ゲームボーイ」で `row[7]` も埋まっており、性質が違う（**今回は対象外＝未判断のまま残置**）。
他に M718874（Nintendo DS / `NTR-AJRP-UKV` が2要素目）・M718943（PS3 / `BLUS30264`）も残る。
＝**罠#16 の残骸は `row[7]` の空とは相関しない。** 棚卸しをするなら型番の接尾辞を直接数えること。

### 未解決#16 との関係 — 数字は 5→0 になったが、埋めたのではない

★**`row[7]` が空の行は 5 → 0 になった。しかしこれは「機種を実測して埋めた」のではなく、
「行ごと DATA から消えた」結果である。** 8874行の節が
「残った5行は Famicom の `-USA` 型番（北米版混入・罠#16）で、機種の実測とは別問題」
「機種の実測で埋まる類のものではない」と書いたとおりで、**その判断は今も正しい。**
今回やったのは、その別問題のほうを別の手段（除外）で片付けたということ。

| 時点 | `row[7]` が空の行 | 何をしたか |
|---|---:|---|
| 2026-07-26 | 1,389 | `index.html` の `noProxy` コメントに記録された数字 |
| 2026-08-03 | 1,137 | Xbox 360 / Xbox / Wii U / 64DD を実測して 252行を埋めた |
| 2026-08-04（Switch/PS5/Xbox One） | 5 | 1,132行を実測して埋めた |
| **2026-08-04（今回・罠#16 除外）** | **0** | **5行を DATA から除外＝埋めたのではなく消えた** |

★**「未解決#16 が解決した」とは書かない。** 未解決#16 は「`row[7]` が空の行を埋められるか」で、
最後の5行は埋めずに落としたのだから、問いに答えたのではなく問いの対象が無くなった形。
8874行の節の「未実測の機種に由来する空行は 0 になった」は 8/4 時点で既に成立しており、
今回はそこに含まれていなかった5行が母集団ごと消えただけである。
2974行・3562行の記述への打ち消し線は、従来どおり**別作業**とする
（3563行の判断と同じく、追記と既存行の変更は承認経路が違う）。

★`index.html` の `noProxy` コメントに残る **1,389 は依然として古い**（現在 0）。
今回も index.html のコメントには触れていないので、**修正は別作業**のまま。

### 副次的発見 — 未解決#26

- **未解決#26.** 日本版ダックハント（M727853 / Famicom / `HVC-DH` / 「ダックハント」）が
  `title_en` を持たないため、**英語で `duck hunt` と打っても引けない。**
  除外前はこのクエリの結果が北米版2件だけで、除外後は **0件（No match）** になった。
  ＝**「誤った答えが消えた」のであって「正しい答えが出るようになった」のではない。**
  罠#27（「誤った値より無いほうがまし」）の線では正しい状態だが、英語圏の利用者から見れば
  有名タイトルが引けないままである。`ダックハント` と日本語で打てば従来どおり1件出る。
  → **対応は今回していない（記録のみ）。** 直すなら `TITLE_EN` への手書き追加だが、
  同型の行（英題が無く日本語名がカナだけの Famicom 行）が他に何行あるかを先に数えること。
  1行だけ手で足すと、次に読む人が「英題は埋まっている」と誤解する。


---

## デプロイ（2026-08-04）— 罠#16 の北米版5行を DATA から除外

`gh` contents API で **`build_master.py` と `index.html` の2ファイル**。remote 未設定のため
通常の push は使えない（5997行の方式）。**index.html だけでない回**なので、上げた順も残す
（`build_master.py` → `index.html`。生成側を先に上げて、生成物が後から追いつく形）。

| 項目 | 値 |
|---|---|
| **drift 確認（上げる前）** | live blob `8c8e28bf878491cb801ece4e7a424926d9d349e4` == `git hash-object index.html.20260804-bak-pre-trap16-exclude` == `git rev-parse HEAD:index.html` → **一致＝drift なし**。前回（Switch/PS5/Xbox One の `row[7]`・`08c36ea`）で上げた blob と鎖が繋がる |
| build_master.py の drift | live blob `c5871f31f26cc59b8be11330cfda5d991b85e1a4` == HEAD == 編集前バックアップ → **一致＝drift なし** |
| 上げる前の一致確認 | `git hash-object index.html` == `git rev-parse HEAD:index.html` = `92395a9978a9f9f55b71580c54134eaf93d7d0dc` → working tree == HEAD |
| 編集前バックアップ | `index.html.20260804-bak-pre-trap16-exclude` / `build_master.py.20260804-bak-pre-trap16-exclude` / `data/master_final.csv.20260804-bak-pre-trap16-exclude` / `data/data_line.js.20260804-bak-pre-trap16-exclude` |
| ローカルコミット | `1cea8b8`（ノート）→ `51962d4`（build_master.py + index.html） |
| 方式 | `base64 -i <file> \| tr -d '\n'` → `jq -n --rawfile c … '{message,content,sha,branch:"main"}'` → `gh api -X PUT repos/english-tadashi/bin-diver/contents/<file> --input payload.json` |
| payload | index.html **10,830,000 bytes** |
| **リモートコミット** | `139561109f3a86dd8b39f72ae4ebeeaac0536464`（build_master.py）→ **`de0f2c027da8ee1bd39e92f04c19e06da0351ec0`**（index.html） |
| **blob sha 4者一致（index.html）** | live 実物の `git hash-object` / ローカル HEAD / PUT の戻り値 / contents API＝すべて **`92395a9978a9f9f55b71580c54134eaf93d7d0dc`** |
| blob sha 一致（build_master.py） | contents API == ローカル HEAD ＝ **`ae5d4f6ca24b4c3cf71da249fa843b0e63b4fedd`** |
| **live 全体md5** | **`014396a06142369c5015ad82b5963ac5`** ＝ローカルと一致 |
| **DATA行(986) md5** | **`5a6a91e60c89a8c2f823637272664882`**（**末尾改行なし方式＝下記で正典と定める形**）。末尾改行あり方式では `b8052e260aee071144de4a236cc3e402`。**live 実物から再計算して一致** |
| live head区間(1..985) md5 | `5aca5ff2d6bbd10f12136fb52f49ee30` ＝ローカルと一致（**不変**） |
| live ロジック区間(987..末尾) md5 | `b9f1ee4473a22f6dbc78ab8554c82960` ＝ローカルと一致（**不変**） |
| 反映時刻 | PUT から約45秒。**19:13:33 に新 blob へ切り替わり**（15秒間隔のポーリングで検出） |
| 差分規模 | `build_master.py` +73 / −0 ／ `index.html` は986行目（DATA行）のみ |

★**今回は DATA を意図的に変えた回。** `head` 区間と `logic` 区間の md5 がローカルと一致し、
かつ**両区間とも前回から不変**であることを確認している＝変わったのは DATA 行だけ。

### live 実物での確認 — 北米版行が本番の検索結果から消えている

live（`https://gamekataban.com/index.html`）を落とし、**その実物のソース行から
`run()` の依存部（`DATA` / `codeKey` / `fold` / `HAY` / `HAY4` / `RJ` / `CODEKEYS`）を
そのまま取り出して**、lane1〜4と重複畳み込みまで含めて再構成して実行した。
ローカルのコピーではなく live の実物で測っている（罠#141・罠#146 への答え）。

| 確認項目 | 結果 |
|---|---|
| live DATA 行数 | **37,850** |
| live Famicom 行数 | **1,252** |
| 除外対象5型番の残存 | **0件** |
| live で `row[7]` が空の行 | **0** |

| live での検索 | 結果 |
|---|---|
| `XEVIOUS` | **5件**。北米版 `NES-XV-USA` は消え、日本版 **`NXV-4900`（#5）** が残存 |
| `Final Fantasy II` | **11件**。北米SNES版 `SNS-F4-USA` は消え、日本版ファミコン FFII **`SQF-FY`（#5）** が残存 |
| `FINAL FANTASY 2` | 3件（PS / WonderSwan / PSP）。除外前は4件で**2位が北米版**だった |
| `ゼビウス` | 8件・**除外前と変化なし**（日本語では元から北米版が出ていなかった） |
| `duck hunt` | **0件**（未解決#26 のとおり。誤った答えが消えただけで、正しい答えは出ていない） |

★**実機のクリック確認はまだ行っていない。** 前回（`/` 置換）は Koha が本番実機で
Buyee ボタンの着地まで見ているが、今回はコード上の確認までである。

---

## md5 記法の揺れ — 以後は「末尾改行なし」を正典とする（2026-08-04）

**過去の記述は append-only の原則どおり両方そのまま残す。この節は運用の宣言のみ。**

### 何が混在していたか

同じ DATA 行に対して、2つの方式の md5 が「canonical」として記録されていた。

| 方式 | コマンド | 88cc9cb 時点の DATA 行 |
|---|---|---|
| **末尾改行あり** | `sed -n '986p' index.html \| md5 -q` | `381d91452d3e5701482bfb4f922d5733`（8191行・8221行・8482行で「5代目canonical」） |
| **末尾改行なし** | `sed -n '986p' index.html \| tr -d '\n' \| md5 -q` | `0dd350850ed89c074dfbe87cec146c97`（8578行で「不変」） |

★**この2つは同じ内容に対する別方式の値であって、矛盾ではない。**
`sed`/`awk` は行を取り出すときに末尾へ改行を1つ付けるので、その1バイトの有無で値が変わる。
8482行と8578行が別の値を挙げて両方「canonical」と書いているのは、**方式を明記していなかった**
ことによる。内容が食い違っていたわけではない。

実測（`88cc9cb:index.html` の986行目を2通りで計算）:

```
sed -n '986p' | md5 -q            → 381d91452d3e5701482bfb4f922d5733
sed -n '986p' | tr -d '\n' | md5 -q → 0dd350850ed89c074dfbe87cec146c97
```

### 決めたこと — 以後は「末尾改行なし」を正典とする

**理由**: `md5sum` をはじめ一般のハッシュ確認ツールは「対象そのもののバイト列」を数える使い方が
標準で、行を取り出す都合で付いた改行1バイトを混ぜないほうが素直である。
他人の環境・他のツールと突き合わせたときに再現しやすく、今後の混同を減らせる。

**正典の計算式**（どれも同値であることを実測で確認済み）:

```bash
sed -n '986p' index.html | tr -d '\n' | md5 -q
awk 'NR==986'  index.html | tr -d '\n' | md5 -q
grep -m1 '^const DATA = ' index.html | tr -d '\n' | md5 -q   # 行番号が動いても効く
sed -n '986p' index.html | tr -d '\n' | md5sum               # Linux 側もこの値
```

★**行番号で取らない形（`grep -m1 '^const DATA = '`）を併記しておく。** DATA 行の行番号は
実際に 967 → 978 → 986 と動いてきた（8195行・8483行）。番号を書き写す運用は、
`swap_data.py` が行番号を直書きしないのと同じ理由で壊れる。

### 運用

- **今後この節以降に書く DATA 行の md5 は、すべて末尾改行なし方式**とする。
  方式を書き添える必要は無い（既定がこれになるため）。例外的に改行あり方式を併記するときだけ明記する。
- **過去の記述は一切書き換えない。** 参照するときは「`381d9145…`（**末尾改行あり方式**）」の
  ように**方式を明記して引用する**こと。方式を書かずに数字だけ引くと、次に読む人が
  「値が変わっている＝DATA が変わった」と誤読する。
- **現時点の正典値**（2026-08-04・罠#16 除外後の DATA 行）:
  **`5a6a91e60c89a8c2f823637272664882`**（末尾改行なし）。
  参考: 末尾改行あり方式では `b8052e260aee071144de4a236cc3e402`。
- 系譜（すべて**末尾改行なし方式**に揃えて並べ直したもの。過去の節の数字とは方式が違うので、
  そちらを書き換えたのではなく、ここで**新たに計算し直した**値である）:

  | コミット | 内容 | DATA行 md5（末尾改行なし） |
  |---|---|---|
  | `88cc9cb` | Buyee検索キーの `/` 置換（DATA は不変） | `0dd350850ed89c074dfbe87cec146c97` |
  | `08c36ea` | Switch/PS5/Xbox One の `row[7]` を埋める（1,132行） | `1d9968fa63085cd4d62faa60695ee314` |
  | **`51962d4`** | **罠#16 の北米版5行を除外（37,855→37,850）** | **`5a6a91e60c89a8c2f823637272664882`** |

---

## 未解決#26 への対応 — 束1（title_ja がラテン文字だけの行）の英題を原文複製で埋めた（2026-08-05）

**append-only。9150行の未解決#26 も、その他の既存記述も一切書き換えていない。**
未解決#26 は**まだ閉じていない**（ダックハント自身は束3に残っている。後述）。

### 発端 — 未解決#26 が言っていたこと

9150行（2026-08-04・罠#16 除外の副次的発見）:

> 日本版ダックハント（M727853 / Famicom / `HVC-DH` / 「ダックハント」）が `title_en` を持たないため、
> **英語で `duck hunt` と打っても引けない。**（中略）直すなら `TITLE_EN` への手書き追加だが、
> **同型の行（英題が無く日本語名がカナだけの Famicom 行）が他に何行あるかを先に数えること。**
> 1行だけ手で足すと、次に読む人が「英題は埋まっている」と誤解する。

★**この「先に数えろ」に従って、まず数えた。** 手書き1行の追加はしていない。

### 調査（2026-08-05）— `_experiments/survey_missing_title_en_20260805.py`

出力: `_experiments/missing_title_en_survey.txt`（559行）。`data/master_final.csv` を読むだけ。

| 項目 | 実測 |
|---|---:|
| master_final.csv 全行 | 39,782 |
| DATA（KEEP機種35＋罠#16除外を通したあと） | 37,850 |
| `title_en` あり | 13,484（35.6%） |
| **`title_en` が空** | **24,366（64.4%）** |
| うち Famicom | 912 / 1,252行（72.8%） |

★**空欄のほうが多い。** 「空を全部埋める」は最初から成立しない規模で、だから
**どれを先に埋めるか**が問題になる。空欄24,366行を title_ja の字種で3つに割った:

| 束 | 定義 | 行数 | 性質 |
|---|---|---:|---|
| **束1** | ラテン文字のみ（日本語の文字が1字も無い） | **2,676** | 原文の複製で埋まる。調査ゼロ |
| **束2** | 音写系（漢字ひらがな無し・カナ含む）×レトロ機×他行に同じ title_ja | **216** | 手元の英題の転記。目視は同一性の確認だけ |
| **束3** | 同上だが手がかり無し | **2,243** | ここだけが本当の調べ物 |
| （束外） | 漢字・ひらがな混じり | 15,479 | 翻訳が要る。機械的な絞り込みでは前に進まない |

（音写系は全機種で 6,211行、Famicom では 389行。ダックハント／バーガータイム／
マイクタイソン・パンチアウト!! などがここ。機種別の全表は調査ファイルにある。）

★**Koha が挙げていた「title_ja_kana がある行を優先」案は実測で否定された。**
`title_ja_kana` は DATA 全体で 12,363行に付いているが、**そのうち 11,422行は既に英題がある行**で、
英題が空の行では 941行（音写系に絞ると 207行）しか無い。優先の物差しにすると母集団を取り違える。
`title_romaji` も空欄24,366行のうち24,160行に付いていて、ふるいとして何も落とさない。

★**版元での足切りもしない。** レトロ機×音写系 2,459行のうち `publisher_en` が日本語のまま
残っている行が343行・空が407行ある（スペランカー＝北米NESで名の通った作品がここに落ちる）。
英字の版元名で機械的に絞ると有名作を取り逃がす。使うなら並べ替えの重みとしてだけ。

### 実装 — 束1のみ。`build_master.py` で `title_en` に `title_ja` を原文複製

```python
title_en = r["title_en"]
if not title_en and is_latin_only(r["title_ja"]):   # 束1。空のときだけ、原文をそのまま写す
    title_en = r["title_ja"]
```

★**罠#141（外部からの推測値を DATA に書かない）には抵触しない。**
`title_ja` が既にラテン文字のみのため、`title_en` への転記は**外部からの推測値ではなく原文の複製**である。
MADB が同じレコード内に持っている文字列を、別の列へ1文字も変えずに写しているだけで、
外部サイトから値を持ってきていない。7112行（罠#146 の `AGMJ`）と同じ線。

★**だから翻訳も整形もしない。** 大文字小文字の直し・記号の正規化・綴りの統一、どれもやらない。
`Final fantasy Ⅶ remake` は `Final fantasy Ⅶ remake` のまま入る。**見栄えを整えた瞬間に
「原文の複製」ではなくなり、罠#141 の側へ落ちる。** 整えたくなったら別工程として立てること。
転記2,676行のうち **235行**が `Ⅱ` `×` `ö` 等の非ASCII文字を含むが、日本語の文字ではないので
判定を通り、原文のまま入っている。

★**上書きはしない。** 既に `title_en` がある行（Wikidata由来／`TITLE_EN` の手書き）は素通し。
効くのは「空 かつ ラテンのみ」だけ＝**英題が増える方向にしか動かない**。

★**`master_final.csv` は触らない。** 罠#16 の名指し除外・パブリッシャー名寄せ v2 と同じで、
アプリ向けの加工は `data_line.js` 側だけに効かせる。master は MADB の完全な記録として保つ。
（実測: 再生成後の `master_final.csv` は編集前と `cmp` でバイト単位一致）

★件数はビルドのたびに `英題を原文複製(束1) : 2,676 行` として印字される。MADB 更新で
数が動いたら気づける形にしてある ―― 「入れたつもりで効いていない」を黙らせない（罠#16 の印字と同じ思想）。

### `is_latin_only()` を `build_master.py` 側に置き、`_experiments/` 側が import する理由

Koha の指示は「調査スクリプトの判定をそのまま流用し、再実装しない」だった。定義を1つに
統一した点はそのとおりだが、**依存の向きは指示と逆にした。**

- `.gitignore` の17行目で **`/_experiments/` は丸ごと追跡対象外**。
- 追跡されている `build_master.py` が、追跡外の使い捨て実験フォルダを import すると、
  **そのフォルダを消した瞬間に本番ビルドが動かなくなる。** 依存は追跡されている側へ向ける。
- 関数の中身は1文字も変えていない。切り替え後に調査スクリプトを再実行し、
  **出力が `diff` でバイト単位まで完全同一**であることを確認済み。

★**2本書いて「同じはず」と信じる形にはしない。** 目視で揃えたコピーは必ずズレる（罠#141）。
件数が動いたら、それは調査とビルドが別物になった合図である。
なお `main()` 内の `_script()` とは別物 ―― あちらは title_ja の**先頭1文字**だけを見る統計用で、
粒度が違う。混ぜないこと（コードにも注記した）。

### card() 側で連動して変わること（3点）— 意図どおりだが、記録しておく

`title_en` が埋まると、その行のカードは表示だけでなく**リンクの出方も変わる**。

| # | 変わること | 判断 |
|---|---|---|
| 1 | `${en?...}` の英題フィールドが出る | 狙いどおり |
| 2 | `hideJa` が真になり「Japanese reading」の重複ブロックが消える | **1721行のコメントが元から想定していた挙動**（「row[0] が空の行では従来どおり出す＝見出しが英題側に無いため」）。en と jp が `codeKey` で一致する行だけ消える設計で、今回の2,676行はまさにそれ |
| 3 | **`sgComUrl` が空文字から実URLになり、駿河屋.com（英語・海外発送）のリンクが新たに出る** | 狙いの方向ではあるが、**この2,676行での当たり率は未測定**。リンクが増えること自体が今回の副作用として残る |
| （4） | eBay の `kwEn` は `enc(en||jp)` で、今回は `en === jp` のため**値は変わらない** | 変化なし（確認済み） |

★**3 は次に測るべきもの。** 駿河屋.com リンクは今まで「英題のある13,484行」にしか出ていなかった。
今回2,676行ぶん増えたが、その英題は MADB の日本語名欄に入っていたラテン文字列であって、
駿河屋.com の英語検索に当たる保証はない。**未測定のまま出ている**ことを忘れないこと。

### 未着手のまま残っているもの

- **束2（216行）** — 音写系×レトロ機で、**他行が既に持っている英題と title_ja が一致**する行。
  推測ではなく転記なので確度は高い。**未着手。**
  （例: `ソニック・ザ・ヘッジホッグ` → 他行の `Sonic the Hedgehog` / `バーチャファイター` → `Virtua Fighter`）
- **束3（2,243行）** — 手がかり無し。**未着手。** ここだけが本当の調べ物で、
  **ダックハント（M727853 / `HVC-DH`）自身はこの束にいる。**
- したがって **未解決#26 は閉じていない。** live で `duck hunt` と打っても依然 **0件**のまま。
  今回埋まったのは束1＝**元からラテン文字で書かれていた行**であって、
  カナ書きの有名タイトルには1行も触れていない。
- 前方一致による照合（5,167行に手がかりあり）は**埋める道具にしない**と決めた。
  `ドラゴンボールZ Kakarot` が `Dragon Ball Z: Budokai` を拾うような外れが混ざる。
  候補を出す道具までに留める。
- 束2・束3 に着手するなら、先に決めることが1つある ―― **英題の出典をどこに書くか。**
  MADB に無い値を入れる以上、外部の出典が要る。書きどころが決まっていないと、
  あとで「この英題はどこから来た」が追えなくなる。

### 編集前バックアップ

`build_master.py.20260805-bak-pre-latin-copy` / `index.html.20260805-bak-pre-latin-copy` /
`data/master_final.csv.20260805-bak-pre-latin-copy` / `data/data_line.js.20260805-bak-pre-latin-copy` /
`BIN_DIVER_NOTES.md.20260805-bak-pre-bundle1-note` /
`_experiments/survey_missing_title_en_20260805.py.bak-pre-shared-def`
（加えて `swap_data.py` が `index.html.20260805-0817.bak` を自動作成）

### ローカル確認（デプロイ前）

| 確認項目 | 想定 | 実測 |
|---|---:|---:|
| DATA 総行数 | 37,850 | **37,850**（不変） |
| 転記対象行数（ビルドの印字） | 2,676 | **2,676** |
| `title_en` が空→値ありになった行 | 2,676 | **2,676** |
| 既存 `title_en` を持つ行の変化 | 0 | **0**（13,484 → 16,160 ＝ 13,484+2,676） |
| `[1]`〜`[12]`（英題以外）が動いた行 | 0 | **0** |
| `master_final.csv` の差分 | なし | **`cmp` でバイト単位一致** |

★最終確認は中間物ではなく**貼り込み後の `index.html` から DATA を切り出して**測っている（罠#141・罠#146）。

★**`swap_data.py` の内訳表示は `英題が空→値あり 2,295 行` と出るが、取りこぼしではない。**
あの内訳は `lost`（Counter）を**タプル単位**で回すため、`[0]`〜`[6]` が同一の重複行がまとまる
（一意タプル 2,295 / 実行数 2,676）。同じ行の `[0]〜[6] 中身で照合` 側は **2,676** と出ており、
そちらが行数である。**方式の違う2つの数字**であって矛盾ではない（罠#141 と同じ構図なので明記しておく）。

---

## デプロイ（2026-08-05）— 束1: ラテン表記行 2,676件の英題

`gh` contents API で **`build_master.py` と `index.html` の2ファイル**。remote 未設定のため
通常の push は使えない（5997行の方式）。上げた順も残す（`build_master.py` → `index.html`。
生成側を先に上げて、生成物が後から追いつく形。前回 8930行と同じ）。

| 項目 | 値 |
|---|---|
| **drift 確認（上げる前）** | live blob `92395a9978a9f9f55b71580c54134eaf93d7d0dc` == `git hash-object index.html.20260805-bak-pre-latin-copy` == `git rev-parse HEAD:index.html` → **一致＝drift なし**。前回（罠#16 除外・`51962d4`）で上げた blob と鎖が繋がる |
| build_master.py の drift | live blob `ae5d4f6ca24b4c3cf71da249fa843b0e63b4fedd` == HEAD == 編集前バックアップ → **一致＝drift なし** |
| 上げる前の一致確認 | `git hash-object index.html` == `git rev-parse HEAD:index.html` = `a1031980124faeafa7bd49c7c9108b820d9b79f4` → working tree == HEAD |
| 編集前バックアップ | `index.html.20260805-bak-pre-latin-copy` / `build_master.py.20260805-bak-pre-latin-copy` / `data/master_final.csv.20260805-bak-pre-latin-copy` / `data/data_line.js.20260805-bak-pre-latin-copy` / `BIN_DIVER_NOTES.md.20260805-bak-pre-bundle1-note` |
| ローカルコミット | `45e95a7`（ノート）→ `d662471`（build_master.py + index.html） |
| 方式 | `base64 -i <file> \| tr -d '\n'` → `jq -n --rawfile c … '{message,content,sha,branch:"main"}'` → `gh api -X PUT repos/english-tadashi/bin-diver/contents/<file> --input payload.json` |
| payload | index.html **10,897,818 bytes** |
| **リモートコミット** | `316e2acfd9ebb0a3f29908f175160ac9ea9e5120`（build_master.py）→ **`242860ab0d3e15eb90d983a04c576abae0b38f4c`**（index.html） |
| **blob sha 4者一致（index.html）** | live 実物の `git hash-object` / ローカル HEAD / PUT の戻り値 / contents API＝すべて **`a1031980124faeafa7bd49c7c9108b820d9b79f4`** |
| blob sha 一致（build_master.py） | contents API == ローカル HEAD ＝ **`9df71fc3e61fc6b2ca7389733c4994ee8db83c13`** |
| **live 全体md5** | **`0181587bed7363f927bc538b63f58643`** ＝ローカルと一致 |
| **DATA行(986) md5** | **`922dc7602ab26b3d7b29086a54f06162`**（正典＝末尾改行なし方式）。**live 実物から再計算して一致** |
| live head区間(1..985) md5 | `5aca5ff2d6bbd10f12136fb52f49ee30` ＝ローカルと一致（**前回から不変**） |
| live ロジック区間(987..末尾) md5 | `b9f1ee4473a22f6dbc78ab8554c82960` ＝ローカルと一致（**前回から不変**） |
| 反映時刻 | PUT から約45秒。**08:23:54 に新 blob へ切り替わり**（15秒間隔のポーリングで検出） |
| 差分規模 | `build_master.py` +50 / −1 ／ `index.html` は986行目（DATA行）のみ |

★**今回も DATA を意図的に変えた回。** `head` 区間と `logic` 区間の md5 が**前回（罠#16 除外）の
記録値とそのまま一致**しており＝変わったのは DATA 行だけ。index.html のロジックには1バイトも触れていない。

### DATA行 md5 の系譜（すべて末尾改行なし方式）

| コミット | 内容 | DATA行 md5 |
|---|---|---|
| `88cc9cb` | Buyee検索キーの `/` 置換（DATA は不変） | `0dd350850ed89c074dfbe87cec146c97` |
| `08c36ea` | Switch/PS5/Xbox One の `row[7]` を埋める（1,132行） | `1d9968fa63085cd4d62faa60695ee314` |
| `51962d4` | 罠#16 の北米版5行を除外（37,855→37,850） | `5a6a91e60c89a8c2f823637272664882` |
| **`d662471`** | **束1: ラテン表記行 2,676件に英題を原文複製（行数は不変）** | **`922dc7602ab26b3d7b29086a54f06162`** |

### live 実物での確認 — Winning Post 2（SFC）のカードに英題が出る

live（`https://gamekataban.com/index.html`）を落とし、**その実物のソース行から
`enc` / `codeKey` / `SG_COM` / `sgCom` / `esc` の定義と、`card()` の
`hasEn` / `kwEn` / `sgComUrl` / `jaLatin` / `hideJa` の式をそのまま切り出して**評価した。
ローカルのコピーではなく live のバイト列を見ている（罠#141・罠#146）。
対象行は**添字ではなく型番 `SHVC-P-AWPJ` で引いている**（罠#21: 行番号・添字の直書きはしない）。

**選定理由**: この行は 7037行の駿河屋 `-P-` 落とし測定で「回復した15行」の1つとして
`8417 Winning Post 2` と名指しで記録されており、過去に実地で見ている行だから。
（2690行のコントラスト確認で使った Winning Post は **3DO 版**で、そちらは元から英題があり今回の対象外。）

| 確認項目 | 結果 |
|---|---|
| live の対象行 `row[0..6]` | `["Winning Post 2","Winning Post 2","Super Famicom","SHVC-P-AWPJ","1995","光栄","Koei Tecmo"]` |
| `row[0]`（英題） | **`Winning Post 2`** ＝デプロイ前は空だった行に入っている |
| `hasEn` | **true** → 英題フィールドが描画される |
| 描画される英題ブロック | `<div class="en-title">Winning Post 2</div>` |
| `hideJa` | **true** → 同一文字列の「Japanese reading」重複ブロックを隠す（1721行の設計どおり） |
| `kwEn`（eBay） | `Winning%20Post%202` ＝ `enc(en||jp)` で `en === jp` のため**デプロイ前と同値** |
| `sgComUrl`（駿河屋.com） | `https://www.suruga-ya.com/en/products?category=2&keyword=Winning+Post+2+Super+Famicom` ＝**デプロイ前は `''` でリンクごと出ていなかった** |
| live DATA 行数 | **37,850**（不変） |
| live `title_en` あり | **16,160**（13,484 + 2,676） |
| live `en === jp` の行 | 3,322（うち今回の転記が2,676。残り646は元から一致していた行） |
| live Famicom 行数 | **1,252**（不変） |

### live 実物での確認 — 未解決#26 はまだ閉じていない

| 確認項目 | 結果 |
|---|---|
| live のダックハント行（`HVC-DH`） | `["","ダックハント","Famicom","HVC-DH","1984"]` |
| `title_en` | **空のまま** |

★**狙いどおりであって、取りこぼしではない。** ダックハントは束3（手がかり無し・2,243行）にいる。
今回埋めたのは束1＝**元からラテン文字で書かれていた行**だけで、カナ書きの有名タイトルには
1行も触れていない。`duck hunt` の英語検索は依然 0件のままである。

★**実機のクリック確認はまだ行っていない。** 今回もコード上の確認まで。
とくに**新たに出るようになった駿河屋.com リンク（2,676行ぶん）は当たり率が未測定**である。

---

## title_ja_kana の誤連結17行を名指しで直した（2026-08-05）― 罠#148

**append-only。既存記述は1行も書き換えていない。**

### 発端 — 実機で見つかった「変な検索語」

Koha が本番のカードから駿河屋.jp (title) を踏み、`search_word` がこうなっているのを見つけた:

```
ウイニングポスト2#Winning Post for ゲームボーイアドバンス
```

該当は `Winning Post 2 プログラム'96`（SLPS91082）と `Winning Post 2 ファイナル'97`（SLPS 01020）。
**PS1 の行なのに、GBA のタイトルが `#` で繋がって検索語になっていた。**

★**実機で踏んで見つかった。** 数字の点検では出てこない種類の壊れ方で、
リンクの着地を人が見るまで誰も気づいていなかった。

### 追跡 — `#` はどこで作られたか（下流から上流へ）

`_experiments/survey_suruga_title_url_bug_20260805.py` / 出力 `suruga_title_url_bug_investigation.txt`。

```
Suruga-ya.jp (title) の href = sgAf(k.kwKana)                    index.html:1495,1360
kwKana = enc(dropCatalogParen(r[8] || jp || en))                 index.html:1707
r[8]   = title_ja_kana ← ja_titles.get(madb_id,"") ← load_ja_titles() は素通し
```

★**`#` は index.html でも build_master.py でも作られていない。結合処理はどこにも無い。**
`data/wikidata_ja.json` に最初からその文字列で入っていた。

### `#` の正体 — 衝突を捨てずに繋いだ区切り文字

`wikidata_ja.json`（28,969件）で `#` を含むのは **29件**。2種類に割れる。

| 型 | 中身 | JSON件数 | DATA行数 |
|---|---|---:|---:|
| **A** | タイトル自体に `#` がある（`Memories Off #5 とぎれたフィルム` 等） | 12 | **10** |
| **B** | 別々のタイトルが `#` で繋がれている | 17 | **17** |

（JSON 29件と DATA 27行の差2は C番号 `C33799` / `C33800` ＝ master に当たらない死票。
4623行の「ja の C側は死票」と同じ現象。）

★**A は正常。直す対象ではない。** `#5` はナンバリングの一部で、`#` の直後が数字であることで
B と機械的に見分けられる。

★B は「同じ madb_id に複数の日本語ラベルがぶら下がったときの区切り」。裏付けは
`build_master.py` の**非対称**にある:

```
load_english_titles(): madb_id ごとに set を作り len(v)==1 だけ残す＝衝突は捨てる
                       （起動時に「英題テーブル : 16,393 件（衝突により除外 90 件）」と印字）
load_ja_titles()     : すでに1対1に潰された dict を受け取り、そのまま返す
```

ja 側は衝突除去が**生成スクリプト側で済んでいる前提**で素通しになっている。ところが
4623行（罠#114）が記録するとおり「ja の整形（dict化＋衝突除去）スクリプトは repo に無い」。
**その失われたスクリプトが、衝突除去のかわりに `#` 連結をしていた**というのが実測結果。
（`#` を選んだ理由まではスクリプトが無いので辿れない。SPARQL の GROUP_CONCAT に近い形だが、
現物が無い以上そこは推測であり、推測として書いておく。）

### 「Winning Post for ゲームボーイアドバンス」の出どころ — 原因は2段重ね

5行とも Winning Post **2**（PS1×4 / Sega Saturn×1）で、GBA の行は1つも無い。
そして**本物の GBA 行（M745646 / `ウイニングポスト for ゲームボーイアドバンス`）の
`title_ja_kana` は空**だった。

- **(1) 上流（Wikidata）**: 別作品の項目が同じ MADB ID を P7886 に持っている。
  `build_master.py` の docstring 32〜34行が warning している型そのもの
  （「Shining Wisdom の項目にシーバス・フィッシングの MADB ID が貼られている」）。
- **(2) こちら側（整形スクリプト）**: その衝突を捨てずに `#` で連結した。

★**(1) だけなら英題側と同じ「衝突は捨てる」で無害化できた。
(2) が、捨てるべき値を1つの文字列に閉じ込めて生き残らせている。**

### 17行の判定 — どちらの側がその行の作品か

`_experiments/verify_kana_split_20260805.py` / 出力 `kana_split_verification.txt`。
17行を1行ずつ、3つの規則で照合した。

| 規則 | 内容 |
|---|---|
| **A（尾）** | 片側がもう片側の先頭に含まれるとき、はみ出した尾が行の題に在るか |
| **B（機種）** | 側が機種名を名乗るなら行の機種と一致必須。しなければ**その側を落とす** |
| **C（番号）** | 側に在る作品番号が行の題に在るか（`2` の有無が WP と WP2 を分ける） |

判定結果 ―― **採る側は値ごとに違う（前2つ・後2つ）**:

| 値 | 採る側 | 修正後 | 行数 |
|---|---|---|---:|
| `ウイニングポスト2#Winning Post for ゲームボーイアドバンス` | 前 | `ウイニングポスト2` | 5 |
| `モンスターハンター ポータブル#…2nd G` | **後** | `モンスターハンター ポータブル 2nd G` | 9 |
| `クイズ殿様の野望#クイズ殿様の野望（第1作）` | 前 | `クイズ殿様の野望` | 2 |
| `なつきクライシス#なつきクライシスバトル` | **後** | `なつきクライシスバトル` | 1 |

17行すべて判定でき、「決められない」は0。

### ★自己訂正1 — モンハン9行「誤ヒット0件」は値が正しいことの証明ではなかった

前段の調査で「`2nd G` の検索誤ヒットは0件」と測り、そのまま報告した。**測定自体は正しい。**
連結値の中に正しい側（2nd G）が含まれているので、検索では正しく引ける。

**しかし title_ja_kana の値としては誤りだった。** 中身が「無印 + 2nd G」の2作品ぶんで、
前半（無印）はこの9行の作品ではない。9行すべてが 2nd G（title_en は9行とも
`Monster Hunter Freedom Unite` ＝2nd G の海外版名）。

★**検索が無事なのは偶然で、値が正しいことの証明にはならない。**
現に店リンクは `モンスターハンター ポータブル#モンスターハンター ポータブル 2nd G` という
どの店にも無い文字列で飛んでいた。**「実害が出ていない」を「値が正しい」と読み替えない。**

### ★自己訂正2 — 語の突き合わせで Winning Post 5行を一度逆に判定した

最初は「片側にしかない語」で判定するコードを書き、5行を**後（GBA側）**と出した。
原因は、連結値が**片側カナ・片側ラテン**の組み合わせだったこと。
`ウイニングポスト2` と `Winning Post for …` を語で比べると、同じ語（Winning / Post）が
字種違いのせいで「後にしかない語」に見え、それが決め手として効いてしまった。

→ **字種をまたぐ語の一致は当てにしない。** 規則A/B/C に組み替えたところ 5/5 で前になり、
規則B（機種）と規則C（番号）が独立に同じ答えを出した。罠#90「完全一致で持て」の親戚で、
**字形・語形の近さで同一性を判定すると、字種が変わっただけで壊れる。**

### 罠#148. 「実害が出ていない」は「値が正しい」ではない

モンハン9行がこれ。検索の誤ヒットは0件で、外から見れば何も起きていない。
だが値は2作品ぶんの連結で、店リンクはどの店にも無い文字列を送っていた。
**害が観測されないのは、たまたま正しい側が文字列に含まれていたからにすぎない。**
- 対策: 壊れた値を見つけたら、**実害の有無とは別に値そのものの正しさを確かめる。**
  「実害0件」は直さない理由にならない。
- 罠#141（目視値≠実装生成値）の親戚。あちらは「測る対象を間違える」、
  こちらは「測った結果の意味を広げすぎる」。

### 実装 — `KANA_SPLIT_FIX`（madb_id → 正しい値・17件）

`build_master.py` に `PRODUCT_CODE_TYPO` と同じ形式の名指し表を追加し、`load_ja_titles()` 内で適用。

★**捨てるのではなく片側を採った。** 捨てると `title_ja_kana` が空になり検索語が `title_ja` に
落ちる。17行のうち**7行は title_ja がラテン表記**（`MONSTER HUNTER PORTABLE 2nd G` 等）で、
カタログ店はカナで棚を持つため、そこでカナを失うと噛み合わなくなる。

★**罠#141 に抵触しない。** 採ったのは**元の JSON に在った2つの文字列の一方**であって、
外から持ってきた値ではない。表記も一切いじっていない（束1の原文複製と同じ線）。
モンハン9行は箱の題が「ハンターポータブル」と詰まる行もあるが、`title_ja_kana` は箱の印字ではなく
カタログ店の正規形なので、元 JSON の後側をそのまま採り**表記を寄せていない**。

★**条件付きで当てている。** 適用するのは「今まさに `#` で連結されている値」だけ。
JSON を作り直して衝突除去が入れば、この表は**自動的に効かなくなる**。
名指し表を置きっぱなしにするときの最大の危険は「上流が直ったあとも古い判断を焼き付け続ける」
ことなので、そこを塞いだ。見つからない/既に連結でない場合はビルド時に `★` 付きで印字する
（罠#16 の名指し除外と同じ思想）。毎回 `カナ連結の名指し修正   : 17/17 件` が出る。

★**この修正は `master_final.csv` にも及ぶ**（罠#16 や束1の「master は素通し」とは扱いが違う）。
`title_ja_kana` はもともと MADB ではなく Wikidata 由来の外部値で、今回は表示上の加工ではなく
**誤った値の訂正**だから。だから `load_ja_titles()` に置いた。

### 影響範囲 — (title) リンクだけではなかった

`r[8]` を読むのは4か所: Suruga-ya.jp (title) / Book-Off / Hard-Off / **検索索引 HAY**。
壊れた行では店リンク3つが同じ検索語で外れ、さらにその行が別作品の名前で検索に引っかかっていた。

検索索引への実害は `index.html` の DATA と `RN_VAL`〜`fold` 定義区間・`const HAY` の行を
**そのまま切り出して実行**して測った（`_experiments/measure_hay_contamination_20260805.js`）。
判定は「r[8] を索引から外したら消える行」＝連結された余分な題だけを頼りに出てきた行。

| 検索語 | 修正前 | 修正後 |
|---|---|---|
| `ゲームボーイアドバンス` | 8件中 **5件が誤ヒット**（PS1×4/Saturn×1 の Winning Post 2） | 3件・**誤ヒット0** |
| `第1作` | 3件中 2件が誤ヒット | 1件・**誤ヒット0** |
| `2nd G` | 12件・誤ヒット0（前述のとおり偶然） | 12件・誤ヒット0 |
| `なつきクライシスバトル` | 1件・誤ヒット0 | 1件・誤ヒット0 |

★`ゲームボーイアドバンス` の誤ヒットは質が悪い。DATA の機種欄(`r[2]`)は英語なので、
**日本語の機種名で検索が当たる経路は `r[8]` しかない**＝汚染がそのまま検索結果になっていた。

★**『壊れた値=17行』と『検索の実害=5行』は別の数字。** 混ぜないこと。

### ローカル確認（デプロイ前）

| 確認項目 | 想定 | 実測 |
|---|---:|---:|
| DATA 総行数 | 37,850 | **37,850**（不変） |
| 名指し修正が当たった件数 | 17 | **17/17**（ビルドの印字） |
| `row[8]` が変わった行 | 17 | **17** |
| `row[8]` 以外が動いた行 | 0 | **0** |
| DATA に残る誤連結値 | 0 | **0** |
| 型A（`Memories Off #5` 系）の行 | 10・不変 | **10・不変** |
| `master_final.csv` の差分 | 17行・`title_ja_kana` 列のみ | **17行 / 変わった列は `title_ja_kana` のみ** |
| 束1の英題（前回デプロイ分） | 16,160 | **16,160**（維持） |

`swap_data.py` は `[0]〜[6]` の差分**0行**で通過（`row[8]` はガードの範囲外）。

### 未測定のまま残ること

★**直った17行の検索語が、実際に店で当たるかは測っていない。**
`ウイニングポスト2` / `モンスターハンター ポータブル 2nd G` / `クイズ殿様の野望` /
`なつきクライシスバトル` を駿河屋・ブックオフ・ハードオフに投げた結果は見ていない。
**値が正しくなることと、店で当たることは別である。**
とくに M746728（Mega Drive）は箱の題が `カプコンのクイズ殿様の野望` で、採った値は
頭の「カプコンの」が落ちた短い形になる。ここは実地で確かめる価値がある。

★型B以外の連結（`#` も機種名も含まない繋ぎ方）が残っている可能性は消せていない。
2つの網（`#` / 自分の機種でない機種名）を重ねて17行としたが、網に掛からない形は残りうる。

### 編集前バックアップ

`build_master.py.20260805-bak-pre-kana-fix` / `index.html.20260805-bak-pre-kana-fix` /
`data/master_final.csv.20260805-bak-pre-kana-fix` / `data/data_line.js.20260805-bak-pre-kana-fix` /
`BIN_DIVER_NOTES.md.20260805-bak-pre-kana-note`
（加えて `swap_data.py` が `index.html.20260805-0901.bak` を自動作成）

---

## デプロイ（2026-08-05・2回目）— 罠#148 の誤連結17行

`gh` contents API で **`build_master.py` と `index.html` の2ファイル**。remote 未設定のため
通常の push は使えない（5997行の方式）。上げた順は前回と同じ（生成側 → 生成物）。

| 項目 | 値 |
|---|---|
| **drift 確認（上げる前）** | live blob `a1031980124faeafa7bd49c7c9108b820d9b79f4` == `git hash-object index.html.20260805-bak-pre-kana-fix` == `git rev-parse HEAD:index.html` → **一致＝drift なし**。同日1回目（束1・`d662471`）で上げた blob と鎖が繋がる |
| build_master.py の drift | live blob `9df71fc3e61fc6b2ca7389733c4994ee8db83c13` == HEAD == 編集前バックアップ → **一致＝drift なし** |
| 上げる前の一致確認 | `git hash-object index.html` == `git rev-parse HEAD:index.html` = `f8a389000e81ff5fc71b14c9ae6c0598f4446f52` → working tree == HEAD |
| 編集前バックアップ | `index.html.20260805-bak-pre-kana-fix` / `build_master.py.20260805-bak-pre-kana-fix` / `data/master_final.csv.20260805-bak-pre-kana-fix` / `data/data_line.js.20260805-bak-pre-kana-fix` / `BIN_DIVER_NOTES.md.20260805-bak-pre-kana-note` |
| ローカルコミット | `3d6a3ce`（ノート）→ `f60654b`（build_master.py + index.html） |
| 方式 | `base64 -i <file> \| tr -d '\n'` → `jq -n --rawfile c … '{message,content,sha,branch:"main"}'` → `gh api -X PUT repos/english-tadashi/bin-diver/contents/<file> --input payload.json` |
| payload | index.html **10,896,826 bytes** |
| **リモートコミット** | `831c5ddc08c4b1e2de66374bb5609d9be9b5925f`（build_master.py）→ **`6423c2bf849f4692ab9f520d3074f02fca0cd881`**（index.html） |
| **blob sha 4者一致（index.html）** | live 実物の `git hash-object` / ローカル HEAD / PUT の戻り値 / contents API＝すべて **`f8a389000e81ff5fc71b14c9ae6c0598f4446f52`** |
| blob sha 一致（build_master.py） | contents API == ローカル HEAD ＝ **`6f8c6634a1c9bdd975073023c71e4598f8302c28`** |
| **live 全体md5** | **`9ab517ea853dde387ebec9b0e7c52736`** ＝ローカルと一致 |
| **DATA行(986) md5** | **`9983168149001ff60420c255407cc88f`**（正典＝末尾改行なし方式）。**live 実物から再計算して一致** |
| live head区間(1..985) md5 | `5aca5ff2d6bbd10f12136fb52f49ee30` ＝ローカルと一致（**前回から不変**） |
| live ロジック区間(987..末尾) md5 | `b9f1ee4473a22f6dbc78ab8554c82960` ＝ローカルと一致（**前回から不変**） |
| 反映時刻 | PUT から約45秒。**09:07:33 に新 blob へ切り替わり**（15秒間隔のポーリングで検出） |
| 差分規模 | `build_master.py` +92 / −1 ／ `index.html` は986行目（DATA行）のみ |

★`head` 区間と `logic` 区間の md5 は**同日1回目（束1）の記録値とそのまま一致**。
＝2回のデプロイを通じて index.html のロジックには1バイトも触れていない。変わったのは DATA 行だけ。

### DATA行 md5 の系譜（すべて末尾改行なし方式）

| コミット | 内容 | DATA行 md5 |
|---|---|---|
| `88cc9cb` | Buyee検索キーの `/` 置換（DATA は不変） | `0dd350850ed89c074dfbe87cec146c97` |
| `08c36ea` | Switch/PS5/Xbox One の `row[7]` を埋める（1,132行） | `1d9968fa63085cd4d62faa60695ee314` |
| `51962d4` | 罠#16 の北米版5行を除外（37,855→37,850） | `5a6a91e60c89a8c2f823637272664882` |
| `d662471` | 束1: ラテン表記行 2,676件に英題を原文複製 | `922dc7602ab26b3d7b29086a54f06162` |
| **`f60654b`** | **罠#148: 誤連結17行の title_ja_kana を修正** | **`9983168149001ff60420c255407cc88f`** |

### live 実物での確認 — Winning Post 2（SLPS91082）の検索語

live を落とし、**その実物のソース行から `enc` / `SG_AF` / `SG_UTM` / `sgAf` / `CAT_PAREN_OK` /
`dropCatalogParen` の定義と `kwKana` の式をそのまま切り出して**実行した。
対象行は**添字ではなく型番 `SLPS91082` で引いている**（罠#21）。

| 確認項目 | 結果 |
|---|---|
| `title_ja` (r[1]) | `Winning Post 2 プログラム'96` |
| `title_en` (r[0]) | `Winning Post 2` |
| **`title_ja_kana` (r[8])** | **`ウイニングポスト2`** ＝デプロイ前は `ウイニングポスト2#Winning Post for ゲームボーイアドバンス` |
| **Suruga-ya.jp (title) の `search_word`** | **`ウイニングポスト2`** |
| `#` を含むか | **false** |
| 期待値と一致 | **true** |
| live DATA 行数 | **37,850**（不変） |
| live に残る誤連結値（型B） | **0** |
| live の型A（`Memories Off #5` 系） | **10**（残っているのが正しい） |
| live `title_en` あり | **16,160**（束1の2,676行は維持） |

### live 実物での確認 — 検索索引の汚染が消えている

live の DATA と `RN_VAL`〜`fold` 定義区間・`const HAY` の行を切り出して再測定した。

| 検索語 | 修正前 | **live（修正後）** |
|---|---|---|
| `ゲームボーイアドバンス` | 8件・誤ヒット**5** | **3件・誤ヒット0** |
| `第1作` | 3件・誤ヒット2 | **1件・誤ヒット0** |
| `2nd G` | 12件・誤ヒット0 | 12件・誤ヒット0 |
| `なつきクライシスバトル` | 1件・誤ヒット0 | 1件・誤ヒット0 |

★**実機のクリック確認はまだ行っていない。** 今回もコード上の確認まで。
**直った17行の検索語が実際に店で当たるかは未測定**である（前節の「未測定のまま残ること」参照）。
値が正しくなったことと、店で当たることは別。とくに M746728（Mega Drive・箱の題は
`カプコンのクイズ殿様の野望`）は採った値が短い形になっており、実地で確かめる価値がある。


---

## 未解決#26 への対応（続き）— 束2（216行）を調べ、182行の英題を別行から名指しで転記した（2026-08-05）

**append-only。9150行の未解決#26 も、9280行以降の束1の記述も、その他の既存記述も一切書き換えていない。**
**未解決#26 はこれでも閉じていない。**（ダックハントは束3に残ったまま。後述）

### 発端 — 束1のあとに残っていた宿題

9395行（束1の記録）に「**束2（216行）… 未着手**」と書いた、その続きである。
束2の定義は同じ調査（`_experiments/survey_missing_title_en_20260805.py`）が出したもので、

> `title_en` が空 × 音写系（漢字ひらがな無し・カナ含む）× レトロ機 ×
> **他行に同じ `title_ja` がある** = 216行

★**まず216行を再現できることを確かめてから着手した。** 調査スクリプトの
`norm_key()` / `is_transliterated()` / `RETRO_PLATFORMS` を **import して借り**、
母数 39,782 → DATA 37,850 → 空 24,366 → 音写系 6,211 → レトロ機 2,459 → **216** を再度通した。
判定を書き直していたら「216のはずが217」が静かに起きる（罠#141 と同じ構図）。

### ★照合はローマ字ではない ―― 完全一致キーである

着手前の依頼は「title_ja をローマ字化して title_en と突き合わせる」だったが、**その方式は採っていない。**

- `ダックハント` → `dakkuhanto` は `Duck Hunt` に**当たらない**。正しい対応ほど綴りが遠い。
- 逆に、当たってはいけない綴りに当たる。ローマ字は音を写すが、英題は音写ではない。
- 実測: 182行のうち **ローマ字と英題の類似度が 0.45 未満の行が72行**ある。
  `アイスクライマー`(0.27) → `Ice Climber`、`サッカー`(0.17) → `Soccer` のような
  **正しい組がこの帯に大量に落ちる**。ローマ字を判定に使ったら、これらを全部落としていた。

実際に使ったのは **`title_ja` の正規化キーの完全一致**である。
`norm_key()` で空白・中黒・長音・記号を落として小文字化した鍵が一致する行が DATA 内にあり、
そちらに `title_en` が入っているとき、その値をそのまま借りる。
例: `ソニック・ザ・ヘッジホッグ`(Game Gear・英題なし) ← 同鍵の Nintendo 3DS 行が持つ `Sonic the Hedgehog`。

★**ローマ字は捨てていない。目視の手がかりとして全行に併記した**（`build_master.py` の
`romaji()` を借りている）。**判定には使わず、人が見る材料としてだけ使う**という切り分けである。

### ★前方一致は1件も採用していない（構造的にリスクが混入しない）

9406行で「前方一致は埋める道具にしない。`ドラゴンボールZ Kakarot` が
`Dragon Ball Z: Budokai` を拾うような外れが混ざる」と決めた。**その決定を守っている。**

★**守ったというより、束2の定義上そもそも混入しない。** 束2は「鍵の**完全一致**がある行」
として定義されているので、前方一致でしか手がかりが付かない行はこの216行に入っていない。
前方一致で手がかりが付く行は DATA 全体で 5,167行あるが、**それは別の母集団**である。

★ただし監査として「**もし前方一致で埋めていたら別の英題を掴んでいた行**」を数えた ―― **24行**。
これは実害ではなく、前方一致の危うさが実測で見える形として残す:

| 行 | 完全一致で採る値（採用） | 前方一致だと（外れ） |
|---|---|---|
| `グラディウス2`(Famicom) | `Nemesis 2` | `Gradius` |
| `ロックマンワールド2`(Game Boy) | `Mega Man II` | `Mega Man: Dr. Wily's Revenge` |
| `バイオハザード 2`(Nintendo 64) | `Resident Evil 2` | `Resident Evil` |
| `スーパーマリオブラザーズ2`(Famicom) | `Super Mario Bros.: The Lost Levels` | `Super Mario Bros.` |
| `ロックマンエグゼ4 トーナメント ブルームーン` | `Mega Man Battle Network 4` | `Mega Man` |

（全24行は `_experiments/bucket2_candidates_20260805.txt` の 2-b に並べてある）

### 確認用リスト — `_experiments/bucket2_candidates_20260805.txt`（2,044行）

216行を**1行ずつ**、採る英題・借用元の madb_id・ローマ字読みつきで並べたもの。
生成は `_experiments/gen_bucket2_candidates_20260805.py`（読み取りのみ）。
機械的に付けられる印を3つ置き、**印のある行は実装から外した**。

| 印 | 意味 | 行数 | 例 |
|---|---|---:|---|
| ★ | 同じ鍵に**別々の英題**がぶら下がる | 3 | `ロックマン` に `Mega Man` と `Rockman Complete Works` |
| ◆ | 和題と英題で**通し番号が合わない** | 9 | `アドベンチャーズ オブ ロロ` に `Adventures of Lolo 2` |
| ・ | **ローマ字が遠く、版元も借用元と違う** | 25 | `ウルトラマン`(Game Boy) に `Ultraman: Towards the Future`(SFC) |

★**印は重なる。3+9+25=37 だが実体は34行。**（`スペースインベーダー`(WS)=★＋・、
`アドベンチャーズ オブ ロロ`=◆＋・、`レインボーアイランド`=◆＋・）
**216 − 34 = 182 行**が印なし。着手時の見積もりは「213 − 9 − 25 = 179行」だったが、
これは重なりを二重に引いた数で、**正しくは182行**。3行の重なりを実測で名指ししてから直した。

★**◆（通し番号）が最も外れを掴む印だった。** ローマ字の近さでは出てこない ――
`アドベンチャーズ オブ ロロ` と `Adventures of Lolo 2` は綴りがむしろ近く、目でも滑る。
一方で `スーパーマリオブラザーズ2` → `Super Mario Bros.: The Lost Levels` は
**番号が消えるのが正しい**（北米での正式な英題）。だから印は「間違い」ではなく
「**数が合わない＝人が見ろ**」としか言っていない。

★**・（版元の食い違い）は弱い印**。`publisher` 列は生の文字列で、著作権表記がそのまま
入っていたり（`©1992 KONAMI`）、社名の表記ゆれ（`光栄`/`KOEI`）や VC 再発の配信元
（`D4エンタープライズ`）が混ざる。**この印だけでは落とせない**ので、
「ローマ字が遠い」と重ねて初めて使った。

### 実装 — `build_master.py` の `TRANSLITERATED_TITLE_EN`（182件・madb_id 名指し）

`PRODUCT_CODE_TYPO` / `KANA_SPLIT_FIX` と同じ**行単位の名指しテーブル**。各行に借用元を書いてある。

```python
"M726688": "Batman Returns",   # バットマン・リターンズ / Game Gear  ← M726578（バットマン・リターンズ / Mega Drive）
```

★**罠#141（外部からの推測値を DATA に書かない）には抵触しない。**
Wikipedia や販売サイトを見て英題を決めたのではなく、**同じ DATA の中に既に在る値**を
別の行へ写しているだけで、外から持ってきた値は1つも無い。束1（ラテン表記の原文複製）と同じ線で、
**借用元も DATA 内に居るので出所が追える** ―― 各行のコメントに madb_id を書いたのはそのため。
だから翻訳も整形もしない。借用元の綴りをそのまま入れる。

★**なぜ鍵一致で一括処理せず、madb_id で名指しするのか。** 鍵が一致しても別の作品である
ことがあり、機械には見分けられないため。一括にすると上の34行が黙って混ざる。

★**上書きはしない。** `title_en` が空の行にだけ入る＝英題が増える方向にしか動かない。
★**`master_final.csv` は触らない。** 束1・罠#16 と同じで、加工は `data_line.js` 側だけに効かせる。
★**件数はビルドのたびに `英題を別行から転記(束2): 182/182 行` として印字される。**
既に英題が入っていた行・DATA に届かなかった madb_id は ★ 付きで名指し印字する（罠#16 の印字と同じ思想）。

### ローカル確認（デプロイ前）

最終確認は中間物ではなく**貼り込み後の `index.html` から DATA を切り出して**測っている（罠#141・罠#146）。

| 確認項目 | 想定 | 実測 |
|---|---:|---:|
| DATA 総行数 | 37,850 | **37,850**（不変） |
| 転記件数（ビルドの印字） | 182 | **182/182**（★警告なし） |
| `title_en` あり | 16,160 → 16,342 | **16,342** |
| 変化した行 | 182 | **182** |
| 動いた列 | `[0]` のみ | **`[0]` のみ** |
| `[0]` が 空→値あり | 182 | **182** |
| `[0]` が 値あり→別値 | 0 | **0** |
| **既存16,160行（束1含む）のうち変化した行** | 0 | **0** |
| 変化した madb_id が表と一致 | 一致 | **完全一致・値のずれ0件** |
| `master_final.csv` の差分 | なし | **`cmp` でバイト単位一致** |

★`swap_data.py` の表示は `英題が空→値あり 180 行` と出るが**壊れていない**。
あれは `[0]〜[6]` の**重複しないタプル数**を数えており、`フォトジェニック`(PS1) と
`ポリスノーツ`(3DO) が各2行、同一の `[0]〜[6]` を持つため 182行が180タプルに畳まれる。
（行数合計は182で一致することを別途確認済み。**次に読む人がここで詰まらないよう書いておく**）

### 未着手のまま残っているもの

★**除外した34行は未着手のまま残っている。** 内訳は上表のとおり ―― ★3 / ◆9 / ・25
（重なり3行を含むため実体34行）。**この34行には1件も手を入れていない。**
機械では決まらないので人が1件ずつ見るしかなく、見どころは
`_experiments/bucket2_candidates_20260805.txt` の 2-a / 2-d / 2-e に並べてある。

★**束3（2,243行）も未着手。** ここだけが本当の調べ物。

★**ダックハント（M727853 / Famicom / `HVC-DH`）は束2にも入っていない。**
実測: 鍵 `ダックハント` を持つ**英題付きの行が DATA に1行も無い**ため、束3に残る。
**したがって未解決#26 は閉じていない。live で `duck hunt` と打っても依然 0件。**
今回埋まった182行は「他行が既に英題を持っていた行」であって、
**手がかりの無いカナ書きタイトルには1行も触れていない。**

★駿河屋.com リンクの当たり率は今回も未測定。`title_en` が埋まると `sgComUrl` が実URLになる
（9370行の表の3番）。182行ぶん増えたが、**当たるかは測っていない。**

### 編集前バックアップ

`build_master.py.20260805-bak-pre-bundle2` / `index.html.20260805-bak-pre-bundle2` /
`data/master_final.csv.20260805-bak-pre-bundle2` / `data/data_line.js.20260805-bak-pre-bundle2` /
`BIN_DIVER_NOTES.md.20260805-bak-pre-bundle2-note`
（加えて `swap_data.py` が `index.html.20260805-0931.bak` を自動作成）

---

## デプロイ（2026-08-05・3回目）— 束2の182行

`gh` contents API で **`build_master.py` と `index.html` の2ファイル**。remote 未設定のため
通常の push は使えない（5997行の方式）。上げた順は前回と同じ（生成側 → 生成物）。

| 項目 | 値 |
|---|---|
| **drift 確認（上げる前）** | live blob `f8a389000e81ff5fc71b14c9ae6c0598f4446f52` == `git hash-object index.html.20260805-bak-pre-bundle2` == `git rev-parse HEAD:index.html` → **一致＝drift なし**。同日2回目（罠#148・`f60654b`）で上げた blob と鎖が繋がる |
| build_master.py の drift | live blob `6f8c6634a1c9bdd975073023c71e4598f8302c28` == HEAD == 編集前バックアップ → **一致＝drift なし** |
| 上げる前の一致確認 | `git hash-object index.html` == `git rev-parse HEAD:index.html` = `c98113c7120a522fb427fffd2c23a5c2b5309e94` → working tree == HEAD |
| 編集前バックアップ | `index.html.20260805-bak-pre-bundle2` / `build_master.py.20260805-bak-pre-bundle2` / `data/master_final.csv.20260805-bak-pre-bundle2` / `data/data_line.js.20260805-bak-pre-bundle2` / `BIN_DIVER_NOTES.md.20260805-bak-pre-bundle2-note` |
| ローカルコミット | `f63aef4`（ノート）→ `7148f43`（build_master.py + index.html） |
| 方式 | `base64 -i <file> \| tr -d '\n'` → `jq -n --rawfile c … '{message,content,sha,branch:"main"}'` → `gh api -X PUT repos/english-tadashi/bin-diver/contents/<file> --input payload.json` |
| payload | index.html **10,899,999 bytes** |
| **リモートコミット** | `0018b004bb8bd336cdc6a1180aee541d8d25dd4f`（build_master.py）→ **`71daf76d6494770b072f8f0553227a5581e269d4`**（index.html） |
| **blob sha 4者一致（index.html）** | live 実物の `git hash-object` / ローカル HEAD / PUT の戻り値 / contents API＝すべて **`c98113c7120a522fb427fffd2c23a5c2b5309e94`** |
| blob sha 一致（build_master.py） | contents API == ローカル HEAD ＝ **`e2271a4e6af211b97385d62a5eda56679c58f9d0`** |
| **live 全体md5** | **`26ac142a4c05d7072ec3fbf9e66a0655`** ＝ローカルと一致 |
| **DATA行(986) md5** | **`2aa45542ceb13a3a10fb3c9ab086a6ad`**（正典＝末尾改行なし方式）。**live 実物から再計算して一致** |
| live head区間(1..985) md5 | `5aca5ff2d6bbd10f12136fb52f49ee30` ＝ローカルと一致（**前回から不変**） |
| live ロジック区間(987..末尾) md5 | `b9f1ee4473a22f6dbc78ab8554c82960` ＝ローカルと一致（**前回から不変**） |
| 反映確認 | **09:42:51 に新 blob を確認**。切り替わりの正確な時刻は**測れていない**（下記の URL 取り違えのため、その前の約6分は別物を見ていた） |
| 差分規模 | `build_master.py` +250 / −2 ／ `index.html` は986行目（DATA行）のみ |

★`head` 区間と `logic` 区間の md5 は**同日1回目・2回目の記録値とそのまま一致**。
＝3回のデプロイを通じて index.html のロジックには1バイトも触れていない。変わったのは DATA 行だけ。

### ★踏んだ罠 ―― live の URL は `github.io` ではない（反映確認が6分空振りした）

反映のポーリングを `https://english-tadashi.github.io/bin-diver/index.html` に投げたが、
**24回すべて blob が変わらなかった。** 中身を見たら 162バイトの
`301 Moved Permanently`（nginx）で、**index.html ですらなかった。**

- このリポジトリの Pages には**カスタムドメインが設定されている**:
  `gh api repos/english-tadashi/bin-diver/pages` → `cname: gamekataban.com` / `html_url: https://gamekataban.com/`
- `github.io` 側はそこへ 301 する。`curl -s`（`-L` なし）だとリダイレクト**ページ本文**が保存され、
  それを `git hash-object` に掛けるので、**毎回同じ「変わらない」ハッシュが出る。**
  ―― **変わらないのは live が古いからではなく、live を見ていなかったから。**
- `https://gamekataban.com/` に投げ直したら**1回目で新 blob**。実際にはとうに反映されていた。

★**このノートの過去の記述に `https://english-tadashi.github.io/bin-diver/` が2箇所ある。**
過去の確認がそこを見ていたなら `-L` が効いていたということで、
**URL を書き写す運用が危ない**（DATA 行の行番号と同じ話・9260行）。
次からは**確認先を固定値で書かず** `gh api …/pages --jq .html_url` から取ること。
リダイレクトを追う `-L` を付けるのは当然として、**それだけでは「本物を見た」保証にならない** ――
今回のように本文が別物でも `curl` は成功扱いで返る。**落とした中身に DATA 行が在るかまで見ること。**

### DATA行 md5 の系譜（すべて末尾改行なし方式）

| コミット | 内容 | DATA行 md5 |
|---|---|---|
| `88cc9cb` | Buyee検索キーの `/` 置換（DATA は不変） | `0dd350850ed89c074dfbe87cec146c97` |
| `08c36ea` | Switch/PS5/Xbox One の `row[7]` を埋める（1,132行） | `1d9968fa63085cd4d62faa60695ee314` |
| `51962d4` | 罠#16 の北米版5行を除外（37,855→37,850） | `5a6a91e60c89a8c2f823637272664882` |
| `d662471` | 束1: ラテン表記行 2,676件に英題を原文複製 | `922dc7602ab26b3d7b29086a54f06162` |
| `f60654b` | 罠#148: 誤連結17行の title_ja_kana を修正 | `9983168149001ff60420c255407cc88f` |
| **`7148f43`** | **束2: 音写系182行の title_en を別行から転記** | **`2aa45542ceb13a3a10fb3c9ab086a6ad`** |

### live 実物での確認 — バットマン・リターンズ（G-3314 / Game Gear）

live（`https://gamekataban.com/`）を落とし、**その実物のソース行から `SG_COM` / `sgCom` の定義と
`card()` の `en` まわりの式をそのまま切り出して**実行した。
対象行は**添字ではなく型番 `G-3314` で引いている**（罠#21）。

| 確認項目 | 結果 |
|---|---|
| `title_ja` (r[1]) | `バットマン・リターンズ` |
| `platform` (r[2]) | `Game Gear` |
| **`title_en` (r[0])** | **`Batman Returns`** ＝デプロイ前は空 |
| 借用元 M726578（`G-4092` / Mega Drive）の `title_en` | `Batman Returns` ＝**借用元と同一の値**であることを live 上で確認 |
| `hasEn` / English フィールドを出すか (`en?`) | **true** ＝カードに English 欄が出る |
| 見出し `title`（`hasEn?en:jp`） | **`Batman Returns`** |
| `sgComUrl`（`en ? sgCom(en,pf) : ''`） | **`https://www.suruga-ya.com/en/products?category=2&keyword=Batman+Returns+Game+Gear`** ＝空文字から実URLになった |
| live DATA 行数 | **37,850**（不変） |
| **live `title_en` あり** | **16,342** ＝ 16,160 + 182 |

### 未測定のまま残ること

★**駿河屋.com のリンクが当たるかは測っていない。** 上のとおり182行ぶん `sgComUrl` が
実URLになったが、**その英題で .com の英語検索に当たる保証はない**（束1の2,676行についても
9370行で同じことを書いたまま、依然未測定）。**リンクが増えること自体が副作用として残っている。**

★**除外した34行**（★3 / ◆9 / ・25・重なり3行を含め実体34）は**未着手のまま**。
★**束3（2,243行）も未着手**で、**ダックハント（M727853）はそこに残っている。**
**未解決#26 は閉じていない。** live で `duck hunt` と打っても依然 0件。
